---
phase: 03-lint
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/core/templates/ticket-build.md
  - packages/core/templates/ticket-maintain.md
  - packages/core/src/generated/templates.ts
  - packages/core/test/templates.test.ts
  - packages/core/test/__golden__/ticket-build.verified-empty.md
autonomous: true
requirements: [FMT-09, FMT-10]
coupling_justified:
  - "03-02: 03-02 rewrites lint/ticket.ts in this wave and this plan's gate runs typecheck over the package; no shared file or golden, order irrelevant; a typecheck failure caused only by a sibling file mid-write is rerun once 03-02's task lands"

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "`ticket-build.md` and `ticket-maintain.md` end with a `## Verification notes` section whose only content is one HTML guidance comment naming `### @ac-n` blocks and that the section stays last; `epic.md` is unchanged (D-70, Claude's Discretion)"
    - "The `## Acceptance criteria` guidance comment in both story templates mentions the `@test:<id>` tag on scenarios that are not `@ui` (D-69)"
    - "`templates.test.ts` pins the six headings for build and maintain and three for epic; build and maintain still differ only inside HTML comments; `src/generated/templates.ts` matches `templates/` (drift test)"
    - "A ticket written from the new template lints with no `lint.notes-not-last` and no `lint.note-orphan` (the notes section is last and empty)"
  artifacts:
    - path: "packages/core/templates/ticket-build.md"
      provides: "story template with the notes section and @test: guidance"
      contains: "## Verification notes"
    - path: "packages/core/src/generated/templates.ts"
      provides: "regenerated template module"
      contains: "## Verification notes"
  key_links:
    - from: "packages/core/templates/ticket-build.md"
      to: "packages/core/src/generated/templates.ts"
      via: "npm run gen"
      pattern: "Verification notes"
    - from: "packages/core/templates/ticket-build.md"
      to: "packages/core/test/__golden__/ticket-build.verified-empty.md"
      via: "write.test.ts derives the golden from the template"
      pattern: "Verification notes"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; no attribution trailer is written"
      status: flagged
      verification: unverified
    - statement: "No template text names another tool, plugin, harness, test runner, or planning system; the report is 'the test report' and ids are 'the id from the report'"
      status: flagged
      verification: unverified
    - statement: "The frontmatter block of both story templates is byte-identical before and after this plan; only body comments and the appended section change"
      status: flagged
      verification: unverified
---

<objective>
**As a** developer finishing a story, **I want** the ticket template to already show where verification notes go and how to tag a scenario with its test id, **so that** the lint rules from plans 03-02 and 03-05 point at a place the template taught me about.

Purpose: D-69 and D-70 guidance lands in the shipped templates (Claude's Discretion in CONTEXT.md; RESEARCH recommends it); the hidden couplings (template heading pin, `npm run gen`, the write golden) are handled in one place.
Output: two templates, the regenerated module, the updated heading pin, the regenerated write golden.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-lint/03-CONTEXT.md
@.planning/phases/03-lint/03-RESEARCH.md
@packages/core/templates/ticket-build.md
@packages/core/templates/ticket-maintain.md
@packages/core/test/templates.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: `## Verification notes` section and `@test:` guidance in the story templates</name>
  <read_first>
    - packages/core/templates/ticket-build.md and packages/core/templates/ticket-maintain.md (current body; the section shape is `## Heading` then one `<!-- Role. guidance -->` comment)
    - packages/core/templates/epic.md (must stay unchanged)
    - packages/core/test/templates.test.ts lines 60-135 (heading pin, build/maintain comment-only diff, `TICKET-ID` id pin, drift test)
    - packages/core/test/write.test.ts lines 50-56 (the golden derived from `templates['ticket-build.md']`)
    - packages/core/scripts/gen-templates.mjs (what `npm run gen` writes)
    - .planning/phases/03-lint/03-RESEARCH.md "Verification Notes and Templates (FMT-10)"
    - docs/design.md §2 (the Verification notes paragraph, for the wording to echo)
  </read_first>
  <files>packages/core/templates/ticket-build.md, packages/core/templates/ticket-maintain.md, packages/core/src/generated/templates.ts, packages/core/test/templates.test.ts, packages/core/test/__golden__/ticket-build.verified-empty.md</files>
  <action>
In both `ticket-build.md` and `ticket-maintain.md`:

1. Extend the `## Acceptance criteria` guidance comment with one sentence: `A scenario that is not @ui also carries one @test:<id> tag, where <id> is the case id from the test report (classname#name, spaces as -); add it after Ready, it is outside the AC hash.` Keep the sentence identical in both files (the build/maintain test allows differences only inside comments, but identical is simpler).
2. Append after `## Plan`'s comment a blank line and:
   `## Verification notes`
   `<!-- Developer, at Done. One "### @ac-n <scenario name>" block per ticked scenario, a few lines each: what was run and what was seen. This section stays last; evidence with a result lives in tickets/<id>/verification.md. -->`
   Keep the comment identical in both files.

Do not touch the frontmatter block or `epic.md`. Run `npm run gen` (regenerates `src/generated/templates.ts`; the drift test fails otherwise). Update `templates.test.ts` "headings are in D-07 order": `story` becomes the five headings plus `'## Verification notes'`; epic stays three. Regenerate the write golden with `npm test -- --project core write -u -t "verified-empty"` (scoped: the `LOGIN-1.*.md` goldens derive from the fixture, not the template, and must not change; check `git status` shows only `ticket-build.verified-empty.md` under `__golden__`). Run `npm test -- --project core templates && npm test -- --project core write`. Sanity check that the new section lints clean: `node -e` is not needed; instead confirm by reading that the section is last and contains only a comment (the `lint-hygiene` fixture's `CLEAN.md` in 03-02 pins the same shape). Leave uncommitted.
  </action>
  <verify>
    <automated>npm run gen && npm test -- --project core templates && npm test -- --project core write && npm run typecheck</automated>
    <fails_when>non-zero exit; or `drifted: run npm run gen` in the output; or a golden reported obsolete or mismatched; or `No test files found`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Verification notes$" packages/core/templates/ticket-build.md packages/core/templates/ticket-maintain.md` prints 1 for each and `grep -c "Verification notes" packages/core/templates/epic.md` prints 0
    - `tail -n 2 packages/core/templates/ticket-build.md` shows the notes heading followed by a line starting with `<!--` and ending with `-->`, and the same for `ticket-maintain.md`
    - both templates contain `@test:<id>` inside the Acceptance criteria comment
    - `packages/core/test/templates.test.ts` contains `'## Verification notes'` in the `story` headings array
    - `packages/core/src/generated/templates.ts` contains `## Verification notes` twice (build and maintain)
    - `git status --short packages/core/test/__golden__` lists only `ticket-build.verified-empty.md`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>New tickets carry the notes section last and the `@test:` guidance, the generated module and the write golden match, and the template heading pin is updated.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Template section: `## Verification notes` in `ticket-build.md` and `ticket-maintain.md`; `@test:<id>` sentence in the AC comment
- Regenerated: `packages/core/src/generated/templates.ts`, `packages/core/test/__golden__/ticket-build.verified-empty.md`
- Test expectation change: six-heading pin in `templates.test.ts`
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

| Source | Item | Disposition |
|---|---|---|
| Claude's Discretion | Story and bug templates gain the empty notes section and the `@test:` sentence; `epic.md` gains neither | Adopted per RESEARCH recommendation |
| Constraint "no other tools named" | The comment says "the test report" and "classname#name", never a runner name | Adopted |
| D-70 | The notes comment names `verification.md` as where results with evidence live, so the two files are not confused | Chosen wording; edit freely |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template text → every new ticket | Text shipped to users; no code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | generated module drifting from `templates/` | low | mitigate | `npm run gen` in the task; the drift test in `templates.test.ts` |
| T-03-SC | Tampering | npm installs | high | mitigate | No package added |
</threat_model>

<verification>
- The `<automated>` command; wave-end `npm test` green with 03-02 and 03-03.
</verification>

<success_criteria>
- Templates teach D-69 and D-70; all pinned template tests pass; nothing committed.
</success_criteria>

<output>
Create `.planning/phases/03-lint/03-04-SUMMARY.md` when done. Do not commit.
</output>
