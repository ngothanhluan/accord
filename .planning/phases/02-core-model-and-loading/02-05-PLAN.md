---
phase: 02-core-model-and-loading
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/load/verification.ts
  - packages/core/src/load/snapshot.ts
  - packages/core/test/verification.test.ts
  - packages/core/test/__golden__/verification-edges.snapshot.json
  - packages/core/test/fixtures/verification-edges/accord/config.yml
  - packages/core/test/fixtures/verification-edges/accord/tickets/A.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/A/verification.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/A/plan.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/B.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/B/verification.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/C.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/C/verification.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/ORPHAN/verification.md
  - packages/core/test/fixtures/verification-edges/accord/tickets/notes.txt
  - packages/core/test/fixtures/verification-edges/accord/assets/A/prototype.html
autonomous: true
requirements: [CORE-06]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In `verification.md`, each `## @ac-n <name>` heading is one `EvidenceBlock` keyed by tag with the name kept for humans; a block without a `Result:` line, or with a value other than `pass`, `fail`, `blocked`, yields `load.result-invalid` at that block's line (or the `Result:` line when present), and `result` is absent; `evidence` is the text after `Evidence:` to the end of the block, comments stripped, trimmed, possibly empty (D-42)"
    - "A `tickets/<id>/verification.md` whose `tickets/<id>.md` does not exist yields `load.verification-orphan`; one whose ticket exists but has broken frontmatter is not an orphan (D-37, D-32)"
    - "`tickets/notes.txt`, `tickets/<id>/plan.md`, and `assets/<id>/prototype.html` produce no entry and no finding (D-37)"
    - "A verification record with invalid frontmatter keeps its parsed blocks and has `frontmatter` absent, with the schema finding at the offending line (D-32 applied to verification records)"
    - "A duplicated `## @ac-1` block yields `load.heading-duplicate` at the second heading; the first block is kept (D-40 applied to evidence blocks — flagged assumption)"
  artifacts:
    - path: "packages/core/test/__golden__/verification-edges.snapshot.json"
      provides: "golden covering result/evidence edges, orphan, ignored files, broken frontmatter"
      contains: "load.verification-orphan"
    - path: "packages/core/test/verification.test.ts"
      provides: "exact block and finding assertions"
      min_lines: 50
  key_links:
    - from: "packages/core/src/load/snapshot.ts"
      to: "packages/core/src/load/verification.ts"
      via: "parseVerification(id, file, text) for keys matching accord/tickets/<id>/verification.md"
      pattern: "parseVerification"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "The loader never checks evidence content, never resolves an evidence reference against `tree`, and never compares block tags with scenario tags or `verified`; those are Phase 4 (GATE-02, GATE-04)"
      status: resolved
      verification: judgment
    - statement: "Stray files under `accord/` are ignored silently — no warning, no finding — so a team's notes or plans never make a gate noisy (D-37, rejected idea in CONTEXT.md)"
      status: resolved
      verification: test
---

<objective>
**As a** reviewer whose fresh-context review wrote `verification.md`, **I want** each `@ac-n` block's result and evidence captured exactly, a missing or misspelt `Result:` pointed at, and a stray record with no ticket flagged, **so that** the Done gate compares real evidence against real scenarios.

Purpose: 02-01 parsed one clean record. This slice proves D-42 and D-37 against every record shape and the folder-convention edges, and hardens `load/verification.ts` and the classification in `load/snapshot.ts`.

Output: the `verification-edges` fixture and golden, `verification.test.ts`, and corrections to the two modules.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-model-and-loading/02-CONTEXT.md
@.planning/phases/02-core-model-and-loading/02-01-SUMMARY.md
@packages/core/src/load/verification.ts
@packages/core/src/load/snapshot.ts
@packages/core/test/helpers/fixture.ts
@packages/core/templates/verification.md
@packages/core/schemas/verification.schema.json

<interfaces>
<!-- From 02-01. -->
```typescript
import { parseVerification } from '../src/load/verification.js';
// parseVerification(id, file, text) -> { verification: Verification; findings: Finding[] }
// Duplicate blocks (D-40 over D-42): duplicateHeadings is keyed by the ac-n tag alone (02-01 step 6) and parseVerification
// skips a section whose tag was already seen (02-01 step 8) — so `## @ac-1 Một` then `## @ac-1 Một lần nữa` is exactly one
// load.heading-duplicate finding and one block, whatever the names. Both live in load/verification.ts (this plan's file);
// load/sections.ts belongs to 02-03 in this wave and is not edited here.
// snapshot.test.ts wraps each fixture in describe('fixture <name>'); scope golden creation with -t "fixture verification-edges".
// Verification = { ticket, file, frontmatter?: { ticket, commit, reviewed_on }, blocks: EvidenceBlock[] }
// EvidenceBlock = { acTag: 'ac-1', name, line, result?: 'pass' | 'fail' | 'blocked', evidence: string }
// loadSnapshot classifies keys: /^accord\/tickets\/([^/]+)\.md$/ -> ticket; /^accord\/tickets\/([^/]+)\/verification\.md$/ -> verification; else ignored
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Result and Evidence edges on one record, invalid frontmatter on another</name>
  <read_first>
    - packages/core/src/load/verification.ts (code under test)
    - packages/core/templates/verification.md (the layout: `Result:` then a guidance comment, `Evidence:` free text)
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-42, D-40, D-32; Phase 1 D-09
    - packages/core/schemas/verification.schema.json (`commit` pattern)
  </read_first>
  <files>packages/core/src/load/verification.ts, packages/core/test/verification.test.ts, packages/core/test/fixtures/verification-edges/accord/config.yml, packages/core/test/fixtures/verification-edges/accord/tickets/A.md, packages/core/test/fixtures/verification-edges/accord/tickets/A/verification.md, packages/core/test/fixtures/verification-edges/accord/tickets/B.md, packages/core/test/fixtures/verification-edges/accord/tickets/B/verification.md</files>
  <action>
Create `packages/core/test/fixtures/verification-edges/accord/` with a valid `config.yml` and:

- `tickets/A.md`: valid story (six-line frontmatter) whose `## Acceptance criteria` fence has `@ac-1` and `@ac-2` scenarios.
- `tickets/A/verification.md`, numbered as written: L1–L5 frontmatter `ticket: A`, `commit: abcdef0`, `reviewed_on: 2026-09-02`; then blocks: `## @ac-1 Một` with `Result: pass`, the template's `<!-- pass | fail | blocked -->` comment, `Evidence: dòng một`, `dòng hai <!-- bỏ qua -->`, `dòng ba` → `result` `pass`, `evidence` `dòng một\ndòng hai\ndòng ba` (comment removed, line trimmed); `## @ac-2 Hai` with an `Evidence: chạy npm test` line but no `Result:` → `load.result-invalid` at the block heading line, `result` absent, `evidence` `chạy npm test`; `## @ac-3 Ba` with `Result: PASS` → `load.result-invalid` at that `Result:` line (values are lowercase — flagged assumption), `result` absent; `## @ac-4 Bốn` with `Result: blocked` and no `Evidence:` label → `result` `blocked`, `evidence` `''`; `## Notes` with any text → no block, no finding; `## @ac-1 Một lần nữa` with `Result: fail` → `load.heading-duplicate` at this heading's line; `blocks` still has exactly four entries (`ac-1` from the first block, `ac-2`, `ac-3`, `ac-4`) — the two headings differ in name, and identity is the tag (D-42; 02-01 steps 6 and 8), so if a fifth block appears or no finding is raised, fix `tagOf`/the `seen` set in `load/verification.ts`, not `load/sections.ts`.
- `tickets/B.md`: valid story. `tickets/B/verification.md`: L1 `---`, L2 `ticket: B`, L3 `commit: xyz`, L4 `reviewed_on: 2026-09-02`, L5 `---`, then one valid `## @ac-1 B1` block with `Result: pass` and `Evidence: ok` → `schema.pattern` finding pointer `/commit` line 3, `frontmatter` absent, `blocks` has one entry with `result` `pass`.

Write `packages/core/test/verification.test.ts`: load the fixture once with `loadSnapshot(readFixture('verification-edges'))` and assert every expectation above with exact lines; also call `parseVerification` directly on an inline record whose `Evidence:` spans three lines ending in blank lines to assert trailing whitespace is trimmed, and on one where `Result:` comes after `Evidence:` to assert order does not matter (flagged assumption). Fix `load/verification.ts` where expectations fail. Run `npm test -- --project core verification` (the golden is created in Task 2 once the fixture is complete). Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core verification</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary reports fewer than 6 passing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/verification.test.ts` contains `load.result-invalid`, `load.heading-duplicate`, `/commit`, `Result: PASS` or `'PASS'`, and `dòng một\ndòng hai\ndòng ba`
    - `npm test -- --project core verification` reports at least 6 passing tests and 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Every `Result:`/`Evidence:` shape in D-42 and a broken verification frontmatter are pinned with exact lines, and duplicate blocks are pointed at.</done>
</task>

<task type="auto">
  <name>Task 2: Orphans, broken-ticket owners, ignored files — the folder convention under load</name>
  <read_first>
    - packages/core/src/load/snapshot.ts (classification regexes and the orphan pass)
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-37 (ignore silently; orphan is the one exception) and Phase 1 D-06 (folder rule)
    - packages/core/test/snapshot.test.ts (discovery loop)
  </read_first>
  <files>packages/core/src/load/snapshot.ts, packages/core/test/verification.test.ts, packages/core/test/__golden__/verification-edges.snapshot.json, packages/core/test/fixtures/verification-edges/accord/tickets/C.md, packages/core/test/fixtures/verification-edges/accord/tickets/C/verification.md, packages/core/test/fixtures/verification-edges/accord/tickets/ORPHAN/verification.md, packages/core/test/fixtures/verification-edges/accord/tickets/A/plan.md, packages/core/test/fixtures/verification-edges/accord/tickets/notes.txt, packages/core/test/fixtures/verification-edges/accord/assets/A/prototype.html</files>
  <action>
Extend the fixture:

- `tickets/C.md`: a file with NO frontmatter (`## Intent` and one line) and `tickets/C/verification.md`: a valid record with one `## @ac-1 C1` block → `tickets.C` exists with `frontmatter` absent and `load.frontmatter-missing`; `verifications.C` exists; NO orphan finding (the ticket entry exists even though it is broken).
- `tickets/ORPHAN/verification.md`: a valid record → `load.verification-orphan` with `file` `accord/tickets/ORPHAN/verification.md`, no `line`; `verifications.ORPHAN` is still present in the snapshot (the record is loaded; the finding is the signal).
- `tickets/A/plan.md` (any text), `tickets/notes.txt` (any text), `assets/A/prototype.html` (`<!doctype html>` and one line) → no ticket, no verification, no finding for any of the three.

Append `describe('folder convention (D-37)')` to `verification.test.ts` asserting: `Object.keys(snapshot.tickets)` sorted equals `['A','B','C']`; `Object.keys(snapshot.verifications)` sorted equals `['A','B','C','ORPHAN']`; exactly one `load.verification-orphan` finding; no finding names `plan.md`, `notes.txt`, or `prototype.html`; and (D-30) `snapshot.tree` contains all three ignored paths. Fix `load/snapshot.ts` where expectations fail. Record `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` first. Run `npm test -- --project core snapshot -u -t "fixture verification-edges"` once to create `verification-edges.snapshot.json` — never `-u` without the `-t` filter in this wave: the discovery loop walks every fixture folder and an unscoped `-u` would rewrite the tracer golden and sibling plans' half-written goldens. Read it, verify, then run `npm test -- --project core snapshot -t "fixture (verification-edges|valid-build)"` without `-u`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core verification && npm test -- --project core snapshot -t "fixture (verification-edges|valid-build)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a `|core|` summary reports 0 tests; or a golden is reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/verification-edges.snapshot.json` parses; `Object.keys(verifications)` sorted equals `["A","B","C","ORPHAN"]`; `Object.keys(tickets)` sorted equals `["A","B","C"]`
    - In the golden, `errors` contains exactly one object with rule `load.verification-orphan`, whose `file` is `accord/tickets/ORPHAN/verification.md` and which has no `line` key; no `errors[].file` ends with `plan.md`, `notes.txt`, or `prototype.html`
    - In the golden, `verifications.A.blocks.map(b => b.acTag)` equals `["ac-1","ac-2","ac-3","ac-4"]`; `verifications.A.blocks[0].evidence` is `"dòng một\ndòng hai\ndòng ba"`; `verifications.A.blocks[1]` has no `result` key; `verifications.A.blocks[3].evidence` is `""`
    - In the golden, `errors` contains for `accord/tickets/A/verification.md` two `load.result-invalid` findings and one `load.heading-duplicate` finding; for `accord/tickets/B/verification.md` one `schema.pattern` finding with `pointer` `/commit` and `line` 3; `verifications.B` has no `frontmatter` key and `verifications.B.blocks.length` is 1
    - `npm test -- --project core verification` and `npm test -- --project core snapshot -t "fixture (verification-edges|valid-build)"` report 0 failures; `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` is unchanged from the start of the plan (the tracer golden was compared, never rewritten)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The folder convention's edges — orphan record, record for a broken ticket, and stray files — behave exactly as D-37 says, and the whole fixture is pinned by a golden.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Fixture: `packages/core/test/fixtures/verification-edges/` (config, 3 tickets, 4 records, 3 ignored files)
- Golden: `packages/core/test/__golden__/verification-edges.snapshot.json`
- Test: `packages/core/test/verification.test.ts`
- Hardened internals (no new exports): `packages/core/src/load/verification.ts`, `packages/core/src/load/snapshot.ts`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions

| Source | Item | Disposition |
|---|---|---|
| D-42 | `Result:` value case | Assumed case-sensitive lowercase (`pass`, `fail`, `blocked`) as the template comment shows; `PASS` is a finding. Alternative: case-insensitive. One-line change. |
| D-42 | missing `Evidence:` label | Assumed: not a loader finding; `evidence` is `''` and Phase 4 GATE-04 decides. |
| D-42 | `Result:` after `Evidence:` | Assumed: order inside a block does not matter; `evidence` stops at the block's end, so a `Result:` line after it becomes part of the evidence text only if it appears after the `Evidence:` label — pinned by a unit test so the behaviour is explicit. |
| D-40 | duplicate `## @ac-n` block | Assumed: D-40 applies (error at the second, first kept). Alternative: last wins or both kept for Phase 4 to flag. |
| D-37 | orphan record still loaded | Assumed: `verifications[id]` is populated for an orphan so `status` can show it; the finding is the error signal. |
| D-37 | a `## @ac-n` heading with extra text after the tag | The name is free text; a heading like `## @ac-1` with no name yields `name` `''`. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `verification.md` text → `parseVerification` | Written by the review context; untrusted like any file |
| snapshot keys → classification | Any path under `accord/` decides what is a ticket or a record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Repudiation | a record that names no ticket, or a `Result:` that is not one of the three values, passing as evidence | medium | mitigate | `load.result-invalid` and `load.verification-orphan` findings; `result` absent rather than guessed; the schema requires `commit` and `reviewed_on` |
| T-02-15 | Spoofing | `tickets/<id>/verification.md` for an id whose ticket is a different case on a case-insensitive filesystem | low | accept | Keys are matched exactly; case-sensitivity of ids is a Phase 3 lint concern (D-34) |
| T-02-16 | Tampering | HTML comments used to hide text inside evidence | low | mitigate | Comments are stripped before `evidence` is captured (fixture pins `<!-- bỏ qua -->` removal) |
| T-02-SC | Tampering | npm installs | high | mitigate | No packages added |
</threat_model>

<verification>
- Per-plan gates are the scoped commands in each `<verify>`; golden reviewed against the tables above; the tracer golden's hash unchanged.
- Wave-end check, not a per-plan gate: once every Wave 2 plan (02-02..02-05, 02-07) has finished, `npm test -- --project core` must report 0 failures. While a sibling plan's fixture is still being written, a failure inside that sibling's `fixture <name>` describe is not this plan's failure.
</verification>

<success_criteria>
- D-37, D-42, and the D-32/D-40 analogues for records each have executable assertions; the snapshot's classification of every path under `accord/` is pinned.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/02-core-model-and-loading/02-05-SUMMARY.md` when done. Do not commit.
</output>
