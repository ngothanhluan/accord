---
phase: 03-lint
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/core/src/lint/ticket.ts
  - packages/core/src/lint/rules.ts
  - packages/core/src/load/sections.ts
  - packages/core/test/lint.test.ts
  - packages/core/test/fixtures/lint-hygiene/accord/config.yml
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/HYGIENE.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/SIZE.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/HEADINGS.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/EPIC.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/NOTES.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/PLAN-EMPTY.md
  - packages/core/test/fixtures/lint-hygiene/accord/tickets/CLEAN.md
  - packages/core/test/__golden__/lint-hygiene.lint.json
  - packages/core/test/__golden__/lint-hygiene.snapshot.json
  - packages/core/test/__golden__/frontmatter-errors.lint.json
  - packages/core/test/__golden__/body-edges.lint.json
  - packages/core/test/__golden__/gherkin-shapes.lint.json
  - packages/core/test/__golden__/no-config.lint.json
  - packages/core/test/__golden__/bad-config.lint.json
  - packages/core/test/__golden__/config-syntax.lint.json
  - packages/core/test/__golden__/valid-build.lint.json
  - packages/core/test/__golden__/verification-edges.lint.json
autonomous: true
requirements: [LINT-05, LINT-06, LINT-07, LINT-08, FMT-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`lint.heading-missing` (error, no line) fires once per required D-07 heading absent for the ticket's type: story and bug need Intent, Requirements, Acceptance criteria, Open questions, Plan; epic needs Intent, Requirements, Open questions; `## Verification notes` is never required; a ticket without frontmatter gets none (D-71, Pitfall 9)"
    - "`lint.sentinel` (warning) fires once per occurrence of an uppercase whole-word `TODO`, `TBD`, `FIXME`, or the literal `TICKET-ID` on any comment-stripped body line, fences included, HTML comment text excluded; and once per angle-bracket placeholder such as `<observable outcome>`; and once per scenario step whose text is only `...` at the scenario line (D-73)"
    - "Two sentinels on one line yield two findings at that line, ordered by reason (LINT-06 adjacency edge, verification: explicit); a `TBD` in a requirement line yields both `lint.sentinel` and `lint.vague-wording` (owner decision, RESEARCH Open Question 2 resolved 2026-09-14)"
    - "The Phase 2 fixtures record the new rows' effect: every typed ticket lacking a required heading (`MISMATCH`, `TYPING`, all six `body-edges` tickets, all nine `gherkin-shapes` tickets, `ONLY`, both `T.md`) carries `lint.heading-missing` in its lint golden, and `LOGIN-1`, `verification-edges/A`, `verification-edges/B`, `body-edges/FENCE` carry `lint.plan-empty`; no other existing golden changes in this plan"
    - "`lint.open-question` (warning) fires at each `- [ ]` item line under `## Open questions`; a `- [ ]` inside an HTML comment is ignored; an empty section, a ticket with no assumptions, and `verified: []` produce nothing (D-73; LINT-06 empty edge, verification: explicit)"
    - "`lint.assumption-unconfirmed` (warning, pointer `/assumptions/<i>/confirmed`, no line) fires per `confirmed: false`; `lint.tick-orphan` (warning, pointer `/verified/<i>`) fires per `verified` entry with no scenario whose `acTag` equals it exactly, case-sensitive (LINT-05; LINT-05 encoding edge, verification: explicit; D-73)"
    - "`lint.vague-wording` (warning, never error) fires once per line containing one of `depends`, `maybe`, `probably`, `mix of`, `somewhere between`, `not sure`, `TBD` as a whole word or phrase, case-insensitive with Unicode-letter boundaries, on requirement lines, comment-stripped `## Acceptance criteria` lines including fence content, and `- [x]` items under `## Open questions`; unanswered `- [ ]` items and other sections are not scanned (ROADMAP criterion 7, roadmap-sourced, not a CONTEXT.md decision)"
    - "`lint.intent-oversize`, `lint.requirements-oversize`, `lint.scenarios-oversize` (warnings, at the section heading line) fire when non-blank comment-stripped Intent lines exceed 5, `requirements.length` exceeds 15, or `scenarios.length` exceeds 5; exactly 5, 15, and 5 are silent; intent and requirements limits apply to epics (LINT-07, Claude's Discretion)"
    - "`lint.plan-step-untagged` (warning, item line) fires per `## Plan` list item with no `@ac-n`; `lint.plan-tags-differ` (warning, Plan heading line) fires once with reason `plan lacks <tags or nothing>; scenarios lack <tags or nothing>`, tags ascending by n; `lint.plan-empty` (warning, Plan heading line) fires when the ticket has scenarios and the section has no list item; equal sets are silent; a story with no scenarios and an empty plan is silent; epics are skipped (D-74; LINT-08 adjacency, empty, and ordering edges, verification: explicit)"
    - "`lint.note-orphan` (warning, block line) fires per `### @ac-n` block under `## Verification notes` whose tag matches no scenario; `lint.notes-not-last` (warning, heading line) fires when that section is not the last `##` section; a last, empty notes section is silent; `### @AC-1` is not a block (D-70; FMT-10 empty, encoding, and ordering edges, verification: explicit)"
    - statement: "A second `### @ac-1` block under `## Verification notes` for the same tag produces no lint finding in v0.1; duplicate and content checks belong to GATE-10 (FMT-10 adjacency edge)"
      verification: backstop
    - "`lint.tracker-empty` (warning, pointer `/tracker`) fires when `tracker` is `{}` (D-22, Claude's Discretion)"
    - "The `lint-hygiene` fixture's `CLEAN.md` yields zero findings; the fixture's lint golden is identical across the five encoding variants"
  artifacts:
    - path: "packages/core/src/lint/ticket.ts"
      provides: "fifteen check functions over Ticket: headings, hygiene, vague wording, sizes, plan, notes, ticks"
      contains: "export const planTagsDiffer"
    - path: "packages/core/test/fixtures/lint-hygiene/accord/tickets/HYGIENE.md"
      provides: "sentinel, placeholder, open question, unconfirmed assumption, orphan tick, vague wording, tracker {} in one ticket"
      contains: "TICKET-ID"
    - path: "packages/core/test/__golden__/lint-hygiene.lint.json"
      provides: "golden for every LINT-05..08 and FMT-10 rule"
      contains: "lint.plan-tags-differ"
  key_links:
    - from: "packages/core/src/lint/ticket.ts"
      to: "packages/core/src/load/sections.ts"
      via: "headingKey, stripHtmlComments, LIST_MARKER (newly exported); rules read ticket.sections and never re-scan text"
      pattern: "stripHtmlComments"
    - from: "packages/core/src/lint/rules.ts"
      to: "packages/core/src/lint/ticket.ts"
      via: "fifteen new rows with their levels"
      pattern: "lint.notes-not-last"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; no attribution trailer is written"
      status: flagged
      verification: unverified
    - statement: "No rule in `ticket.ts` re-scans ticket text with its own heading or fence parser; every body rule reads `ticket.sections`, `ticket.requirements`, `ticket.scenarios` through `headingKey`, `stripHtmlComments`, and `LIST_MARKER` (Don't Hand-Roll)"
      status: flagged
      verification: unverified
    - statement: "No level literal for a `lint.*` rule appears in `ticket.ts`; levels live in `rules.ts` only (D-57)"
      status: flagged
      verification: unverified
    - statement: "Nothing under `packages/core/src/` imports a `node:` module or a bare Node built-in"
      status: flagged
      verification: unverified
---

<objective>
**As a** BA drafting a ticket, **I want** lint to point at every unfilled placeholder, open question, unconfirmed assumption, oversize section, vague phrase, untagged plan step, and stray verification note with its line, **so that** `gate ready` (Phase 4) has nothing left to discover.

This plan adds fifteen rows to the `RULES` table from plan 03-01, all implemented in `lint/ticket.ts` over the Phase 2 model, and the `lint-hygiene` fixture that pins each one. Criterion 7 (vague wording) is roadmap-sourced (added 2026-09-14, after CONTEXT.md) and confirmed in scope by the owner (RESEARCH Open Question 1, resolved 2026-09-14); it is covered here as `lint.vague-wording`. Two of the new rows (`lint.heading-missing`, `lint.plan-empty`) fire on Phase 2 fixtures whose tickets are deliberately partial, so eight existing lint goldens are regenerated here with their expected delta stated per ticket.

Purpose: LINT-05, LINT-06, LINT-07, LINT-08, FMT-10, and D-70, D-71, D-73, D-74 become executable.
Output: `lint/ticket.ts` extended, `rules.ts` with seventeen rows, `LIST_MARKER` exported, the `lint-hygiene` fixture with seven tickets and its two goldens, eight regenerated `*.lint.json` goldens.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-lint/03-CONTEXT.md
@.planning/phases/03-lint/03-RESEARCH.md
@.planning/phases/03-lint/03-PATTERNS.md
@.planning/phases/03-lint/03-01-SUMMARY.md
@packages/core/src/lint/rules.ts
@packages/core/src/lint/ticket.ts
@packages/core/src/load/sections.ts

<interfaces>
From 03-01 (do not change): `Draft = Omit<Finding, 'level' | 'rule'>`; `Rule = { id, level, profiles, check: (s: RepoSnapshot) => Draft[] }`; `RULES` in `lint/rules.ts`; `idMismatch` already in `lint/ticket.ts`.

New exports this plan adds to `lint/ticket.ts` (one per rule id): `headingMissing`, `trackerEmpty`, `tickOrphan`, `assumptionUnconfirmed`, `sentinel`, `openQuestion`, `vagueWording`, `intentOversize`, `requirementsOversize`, `scenariosOversize`, `planStepUntagged`, `planTagsDiffer`, `planEmpty`, `noteOrphan`, `notesNotLast`.

Constants (module-private in `lint/ticket.ts`):
- `WORD = /(?:^|[^A-Za-z0-9_])(TODO|TBD|FIXME|TICKET-ID)(?![A-Za-z0-9_])/g` (case-sensitive)
- `PLACEHOLDER = /<[a-z][a-z]* [^<>="/]+>/g`
- `PLACEHOLDER_STEP = /^\S+ \.\.\.$/` over `ScenarioRef.steps`
- `OPEN = /^\s*[-*+]\s+\[ \]/`, `DONE = /^\s*[-*+]\s+\[[xX]\]/`
- `VAGUE = /(?<!\p{L})(depends|maybe|probably|mix of|somewhere between|not sure|TBD)(?!\p{L})/iu`
- `AC = /@ac-[1-9][0-9]*/g`; `NOTE_BLOCK = /^###\s+@(ac-[1-9][0-9]*)\b/`
- `LIMITS = { intent: 5, requirements: 15, scenarios: 5 }`
- `REQUIRED: Record<'story' | 'bug' | 'epic', string[]>` of heading display names (`Intent`, `Requirements`, `Acceptance criteria`, `Open questions`, `Plan` for story and bug; the first, second, and fourth for epic); compare with `headingKey`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headings, hygiene, vague wording, and size rules with the HYGIENE, SIZE, HEADINGS, EPIC tickets</name>
  <read_first>
    - packages/core/src/lint/ticket.ts and packages/core/src/lint/rules.ts (from 03-01; extend, keep the row shape)
    - packages/core/src/load/sections.ts (`headingKey`, `stripHtmlComments`, `LIST_MARKER` at the bottom, `requirementLines` for how requirement lines were derived)
    - packages/core/src/load/verification.ts lines 21-58 (the section-walk analog: find the section, strip comments, test lines, fall back to `section.line`)
    - packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1.md (frontmatter and body shape to copy for fixture tickets)
    - .planning/phases/03-lint/03-RESEARCH.md "Rule Catalogue" rows for these rules, "Code Examples: Sentinels and open questions", "Scope Note: Success Criterion 7", Pitfall 3
    - .planning/phases/03-lint/03-PATTERNS.md `lint/ticket.ts` section (section-walk pattern, pointer-only findings, the `LIST_MARKER` export gap)
  </read_first>
  <files>packages/core/src/lint/ticket.ts, packages/core/src/lint/rules.ts, packages/core/src/load/sections.ts, packages/core/test/lint.test.ts, packages/core/test/fixtures/lint-hygiene/accord/config.yml, packages/core/test/fixtures/lint-hygiene/accord/tickets/HYGIENE.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/SIZE.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/HEADINGS.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/EPIC.md, packages/core/test/__golden__/lint-hygiene.lint.json, packages/core/test/__golden__/lint-hygiene.snapshot.json, packages/core/test/__golden__/frontmatter-errors.lint.json, packages/core/test/__golden__/body-edges.lint.json, packages/core/test/__golden__/gherkin-shapes.lint.json, packages/core/test/__golden__/no-config.lint.json, packages/core/test/__golden__/bad-config.lint.json, packages/core/test/__golden__/config-syntax.lint.json</files>
  <action>
**`sections.ts`:** add `export` to `const LIST_MARKER` (no other change).

**Rules (all in `lint/ticket.ts`, each a `Rule['check']` iterating `Object.values(snapshot.tickets)`; skip type-dependent rules when `t.frontmatter` is undefined per Pitfall 9):**

- `headingMissing` (D-71): for each display name in `REQUIRED[t.frontmatter.type]` with no section whose `headingKey(s.heading)` equals `headingKey(name)`, draft `{ file, reason: \`missing "## ${name}"\` }` with no line, in `REQUIRED` order.
- `trackerEmpty` (D-22): `tracker` defined and `Object.keys(tracker).length === 0` → `{ file, pointer: '/tracker', reason: 'tracker is empty; remove the key or add an adapter id' }`.
- `tickOrphan` (LINT-05): for each `verified[i]` not equal to any `s.acTag` → `{ file, pointer: \`/verified/${i}\`, reason: \`verified names "${tag}" but no scenario carries @${tag}\` }`. Exact string comparison.
- `assumptionUnconfirmed` (D-73): per `assumptions[i].confirmed === false` → `{ file, pointer: \`/assumptions/${i}/confirmed\`, reason: \`assumption "${text}" is not confirmed\` }`.
- `sentinel` (D-73): over every section, `stripHtmlComments(section.lines)`, for each `WORD` match one draft at `l.line` with reason `contains sentinel "TODO"` (the matched word); for each `PLACEHOLDER` match one draft with reason `contains placeholder "<observable outcome>"` (the matched text); then over `t.scenarios`, each step matching `PLACEHOLDER_STEP` yields one draft at `s.line` with reason `scenario "${s.name}" has a placeholder step "${step}"`. Reset `lastIndex` per line (use `matchAll`).
- `openQuestion` (D-73): section with key `open questions`, comment-stripped lines matching `OPEN` → draft at `l.line`, reason `open question is unchecked`.
- `vagueWording` (criterion 7): collect candidate lines: (a) `t.requirements` (already comment-stripped and marker-stripped); (b) comment-stripped lines of the `acceptance criteria` section; (c) comment-stripped lines of `open questions` matching `DONE`. For each line where `VAGUE` matches, one draft at `l.line` with reason `vague wording "${match[1]}"` (first match only). A line reachable through both (a) and another list cannot happen (sections are disjoint), so no dedupe is needed.
- `intentOversize`, `requirementsOversize`, `scenariosOversize` (LINT-07): Intent count = comment-stripped lines under `intent` with non-empty `trim()`; requirements count = `t.requirements.length`; scenarios count = `t.scenarios.length`. Fire at the section heading line (Intent, Requirements, Acceptance criteria respectively) when the count is greater than the limit, reasons `## Intent has ${n} lines; the limit is 5`, `## Requirements has ${n} EARS lines; the limit is 15`, `## Acceptance criteria has ${n} scenarios; the limit is 5`. Intent and requirements apply to epics; scenarios cannot exceed on an epic (no AC parsing) but the rule needs no type check.

**`rules.ts`:** append rows `lint.heading-missing` error; `lint.tracker-empty` warning; `lint.tick-orphan` warning; `lint.assumption-unconfirmed` warning; `lint.sentinel` warning; `lint.open-question` warning; `lint.vague-wording` warning; `lint.intent-oversize`, `lint.requirements-oversize`, `lint.scenarios-oversize` warning; all with `profiles: ['build', 'maintain']`.

**Fixture `lint-hygiene`:** `accord/config.yml` copied from `valid-build` with `design:\n  tokens: ""` (no tokens file, no prototype). Four tickets in this task, all with valid frontmatter (`id` equal to the stem, `title`, `type`, `status: draft`):

- `HYGIENE.md` (story; frontmatter also `tracker: {}`, `assumptions:` with `{ text: "Người dùng đã có tài khoản", confirmed: false }` then `{ text: "Đã có SSO", confirmed: true }`, `verified: [ac-1, ac-9]`). `## Intent`: line 1 `Người dùng đăng nhập. TODO viết thêm. FIXME nữa.`; line 2 `Kết quả: <observable outcome> cho người dùng.`; line 3 an HTML comment containing `TODO`, `- [ ]`, `<placeholder text>`, and `TICKET-ID` (all ignored). `## Requirements`: `- WHEN người dùng gửi form the system SHALL lưu TBD` and `- WHEN người dùng huỷ the system SHALL maybe đóng form`. `## Acceptance criteria`: a `gherkin` fence with `Feature: TICKET-ID`, one scenario tagged `@ac-1 @test:t#a` named `Đăng nhập`, steps `Given probably một người dùng`, `When ...`, `Then thấy bảng điều khiển`. `## Open questions`: `- [ ] Chưa rõ luồng SSO`, `- [x] It depends on the tracker`, `- [x] Đã rõ`. `## Plan`: `- Làm form @ac-1`, `- Viết test`, `- Nối tracker @ac-2`.
  Expected from this task's rules: `lint.tracker-empty` (pointer `/tracker`), `lint.assumption-unconfirmed` (`/assumptions/0/confirmed`), `lint.tick-orphan` (`/verified/1`, reason names `ac-9`), `lint.sentinel` × 5 (`TODO` and `FIXME` on intent line 1, `<observable outcome>` on intent line 2, `TBD` on the first requirement line, `TICKET-ID` on the Feature line) plus one placeholder-step sentinel at the scenario line, `lint.vague-wording` × 4 (`TBD`, `maybe`, `probably`, `depends`), `lint.open-question` × 1. Task 2 adds the plan findings.
- `SIZE.md` (story): `## Intent` with six non-blank lines separated by a blank line and a comment line; `## Requirements` with sixteen lines `- WHEN sự kiện ${n} xảy ra the system SHALL ghi nhận ${n}`; `## Acceptance criteria` fence with six scenarios `@ac-${n} @test:t#${n}` each with one `Given` step; `## Open questions` empty; `## Plan` one item `- Một bước @ac-1 @ac-2 @ac-3 @ac-4 @ac-5 @ac-6`. Expected: the three oversize warnings at the three heading lines, nothing else.
- `HEADINGS.md` (bug): only `## Intent` (one line) and `## Acceptance criteria` with one scenario tagged `@ac-1 @ui`. Expected: `lint.heading-missing` × 3 with reasons `missing "## Requirements"`, `missing "## Open questions"`, `missing "## Plan"`, no line.
- `EPIC.md` (epic): `## Intent` six lines, `## Requirements` one valid EARS line (`- WHEN ... the system SHALL ...`, so the 03-05 classifier stays silent), `## Open questions` empty. Expected: `lint.intent-oversize` only (no plan, no AC, no `heading-missing`).

Add `describe('fixture lint-hygiene: pinned lint values')` to `lint.test.ts` asserting, per ticket file, the exact multiset of `rule` ids listed above (use a helper `rulesOf(result, file)` returning sorted rule ids) and the reasons `contains sentinel "TODO"`, `vague wording "depends"`, `missing "## Plan"`, `## Requirements has 16 EARS lines; the limit is 15`. Generate the goldens with `npm test -- --project core snapshot -u -t "fixture lint-hygiene"` and `npm test -- --project core lint -u -t "fixture lint-hygiene"` (scoped `-u` only: sibling plans 03-03 and 03-04 write their own goldens in this wave), read `lint-hygiene.lint.json`, verify, then rerun without `-u`.

**Existing goldens that change in this task (`lint.heading-missing` is the only new row that fires on a Phase 2 fixture; it needs `frontmatter` defined, so untyped tickets gain nothing).** Regenerate each with `npm test -- --project core lint -u -t "fixture <name>"`, one fixture at a time, read the diff, and confirm it is exactly the delta below before running without `-u`. Any other new finding on an existing fixture is a finding for the SUMMARY, not a golden to accept:
- `frontmatter-errors.lint.json`: `MISMATCH.md` (bug, empty body) and `TYPING.md` (story, empty body) each gain five `lint.heading-missing` (`Intent`, `Requirements`, `Acceptance criteria`, `Open questions`, `Plan`), no line; the seven tickets without valid frontmatter gain nothing; `lint.id-mismatch` on `MISMATCH.md` stays.
- `body-edges.lint.json`: `CASE.md` and `DUP.md` gain two (`Open questions`, `Plan`; `## INTENT`, `##   requirements`, and `## Acceptance Criteria ##` count through `headingKey`); `FENCE.md` gains two (`Intent`, `Open questions`); `NOREQ.md` and `UNTERMINATED.md` gain four (`Requirements`, `Acceptance criteria`, `Open questions`, `Plan`; the fence in `UNTERMINATED.md` swallows its `## Requirements`); `TILDE.md` gains four (`Intent`, `Requirements`, `Open questions`, `Plan`). Eighteen new errors in total.
- `gherkin-shapes.lint.json`: `EMPTY-AC.md` gains three (`Intent`, `Requirements`, `Open questions`; it has `## Plan`); the other eight tickets gain four (those three plus `Plan`). Thirty-five new errors in total.
- `no-config.lint.json`, `bad-config.lint.json`, `config-syntax.lint.json`: the single story gains four (`Requirements`, `Acceptance criteria`, `Open questions`, `Plan`).
- `valid-build.lint.json` and `verification-edges.lint.json` do not change in this task: every typed ticket there has its headings, `LOGIN-1`'s assumption is confirmed and its `verified` entry matches `@ac-1`, and no existing fixture line carries a sentinel, placeholder, unchecked question, vague phrase, or oversize section. The gate runs both to prove it.

`npm run lint && npm run typecheck`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core lint -t "fixture (lint-hygiene|valid-build|verification-edges|frontmatter-errors|body-edges|gherkin-shapes|no-config|bad-config|config-syntax)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a golden reported obsolete or mismatched; or the `|core|` summary reports fewer than 9 fixture groups</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/load/sections.ts` contains `export const LIST_MARKER`
    - `packages/core/src/lint/rules.ts` contains the ids `lint.heading-missing`, `lint.tracker-empty`, `lint.tick-orphan`, `lint.assumption-unconfirmed`, `lint.sentinel`, `lint.open-question`, `lint.vague-wording`, `lint.intent-oversize`, `lint.requirements-oversize`, `lint.scenarios-oversize`; `grep -c "level: '" packages/core/src/lint/ticket.ts` prints 0
    - `packages/core/test/__golden__/lint-hygiene.lint.json` contains for `accord/tickets/HYGIENE.md`: 6 findings with rule `lint.sentinel`, 4 with `lint.vague-wording`, 1 `lint.open-question`, 1 `lint.tick-orphan` with `"pointer": "/verified/1"`, 1 `lint.assumption-unconfirmed` with `"pointer": "/assumptions/0/confirmed"`, 1 `lint.tracker-empty`; for `accord/tickets/HEADINGS.md` exactly 3 `lint.heading-missing` and no `line` on them; for `accord/tickets/SIZE.md` exactly 3 findings; for `accord/tickets/EPIC.md` exactly 1 finding with rule `lint.intent-oversize`
    - No finding in `lint-hygiene.lint.json` has a `reason` containing `placeholder text` (the comment line was stripped)
    - `grep -c "lint.heading-missing" packages/core/test/__golden__/frontmatter-errors.lint.json` prints 10, `body-edges.lint.json` 18, `gherkin-shapes.lint.json` 35, and each of `no-config.lint.json`, `bad-config.lint.json`, `config-syntax.lint.json` 4; `git diff --stat -- packages/core/test/__golden__/valid-build.lint.json packages/core/test/__golden__/verification-edges.lint.json` is empty at the end of this task
    - `npm test -- --project core lint -t "fixture lint-hygiene"` exits 0 and `git log -1 --format=%H` is unchanged
  </acceptance_criteria>
  <done>Ten hygiene, heading, vague-wording, and size rules fire from the table with the documented reasons and lines, pinned by a fixture and a golden.</done>
</task>

<task type="auto">
  <name>Task 2: Plan-tag and verification-notes rules with the NOTES, PLAN-EMPTY, CLEAN tickets</name>
  <read_first>
    - packages/core/src/lint/ticket.ts (Task 1 state) and packages/core/src/lint/rules.ts
    - packages/core/src/load/verification.ts line 7 (`BLOCK` tag regex to mirror for `### @ac-n`)
    - packages/core/src/load/sections.ts lines 25-31 (`###` lines belong to the enclosing `##` section)
    - .planning/phases/03-lint/03-RESEARCH.md "Code Examples: Plan step tags (D-74)" and "Verification Notes and Templates (FMT-10)"
  </read_first>
  <files>packages/core/src/lint/ticket.ts, packages/core/src/lint/rules.ts, packages/core/test/lint.test.ts, packages/core/test/fixtures/lint-hygiene/accord/tickets/HYGIENE.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/NOTES.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/PLAN-EMPTY.md, packages/core/test/fixtures/lint-hygiene/accord/tickets/CLEAN.md, packages/core/test/__golden__/lint-hygiene.lint.json, packages/core/test/__golden__/lint-hygiene.snapshot.json, packages/core/test/__golden__/valid-build.lint.json, packages/core/test/__golden__/verification-edges.lint.json, packages/core/test/__golden__/body-edges.lint.json</files>
  <action>
**Rules (in `lint/ticket.ts`):** a module-private helper `planItems(t)` returns `{ section, items }` where `section` is the `plan` section (undefined when absent) and `items` are its comment-stripped lines matching `LIST_MARKER` (nested items count). Skip every plan rule when `t.frontmatter` is undefined or `type === 'epic'` or the section is absent (`lint.heading-missing` covers absence).

- `planStepUntagged` (D-74): per item with no `AC` match → draft at `l.line`, reason `plan step carries no @ac-n tag`.
- `planEmpty` (D-74): `items.length === 0 && t.scenarios.length > 0` → draft at `section.line`, reason `ticket has scenarios but ## Plan has no step`.
- `planTagsDiffer` (D-74): skip when `items.length === 0`; plan set = every `AC` match across items; scenario set = `'@' + s.acTag` for scenarios with a tag; when the sets differ, one draft at `section.line`, reason `plan lacks ${a}; scenarios lack ${b}` where each side lists its missing tags sorted ascending by the number after `@ac-`, joined by a space, or the word `nothing`.
- `noteOrphan` (D-70): section with key `verification notes`; comment-stripped lines matching `NOTE_BLOCK` whose tag matches no `s.acTag` → draft at `l.line`, reason `note block "@${tag}" matches no scenario`.
- `notesNotLast` (D-70): that section exists and is not `t.sections[t.sections.length - 1]` → draft at `section.line`, reason `"## Verification notes" must be the last section`.

**`rules.ts`:** append `lint.plan-step-untagged`, `lint.plan-tags-differ`, `lint.plan-empty`, `lint.note-orphan`, `lint.notes-not-last`, all `warning`, both profiles. The table now has seventeen rows.

**Fixture tickets:**
- `NOTES.md` (story): full five headings in D-07 order with `## Verification notes` inserted between `## Open questions` and `## Plan`; `## Requirements` holds one valid EARS line (`- WHEN ... the system SHALL ...`); one scenario `@ac-1 @test:t#a`; notes section contains `### @ac-1 Đăng nhập` with one line of text, a comment, and `### @ac-9 Không có` with one line; also a line `### @AC-1 nope` (not a block, case-sensitive). `## Plan`: `- Làm @ac-1`. Expected: `lint.notes-not-last` at the notes heading line, `lint.note-orphan` at the `@ac-9` line, nothing else.
- `PLAN-EMPTY.md` (story): five headings, `## Requirements` holding one valid EARS line; one scenario `@ac-1 @test:t#a`; `## Plan` holding only the template comment. Expected: `lint.plan-empty` at the Plan heading line, nothing else.
- `CLEAN.md` (story; `verified: [ac-1]`; `assumptions:` one `confirmed: true`): Intent two lines; Requirements two valid EARS lines (`WHEN ... the system SHALL ...`, `IF ... THEN the system SHALL ...`); AC with `@ac-1 @test:t#a` and `@ac-2 @ui` scenarios; Open questions `- [x] Đã rõ`; Plan `- Bước một @ac-1` and `- Bước hai @ac-2`; `## Verification notes` last, containing `### @ac-1 Đăng nhập` and one line. Expected: zero findings.
- `HYGIENE.md` gains, from Task 1's plan section, `lint.plan-step-untagged` at the `- Viết test` line and `lint.plan-tags-differ` at the Plan heading with reason exactly `plan lacks nothing; scenarios lack @ac-2`.

Extend the pinned `describe` in `lint.test.ts`: `CLEAN.md` has no findings; `NOTES.md` rules are exactly `['lint.note-orphan', 'lint.notes-not-last']`; `PLAN-EMPTY.md` is exactly `['lint.plan-empty']`; `HYGIENE.md` includes the differ reason string above. Add one unit-style `it` in the same file that builds a one-row `RULES`-shaped array with `profiles: ['maintain']` and asserts `lintSnapshot`'s filter would exclude it under a `build` config: since `RULES` is a module constant, assert instead that every row of `RULES` has `profiles` deep-equal to `['build', 'maintain']` (the CORE-04 matrix in v0.1), that ids are unique, and that every id matches `/^lint\.[a-z-]+$/` (D-59). Do not pin the row count: plans 03-05 and 03-06 add rows. Regenerate the two `lint-hygiene` goldens with the scoped `-u` commands from Task 1, read them, then run without `-u`.

**Existing goldens that change in this task (`lint.plan-empty` is the only new row that fires on a Phase 2 fixture: no existing fixture has a `## Plan` list item or a `## Verification notes` section).** Regenerate each with `npm test -- --project core lint -u -t "fixture <name>"`, read the diff, and confirm it is exactly the delta below before running without `-u`; any other change is a finding for the SUMMARY:
- `valid-build.lint.json`: `LOGIN-1.md` gains one `lint.plan-empty` at line 50 (its `## Plan` holds only the template comment; two scenarios); `warnings` goes from 2 to 3, `errors` stays 0.
- `verification-edges.lint.json`: `A.md` gains `lint.plan-empty` at line 31 and `B.md` at line 26 (empty `## Plan`, scenarios present).
- `body-edges.lint.json`: `FENCE.md` gains one `lint.plan-empty` at line 32 (its `## Plan` holds a gherkin fence and no list item; one scenario). `EMPTY-AC.md` in `gherkin-shapes` has `## Plan` but no scenario and stays silent, so that golden is unchanged from Task 1.

`npm run lint && npm run typecheck`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core lint -t "fixture (lint-hygiene|valid-build|verification-edges|frontmatter-errors|body-edges|gherkin-shapes|no-config|bad-config|config-syntax)" && npm test -- --project core snapshot -t "fixture lint-hygiene"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a golden reported obsolete or mismatched; or the `|core|` summary for lint reports fewer than 9 fixture groups</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/lint/rules.ts` contains 17 `id:` entries including `lint.plan-step-untagged`, `lint.plan-tags-differ`, `lint.plan-empty`, `lint.note-orphan`, `lint.notes-not-last`
    - `packages/core/test/__golden__/lint-hygiene.lint.json` contains the literal reason `plan lacks nothing; scenarios lack @ac-2`, exactly one `lint.notes-not-last`, exactly one `lint.note-orphan` whose reason contains `@ac-9`, exactly one `lint.plan-empty`, and no finding with `"file": "accord/tickets/CLEAN.md"`
    - `packages/core/test/__golden__/lint-hygiene.lint.json` has `"errors": 3` (the three heading-missing findings) and every other finding `"level": "warning"`
    - `grep -c "lint.plan-empty" packages/core/test/__golden__/valid-build.lint.json` prints 1 (with `"line": 50` and `"warnings": 3`), `verification-edges.lint.json` 2, `body-edges.lint.json` 1, and `gherkin-shapes.lint.json` 0
    - `npm test -- --project core lint -t "fixture (lint-hygiene|valid-build|verification-edges|frontmatter-errors|body-edges|gherkin-shapes|no-config|bad-config|config-syntax)"` exits 0 and `git log -1 --format=%H` is unchanged
  </acceptance_criteria>
  <done>ROADMAP criterion 6 (plan tag sets named per side, equal sets silent, empty plan warns) and D-70 have executable assertions; the reference-clean ticket proves no false positives.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Functions in `lint/ticket.ts`: `headingMissing`, `trackerEmpty`, `tickOrphan`, `assumptionUnconfirmed`, `sentinel`, `openQuestion`, `vagueWording`, `intentOversize`, `requirementsOversize`, `scenariosOversize`, `planStepUntagged`, `planTagsDiffer`, `planEmpty`, `noteOrphan`, `notesNotLast`; private helper `planItems`
- Rule ids: `lint.heading-missing`, `lint.tracker-empty`, `lint.tick-orphan`, `lint.assumption-unconfirmed`, `lint.sentinel`, `lint.open-question`, `lint.vague-wording`, `lint.intent-oversize`, `lint.requirements-oversize`, `lint.scenarios-oversize`, `lint.plan-step-untagged`, `lint.plan-tags-differ`, `lint.plan-empty`, `lint.note-orphan`, `lint.notes-not-last`
- Export added: `LIST_MARKER` from `load/sections.ts`
- Fixture: `packages/core/test/fixtures/lint-hygiene/` (config + HYGIENE, SIZE, HEADINGS, EPIC, NOTES, PLAN-EMPTY, CLEAN)
- Goldens: `lint-hygiene.lint.json`, `lint-hygiene.snapshot.json`; regenerated `frontmatter-errors`, `body-edges`, `gherkin-shapes`, `no-config`, `bad-config`, `config-syntax`, `valid-build`, `verification-edges` `.lint.json` (heading-missing and plan-empty deltas stated in the tasks)
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

| Source | Item | Disposition |
|---|---|---|
| Owner decision (RESEARCH Open Question 1, resolved 2026-09-14) | `lint.vague-wording` is in scope with exactly the seven roadmap phrases; CONTEXT.md's deferred list (`appropriate`, `reasonable`, `etc`) stays deferred | Owner-confirmed; roadmap-sourced, not a CONTEXT.md decision |
| Owner decision (RESEARCH Open Question 2, resolved 2026-09-14) | `TBD` yields both `lint.sentinel` and `lint.vague-wording` | Owner-confirmed; two rules, two ids, no cross-rule dedupe |
| Criterion 7 wording | One `lint.vague-wording` finding per line (first phrase), while `lint.sentinel` is one per occurrence (D-73 says "one per occurrence") | Chosen; the line is what the BA edits |
| Criterion 7 wording | "answered open question" means an item matching `- [x]` or `- [X]`; unchecked items are not scanned for vagueness | Chosen |
| D-73 | Lines before the first `##` heading are not stored by the scanner, so a sentinel there is not reported | Flagged; Phase 2 design, not changed here |
| D-74 | Nested list items under `## Plan` count as steps; a numbered `1.` item counts | Chosen from `LIST_MARKER` |
| LINT-08 ordering edge | Tags in the `plan-tags-differ` reason are listed ascending by `n` on each side | Chosen for determinism |
| LINT-07 (unclassified edge) | Limits are exclusive: 5, 15, and 5 are silent; 6, 16, and 6 warn; a `###` sub-heading line inside Intent counts as a line | Chosen; thresholds are constants per Claude's Discretion |
| FMT-10 adjacency edge | Duplicate `### @ac-n` blocks are not linted in v0.1 | Backstop truth; GATE-10 territory |
| D-22 | `tracker: {}` reason wording | Chosen |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body text → rule regexes | Untrusted Markdown from any contributor, any language |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Denial of service | oversize tickets (R-02-04, R-02-07 carry-over) | low | mitigate | LINT-07 oversize warnings surface size discipline; hard limits remain out of scope (warning-only per D-57) |
| T-03-06 | Tampering | placeholders or sentinels hidden inside HTML comments so lint stays silent | low | accept | By design (D-73: template guidance lives in comments); a reviewer reads the file |
| T-03-01 | Denial of service | pathological body lines against `WORD`, `PLACEHOLDER`, `VAGUE` | low | mitigate | Every regex is per-line with bounded character classes and no nested quantifier; `PLACEHOLDER` excludes `<` and `>` inside |
| T-03-05 | Information disclosure | reasons echoing body text | low | mitigate | Reasons echo the matched token, the tag, the assumption text, or the scenario name only |
| T-03-SC | Tampering | npm installs | high | mitigate | No package added |
</threat_model>

<verification>
- Task gates are the `<automated>` commands; the wave-end check is `npm test -- --project core` green once 03-03 and 03-04 finish.
- The `-t` filter names the nine fixtures whose lint goldens this plan owns or proves unchanged; `lint-report` is 03-03's and is deliberately excluded while that sibling is mid-write. None of the eight regenerated goldens is touched by 03-03 or 03-04.
</verification>

<success_criteria>
- ROADMAP criteria 5 (sentinels, open questions, assumptions, sizes), 6 (plan tags), 7 (vague wording), and the tick-orphan half of 4 have goldens and pinned assertions.
- D-70, D-71, D-73, D-74 each have a task step and a fixture ticket.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/03-lint/03-02-SUMMARY.md` when done. Do not commit.
</output>
