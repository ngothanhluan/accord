---
phase: 01-workspace-and-formats
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/test/purity.test.ts
  - packages/core/test/fixtures/purity/tsconfig.json
  - packages/core/test/fixtures/purity/probe.ts
  - packages/core/test/bundle.test.ts
autonomous: true
requirements: [CORE-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A file under `packages/core/src/` that imports `node:fs` or bare `path` is rejected by ESLint with rule `@typescript-eslint/no-restricted-imports` (layer A) AND by `tsc -p packages/core` with TS2307 (layer B), so `npm run check` and CI both go red (CORE-01, success criterion 3)"
    - "Both the `node:`-prefixed and the bare built-in specifier forms are rejected: `node:fs`, `fs`, `fs/promises`, `path` each produce an ESLint error (CORE-01 adjacency edge — the two spellings of the same module are treated identically, verification: explicit)"
    - "A file under `packages/core/src/` other than `validate/ajv.ts` that imports `ajv` or `ajv/*` is rejected by ESLint rule `no-restricted-imports` (D-20 seam)"
    - "The built `packages/core/dist/index.js` contains no import or require of a `node:` specifier or of `fs`, `path`, `child_process`, `os`, `url`, `crypto`; `dist/index.d.ts` exists and declares `validate` and `Finding`"
    - "core's `package.json` has no `@types/node` in any dependency block and `packages/core/tsconfig.json` has `\"types\": []`"
    - "The tsc probe compiles a committed fixture (`packages/core/test/fixtures/purity/probe.ts`) through its own `tsconfig.json` that extends core's guarded `tsconfig.json`; the test writes nothing into `packages/core/src/`, so wave-2 plans running `npm run lint` / `npm run typecheck` in parallel never see a probe file"
  artifacts:
    - path: "packages/core/test/purity.test.ts"
      provides: "source-level guard proof: ESLint API probe (node built-ins + ajv seam) and tsc probe"
      min_lines: 40
    - path: "packages/core/test/fixtures/purity/tsconfig.json"
      provides: "probe compiler config: extends core's tsconfig.json (inherits types: []), includes only probe.ts"
      contains: "\"extends\": \"../../../tsconfig.json\""
    - path: "packages/core/test/fixtures/purity/probe.ts"
      provides: "the impure input for the tsc probe (committed, never written by the test)"
      contains: "node:fs"
    - path: "packages/core/test/bundle.test.ts"
      provides: "artifact-level guard proof: dist has no built-in imports; d.ts present; manifest shape; missing-build message proven against an empty temp dir (dist is only read, never deleted)"
      min_lines: 20
  key_links:
    - from: "packages/core/test/purity.test.ts"
      to: "eslint.config.js"
      via: "ESLint API with cwd = repo root, lintText with a filePath under packages/core/src"
      pattern: "new ESLint\\("
    - from: "packages/core/test/purity.test.ts"
      to: "packages/core/test/fixtures/purity/tsconfig.json"
      via: "execFileSync(process.execPath, [tsc, '-p', 'packages/core/test/fixtures/purity'])"
      pattern: "test/fixtures/purity"
    - from: "packages/core/test/fixtures/purity/tsconfig.json"
      to: "packages/core/tsconfig.json"
      via: "extends — inherits guard layer B (types: [])"
      pattern: "\\.\\./\\.\\./\\.\\./tsconfig\\.json"
    - from: "packages/core/test/bundle.test.ts"
      to: "packages/core/dist/index.js"
      via: "readFileSync of the built bundle (read-only; the file never deletes or writes dist/)"
      pattern: "\\.\\./dist/"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "`packages/core/src/**` must never import a Node built-in, with or without the `node:` prefix, and must never depend on a package whose `.d.ts` references `@types/node`"
      status: resolved
      verification: test
    - statement: "The tests must never spawn `npm`, `npx`, or a `.cmd` shim; the only spawned executable is `process.execPath`"
      status: resolved
      verification: test
---

<objective>
**As a** developer of accord, **I want to** see the build go red the moment any core file imports a Node built-in, **so that** the same core is guaranteed to run on the MCP host, which has no filesystem.

Purpose: Plan 01-01 installed both guard layers (ESLint rule, `types: []`). This plan proves they bite, from the source side and from the artifact side, with tests that run in `npm test` on both OSes. Success criterion 3 ("the build fails when any file under core imports a `node:*` module") becomes a green/red signal instead of a belief.

Output: `packages/core/test/purity.test.ts` (ESLint API probe, `tsc` probe), the committed probe fixture `packages/core/test/fixtures/purity/{tsconfig.json,probe.ts}`, `packages/core/test/bundle.test.ts` (dist and manifest checks).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-formats/01-CONTEXT.md
@.planning/phases/01-workspace-and-formats/01-RESEARCH.md
@.planning/phases/01-workspace-and-formats/01-SKELETON.md
@eslint.config.js
@packages/core/tsconfig.json
@packages/core/tsconfig.test.json
@packages/core/package.json

<interfaces>
<!-- From Plan 01-01. Use directly; no exploration needed. -->

eslint.config.js — two blocks on `files: ['packages/core/src/**/*.ts']`:
- rule id `@typescript-eslint/no-restricted-imports`: `paths` = bare built-in names (`fs`, `fs/promises`, `path`, `child_process`, `os`, `url`, `crypto`, `process`, `util`, `stream`, `events`, `buffer`, `module`, `worker_threads`, `net`, `http`, `https`, `tty`, `readline`, `zlib`, `assert`), `patterns: [{ group: ['node:*'] }]`
- rule id `no-restricted-imports` (base), `ignores: ['packages/core/src/validate/ajv.ts']`: `paths: [{ name: 'ajv' }]`, `patterns: ['ajv/*']`

packages/core/tsconfig.json: `{ extends: ../../tsconfig.base.json, compilerOptions: { types: [] }, include: ["src", "schemas/*.json"] }`
packages/core/tsconfig.test.json: `types: ["node"]`, include `["src", "test", "schemas/*.json"]` — so these test files MAY import `node:fs`, `node:child_process`, `node:url`, `node:path`.

Root scripts (package.json): `lint` = `eslint .`; `typecheck` = `tsc -p packages/core && tsc -p packages/core/tsconfig.test.json && tsc -p packages/cli`; `test` = `vitest run`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-level guard proof — ESLint API probe and tsc probe</name>
  <read_first>
    - eslint.config.js (the rule ids and file globs the probes must trigger)
    - packages/core/tsconfig.json and packages/core/tsconfig.test.json (what is guarded, and what the test may import)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md — Pattern 4 (verified error texts: `'node:fs' import is restricted from being used by a pattern`, `'path' import is restricted from being used`, TS2307 for both under `types: []`), Validation Architecture rows CORE-01 (a) and (b), Anti-Patterns (`types: ["node"]` in base)
    - .planning/phases/01-workspace-and-formats/01-CONTEXT.md D-20 (ajv seam), Claude's Discretion "Purity guard, two layers"
  </read_first>
  <files>packages/core/test/purity.test.ts, packages/core/test/fixtures/purity/tsconfig.json, packages/core/test/fixtures/purity/probe.ts</files>
  <action>
Create `packages/core/test/purity.test.ts` (vitest; imports allowed: `vitest`, `eslint`, `node:child_process`, `node:fs`, `node:url`, `node:path` — this file is typechecked by `tsconfig.test.json`). Resolve `repoRoot` as `fileURLToPath(new URL('../../../', import.meta.url))`.

Probe A — ESLint layer (CORE-01 layer A, D-20 seam). Import `{ ESLint }` from `eslint`; construct `new ESLint({ cwd: repoRoot })` once (it loads the root flat config). Use `lintText(code, { filePath })` with a virtual `filePath` that does NOT exist on disk but sits under `packages/core/src/` (for example `<repoRoot>/packages/core/src/__eslint_probe__.ts`) so the config blocks for that glob apply; `lintText` needs no real file and nothing is ever written there. Cases, each its own `it`:
  1. `import { readFileSync } from 'node:fs'; export const x = readFileSync;` → exactly one message with `ruleId` `@typescript-eslint/no-restricted-imports` and `severity` 2.
  2. `import path from 'path'; export const p = path;` → same rule id (bare specifier form).
  3. `import { readFile } from 'fs/promises'; export const r = readFile;` → same rule id (subpath bare form; adjacency: every spelling of a built-in is banned).
  4. `import { Ajv2020 } from 'ajv/dist/2020.js'; export const a = Ajv2020;` with a filePath under `packages/core/src/model/` → exactly one message with `ruleId` `no-restricted-imports` (base rule; the seam block). If this case reports zero messages, the `(draft)` pattern `ajv/*` does not reach the two-level specifier: apply the fallback recorded in 01-01 Task 2 step 3 (`patterns: [{ group: ['ajv', 'ajv/**'], message }]`) in `eslint.config.js` and re-run — do not weaken the assertion.
  5. The same ajv snippet with `filePath` = `<repoRoot>/packages/core/src/validate/ajv.ts` → zero messages with `ruleId` `no-restricted-imports` (the seam file is exempt; other rules may still be silent on this clean snippet).
  6. A clean snippet `export const ok = 1;` → zero messages.
Assert on `results[0].messages` filtered by `ruleId`, not on message text, so wording changes in typescript-eslint do not break the test. If ESLint reports the file as ignored (a `warning` about ignore patterns), the test must fail loudly with that message — it means the glob or cwd is wrong.

Probe B — tsc layer (CORE-01 layer B). Two committed fixture files, and the test performs no writes: `packages/core/test/fixtures/purity/probe.ts` containing exactly `import { readFileSync } from 'node:fs';` and `export const x = readFileSync;`, and `packages/core/test/fixtures/purity/tsconfig.json` (strict JSON, no comments) with `"extends": "../../../tsconfig.json"` — core's own guarded config, so the probe compiles under exactly core's `compilerOptions` including `types: []` — and `"include": ["probe.ts"]`, which replaces core's `include`, so nothing under `src/` is involved. The test runs `execFileSync(process.execPath, [tscBin, '-p', 'packages/core/test/fixtures/purity'], { cwd: repoRoot, encoding: 'utf8', stdio: 'pipe' })` inside `expect(() => ...).toThrow()` and asserts the thrown error's `stdout` contains `TS2307` and `node:fs`. `tscBin` = `<repoRoot>/node_modules/typescript/bin/tsc` (spawned with `process.execPath`, never `tsc.cmd` or `npx`). Mark this `it` with a 60 s timeout (`it(name, fn, 60_000)`). Add a second `it` that pins the fixture wiring so nobody turns the probe green by editing it: parse the fixture `tsconfig.json` and expect `extends` to equal `../../../tsconfig.json` and `include` to deep-equal `['probe.ts']`; read `probe.ts` and expect it to contain `'node:fs'`. Notes: `packages/core/tsconfig.test.json` also sees `probe.ts` through its `test` include, where `node:fs` resolves under `types: ["node"]`, so `npm run typecheck` stays green; ESLint lints the fixture under the `globals.node` test block only (the purity rule is scoped to `packages/core/src/**`), so `npm run lint` stays green. Because the fixture lives outside `packages/core/src/` and is compiled by its own tsconfig, Plans 01-04 and 01-05, which run `npm run lint` / `npm run typecheck` in the same wave, can never observe a probe.

Also assert statically: `JSON.parse(readFileSync('packages/core/tsconfig.json')).compilerOptions.types` deep-equals `[]`, and core's `package.json` has no key `@types/node` under `dependencies`, `devDependencies`, `peerDependencies`, or `optionalDependencies`.

Run `npm test -- --project core purity` and `npm run typecheck`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core purity && npm run typecheck</automated>
    <fails_when>non-zero exit; or vitest prints `No test files found`; or the summary shows fewer than 9 tests passed for `|core|`; or the tsc probe's captured stdout lacks `TS2307` (a wrong `extends` path surfaces as TS5083 instead, meaning the fixture did not inherit `types: []`)</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/purity.test.ts` contains `new ESLint(`, `lintText(`, `@typescript-eslint/no-restricted-imports`, `no-restricted-imports`, `TS2307`, `process.execPath`, and `test/fixtures/purity`
    - The file does not contain the strings `'npm'`, `'npx'`, or `.cmd`, and does not call `writeFileSync` (the test writes nothing)
    - `packages/core/test/fixtures/purity/tsconfig.json` parses as JSON with `extends` equal to `../../../tsconfig.json` and `include` equal to `["probe.ts"]`; `packages/core/test/fixtures/purity/probe.ts` contains `node:fs`
    - `node node_modules/typescript/bin/tsc -p packages/core/test/fixtures/purity` exits non-zero and prints a line containing `TS2307`
    - `npm test -- --project core purity` reports at least 9 passing tests and 0 failing
    - `ls packages/core/src` lists no file whose name contains `probe` after the run (nothing is written into `src/`)
    - `npm run lint` and `npm run typecheck` still exit 0 (the fixture is a clean file under the `globals.node` test block, and `tsconfig.test.json` resolves its `node:fs` under `types: ["node"]`)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Both purity layers are proven red on impure input and green on clean input by tests that run in `npm test`; the ajv seam is enforced; the tsc probe is a committed fixture outside `src/`, so the test writes nothing and cannot disturb a parallel plan's lint or typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact-level guard proof — the built core bundle and manifest</name>
  <read_first>
    - packages/core/package.json (`exports`, `files`, dependencies — the shape under test)
    - packages/core/tsdown.config.ts (`platform: 'neutral'`, `dts: true`, `fixedExtension: false`)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md — Pattern 4 last paragraph (dist grep as the third check), Code Example 4 verified log, Pattern 1 "Hoisting side-effect" (ajv 8 lives under `packages/core/node_modules/ajv`; never assume root `node_modules/ajv`)
    - .planning/research/STACK.md Decision 6 (`exports`, `files`, `type: module`)
  </read_first>
  <files>packages/core/test/bundle.test.ts</files>
  <action>
Create `packages/core/test/bundle.test.ts` (imports: `vitest`, `node:fs`, `node:os`, `node:path`, `node:url` — allowed here because `tsconfig.test.json` applies `types: ["node"]`). Resolve `distDir = fileURLToPath(new URL('../dist/', import.meta.url))`.

Define a small helper `assertBuilt(dir: string): void` that checks `existsSync(join(dir, 'index.js'))` and `existsSync(join(dir, 'index.d.ts'))` and throws an `Error` whose message starts with `run npm run build first` and names the missing file(s) (CI runs `build` before `test`; locally use `npm run check`). Every later `it` reads from `distDir`; the file only ever reads `packages/core/dist/`, it never deletes or writes it.

1. `it('is built')`: `assertBuilt(distDir)` does not throw.
2. `it('names the missing build when dist is empty')`: create an empty directory with `mkdtempSync(join(tmpdir(), 'accord-empty-dist-'))`, then `expect(() => assertBuilt(empty)).toThrow('run npm run build first')`; remove the temp dir afterwards with `rmSync(empty, { recursive: true, force: true })`. This is the proof that a missing build is a clear red, and it touches nothing under the repo.
3. `it('bundle imports no Node built-in')`: read `join(distDir, 'index.js')` as UTF-8 and assert none of these regexes match: `/from\s*["']node:/`, `/require\(\s*["']node:/`, `/from\s*["'](fs|fs\/promises|path|child_process|os|url|crypto|process|util|stream|events|buffer|module|worker_threads|net|http|https|tty|readline|zlib|assert)["']/`, `/import\(\s*["']node:/`. Also assert the bundle DOES contain `ajv/dist/2020` (ajv stays external as a declared dependency; if it ever disappears, something inlined ajv and its `require`s, which would hide a built-in behind a bundled shim).
4. `it('declarations expose the seam')`: `join(distDir, 'index.d.ts')` contains `validate`, `SchemaId`, `schemaIds`, and `Finding`, and does not contain `ajv` in any exported type name (the public types must not leak the implementation — `ErrorObject` must not appear).
5. `it('manifest shape')`: parse `../package.json`; `exports['.'].types` === `./dist/index.d.ts`, `exports['.'].default` === `./dist/index.js`, `exports['./schemas/*']` === `./schemas/*` (D-19), `files` contains `dist`, `schemas`, `templates`, `type` === `module`, `name` === `@accord-dev/accord-core`, `version` === `0.1.0`, and each listed schema in `schemas/` (`ticket`, `verification`, `config`) exists as `<name>.schema.json`.

Wave note: Plan 01-04 Task 2 also runs `npm run build` in this wave, so both plans rebuild `packages/core/dist/`. `npm run build` is idempotent, and every assertion above holds for the bundle both before and after 01-04 adds the `templates` export, so a concurrent rebuild only mattered for a destructive step against `dist/`, and this task has none (a read that lands mid-write shows up as `run npm run build first` and is green on re-run).

Run `npm run build && npm test -- --project core bundle`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run build && npm test -- --project core bundle</automated>
    <fails_when>non-zero exit; or vitest prints `No test files found`; or fewer than 5 tests pass for `|core|`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/bundle.test.ts` contains `../dist/`, `index.d.ts`, `run npm run build first`, `mkdtempSync(`, `tmpdir()`, `./schemas/*`, and `ajv/dist/2020`
    - `npm test -- --project core bundle` reports 5 passing tests
    - The missing-build message is proven inside the suite: the `it` that points `assertBuilt` at an empty `mkdtempSync` directory passes with `toThrow('run npm run build first')`; `packages/core/dist` is never deleted, and the only path the file creates or removes is that temp dir
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The shipped artifact is proven pure and correctly shaped by a test that runs on both OSes; a missing build is a clear red, not a confusing error, proven against an empty temp dir without touching `packages/core/dist`.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `packages/core/test/purity.test.ts` — ESLint API probe (rule ids `@typescript-eslint/no-restricted-imports`, `no-restricted-imports`) and `tsc` probe (TS2307) for CORE-01
- `packages/core/test/fixtures/purity/tsconfig.json` and `probe.ts` — the committed tsc-probe fixture (extends core's `tsconfig.json`; includes only `probe.ts`)
- `packages/core/test/bundle.test.ts` — dist purity, declaration surface, manifest shape

No new exported symbols; no new config files.
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (spec-less probe rows surfaced, not silently dropped)

| Requirement | Probe | Disposition |
|---|---|---|
| CORE-01 | adjacency — When two things are exactly equal or just touch, do they merge, collide, or separate? | Resolved explicit (Task 1 cases 1–3): `node:fs`, `fs`, `fs/promises`, and `path` are all rejected; the prefixed and bare spellings of a built-in are treated as the same banned module. |
| CORE-01 | ordering — When elements compare equal, is output order specified and stable? | Resolved explicit (see 01-03 must_haves): `validate` returns findings in ajv schema-evaluation order, identical across runs; the goldens pin that order. This plan's tests assert on filtered rule ids, not on message order. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → `packages/core/test/fixtures/purity` | Task 1's tsc probe compiles a committed impure fixture through its own tsconfig; nothing under `src/` is written or read beyond core's `tsconfig.json` via `extends` |
| dependency `.d.ts` → core typecheck | A transitive `/// <reference types="node" />` would re-admit Node globals |
| test process → `packages/core/dist/` | Task 2 reads the built bundle; the missing-build path is exercised against an `mkdtempSync` directory, so the real `dist/` (also rebuilt by 01-04 in this wave) is never deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of privilege | transitive `@types/node` via a dependency `.d.ts` | medium | mitigate | Layer B `types: []` + Task 1 tsc probe + Task 2 dist grep; RESEARCH verified ajv/yaml/gherkin carry no node reference |
| T-01-08 | Denial of service | a probe file inside `packages/core/src/` breaking `build`/`lint`/`typecheck` for every developer and for the parallel wave-2 plans | low | mitigate | The probe is a committed fixture outside `src/` compiled by its own tsconfig; the test performs no writes; a second `it` pins the fixture wiring (`extends`, `include`, `node:fs`) |
| T-01-09 | Tampering | test spawning a shell shim (`tsc.cmd`) that could be shadowed on PATH | low | mitigate | Spawn only `process.execPath` with the absolute `node_modules/typescript/bin/tsc` path; never `npx`/`npm`/`.cmd` |
| T-01-16 | Denial of service | a test deleting `packages/core/dist/` while the parallel wave-2 plan 01-04 builds or typechecks against it | low | mitigate | `bundle.test.ts` only reads `dist/`; the missing-build message is proven against an empty temp dir; `npm run build` is idempotent, so concurrent rebuilds converge on the same output |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; pins and lockfile from 01-01 unchanged |
</threat_model>

<verification>
- `npm test -- --project core purity` and `npm test -- --project core bundle` green on Windows locally; CI runs them on both OSes.
- The missing-build message `run npm run build first` is proven by the in-suite empty-temp-dir case; nothing in this plan deletes `packages/core/dist`.
</verification>

<success_criteria>
- CORE-01 / success criterion 3: an impure core import is proven to fail ESLint and tsc, and the built bundle is proven free of built-in imports, all inside `npm test`.
- D-20: an ajv import outside `validate/ajv.ts` is proven to fail ESLint.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-and-formats/01-02-SUMMARY.md` when done. Do not commit.
</output>
