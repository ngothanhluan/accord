---
phase: 07-scaffolding-and-example-repo
plan: 11
type: execute
wave: 3
depends_on: ["07-09", "07-10"]
files_modified:
  - packages/core/templates/ticket-build.md
  - packages/core/templates/ticket-maintain.md
  - packages/core/templates/epic.md
  - packages/core/src/generated/templates.ts
  - packages/core/test/templates.test.ts
  - packages/core/test/__golden__/ticket-build.verified-empty.md
  - packages/cli/test/new-ticket.test.ts
autonomous: true
gap_closure: true
requirements: [CLI-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No file under `packages/core/templates/` names another tool, plugin, harness, or planning system — the five offending lines carry a neutral example URL and neutral guidance prose instead (the hard `.claude/CLAUDE.md` constraint, machine-held)"
    - "The `deniedNames` scan 07-04 built covers `packages/core/templates/` as well as the four Phase 7 text surfaces and the rendered skill bodies, so a reintroduction on any shipped template turns a test red instead of shipping silently"
    - "The widened scan is guarded against itself: a probe carrying a denied name is asserted to be reported before the real surfaces are asserted clean, so a scan that silently stopped matching cannot read as a pass"
    - "`accord new ticket` still renders the build template under `profile: build` and the maintain template under `profile: maintain`, distinguished by an assertion on the one sentence that differs between them"
    - "The example URL on the three `design:` comment lines still satisfies `ticket.schema.json`'s `design` pattern `^https://`, so a reader who uncomments it and fills it in has a schema-valid ticket"
    - "`packages/core/src/generated/templates.ts` is regenerated in the same change, and a further `npm run gen` leaves every file under `packages/core/src/generated/` byte-identical — the drift test cannot be green over a stale module"
    - "SKILL-04's suite and CLI-03's pointer-block scan both stay green, proving the shared `deniedNames` helper still guards the surfaces it already guarded. Both are regression evidence for requirements this plan does not deliver — the requirement it delivers against is CLI-01, the templates `accord init` ships and `accord new ticket` renders"
  artifacts:
    - packages/core/templates/ticket-build.md
    - packages/core/templates/ticket-maintain.md
    - packages/core/templates/epic.md
    - packages/core/test/templates.test.ts
  key_links:
    - "the drift case in `packages/core/test/templates.test.ts` binds the generated `templates` record byte-for-byte to `packages/core/templates/`, which is what makes a scan over the generated record a scan over the directory — and what stops a new template file slipping past the scan without `npm run gen`"
    - "`packages/core/test/helpers/denied.ts` holds one list and one scan for every shipped text surface (07-04); this plan adds a caller, never a second list"
    - "`packages/core/test/__golden__/ticket-build.verified-empty.md` is a `toMatchFileSnapshot` golden of the build template taken by `packages/core/test/write.test.ts:55`, so it moves whenever the template does"
  prohibitions:
    - statement: "the naming constraint must never be satisfied by an exception list — a denied name is removed, never allowlisted per file or per line; a written carve-out for example URLs was put to the owner and rejected"
      status: unverified
      verification: judgment
    - statement: "the widened scan must never be narrowed to make a future addition pass — a new shipped template that trips it is a prose problem, not a scan problem"
      status: unverified
      verification: judgment
    - statement: "accord must never change what a template asks a BA to think about in order to remove a name — the guidance keeps its meaning and loses only the vendor"
      status: unverified
      verification: judgment
---

<user_story>
**As a** team that receives accord's ticket templates,
**I want to** read guidance that names no product I may not have,
**so that** the instruction is about my design reference rather than about someone else's tool.

*Note on provenance:* the ROADMAP `**Goal:**` line for Phase 7 is prose, not a user story; 07-01-PLAN.md
set the precedent of deriving a per-plan story from the criterion the plan serves. The phase-level story
was not invented here.
</user_story>

<objective>
Gap 3 (F-2, status `partial`): five lines across three shipped ticket templates name a design tool, in
example URLs and in guidance prose — `packages/core/templates/ticket-build.md` lines 13 and 32,
`packages/core/templates/ticket-maintain.md` lines 13 and 33, `packages/core/templates/epic.md` line 13.
These files are inside the published `files:` list and are what `accord new ticket` renders. This
breaches the hard `.claude/CLAUDE.md` constraint "nothing accord ships names another tool", and the
`deniedNames` scan 07-04 added covers the four text surfaces Phase 7 introduced plus the rendered skill
bodies — but not `packages/core/templates/`, so nothing detects it. The finding has now gone unruled
across three plans.

**The owner ruled both halves, and they are binding:**

1. Replace the named tool with a **neutral example URL** on all five lines in the three templates, and
   update the **two `new-ticket.test.ts` assertions** that pin that prose.
2. **Widen the `deniedNames` scan to cover `packages/core/templates/`**, so a reintroduction turns a test
   red instead of shipping silently.

A written carve-out for example URLs was explicitly **rejected**: a constraint with an exception is one
nobody can enforce later. Do not plan one, do not add one, do not propose one in the summary.

Purpose: the constraint becomes machine-held on every shipped surface rather than on four of five.
Output: three edited templates, one regenerated module, two updated assertions, two regenerated goldens,
and one new scan case.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/07-scaffolding-and-example-repo/07-CONTEXT.md
@.planning/phases/07-scaffolding-and-example-repo/07-VERIFICATION.md
@.planning/phases/07-scaffolding-and-example-repo/07-04-SUMMARY.md
</context>

<surfaced_assumptions>
- **A-36 — the scan's surface is the generated record, and the drift case is what makes that a scan of
  the directory.** `packages/core/test/templates.test.ts` already imports the generated `templates` record
  and already asserts, in its `generated module matches templates/ (drift)` case, that the record's key
  set and every value equal the files on disk. Scanning the record therefore scans the directory, and it
  scans the exact bytes a consumer receives after `npm install` — which the directory itself is not,
  because `packages/core/templates/` is not published from the CLI package. A second `readdirSync` walk
  in the same file would be a second answer to a question the drift case already answers.
  *Alternative considered:* scanning `templatesDir` off disk. Rejected for the reason above; if the drift
  case is ever removed, this decision must be revisited in the same change.

- **A-37 — the scope is `packages/core/templates/` and nothing else.** The same name appears in
  `packages/core/src/model/snapshot.ts:42` (a type comment), in `packages/core/test/fixtures/valid-build/`
  and several goldens, in `packages/core/test/schemas.test.ts`, and in `docs/design.md`. **None of those
  are in scope.** Fixtures and goldens are test data, not shipped text; `docs/design.md` is a repository
  document with its own history; the `snapshot.ts` comment is a separate surface the owner has not ruled
  on. Widening the scan to reach any of them is out of scope for this run and would turn a three-defect
  gap closure into a repository-wide edit. Record `snapshot.ts:42` in the summary as a candidate for a
  later ticket; change nothing there.

- **A-38 — the guidance keeps its meaning.** The two prose lines tell a BA which design reference Ready
  will demand on each profile. That distinction is the whole reason the two templates differ, and
  `templates.test.ts`'s `build and maintain differ only inside body HTML comments` case plus the two
  `new-ticket.test.ts` assertions all rest on it. The replacement wording must keep the build/maintain
  asymmetry and must keep a substring that is present in exactly one of the two templates, or the
  `new-ticket` cases lose the only thing they can name.

- **A-40 — this plan runs alone, in wave 3, after both siblings.** `files_modified` is disjoint from
  07-10's, so the wave guard saw nothing — but Task 2 **deliberately reintroduces the vendor name and
  re-runs `npm run gen`** to prove the scan fails when it should, and both plans end on a full
  `npm run build && npm test`. Run concurrently, two `tsdown` builds write the same `packages/*/dist`, each
  plan's full-suite run executes the sibling's half-edited test file, and 07-10's run would meet a
  genuinely red templates suite belonging to neither plan. The property that buys: a `<verify>` failure in
  this plan is attributable to this plan. `depends_on: ["07-09", "07-10"]` and `wave: 3` are how it is
  bought, and the plan that deliberately makes the tree red goes last.
  *Alternative rejected:* narrowing each plan's final verify to its own suite. The full-suite run at the
  end of each plan is the regression evidence the owner asked for, and three sequential plans cost only
  wall-clock here.

- **A-39 — two goldens move, and both are regenerated rather than hand-edited.**
  `packages/core/test/__golden__/ticket-build.verified-empty.md` is a `toMatchFileSnapshot` of the build
  template taken by `packages/core/test/write.test.ts:55`; it carries the **build** template's lines 13
  and **32** — 33 is the maintain template's guidance line, not the build one's — and will go red.
  `packages/core/src/generated/templates.ts` is emitted by `npm run gen`. Regenerate both
  with their own tooling — `npm run gen` and `vitest -u` scoped to the one suite — and never by editing
  the file.
</surfaced_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Five lines lose the vendor and keep the instruction</name>
  <files>packages/core/templates/ticket-build.md, packages/core/templates/ticket-maintain.md, packages/core/templates/epic.md, packages/core/src/generated/templates.ts, packages/cli/test/new-ticket.test.ts, packages/core/test/__golden__/ticket-build.verified-empty.md</files>
  <read_first>
    - packages/core/templates/ticket-build.md (the whole file — line 13 is the commented `design:` example, line 32 is the Intent guidance line inside the HTML comment)
    - packages/core/templates/ticket-maintain.md (the whole file — line 13 the same commented example, line 33 the maintain counterpart of the guidance line)
    - packages/core/templates/epic.md (lines 1-20 — line 13 is the commented `design:` example; the epic has no Intent guidance line naming a tool)
    - packages/core/schemas/ticket.schema.json (line 20 — `design` is `{ "type": "string", "pattern": "^https://" }`, which is what the replacement URL must satisfy)
    - packages/cli/test/new-ticket.test.ts (lines 80-100 — the two assertions that pin the build-only sentence, one positive and one negative, inside a `for` loop over `story` and `bug`)
    - packages/core/test/templates.test.ts (lines 77-86 `build and maintain differ only inside body HTML comments`; lines 95-102 `ownership guidance is present`; lines 128-144 the drift and LF/BOM cases — all four constrain what an edit here may do)
    - packages/core/scripts/gen-templates.mjs (the whole file, 20 lines — the `.md`/`.html` filter, the BOM and CRLF normalisation, and the fact that `npm run gen` is manual with no prebuild hook)
    - packages/core/test/write.test.ts (lines 52-56 — the `toMatchFileSnapshot` call that owns the golden A-39 names)
    - .planning/phases/07-scaffolding-and-example-repo/07-04-SUMMARY.md §F-2 (the finding as first raised, and the carve-out that was rejected)
  </read_first>
  <action>
**Before your first edit in this plan**, capture the baseline the A-37 guard compares against. Phases 6
and 7 are entirely uncommitted by the owner's standing rule, so `HEAD` is not a picture of the tree this
plan starts from — `examples/` and `packages/core/test/fixtures/wrong-plan/` are untracked, so
`git status --porcelain` already names them, and an edit *inside* an untracked directory does not change
that output at all. Content can answer what git cannot:

```sh
find packages/core/test/fixtures docs examples -type f | sort | xargs sha256sum > /tmp/07-11-untouched-before.txt
```

**Precondition for every content-hash block in this plan.** `find`, `sort`, `xargs`, `sha256sum`,
`diff` and the trailing `test $? -eq 1` idiom are POSIX: run these blocks in **Git Bash**, not
PowerShell, and always **from the repository root** — `sha256sum` embeds `find`'s cwd-relative paths, so
a baseline taken from one directory and a comparison run from another differ on every line while nothing
has changed.

**If that file is missing when a verify command runs, STOP and fail the task. Do not re-take it.**
A baseline captured after the edits diffs clean against the post-edit tree by construction, so re-taking
it yields a guard that reports green while proving nothing. A missing baseline means this plan can no
longer demonstrate that A-37 held, and that is a stop, not a shrug.

Then replace the vendor name on the five lines this task's reading list names — `ticket-build.md` 13 and 32,
`ticket-maintain.md` 13 and 33, `epic.md` 13 — and nowhere else.

**The three commented `design:` example lines** (`ticket-build.md:13`, `ticket-maintain.md:13`,
`epic.md:13`) carry a vendor host inside an example URL. Replace the whole URL with a neutral one built on
the IANA reserved example domain, keeping the `https://` scheme so `ticket.schema.json`'s `^https://`
pattern still matches, keeping the trailing ellipsis that marks it as a placeholder, and keeping the line
commented and byte-aligned with its neighbours. The three lines must end up identical to each other, as
they are today — `templates.test.ts`'s frontmatter-block equality case between build and maintain depends
on it.

**The two guidance lines** (`ticket-build.md:32`, `ticket-maintain.md:33`) name the vendor inside prose
that tells a BA which design reference Ready will demand. Rewrite each so the sentence keeps its meaning
and loses only the vendor: the build line still says that a story with a screen needs a design reference
recorded in `design:`, and the maintain line still says that such a reference is optional there because
the prototype is the primary reference (A-38). Refer to it as a design link or a design reference — a
category, not a product.

Then, in `packages/cli/test/new-ticket.test.ts`, update the two assertions at lines 87 and 97 to the new
build-only sentence. They are a matched pair — one positive on the build render, one negative on the
maintain render — so both must name the same string, and that string must appear in `ticket-build.md` and
not in `ticket-maintain.md`. Verify that by inspection of the two files before running the suite; if the
new wording appears in both, the case has stopped distinguishing the templates and the wording is wrong.

Regenerate, do not hand-edit:

- `npm run gen` rewrites `packages/core/src/generated/templates.ts` from the directory. Run it and leave
  the result in the working tree; the drift case is the standing staleness signal, and the property this
  task proves now is *idempotence* — a second `npm run gen` must leave every file under
  `packages/core/src/generated/` byte-identical, meaning what is in the worktree already matches the
  directory. Do not ask git that question: `npm run gen` runs `gen-templates.mjs && gen-skills.mjs`, and
  `skills.ts` is untracked, so `git status --porcelain packages/core/src/generated` can never be empty in
  this tree. Hash the files instead — the verify command below does exactly that.
- `packages/core/test/__golden__/ticket-build.verified-empty.md` carries the build template's lines 13
  and 32 and will go red. Regenerate it with vitest's own update flag scoped to the `write` suite, then
  read the resulting diff and confirm it contains only the two changed lines. This golden *is* tracked, so
  `git diff` works on it — but read it with `--numstat` or `-U0`, not `--stat`, which reports insertions
  and deletions rather than the lines themselves.

Nothing else in the three templates moves — not the heading order, not the frontmatter key order, not the
`Developer fills this in. BA leaves it empty.` line, not line endings. The files are LF with no BOM and
must stay that way.
  </action>
  <verify>
    <automated>npm run gen &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core templates</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or an assertion message containing `drifted: run npm run gen`</fails_when>
    <automated>npm test -- --project core write</automated>
    <fails_when>non-zero exit, or an assertion message naming `__golden__/ticket-build.verified-empty.md` — the golden was not regenerated</fails_when>
    <automated>npm test -- --project cli new-ticket</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line — the two profile-distinguishing cases must still pass and must still distinguish</fails_when>
    <automated>git hash-object packages/core/src/generated/*.ts > /tmp/07-11-gen-before.txt &amp;&amp; npm run gen &amp;&amp; git hash-object packages/core/src/generated/*.ts | diff -u /tmp/07-11-gen-before.txt -</automated>
    <fails_when>non-zero exit — a second `npm run gen` changed a file under `packages/core/src/generated/`, so what is in the working tree is stale. `git hash-object` reads content and does not consult the index, which is what makes this work over the untracked `skills.ts` that `npm run gen` also emits; the shell sorts the glob, so an added or removed generated file changes the line count and fails the same way.</fails_when>
    <automated>grep -rniE 'F[i]gma' packages/core/templates/ ; test $? -eq 1</automated>
    <fails_when>non-zero exit — a shipped template still carries the vendor name. The bracket in the pattern is deliberate, matching this repository's convention of splitting a forbidden literal so the file scanning for it does not carry it</fails_when>
    <automated>find packages/core/test/fixtures docs examples -type f | sort | xargs sha256sum | diff -u /tmp/07-11-untouched-before.txt -</automated>
    <fails_when>non-zero exit — the diff names a fixture, `docs/` or `examples/` path this task changed since the baseline taken at the top of this plan. A-37 enumerates the out-of-scope vendor occurrences in exactly those trees and requires them recorded rather than changed, so this guard runs here, after the last edit, rather than being left to a human reading an acceptance criterion.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rniE 'F[i]gma' packages/core/templates/` reports no match (exit 1).
    - `packages/core/templates/ticket-build.md` line 13, `packages/core/templates/ticket-maintain.md` line 13, and `packages/core/templates/epic.md` line 13 are byte-identical to one another and each begins `# design: "https://`.
    - The URL on those three lines validates against `ticket.schema.json`'s `design` pattern: stripping the leading `# design: ` and the surrounding quotes leaves a string starting `https://`.
    - `packages/cli/test/new-ticket.test.ts` lines 87 and 97 name the same string, that string is present in `packages/core/templates/ticket-build.md` and absent from `packages/core/templates/ticket-maintain.md`, and both cases pass for `--type story` and `--type bug`.
    - `packages/core/test/templates.test.ts`'s `build and maintain differ only inside body HTML comments` case passes unmodified.
    - `packages/core/test/templates.test.ts`'s `LF and no BOM on disk` case passes — no CR and no BOM was introduced by the edit.
    - `git diff --numstat -- packages/core/test/__golden__/ticket-build.verified-empty.md` reports `2` added and `2` removed, and `git diff -U0 --` on the same path shows exactly two `-` lines and two `+` lines, both from the template's lines 13 and 32. (This golden is tracked, so `git diff` is a valid oracle for it — unlike everything else in this plan.)
    - A second `npm run gen` leaves `git hash-object packages/core/src/generated/*.ts` byte-identical to its value beforehand.
    - `find packages/core/test/fixtures docs examples -type f | sort | xargs sha256sum` produces output byte-identical to `/tmp/07-11-untouched-before.txt` — A-37 keeps the fixtures, the design document, and the examples out of this diff, and content hashing is the only oracle that can say so over two untracked trees.
  </acceptance_criteria>
  <done>Every shipped ticket template names a category of design reference instead of a product, and the generated module and both goldens moved with them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The scan reaches the templates, and fails when it should</name>
  <files>packages/core/test/templates.test.ts</files>
  <read_first>
    - packages/core/test/helpers/denied.ts (the whole file, 44 lines — `DENIED` as split literals with its rationale, and `deniedNames(files)` returning sorted `path:line: name` offenders)
    - packages/core/test/templates.test.ts (lines 1-43 for the imports, `templatesDir`, `normalise` and the `names` list; lines 128-144 for the drift and LF/BOM describe, which is where this case belongs and which is the binding A-36 relies on)
    - packages/core/test/scaffold.test.ts (lines 334-354 — the existing Phase 7 scan case, including its two guard-the-guard steps and the split-literal probe; this is the shape to follow, not to re-invent)
    - packages/core/test/skills.test.ts (line 294 — the one-line form the same helper takes over the rendered skill bodies, so the new caller reads like its siblings)
    - packages/core/test/examples.test.ts (lines 86-91 — the third existing caller, over the example prose)
  </read_first>
  <behavior>
    - A probe list carrying a denied name on its second line is reported as exactly one `probe.md:2: <name>` offender — proving the scan still matches before anything is asserted clean.
    - The generated `templates` record, scanned entry by entry as `{ path: <file name>, text }`, produces an empty offender list.
    - The scan is given a non-empty input: a guard asserts the record holds at least as many entries as the directory does, so an empty or truncated record cannot read as a pass.
    - Reintroducing the vendor name on any line of any file under `packages/core/templates/` and running `npm run gen` turns this case red, naming the file and the line.
  </behavior>
  <action>
**Before your first edit in this task**, capture the helper baseline this task's last verify compares
against:

```sh
git hash-object packages/core/test/helpers/*.ts > /tmp/07-11-helpers-before.txt
```

Add one case to `packages/core/test/templates.test.ts`, inside the `generated module` describe, beside the
drift case that binds the record to the directory (A-36).

Import `deniedNames` from `./helpers/denied.js` — the same helper `scaffold.test.ts`, `skills.test.ts` and
`examples.test.ts` already call. Do not declare a second list, do not copy `DENIED`, and do not add an
allowlist parameter; the helper takes files and returns offenders, and that is the whole interface.

Write it RED first. Before Task 1's edits are in place — or by temporarily reintroducing the name on one
template line and running `npm run gen` — confirm the case reports the offending `path:line: name` entries
and that it names the file and the line rather than failing on a bare boolean. Undo the reintroduction
before proceeding.

The case follows `scaffold.test.ts`'s shape: guard the input is non-empty, guard the scan still matches by
running it over a probe built from a split literal, then assert the real surface is clean. Build the
scanned list from `Object.entries(templates)` so the offender path is the template's file name, and use
the split-literal style for the probe so this test file does not itself carry a name the project forbids —
`templates.test.ts` already builds `new RegExp('features' + '/')` that way at line 105, for the same
reason.

Then re-run the two suites that share the helper and record their case counts in the summary: SKILL-04's
suite, which calls it over the rendered skill bodies, and the scaffold suite, which calls it over the
CLI-03 pointer block and the generated config and workflow. Both are **regression evidence for
requirements this plan does not deliver** — this plan's `requirements` field names CLI-01 alone, because
what it changes is the templates `init` ships and `new ticket` renders. Neither count may shrink. The
helper is shared, so a change that broke either would be a change to a project constraint's one
implementation.
  </action>
  <verify>
    <automated>npm run gen &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core templates</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary line, or an assertion message listing an offender whose path begins `ticket-build.md:`, `ticket-maintain.md:` or `epic.md:`</fails_when>
    <automated>npm test -- --project core skills</automated>
    <fails_when>non-zero exit, or fewer than 42 passing cases in the summary — SKILL-04's coverage must not shrink. 42 is what this command prints today, measured; 07-VERIFICATION.md's "75" is the combined `skills.test.ts scaffold.test.ts` run, 42 + 33.</fails_when>
    <automated>npm test -- --project core scaffold</automated>
    <fails_when>non-zero exit, or fewer than 33 passing cases in the summary — the CLI-03 pointer-block scan and the generated-config scan share the helper this task adds a caller to. 33 is today's count; 07-10 adds cases to this suite and runs first, so a higher number is expected and only a lower one is a regression.</fails_when>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or any suite reports a failure</fails_when>
    <automated>git hash-object packages/core/test/helpers/*.ts | diff -u /tmp/07-11-helpers-before.txt -</automated>
    <fails_when>non-zero exit — a file under `packages/core/test/helpers/` changed since the baseline at the top of this task, when this task adds a caller and nothing else. Hashing, not `git status`: `denied.ts` is untracked and `fixture.ts` is already modified, so that command names both before this task starts and would be red no matter what the task does.</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/templates.test.ts` imports `deniedNames` from `./helpers/denied.js` and contains exactly one case calling it.
    - The case asserts a probe offender before it asserts the real surface clean, and the probe's denied name is built from a split literal so the test file does not contain the name verbatim.
    - The case asserts the scanned input is non-empty before asserting the offender list is empty.
    - `packages/core/test/helpers/denied.ts` is unmodified: `git hash-object packages/core/test/helpers/*.ts` is byte-identical to `/tmp/07-11-helpers-before.txt`.
    - Temporarily reintroducing the vendor name on one line of one template and running `npm run gen` makes `npm test -- --project core templates` fail with a message naming that file and that line — record the observed message in the summary, then revert.
    - `npm test -- --project core skills` reports at least 42 passing cases — the count that command prints today, measured directly rather than read off 07-VERIFICATION.md, whose 75 is the combined `skills.test.ts scaffold.test.ts` run.
    - `npm test -- --project core scaffold` reports at least 33 passing cases — today's count, plus whatever 07-10 added to that suite before this plan ran.
    - The summary records `packages/core/src/model/snapshot.ts:42` as an out-of-scope candidate for a later ticket (A-37), with no change made there.
  </acceptance_criteria>
  <done>The CLAUDE.md naming constraint is machine-held on every shipped text surface, and the scan is proven to fail in the direction that matters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped template text → every consumer's repository | `packages/core/templates/` is inside the published `files:` list and is what `accord new ticket` renders into a team's tickets |
| template text → the generated module | `npm run gen` is manual, with no prebuild or pretest hook, so the module can lag the directory |
| the `deniedNames` helper → four callers | one list and one scan enforce a project constraint for the skill bodies, the scaffold surfaces, the examples, and now the templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-45 | Information disclosure | shipped templates naming a third-party product | high | mitigate | Task 1 removes the name from all five lines; Task 2 widens the existing scan over `packages/core/templates/` so a reintroduction is a red test rather than a release. A carve-out was put to the owner and rejected, so no exception path exists to be abused. |
| T-07-46 | Tampering | a stale `packages/core/src/generated/templates.ts` shipping the old text after the directory is fixed | high | mitigate | `npm run gen` is run in the same task, and Task 1's verify hashes `packages/core/src/generated/*.ts` either side of a second `npm run gen` and requires the two listings to be identical; the drift case is the standing signal thereafter. Git is not asked, because `skills.ts` under that directory is untracked and `npm run gen` emits it on every run. |
| T-07-47 | Spoofing | a scan that silently stopped matching and reads as a pass | medium | mitigate | Task 2 asserts a probe offender and a non-empty input before asserting the surface clean, and requires an observed red from a temporary reintroduction to be recorded in the summary. |
| T-07-48 | Tampering | the replacement example URL failing `ticket.schema.json`'s `design` pattern | medium | mitigate | The pattern is `^https://`; Task 1's acceptance criteria assert the scheme survives on all three lines and that the three lines stay identical to one another. |
| T-07-49 | Repudiation | the same name surviving on surfaces outside this run's scope | low | accept | A-37 enumerates them — a type comment, test fixtures, goldens, and `docs/design.md` — and requires the `snapshot.ts` occurrence to be recorded for a later ticket rather than silently left. Fixtures and goldens are test data, not shipped text. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan adds no dependency and runs no package manager beyond the existing `gen`, `build`, `lint`, `typecheck` and `test` scripts against the committed lockfile. |
</threat_model>

<artifacts_this_phase_produces>
This plan creates or renames:

| Kind | Name |
|------|------|
| shipped text changed | the three `# design: "https://…"` example lines in `packages/core/templates/{ticket-build,ticket-maintain,epic}.md` |
| shipped text changed | the Intent guidance sentence in `packages/core/templates/ticket-build.md` and its maintain counterpart in `packages/core/templates/ticket-maintain.md` |
| test case (new) | the `deniedNames` scan over the generated `templates` record in `packages/core/test/templates.test.ts` |
| assertions updated | the two profile-distinguishing assertions in `packages/cli/test/new-ticket.test.ts` |
| golden regenerated | `packages/core/test/__golden__/ticket-build.verified-empty.md` |
| generated module regenerated | `packages/core/src/generated/templates.ts` |
| finding recorded | `packages/core/src/model/snapshot.ts:42` as an out-of-scope occurrence for a later ticket |
</artifacts_this_phase_produces>

<verification>
1. `npm run gen && npm run build && npm run lint && npm run typecheck && npm test` clean from the
   repository root, with a further `npm run gen` leaving `git hash-object packages/core/src/generated/*.ts`
   unchanged afterwards.
2. In a throwaway `git init` sandbox under the session scratchpad, run the built CLI's `init` then
   `new ticket` for a story on each profile, open both rendered tickets, and read the Intent guidance. It
   should tell a BA what Ready will demand without naming a product. Record both rendered sentences in the
   summary.
3. Reintroduce the vendor name on one template line, run `npm run gen`, run
   `npm test -- --project core templates`, record the failure message verbatim, then revert and confirm
   green. This is the only evidence that the widened scan fails in the direction it exists for.
4. Read `.claude/CLAUDE.md`'s constraint list once more against the finished diff. The constraint is
   "nothing accord ships names another tool" — check the diff against that sentence, not against this
   plan's summary of it.
</verification>

<success_criteria>
- No file under `packages/core/templates/` carries a denied name, proven by a grep and by a test.
- The `deniedNames` scan covers the templates directory, with a guard that fails if the scan stops
  matching and a recorded observation of it going red on a deliberate reintroduction.
- `accord new ticket` still renders the right template per profile, distinguished by an assertion on a
  sentence that exists in exactly one of them.
- The generated module and both goldens moved with the templates; the fixtures, `docs/`, and `examples/`
  did not move at all.
</success_criteria>

<output>
Create `.planning/phases/07-scaffolding-and-example-repo/07-11-SUMMARY.md` when done. It must carry the
two rendered Intent sentences, the verbatim failure message from the deliberate reintroduction, the two
consumer-suite case counts, and `snapshot.ts:42` recorded as an out-of-scope candidate.

**Do not commit.** Leave the working tree changed and unstaged for the owner's review. No `git commit`,
no `git push`, no pull request.
</output>
