---
phase: 09-publish-and-dogfood
plan: 07
type: execute
wave: 4
depends_on: ["09-06"]
files_modified:
  - accord/product/glossary.md
  - accord/product/business-rules.md
  - .github/workflows/accord.yml
  - .claude/skills/
  - .claude/CLAUDE.md
autonomous: false
requirements: [OPS-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The contract is installed in this repository by the package published to npm, not by a local build (PROJECT.md: dogfooding from a local build does not close the milestone)"
    - "`accord/config.yml` is byte-identical afterwards, because D-130 skips a path that exists"
    - "`.github/workflows/accord.yml` exists and carries the D-162 `ref:` key, proving the fix shipped inside `0.1.0`"
    - "Only `.claude/skills/` is written, because this repository's config says `runtimes: [claude]`"
    - "`accord lint` runs clean enough to keep a workflow green: zero errors, one expected `lint.tokens-missing` warning"
  artifacts:
    - path: ".github/workflows/accord.yml"
      provides: "the generated CI workflow ROADMAP criterion 2 is about"
      contains: "github.event.pull_request.head.sha"
    - path: "accord/product/glossary.md"
      provides: "part of the scaffold init fills in (D-132)"
    - path: "accord/product/business-rules.md"
      provides: "part of the scaffold init fills in (D-132)"
  key_links:
    - from: ".github/workflows/accord.yml"
      to: "@accord-dev/accord@0.1.0 on npm"
      via: "npx --yes <pkg>@<version>, pinned to the same string accord/config.yml pins (D-135)"
      pattern: "npx --yes"
---

<objective>
Install accord's own contract into accord's own repository, using the package that is now on npm.

D-132 already anticipated this: `init` fills in what is missing in a repository that already has an
`accord/` folder. Today that folder holds exactly one file, `accord/config.yml` — no tickets, no
`product/`, no templates, no skill copies, no `.github/workflows/accord.yml`. So this run creates
roughly the same set of files a greenfield run creates, minus the one that exists.

Purpose: the first half of ROADMAP criterion 2 — *`accord init` from the published package runs on a
real project's repository*. And PROJECT.md is explicit that a local build does not count, so the
invocation is `npx --yes @accord-dev/accord@0.1.0 init` and nothing else. This is also the run that
produces the generated workflow whose greenness criterion 2 is judged on.

Output: a branch carrying the scaffold as its own commit, and a generated `accord.yml` that visibly
contains the D-162 fix.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-publish-and-dogfood/09-CONTEXT.md
@accord/config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch, then run `init` from the published package</name>
  <files>accord/product/glossary.md, accord/product/business-rules.md, .github/workflows/accord.yml, .claude/skills/, .claude/CLAUDE.md</files>
  <precondition>`@accord-dev/accord@0.1.0` is on the npm registry (09-06 Task 4 verified `npx --yes @accord-dev/accord@0.1.0 --version` prints `0.1.0`). Running `init` from `packages/cli/dist/cli.js` instead would not close the milestone — PROJECT.md rules out dogfooding from a local build.</precondition>
  <read_first>
    - `accord/config.yml` — the one file that already exists; note `profile: maintain`, `runtimes: [claude]`, `accord: "0.1.0"`, and the comment recording why the token rule is skipped in this repository
    - `.planning/phases/07-scaffolding-and-example-repo/07-CONTEXT.md` §D-130 and §D-132 — a path that exists is skipped; `init` fills in what is missing
    - `.planning/phases/09-publish-and-dogfood/09-CONTEXT.md` §"Claude's Discretion" — the item stating `accord init` leaves the existing `accord/config.yml` untouched, including its `runtimes: [claude]`, so only `.claude/skills/` is written here
    - `.planning/phases/09-publish-and-dogfood/09-RESEARCH.md` §"Landmine 5" — the file-count expectation and the dirty-`.claude/CLAUDE.md` warning
    - `git status --short` output at the start of the task
  </read_first>
  <action>
    First, deal with the dirty tree. `git status` currently shows modifications to `.claude/CLAUDE.md`
    and to several `.planning/` files. `init` appends a pointer block to `.claude/CLAUDE.md`
    (CLI-03), and an append into an already-modified file makes the diff unreadable for exactly the
    review this phase exists to produce. Commit or stash the pre-existing `.claude/CLAUDE.md`
    modification before running `init`, and say in the SUMMARY which you did.

    Create the branch for D-155's single pull request. Name it for the dogfood ticket rather than
    for this plan — the scaffold and the ticket ship in one pull request, so the branch belongs to
    the ticket. Record the branch name; 09-08 pushes it.

    Run, from the repository root, exactly:
    `npx --yes @accord-dev/accord@0.1.0 init`

    Not `node packages/cli/dist/cli.js init`. A transcript containing the local build's path is the
    warning sign that the milestone did not close. Capture the full command output — the list of
    created paths is evidence for ROADMAP criterion 2 and goes into the SUMMARY.

    Do not pass a flag that overwrites. D-130's rule is that a path which exists is skipped, and the
    one path that exists — `accord/config.yml` — must come through byte-identical, including its
    `runtimes: [claude]`, which is what keeps this run from writing a second set of skill copies
    under another runtime's directory.

    Do not edit `accord/config.yml`'s `design.tokens` here. This repository ships a CLI and no
    interface; the token measurement happens in a different repository entirely (D-153, D-154) and
    is 09-10's job.
  </action>
  <verify>
    <automated>npx --yes @accord-dev/accord@0.1.0 --version</automated>
    <fails_when>non-zero exit, a `404 Not Found` in npm's output, or a printed version other than `0.1.0`</fails_when>
    <automated>test -n "$(git status --porcelain -- accord .github .claude)"</automated>
    <fails_when>non-zero exit — the porcelain listing is empty, so `init` created nothing, which on a repository holding one accord file means the command did not run or refused</fails_when>
    <automated>test -z "$(git diff -- accord/config.yml)"</automated>
    <fails_when>non-zero exit — D-130 says an existing path is skipped, so a modified `accord/config.yml` means the run overwrote it</fails_when>
  </verify>
  <acceptance_criteria>
    - The command run was `npx --yes @accord-dev/accord@0.1.0 init`, captured verbatim in the SUMMARY.
    - `git diff -- accord/config.yml` is empty.
    - `.github/workflows/accord.yml` exists and did not before.
    - `accord/product/glossary.md` and `accord/product/business-rules.md` exist.
    - A `.claude/skills/` directory exists with at least one `SKILL.md` under it.
    - No directory was created for a runtime other than `claude`.
    - `.claude/CLAUDE.md` shows exactly one appended pointer block in the diff, with no unrelated modification mixed into it.
  </acceptance_criteria>
  <done>This repository now holds the contract it ships, installed the way a stranger would install it.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the generated workflow carries the D-162 fix, then commit the scaffold</name>
  <files>.github/workflows/accord.yml</files>
  <read_first>
    - `.github/workflows/accord.yml` — the file just generated; the subject of this task
    - `packages/core/src/scaffold/workflow.ts` — the generator, so the emitted text can be compared against what `0.1.0` should have shipped
    - `.planning/phases/09-publish-and-dogfood/09-RESEARCH.md` §"Landmine 7" — `accord lint` in this repository is not silent; it emits one `lint.tokens-missing` warning for `docs/tokens.css`, which does not exist, and exits 0
    - `.planning/phases/09-publish-and-dogfood/09-CONTEXT.md` §D-155 — why one pull request exists at all
  </read_first>
  <action>
    Assert three things about the generated `.github/workflows/accord.yml`, because this is the only
    moment they can be checked against a file that actually shipped rather than against a generator:

    1. Its checkout step carries both `fetch-depth: 0` and a `ref:` naming the pull request head
       sha. If `ref:` is absent, `0.1.0` shipped without D-162 and the phase has a real problem —
       stop and report rather than hand-editing the file, because hand-editing it would hide the
       fact that the published package is wrong.
    2. Both `npx --yes` invocations in its script pin the same version string that
       `accord/config.yml`'s `accord:` key holds (D-135). They will both say `0.1.0` today; 09-11
       moves all three together.
    3. It triggers on `pull_request` and declares `permissions: { contents: read }`.

    Then run `npx --yes @accord-dev/accord@0.1.0 lint` from the repository root and record its
    output. Expect zero errors and exactly one warning — `lint.tokens-missing` for
    `docs/tokens.css`, which does not exist and whose absence the config's own comment already
    explains. Exit code 0, so the generated workflow stays green. Record this in the SUMMARY as an
    *expected* warning: the pull request's job log will show it, and a reader taking that log as
    evidence for criterion 2 should not have to wonder.

    Commit the scaffold as its own commit on the branch, separate from the ticket work that follows
    in 09-08. They ship in one pull request (D-155) but they are two different kinds of change, and
    a ~28-file scaffold mixed into a README rewrite is a diff nobody reads.
  </action>
  <verify>
    <automated>node -e "const {parse}=require('yaml');const d=parse(require('fs').readFileSync('.github/workflows/accord.yml','utf8'));const w=d.jobs.accord.steps[0].with;console.log(JSON.stringify({keys:Object.keys(w),depth:w['fetch-depth'],ref:w.ref,on:Object.keys(d.on),perms:d.permissions}))"</automated>
    <fails_when>non-zero exit; or the printed `ref` is null or undefined (meaning `0.1.0` shipped without D-162); or `depth` is not `0`; or `on` is not exactly `["pull_request"]`; or `perms` is not `{contents: "read"}`</fails_when>
    <automated>npx --yes @accord-dev/accord@0.1.0 lint</automated>
    <fails_when>non-zero exit, or the summary line reports more than 0 errors, or it reports more than 1 warning</fails_when>
    <automated>grep -v '^ *#' .github/workflows/accord.yml | grep -c 'npx --yes'</automated>
    <fails_when>the printed count is not 2 — the emitted script carries exactly two pinned invocations, one for lint and one for gate done</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/accord.yml`'s checkout step has exactly the keys `fetch-depth` and `ref`; `fetch-depth` is the number `0`; `ref` is the pull-request head-sha expression.
    - Both `npx --yes` invocations in that file name the same version as `accord/config.yml`'s `accord:` key.
    - `npx --yes @accord-dev/accord@0.1.0 lint` exits 0 with `0 errors, 1 warnings`, the warning being `lint.tokens-missing`.
    - The scaffold is one commit on the branch, containing no change to `README.md` and no ticket file.
    - The expected `lint.tokens-missing` warning is written down in the SUMMARY as expected, not as a finding.
  </acceptance_criteria>
  <done>The workflow whose greenness ROADMAP criterion 2 is judged on exists, and it came out of the published package with the fix in it.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan's contribution to the phase inventory (the full table is in `09-01-PLAN.md`):

| Artifact | Kind |
|---|---|
| `.github/workflows/accord.yml` | new file — generated by `init` from the published package |
| `accord/product/glossary.md`, `accord/product/business-rules.md` | new files — the scaffold D-132 fills in |
| `.claude/skills/**/SKILL.md` | new files — one runtime only, per `runtimes: [claude]` |
| `.claude/CLAUDE.md` pointer block | appended (CLI-03) |
| the pull-request branch | new repository state; 09-08 pushes it |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the npm registry → this repository's filesystem | `npx --yes` downloads and executes a package that then writes ~28 files, including a CI workflow. |
| the generated `accord.yml` → this repository's Actions | A file written by a downloaded package becomes a workflow with repository access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-23 | Tampering | files written by `npx --yes` | medium | mitigate | The package is this project's own, published from this repository's Actions over OIDC with a provenance attestation verified in 09-06. Every written path is reviewed in the commit diff before it is pushed — Task 2's separate scaffold commit exists so that review is possible. |
| T-09-24 | Elevation of privilege | the generated `accord.yml` | medium | mitigate | Task 2 asserts it declares `permissions: { contents: read }` and triggers on `pull_request` only. Its single `run:` body executes no script from the checked-out tree. |
| T-09-25 | Tampering | `accord/config.yml` | medium | mitigate | D-130 skips an existing path; Task 1 verifies `git diff -- accord/config.yml` is empty. An overwrite would replace `runtimes: [claude]` and the token-rule comment. |
| T-09-26 | Repudiation | which build ran `init` | high | mitigate | The exact `npx --yes @accord-dev/accord@0.1.0 init` invocation is captured verbatim; PROJECT.md rules out a local build, so the transcript is the evidence. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | The one package installed is `@accord-dev/accord@0.1.0`, published by this repository with automatic provenance and scanned by `bin.test.ts`'s denied-names case. `09-RESEARCH.md` §"Package Legitimacy Audit" records that this phase introduces no third-party package; no `[ASSUMED]`/`[SUS]`/`[SLOP]` entry exists. |
</threat_model>

<verification>
- `git diff -- accord/config.yml` is empty.
- `.github/workflows/accord.yml` parses and carries `ref:` beside `fetch-depth: 0`.
- `npx --yes @accord-dev/accord@0.1.0 lint` exits 0 with one expected warning.
- The scaffold is a single reviewable commit on a branch.
</verification>

<success_criteria>
- The contract is installed from npm, not from `packages/cli/dist/`.
- The generated workflow visibly contains D-162's fix.
- Only the `claude` runtime path was written.
- Nothing pre-existing was overwritten.
</success_criteria>

<output>
Create `.planning/phases/09-publish-and-dogfood/09-07-SUMMARY.md` when done
</output>
