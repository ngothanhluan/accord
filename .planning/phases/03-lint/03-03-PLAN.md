---
phase: 03-lint
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/core/src/load/junit.ts
  - packages/core/src/load/snapshot.ts
  - packages/core/src/model/snapshot.ts
  - packages/core/schemas/config.schema.json
  - packages/core/test/schemas.test.ts
  - packages/core/test/junit.test.ts
  - packages/core/test/fixtures/lint-report/accord/config.yml
  - packages/core/test/fixtures/lint-report/accord/tickets/REPORT.md
  - packages/core/test/fixtures/lint-report/reports/junit.xml
  - packages/core/test/__golden__/lint-report.snapshot.json
  - packages/core/test/__golden__/lint-report.lint.json
  - packages/cli/src/load/fs.ts
  - packages/cli/test/load.test.ts
autonomous: true
requirements: [FMT-11]
coupling_justified:
  - "03-02: 03-02 rewrites lint.test.ts and lint/ticket.ts in this wave while this plan's Task 2 gate runs the lint-report lint golden and typecheck over the package; neither plan's files or goldens depend on the other's, so either order yields the same result. A gate that fails only because a sibling file is mid-write is rerun once 03-02's task lands"
  - "03-04: 03-04 regenerates src/generated/templates.ts in this wave and this plan's gates typecheck the package; no shared file, no shared golden, order irrelevant; a transient typecheck failure during 03-04's gen is rerun after it lands"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`config.yml` accepts an optional `tests: { report: <path> }`; `tests: {}` and `tests: { report: '', x: 1 }` are schema errors; the six root `required` keys are unchanged and the properties list has seven keys (D-72)"
    - "When `config.tests.report` names a key present in the input, the loader stores its normalised text in `snapshot.files` and `snapshot.tests` is the scanned record; when the key is absent, `snapshot.tests` is absent (not `{}`), so lint can tell 'no report' from 'empty report' (D-72)"
    - "`scanJUnit` maps every `<testcase>` to `norm(classname) + '#' + norm(name)`, or `norm(name)` alone when `classname` is absent or empty, where `norm` trims and turns each whitespace run into `-`; a self-closing or empty case is `passed`; a `<failure>` or `<error>` child is `failed`; a `<skipped>` child is `skipped`; CDATA and comment bodies are blanked before matching; XML entities in attributes are decoded (D-72, RESEARCH A2)"
    - "Duplicate ids keep the worst status, `failed` over `skipped` over `passed`, whatever their order in the file; the same text scanned twice gives the same record; the scanner keeps no module state (RESEARCH A7; FMT-11 concurrency edge, verification: explicit)"
    - "A configured report whose text contains none of `<testsuites`, `<testsuite`, `<testcase` yields one `load.report-invalid` loader finding on the report file at line 1 and `snapshot.tests` is `{}` (RESEARCH A3)"
    - "The CLI loader reads the file named by `tests.report` into `SnapshotInput.files` under its forward-slash repo-relative key, and silently skips a path that resolves outside the repository root or is not a regular file, exactly as it does for `design.tokens` (T-03-02)"
  artifacts:
    - path: "packages/core/src/load/junit.ts"
      provides: "scanJUnit, TestStatus"
      contains: "export function scanJUnit("
    - path: "packages/core/test/junit.test.ts"
      provides: "four runner shapes, CRLF, CDATA, entities, duplicates, garbage report"
      min_lines: 60
    - path: "packages/core/test/fixtures/lint-report/reports/junit.xml"
      provides: "a report with passed, failed, skipped, and duplicate cases"
      contains: "<testcase"
  key_links:
    - from: "packages/core/src/load/snapshot.ts"
      to: "packages/core/src/load/junit.ts"
      via: "tests = scanJUnit(files[reportKey]) when the report key is present"
      pattern: "scanJUnit"
    - from: "packages/cli/src/load/fs.ts"
      to: "packages/core/src/load/snapshot.ts"
      via: "containedPath(root, config.tests?.report) read into input.files"
      pattern: "tests?.report"
    - from: "packages/core/schemas/config.schema.json"
      to: "packages/core/test/schemas.test.ts"
      via: "the seven-key properties pin"
      pattern: "tests"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; no attribution trailer is written"
      status: flagged
      verification: unverified
    - statement: "No XML library is added; `load/junit.ts` uses only `String` and `RegExp` and imports nothing from `node:` (D-72, CORE-01)"
      status: flagged
      verification: unverified
    - statement: "The scanner never guesses an id: no basename fallback, no case folding, no fuzzy match; ids are exact after the one documented normalisation (PROJECT.md 'core never guesses')"
      status: flagged
      verification: unverified
    - statement: "The CLI never reads a file outside the repository root into the snapshot, and never spawns `npm`, `npx`, or a `.cmd`"
      status: flagged
      verification: unverified
    - statement: "No shipped string (schema description, reason, comment in `junit.ts` that renders to users) names a test runner or another tool by name; runner names may appear only in test file names and test descriptions"
      status: flagged
      verification: unverified
---

<objective>
**As a** developer who tags scenarios with `@test:<id>`, **I want** the CI test report in the snapshot as a plain id-to-status record, **so that** lint (plan 03-05) and the Done gate (Phase 4) can join tags to real results without an XML dependency.

This plan lands the FMT-11 loader half: the `tests.report` config key, the CLI read, the core `scanJUnit` line scanner, `snapshot.tests`, and `load.report-invalid`. The two lint rules that consume it (`lint.report-missing`, `lint.test-id-unknown`) are in plan 03-05, which also regenerates this plan's lint golden.

Purpose: D-72 becomes executable at the loader boundary; the `@test:` id shape (A2, confirmed by the owner 2026-09-14) is implemented here before fixtures elsewhere depend on it.
Output: `load/junit.ts`, loader and model edits, schema key, CLI read with containment, `junit.test.ts`, fixture `lint-report`, two goldens, CLI tests.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-lint/03-CONTEXT.md
@.planning/phases/03-lint/03-RESEARCH.md
@.planning/phases/03-lint/03-PATTERNS.md
@.planning/phases/03-lint/03-01-SUMMARY.md
@packages/core/src/load/snapshot.ts
@packages/core/src/load/config.ts
@packages/cli/src/load/fs.ts

<interfaces>
```typescript
// packages/core/src/load/junit.ts
export type TestStatus = 'passed' | 'failed' | 'skipped';
export function scanJUnit(text: string): Record<string, TestStatus>;   // text already normalised by the loader

// packages/core/src/model/snapshot.ts additions
// AccordConfig: tests?: { report: string }                           // D-72
// RepoSnapshot: tests?: Record<string, 'passed' | 'failed' | 'skipped'>  // absent when no report is in files

// packages/core/src/load/snapshot.ts
// const REPORT_TAG = /<testsuites?\b|<testcase\b/;  garbage guard -> load.report-invalid at line 1

// packages/cli/src/load/fs.ts
// function containedPath(root: string, rel: string | undefined): string | undefined   (rename of tokensPath, same body)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `scanJUnit` scanner, `tests` config key, loader `tests` field, `load.report-invalid`</name>
  <read_first>
    - packages/core/src/load/snapshot.ts (03-01 state: `files` filling, config-first ordering, the return spread)
    - packages/core/src/model/snapshot.ts (`AccordConfig`, `RepoSnapshot` with `files` from 03-01)
    - packages/core/src/load/config.ts (signature shape `loadConfig(file, text)` and how findings travel)
    - packages/core/schemas/config.schema.json lines 1-30 (root `required`, the `design` block to copy)
    - packages/core/test/schemas.test.ts lines 120-140 (the six-key pin that must become seven for `properties` only)
    - .planning/phases/03-lint/03-RESEARCH.md "Test Report" section: runner table, "Id formation", the verified `scanJUnit` code (port verbatim minus the BOM/CRLF line), "Schema and loader changes", Pitfalls 6, 7, 8
  </read_first>
  <files>packages/core/src/load/junit.ts, packages/core/src/load/snapshot.ts, packages/core/src/model/snapshot.ts, packages/core/schemas/config.schema.json, packages/core/test/schemas.test.ts, packages/core/test/junit.test.ts</files>
  <behavior>
    - Test 1 (file-path classname, `&gt;` in name, multi-line attributes, CRLF already normalised by caller): `<testcase classname="test/auth.spec.ts" name="auth &gt; rejects bad password" time="0.01">` followed by `</testcase>` maps to key `test/auth.spec.ts#auth->-rejects-bad-password` with `passed`; a sibling with a `<failure message="x">...</failure>` child is `failed`; a sibling with `<skipped/>` is `skipped`
    - Test 2 (dotted classname, `<skipped type="..." message="...">text</skipped>`, `<error>` child, self-closing pass): `classname="tests.test_auth.TestLogin" name="test_bad[1]"` maps to `tests.test_auth.TestLogin#test_bad[1]`; the `<error>` case is `failed`
    - Test 3 (package classname, subtest name, CDATA body containing `<testcase name="fake"/>`): only the real cases appear; `github.com/org/repo/pkg#TestLogin/bad_password` is `failed`
    - Test 4 (single-quoted attributes, `classname` absent or empty, duplicate ids in the order passed, failed, skipped): the key is `norm(name)` alone; the duplicate resolves to `failed`; reversing the order still gives `failed`; two `passed` then one `skipped` gives `skipped`
    - Test 5: `scanJUnit('')` is `{}`; an XML comment containing `<testcase name="c"/>` contributes nothing; `&#x3C;` and `&#60;` in an attribute decode to `<`
    - Test 6: `loadSnapshot` with a config whose `tests.report` is `reports/junit.xml` and `files['reports/junit.xml']` holding Test 1's XML returns `snapshot.tests` with those three keys and `snapshot.files['reports/junit.xml']` equal to the normalised text; without the file key, `'tests' in snapshot` is false and `snapshot.errors` has no report finding; with the file holding `not xml at all`, `snapshot.errors` contains `{ file: 'reports/junit.xml', line: 1, rule: 'load.report-invalid', reason: 'report has no <testsuite> or <testcase> element' }` and `snapshot.tests` is `{}`
    - Test 7 (`schemas.test.ts`): `validate('config', { ...minimal.config, tests: { report: 'reports/junit.xml' } })` is `[]`; `tests: {}` yields `schema.required` at `/tests`; `tests: { report: '' }` yields `schema.minLength` at `/tests/report`; `tests: { report: 'x', extra: 1 }` yields `schema.additionalProperties` at `/tests`; the properties pin becomes the seven keys `['accord', 'profile', 'tracker', 'design', 'roles', 'runtimes', 'tests']` while `configSchema.required` stays the six
  </behavior>
  <action>
Write `packages/core/test/junit.test.ts` first with Tests 1-6 (inline XML strings via a `xml(...lines)` join helper; import `scanJUnit` from `../src/load/junit.js` and `loadSnapshot` from `../src/index.js`), and extend `schemas.test.ts` with Test 7. Then implement:

- `packages/core/src/load/junit.ts`: port RESEARCH.md's verified `scanJUnit` verbatim, dropping the BOM/CRLF replace (the loader normalises). Header comment cites D-72 and A2/A7. Keep `unescapeXml`, `attr`, `norm`, `RANK` module-private; export `TestStatus` and `scanJUnit`. Document the O(n) property in one comment (single forward pass, lazy fixed terminators).
- `packages/core/schemas/config.schema.json`: add property `tests` after `runtimes`: `{ "type": "object", "additionalProperties": false, "required": ["report"], "properties": { "report": { "type": "string", "minLength": 1 } } }`. Root `required` unchanged.
- `packages/core/src/model/snapshot.ts`: `AccordConfig` gains `tests?: { report: string }; // D-72: repo-relative path of the test report`; `RepoSnapshot` gains `tests?: Record<string, 'passed' | 'failed' | 'skipped'>; // D-72: absent when no report is in files`.
- `packages/core/src/load/snapshot.ts`: when `config?.tests?.report` is set, `reportKey = normaliseKey(report)`; if `reportKey in files`, add it to `snapshotFiles` (normalised) and set `tests = scanJUnit(snapshotFiles[reportKey])`; if `!REPORT_TAG.test(text)` push `{ file: reportKey, line: 1, rule: 'load.report-invalid', reason: 'report has no <testsuite> or <testcase> element' }` to `errors` (the regex is `/<testsuites?\b|<testcase\b/`, so `<testsuites` also passes). Return `...(tests === undefined ? {} : { tests })` alongside `files`.

Run `npm test -- --project core junit`, `npm test -- --project core schemas`, `npm run lint && npm run typecheck`. Existing snapshot goldens must not change (no fixture has a report yet): the snapshot run below, scoped with `-t` to the eight Phase 2 fixtures, must stay green without `-u`. The scope matters: 03-02 is writing `lint-hygiene/` in this wave, and an unscoped run would discover that half-written fixture and write or mismatch a golden 03-02 owns. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core junit && npm test -- --project core schemas && npm test -- --project core snapshot -t "fixture (bad-config|body-edges|config-syntax|frontmatter-errors|gherkin-shapes|no-config|valid-build|verification-edges)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or the junit `|core|` summary reports fewer than 6 passing tests; or any of the eight pre-existing `*.snapshot.json` goldens is reported mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/load/junit.ts` exists, contains `export function scanJUnit(` and `export type TestStatus`, and `grep -c "node:" packages/core/src/load/junit.ts` prints 0
    - `packages/core/schemas/config.schema.json` contains `"tests"` with `"required": ["report"]` and `"minLength": 1`; `node -e "const s=require('./packages/core/schemas/config.schema.json');console.log(s.required.length, Object.keys(s.properties).length)"` prints `6 7`
    - `packages/core/test/schemas.test.ts` pins `Object.keys(configSchema.properties)` to seven keys ending in `'tests'` and `configSchema.required` to six
    - `packages/core/test/junit.test.ts` contains the literal keys `test/auth.spec.ts#auth->-rejects-bad-password`, `tests.test_auth.TestLogin#test_bad[1]`, `github.com/org/repo/pkg#TestLogin/bad_password`, and the literal `load.report-invalid`
    - `packages/core/src/load/snapshot.ts` contains `scanJUnit(` and `load.report-invalid`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>A configured report becomes `snapshot.tests` through a dependency-free scanner whose id shape, status ranking, and garbage handling are pinned by unit tests; the schema accepts the key and the Phase 1 pin is updated in the same change.</done>
</task>

<task type="auto">
  <name>Task 2: CLI reads `tests.report` with containment; the `lint-report` fixture and goldens</name>
  <read_first>
    - packages/cli/src/load/fs.ts (`tokensPath` at lines 51-60 and `loadFromFs` at 62-69; rename and call twice)
    - packages/cli/test/load.test.ts (the `makeRepo` helper, the file-list pin at lines 51-68, the containment test at 98-114 to copy for the report)
    - packages/core/test/fixtures/valid-build/accord/config.yml and packages/core/test/fixtures/gherkin-shapes/accord/tickets/TAGS.md (config and ticket shapes)
    - .planning/phases/03-lint/03-RESEARCH.md "Schema and loader changes" bullet on CLI containment (T-02-20) and "Security Domain" first threat row
  </read_first>
  <files>packages/cli/src/load/fs.ts, packages/cli/test/load.test.ts, packages/core/test/junit.test.ts, packages/core/test/fixtures/lint-report/accord/config.yml, packages/core/test/fixtures/lint-report/accord/tickets/REPORT.md, packages/core/test/fixtures/lint-report/reports/junit.xml, packages/core/test/__golden__/lint-report.snapshot.json, packages/core/test/__golden__/lint-report.lint.json</files>
  <action>
**CLI (D-72, T-03-02):** in `packages/cli/src/load/fs.ts` rename `tokensPath` to `containedPath(root, rel)` with the body unchanged (the `relative(root, abs)` containment check is the control). In `loadFromFs`, run the first-pass `loadSnapshot` once into `const config = loadSnapshot({ files, tree }).config`, then for each of `config?.design.tokens` and `config?.tests?.report`: `const key = containedPath(root, value); if (key !== undefined) files[key] = readFileSync(join(root, key), 'utf8')`. Update the comment to say both extra files are read the same way and that a missing file is lint's concern (LINT-04, FMT-11).

**CLI tests** in `packages/cli/test/load.test.ts`: add `it('reads tests.report into files')`: `makeRepo()`, append `tests:\n  report: reports/junit.xml\n` to `accord/config.yml`, `mkdirSync(join(tmp, 'reports'))`, write a two-case XML file, assert `Object.keys(loadFromFs(tmp).files)` contains `reports/junit.xml` and `loadSnapshot(input).tests` has two keys. Add `it('skips a report path outside the repository')` copying the tokens containment test: `report: ../outside.xml` with a file written in the parent; assert no key ends with `outside.xml` and `'tests' in loadSnapshot(input)` is false. Do not change the valid-build file-list pin (that fixture gains a report in plan 03-06).

**Fixture `lint-report`:** `accord/config.yml` from valid-build with `design.tokens: ""` and `tests:\n  report: reports/junit.xml`. `reports/junit.xml`: a `<testsuites>` root with one `<testsuite>` containing: `<testcase classname="test/auth.spec.ts" name="auth &gt; accepts a good password">` passing (empty body), `<testcase classname="test/auth.spec.ts" name="auth &gt; rejects bad password">` with a `<failure message="expected 401">` body, `<testcase classname="test/auth.spec.ts" name="auth &gt; remembers me">` with `<skipped/>`, and a second `<testcase classname="test/auth.spec.ts" name="auth &gt; accepts a good password"/>` (duplicate, passed; the merged status stays `passed`). `accord/tickets/REPORT.md` (story, `status: open`): five D-07 headings; Requirements one valid EARS line; AC fence with scenarios `@ac-1 @test:test/auth.spec.ts#auth->-accepts-a-good-password` (known, passed), `@ac-2 @test:test/auth.spec.ts#auth->-rejects-bad-password` (known, failed), `@ac-3 @test:nope#missing` (unknown; plan 03-05 warns), `@ac-4 @ui` (no test tag, silent); Open questions empty; Plan `- Làm @ac-1 @ac-2 @ac-3 @ac-4`.

**Goldens:** `npm test -- --project core snapshot -u -t "fixture lint-report"` then `npm test -- --project core lint -u -t "fixture lint-report"` (scoped `-u` only; 03-02 and 03-04 run in this wave). Read `lint-report.snapshot.json`: `tests` has exactly three keys with statuses `passed`, `failed`, `skipped`; `files` has `reports/junit.xml`; `errors` is `[]`. `lint-report.lint.json` at this wave has zero findings (every scenario has a `@test:` or `@ui` tag and the unknown-id rule lands in 03-05, which regenerates this golden). Add `describe('fixture lint-report: pinned values')` to `junit.test.ts` (not `lint.test.ts`, which plan 03-02 edits in this wave) using `readFixture('lint-report')` from `./helpers/fixture.js`, asserting the three `tests` entries by exact key and status and that `snapshot.files['reports/junit.xml']` contains no `\r`. Run `npm run build && npm test -- --project cli load`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run build && npm test -- --project cli load && npm test -- --project core snapshot -t "fixture lint-report" && npm test -- --project core lint -t "fixture lint-report"</automated>
    <fails_when>non-zero exit; or `No test files found`; or the cli `|cli|` summary reports fewer than 7 passing tests; or a golden reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/cli/src/load/fs.ts` contains `function containedPath(` and `tests?.report`, does not contain `tokensPath`, and `! grep -Eq "npx|npm|\.cmd|shell: *true" packages/cli/src/load/fs.ts` succeeds
    - `packages/cli/test/load.test.ts` contains `reads tests.report into files` and `skips a report path outside the repository`
    - `packages/core/test/__golden__/lint-report.snapshot.json` contains `"tests"` with exactly the keys `test/auth.spec.ts#auth->-accepts-a-good-password` (`passed`), `test/auth.spec.ts#auth->-rejects-bad-password` (`failed`), `test/auth.spec.ts#auth->-remembers-me` (`skipped`), and `"errors": []`
    - `packages/core/test/__golden__/lint-report.lint.json` has `"errors": 0` and `"warnings": 0`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The host reads the report the way it reads the tokens file, refuses a path outside the root, and the reference report fixture round-trips into `snapshot.tests` with the documented ids.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Functions and types: `scanJUnit`, `TestStatus` (`load/junit.ts`); `containedPath` (`cli/src/load/fs.ts`, rename of `tokensPath`)
- Model: `AccordConfig.tests?: { report }`, `RepoSnapshot.tests?` (`model/snapshot.ts`)
- Loader: `REPORT_TAG` constant, `load.report-invalid` finding (`load/snapshot.ts`)
- Rule id (loader): `load.report-invalid`
- Schema key: `tests.report` in `config.schema.json`
- Tests: `packages/core/test/junit.test.ts`; new cases in `schemas.test.ts` and `cli/test/load.test.ts`
- Fixture: `packages/core/test/fixtures/lint-report/` (config, REPORT.md, reports/junit.xml)
- Goldens: `lint-report.snapshot.json`, `lint-report.lint.json`
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

| Source | Item | Disposition |
|---|---|---|
| Owner decision (RESEARCH Open Question 5 / A2, resolved 2026-09-14) | `@test:` id is `norm(classname)#norm(name)`, whitespace runs become `-`, `norm(name)` alone when `classname` is empty; exact, case-sensitive match, no basename fallback | Owner-confirmed; fixtures and reason strings here and in 03-05 use this shape |
| RESEARCH A3 | `load.report-invalid` (error, report file, line 1) when the text has no `<testsuites`/`<testsuite`/`<testcase`; `tests` is `{}` in that case | Adopted so a garbage report yields one finding, not one per scenario |
| RESEARCH A7 | Duplicate ids resolve to the worst status (`failed` > `skipped` > `passed`) | Adopted; a retry never upgrades a result |
| D-72 | An empty but well-formed report (`<testsuites/>`) gives `tests: {}` and no loader finding; every `@test:` id is then unknown (03-05) | Chosen |
| Threat T-03-03 | A hand-written "all passed" report committed to the repo is not detected by lint | Accepted, carried to Phase 4 / CI design (R-03-01) |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.yml` path → CLI filesystem read | A contributor controls `tests.report`; the host must not read outside the repository |
| report XML → `scanJUnit` | Untrusted XML produced by a test runner or by hand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Information disclosure | `tests.report: ../../secrets.xml` pulling a file outside the repo into the snapshot | high | mitigate | `containedPath` (same `relative(root, abs)` check as T-02-20) applied to the report path in Task 2; `skips a report path outside the repository` test |
| T-03-10 | Tampering | duplicate ids where a later `passed` overwrites an earlier `failed` | medium | mitigate | `RANK` merge keeps the worst status (Task 1, Test 4 both orders) |
| T-03-11 | Tampering | CDATA or comment bodies containing a fake `<testcase>` | medium | mitigate | CDATA and comments blanked before matching (Task 1, Tests 3 and 5) |
| T-03-03 | Tampering | hand-written passing report committed to the repository | medium | accept | Out of lint's scope; Phase 4 gate design and CI ownership of the report (R-03-01) |
| T-03-01 | Denial of service | megabyte report against the scanner | low | mitigate | Single forward pass, `[^>]*?` bounded by `>`, `indexOf` for the closing tag; documented in a comment |
| T-03-SC | Tampering | npm installs | high | mitigate | No XML package added; `package-lock.json` untouched |
</threat_model>

<verification>
- Task gates are the `<automated>` commands. The CLI test needs `npm run build` first (it imports the built core).
- Wave-end: `npm test` (both projects) green once 03-02 and 03-04 finish.
</verification>

<success_criteria>
- FMT-11 loader half done: schema key, host read with containment, core scanner, `snapshot.tests`, garbage guard, all pinned.
- D-72 loader steps and A2 (owner-confirmed), A3, A7 are executable; A3 and A7 are recorded as assumptions for the owner.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/03-lint/03-03-SUMMARY.md` when done. Do not commit.
</output>
