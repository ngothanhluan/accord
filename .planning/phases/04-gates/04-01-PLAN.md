---
phase: 04-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/gate/hash.ts
  - packages/core/src/gate/rules.ts
  - packages/core/src/gate/ready.ts
  - packages/core/src/gate/index.ts
  - packages/core/src/lint/render.ts
  - packages/core/src/index.ts
  - packages/core/src/model/snapshot.ts
  - docs/design.md
  - packages/core/schemas/ticket.schema.json
  - packages/core/templates/ticket-build.md
  - packages/core/templates/ticket-maintain.md
  - packages/core/src/generated/templates.ts
  - packages/core/test/hash.test.ts
  - packages/core/test/gate.test.ts
  - packages/core/test/render.test.ts
  - packages/core/test/schemas.test.ts
  - packages/core/test/fixtures/gate-ready/accord/config.yml
  - packages/core/test/fixtures/gate-ready/accord/tickets/CLEAN.md
  - packages/core/test/fixtures/gate-ready/accord/tickets/HYGIENE.md
  - packages/core/test/fixtures/gate-ready/accord/tickets/NOUI.md
  - packages/core/test/fixtures/gate-ready/accord/tickets/UINOREF.md
  - packages/core/test/fixtures/gate-ready/accord/tickets/SCHEMA.md
  - packages/core/test/fixtures/gate-ready/accord/tickets/THIN.md
  - packages/core/test/fixtures/gate-maintain/accord/config.yml
  - packages/core/test/fixtures/gate-maintain/accord/tickets/UIPROTO.md
  - packages/core/test/fixtures/gate-maintain/accord/tickets/UILINK.md
  - packages/core/test/fixtures/gate-maintain/accord/assets/UIPROTO/prototype.html
  - packages/core/test/__golden__/valid-build.LOGIN-1.ready.json
  - packages/core/test/__golden__/gate-ready.CLEAN.ready.json
  - packages/core/test/__golden__/gate-ready.HYGIENE.ready.json
  - packages/core/test/__golden__/gate-ready.NOUI.ready.json
  - packages/core/test/__golden__/gate-ready.UINOREF.ready.json
  - packages/core/test/__golden__/gate-ready.SCHEMA.ready.json
  - packages/core/test/__golden__/gate-ready.THIN.ready.json
  - packages/core/test/__golden__/gate-maintain.UIPROTO.ready.json
  - packages/core/test/__golden__/gate-maintain.UILINK.ready.json
  - packages/core/test/__golden__/gate-ready.snapshot.json
  - packages/core/test/__golden__/gate-ready.lint.json
  - packages/core/test/__golden__/gate-maintain.snapshot.json
  - packages/core/test/__golden__/gate-maintain.lint.json
  - packages/core/test/__golden__/ticket-build.verified-empty.md
autonomous: true
requirements: [GATE-01, GATE-06, GATE-07]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`gateReady(snapshot, id)` returns `{ gate: 'ready', ticket: id, verdict, findings, acHash }` and is pure: two calls on the same snapshot produce byte-identical `stableJson`, and the snapshot is unchanged afterwards (D-87, structuredClone equality)"
    - "`verdict` is `fail` when and only when at least one entry of `findings` has `level: 'error'`; a result carrying only warnings is `pass` (D-87)"
    - "`gateReady` on fixture `valid-build` ticket `LOGIN-1` is `pass` with `acHash` matching `^fnv1a64:[0-9a-f]{16}$`, and its findings are exactly the three ticket-scoped lint warnings the Phase 3 golden already records (`lint.test-tag-missing` twice, `lint.plan-empty`) (D-87, D-89)"
    - "`acHash` is present on every `gateReady` result whose ticket has at least one `@ac-n`-tagged scenario, on a `fail` verdict as well as a `pass` (D-86)"
    - "`acHash(scenarios)` is FNV-1a 64-bit over the UTF-8 bytes of the join, rendered `fnv1a64:` plus exactly 16 lowercase hex digits, zero-padded (D-75)"
    - "Hash input is, per `@ac-n`-tagged scenario sorted by the numeric part of the tag, the bare tag text followed by that scenario's `steps`; untagged scenarios contribute nothing, and no other tag (`@test:`, `@ui`, `@smoke`) is in the input, so adding a `@test:` tag leaves `acHash` unchanged (D-77, D-69)"
    - "Reordering two whole scenarios in the file leaves `acHash` unchanged; swapping the `@ac-1` and `@ac-2` tags between them changes it (D-77)"
    - "Ready's findings are exactly the lint findings whose `file` is `accord/tickets/<id>.md`, or starts with `accord/tickets/<id>/` or `accord/assets/<id>/`, plus the gate rows; a finding on another ticket or on `accord/config.yml` never appears (D-89)"
    - "`lint.sentinel`, `lint.open-question`, and `lint.assumption-unconfirmed` appear in a Ready result at `level: 'error'` and make the verdict `fail`, while `lint.vague-wording` appears at `level: 'warning'` and leaves the verdict alone (D-89, design.md §5)"
    - "A ticket whose frontmatter fails the schema fails Ready through the ticket-scoped loader errors alone, with no gate rule of its own (D-89, D-58)"
    - "`gate.design-missing` (error) fires for `ui: true` on `maintain` when `accord/assets/<id>/prototype.html` is absent from `snapshot.files`, and for `ui: true` on `build` when both that prototype and a non-empty `design:` URL are absent; `ui: false` is silent on both profiles (design.md §5, D-89)"
    - "`gate.intent-empty` (error) fires when the ticket's `## Intent` section exists and every one of its lines is blank after `stripHtmlComments`, and `gate.ears-missing` (error) fires when no line of `ticket.requirements` returns a `pattern` from the existing `classifyEars`; both stay silent when the heading is absent, because `lint.heading-missing` (error) already owns that (GATE-01 clauses 2 and 3, design.md §5 Ready table)"
    - "A ticket carrying both headings with an empty `## Intent` and a `## Requirements` holding only non-EARS prose fails Ready, so GATE-01's \"intent, at least one EARS line\" clauses are enforced by rules the Ready path evaluates rather than by the shape of the headings alone (GATE-01, ROADMAP criterion 1)"
    - "`docs/design.md` §5's Ready table lists a row for the non-empty `## Intent` and a row for the at-least-one-EARS-line requirement, so the table and `READY_RULES` name the same set of Ready reasons (GATE-01)"
    - "`gate.ticket-unknown` (error) is the whole result when `id` is not a key of `snapshot.tickets`, and `acHash` is then absent"
    - "Every gate rule id matches `^gate\\.[a-z-]+$`, ids are unique across `READY_RULES` and `DONE_RULES`, and every emitted `gate.*` finding carries the `level` the engine stamped from its table row, because a check returns `GateDraft`, whose type omits `level` (D-59, D-57, D-88)"
    - "`downgradeMaintain(findings, profile)` forces `lint.token-hardcoded`, `lint.intent-oversize`, `lint.requirements-oversize`, and `lint.scenarios-oversize` to `level: 'warning'` on profile `maintain` and returns the input untouched on `build`; it is applied once, in the shared engine body, so `gateReady` and `gateDone` both inherit it and GATE-12 stays a one-cell table edit; a synthetic `error`-level row for each of those four ids proves the downgrade, and in v0.1 the real table already levels all four `warning`, so no fixture result differs between profiles (D-88, GATE-07, ROADMAP criterion 5)"
    - "`renderText` accepts a `GateResult` and a `LintResult` alike and produces byte-identical output for the same `findings`; the summary counts are derived from `findings`, and every existing render golden and lint golden is unchanged (D-60, D-87)"
    - "`packages/core/src/index.ts` exports `gateReady` as a value and `GateResult` as a type; `READY_RULES`, `DONE_RULES`, and the gate check functions stay unexported from the barrel (D-55)"
    - "`SnapshotInput` and `RepoSnapshot` carry optional `git?: { commit: string; authors: Record<string, string> }`, and `TicketFrontmatter` carries optional `verified_hash?: string` and `verified_commit?: string`; every Phase 1-3 snapshot golden is byte-unchanged because absent keys are absent, not `undefined` (D-78, D-76, D-54)"
    - "`ticket.schema.json` declares `verified_hash` with pattern `^fnv1a64:[0-9a-f]{16}$` and `verified_commit` with pattern `^[0-9a-fA-F]+$` and `minLength: 7`, both under the existing `additionalProperties: false`; a ticket carrying neither key still validates (D-76, D-75, D-81)"
    - "`packages/core/src/gate/` passes the existing ESLint `no-restricted-imports` core-purity guard and `npm run typecheck` under `types: []`, so the FNV-1a implementation is hand-written against the `TextEncoder` and `BigInt` globals alone (CLAUDE.md isomorphic-core constraint, D-75)"
  artifacts:
    - path: "packages/core/src/gate/hash.ts"
      provides: "acHash and the D-77 join"
      contains: "export function acHash("
    - path: "packages/core/src/gate/index.ts"
      provides: "GateResult, gateReady, the scope filter, and the finding sort"
      contains: "export function gateReady("
    - path: "packages/core/src/gate/rules.ts"
      provides: "GateDraft, GateRule, READY_RULES, DONE_RULES, READY_PROMOTE, MAINTAIN_DOWNGRADE, downgradeMaintain"
      contains: "export const READY_RULES"
    - path: "packages/core/src/gate/ready.ts"
      provides: "designMissing, ticketUnknown, intentEmpty, earsMissing"
      contains: "export const designMissing"
    - path: "packages/core/test/hash.test.ts"
      provides: "pinned FNV-1a vectors, the tag/step join, sort-by-number, and the tag-outside-hash property"
      min_lines: 40
    - path: "packages/core/test/gate.test.ts"
      provides: "the case table, the golden loop, purity and sort checks, and the downgrade unit test"
      min_lines: 60
    - path: "packages/core/test/__golden__/valid-build.LOGIN-1.ready.json"
      provides: "the Ready tracer golden"
      contains: "fnv1a64:"
  key_links:
    - from: "packages/core/src/gate/index.ts"
      to: "packages/core/src/lint/index.ts"
      via: "gateReady calls lintSnapshot and filters its findings by ticket scope instead of re-running any rule"
      pattern: "lintSnapshot"
    - from: "packages/core/src/gate/index.ts"
      to: "packages/core/src/gate/hash.ts"
      via: "acHash over the ticket's tagged scenarios, computed on pass and fail alike"
      pattern: "acHash"
    - from: "packages/core/src/lint/render.ts"
      to: "packages/core/src/gate/index.ts"
      via: "renderText takes anything carrying findings, so GateResult renders with no adapter"
      pattern: "findings"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`, and no attribution trailer is written; the working tree is left dirty for the author to review"
      status: flagged
      verification: unverified
    - statement: "No flag, option, environment variable, or config key added by this plan skips a gate rule or forces a `pass` verdict; the only inputs to `gateReady` are the snapshot and the ticket id (GATE-06)"
      status: flagged
      verification: unverified
    - statement: "No gate check swallows an internal error and returns an empty draft list; a check that cannot evaluate emits a finding rather than silence, so a crash never reads as a pass (PITFALLS §9)"
      status: flagged
      verification: unverified
    - statement: "`verdict` is derived from `findings` and from nothing else; no separate boolean, counter, or short-circuit can make a result `pass` while an `error` finding exists (D-87)"
      status: flagged
      verification: unverified
    - statement: "No `level` literal for a `gate.*` id appears outside `READY_RULES` and `DONE_RULES`; a check body cannot set one, because `GateDraft` omits `level` from the type (D-57, D-59)"
      status: flagged
      verification: unverified
    - statement: "The `maintain` downgrade list is data in one table and is not consulted anywhere else; no rule body branches on `profile` to soften itself, and `downgradeMaintain` is called from exactly one place so no gate can be left out of the matrix (D-88, GATE-07)"
      status: flagged
      verification: unverified
    - statement: "This plan does not promote `lint.token-hardcoded` or any size rule to `error` on either profile; GATE-12 stays deferred (D-66, D-88)"
      status: flagged
      verification: unverified
    - statement: "No shipped rule id, reason string, or comment names a design tool, framework, plugin, or planning system; the design-reference reason names `design:` and `accord/assets/<id>/prototype.html` only (CLAUDE.md 'no other tools named')"
      status: flagged
      verification: unverified
---

<objective>
**As a** BA about to move a story into the sprint, **I want** one call to tell me whether the ticket is Ready and, if not, name every reason by rule id, **so that** the story cannot enter the sprint on somebody's opinion that it looks finished.

This is the tracer for Phase 4: one thin path from `RepoSnapshot` through a gate rule table to a `GateResult` and out through the existing text renderer, proved end to end on the `valid-build` fixture before a second rule exists. It then fills out the Ready reasons and lands the frontmatter contract the Done tick binding needs.

Purpose: ROADMAP criterion 1 (Ready passes only on a complete ticket, and a pass records `ac_hash`) and criterion 5's profile half become executable.
Output: `packages/core/src/gate/` with `hash.ts`, `rules.ts`, `ready.ts`, `index.ts`; `renderText` widened; two new fixtures; two rows added to the `docs/design.md` §5 Ready table; the `git`, `verified_hash`, and `verified_commit` additions to the model, the schema, and the templates.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-gates/04-CONTEXT.md
@.planning/phases/04-gates/04-PATTERNS.md
@.planning/phases/03-lint/03-01-SUMMARY.md
@packages/core/src/lint/rules.ts
@packages/core/src/lint/index.ts
@packages/core/src/model/snapshot.ts

<interfaces>
```typescript
// packages/core/src/gate/hash.ts
export function acHash(scenarios: readonly ScenarioRef[]): string | undefined; // 'fnv1a64:<16 lowercase hex>'; undefined when no scenario carries an @ac-n tag
export function hashInput(scenarios: readonly ScenarioRef[]): string;          // module-internal shape, exported for the unit test only

// packages/core/src/gate/rules.ts
export type GateDraft = Omit<Finding, 'level' | 'rule'>;
export interface GateRule {
  id: `gate.${string}`;
  level: Level;
  profiles: readonly ('build' | 'maintain')[];
  check: (snapshot: RepoSnapshot, id: string) => GateDraft[];
}
export const READY_RULES: readonly GateRule[];
export const DONE_RULES: readonly GateRule[];              // empty in this plan; filled by 04-02..04-04
export const READY_PROMOTE: readonly string[];             // the three LINT-06 hygiene ids
export const MAINTAIN_DOWNGRADE: readonly string[];        // the token and size lint ids
export function downgradeMaintain(findings: Finding[], profile: 'build' | 'maintain'): Finding[];

// packages/core/src/gate/ready.ts
export const designMissing: GateRule['check'];
export const ticketUnknown: GateRule['check'];
export const intentEmpty: GateRule['check'];   // GATE-01 clause 2, added in Task 2
export const earsMissing: GateRule['check'];   // GATE-01 clause 3, added in Task 2

// packages/core/src/gate/index.ts
export interface GateResult {
  gate: 'ready' | 'done';
  ticket: string;
  verdict: 'pass' | 'fail';
  findings: Finding[];
  acHash?: string;
}
export function gateReady(snapshot: RepoSnapshot, id: string): GateResult;
export function scoped(findings: Finding[], id: string): Finding[];   // module-internal, reused by 04-02
export function byFileLineRule(a: Finding, b: Finding): number;       // module-internal, copied from lint/index.ts

// packages/core/src/lint/render.ts (widened, not rewritten)
export function renderText(result: { findings: Finding[] }): string;  // counts derived from findings
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "gate ready on a clean ticket" — one path only</name>
  <reversibility rating="costly">D-75 and D-87: the `fnv1a64:` hash value is written into ticket files that outlive the release, and Phase 5 serialises `GateResult` under `--json`, so both are cheap to write and expensive to change once a real ticket carries them.</reversibility>
  <read_first>
    - packages/core/src/lint/index.ts (whole file, 33 lines): `cmp`, `byFileLineRule`, the `profile` line, and the `.map((d) => ({ ...d, rule: r.id, level: r.level }))` stamping this task copies
    - packages/core/src/lint/rules.ts lines 36-52: the `Draft` type, the `Rule` interface with its template-literal id, and the row shape `READY_RULES` mirrors
    - packages/core/src/lint/render.ts (whole file, 12 lines): the exact output shape that must not change
    - packages/core/test/render.test.ts lines 8-34: the `LintResult` literals whose type must keep compiling after the parameter widens
    - packages/core/src/model/snapshot.ts lines 20-27 (`ScenarioRef`, especially `acTag` and `steps`) and lines 29-42 (`TicketFrontmatter`)
    - packages/core/src/lint/ticket.ts lines 34-44: the `section`, `stripped`, `tickets`, `typed` helpers and the `Pitfall 9` note about type-dependent rules needing frontmatter
    - packages/core/test/lint.test.ts lines 1-45 and 78-108: fixture golden loop, `beforeAll` discipline, `stableJson` + `toMatchFileSnapshot`, and the `valid-build` pinned block that records LOGIN-1's three warnings
    - packages/core/test/helpers/fixture.ts (whole file): `readFixture`, `variants`, `stableJson`
    - packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1.md (the tracer ticket: `ui: true`, a Figma `design:` URL, two `@ac-n` scenarios with a Background, `verified: [ac-1]`)
    - packages/core/test/tokens.test.ts lines 1-6: the unit-test import style (module internals, never the barrel) and the `body(...lines)` literal helper
  </read_first>
  <files>packages/core/src/gate/hash.ts, packages/core/src/gate/rules.ts, packages/core/src/gate/ready.ts, packages/core/src/gate/index.ts, packages/core/src/lint/render.ts, packages/core/src/index.ts, packages/core/test/hash.test.ts, packages/core/test/gate.test.ts, packages/core/test/render.test.ts, packages/core/test/__golden__/valid-build.LOGIN-1.ready.json</files>
  <behavior>
    - Test 1 (`hashInput`, D-77): two scenarios tagged `@ac-2` (steps `A`, `B`) then `@ac-1` (steps `C`) produce the join for `ac-1`,`C`,`ac-2`,`A`,`B` in that order, fields separated by `U+0000`. A third scenario with `acTag` undefined contributes nothing. A scenario also tagged `@test:x` and `@ui` produces the identical join as the same scenario without those tags.
    - Test 2 (`acHash`, D-75): the return matches `^fnv1a64:[0-9a-f]{16}$` for a non-empty input and is `undefined` when no scenario carries an `@ac-n` tag. Two pinned vectors are asserted as literals so the algorithm cannot drift: the empty string and the ASCII string `ac-1` (write the literals the first run produces, after checking by hand that the offset basis is `0xcbf29ce484222325n`, the prime is `0x100000001b3n`, and each step masks with `0xffffffffffffffffn`). A Vietnamese step (`họ thấy bảng điều khiển`) hashes over its UTF-8 bytes, so its hash differs from the hash of the same text with the diacritics stripped.
    - Test 3 (`acHash` stability, D-77): swapping the whole scenario objects in the array leaves the hash unchanged; swapping only their `acTag` values changes it; appending `@test:a-b` to a scenario's `tags` leaves it unchanged.
    - Test 4 (`gateReady` on `valid-build`/`LOGIN-1`): `gate` is `ready`, `ticket` is `LOGIN-1`, `verdict` is `pass`, `acHash` matches the hash pattern, and `findings` is exactly the three ticket-scoped warnings `lint.test-tag-missing` at line 37, `lint.test-tag-missing` at line 42, `lint.plan-empty` at line 50, each `level: 'warning'`.
    - Test 5 (purity and determinism): `gateReady` called twice on the same snapshot returns identical `stableJson`; the snapshot deep-equals a `structuredClone` taken before the call; `findings` is already sorted by `byFileLineRule`.
    - Test 6 (`renderText` on a `GateResult`): rendering the Task-4 result produces three `file:line: warning rule reason` lines followed by `0 errors, 3 warnings` and one trailing newline; rendering the same findings wrapped in a `LintResult` produces byte-identical text.
    - Test 7 (`gate.ticket-unknown`): `gateReady(snapshot, 'NOPE')` is `verdict: 'fail'` with exactly one finding, rule `gate.ticket-unknown`, file `accord/tickets/NOPE.md`, level `error`, and the result carries no `acHash` key.
  </behavior>
  <action>
Write the tests first, then the source.

`packages/core/test/hash.test.ts` covers behaviours 1 to 3, importing `acHash` and `hashInput` from `../src/gate/hash.js` directly (not the barrel), building `ScenarioRef` literals inline.

`packages/core/src/gate/hash.ts`: header comment naming GATE-01, GATE-03, D-75, D-77. Export `hashInput(scenarios)`: keep only scenarios whose `acTag` is defined, sort them by `Number(acTag.slice(3))` (numeric ascending, and where two carry the same tag keep the array order — `lint.ac-tag-duplicate` is already an error so Ready has failed anyway), then flatten each to its `acTag` followed by its `steps` and join every field with the single code unit `U+0000`. Export `acHash(scenarios)`: return `undefined` when the filtered list is empty; otherwise encode `hashInput` with `new TextEncoder()` (a Web-standard global, already verified to typecheck under `types: []`; do not import any Node built-in), fold the bytes with FNV-1a 64-bit using `BigInt` — offset basis `0xcbf29ce484222325n`, per byte XOR then multiply by `0x100000001b3n`, masking with `0xffffffffffffffffn` after every multiply — and return `'fnv1a64:'` plus `h.toString(16).padStart(16, '0')`.

`packages/core/src/gate/rules.ts`: header comment naming GATE-06, GATE-07, D-57, D-59, D-87, D-88. Declare `GateDraft` as `Omit<Finding, 'level' | 'rule'>` and `GateRule` with `id: \`gate.${string}\``, `level: Level`, `profiles: readonly ('build' | 'maintain')[]`, and `check: (snapshot: RepoSnapshot, id: string) => GateDraft[]`. Export `READY_RULES` as `readonly GateRule[]` with two rows this task: `gate.ticket-unknown` (error, both profiles, `ticketUnknown`) and `gate.design-missing` (error, both profiles, `designMissing`). Export `DONE_RULES` as an empty `readonly GateRule[]` for 04-02 to fill. Export `READY_PROMOTE` as `['lint.sentinel', 'lint.open-question', 'lint.assumption-unconfirmed']` and `MAINTAIN_DOWNGRADE` as `['lint.token-hardcoded', 'lint.intent-oversize', 'lint.requirements-oversize', 'lint.scenarios-oversize']`, both `readonly string[]`. Export `downgradeMaintain(findings, profile)` returning `findings` unchanged when `profile` is `build`, and otherwise a new array where every entry whose `rule` is in `MAINTAIN_DOWNGRADE` is spread with `level: 'warning'`.

`packages/core/src/gate/ready.ts`: header comment naming GATE-01, D-89 and `docs/design.md` §5. `ticketUnknown(snapshot, id)` returns one draft `{ file: 'accord/tickets/' + id + '.md', reason: 'no ticket ' + id + ' in the snapshot' }` when `id` is not an own key of `snapshot.tickets`, else `[]`. `designMissing(snapshot, id)` returns `[]` when the ticket is absent, its `frontmatter` is undefined, or `frontmatter.ui` is false; otherwise let `proto` be `'accord/assets/' + id + '/prototype.html'` and `hasProto` be whether `proto` is an own key of `snapshot.files`, and `hasLink` be whether `frontmatter.design` is a non-empty string. On profile `maintain`, emit a draft when `hasProto` is false with pointer `/ui` and a reason naming the missing `accord/assets/<id>/prototype.html`. On profile `build`, emit a draft when neither `hasProto` nor `hasLink` with pointer `/ui` and a reason naming both the `design:` URL and the same prototype path as the two ways to satisfy it. Read the profile as `snapshot.config?.profile ?? 'build'`, the same line `lintSnapshot` uses.

`packages/core/src/gate/index.ts`: header comment naming GATE-01, GATE-06, D-87, D-89. Copy `cmp` and `byFileLineRule` from `lint/index.ts` verbatim (code-point compare, never a locale-aware compare, so the goldens are identical on every host). Declare and export the `GateResult` interface exactly as the interfaces block gives it. A module-internal `scoped(findings, id)` keeps a finding when `f.file` equals `'accord/tickets/' + id + '.md'`, or starts with `'accord/tickets/' + id + '/'`, or starts with `'accord/assets/' + id + '/'`; use string comparison, never a constructed regular expression, because a ticket id may contain `.`, `-`, and `_`. Export `gateReady(snapshot, id)`: read `profile`; if `id` is not an own key of `snapshot.tickets`, return `{ gate: 'ready', ticket: id, verdict: 'fail', findings: [the ticketUnknown draft stamped from its row] }` with no `acHash` key; otherwise build `findings` as (a) `downgradeMaintain(scoped(lintSnapshot(snapshot).findings, id), profile)` with every entry whose `rule` is in `READY_PROMOTE` spread to `level: 'error'` (the two lists are disjoint, so the order of the two passes cannot matter), plus (b) every row of `READY_RULES` whose `profiles` includes `profile`, each check's drafts stamped with `rule` and `level` from the row; sort the union with `byFileLineRule`; compute `acHash` from `snapshot.tickets[id].scenarios`; return the result with `acHash` spread in only when it is defined, and `verdict` as `findings.some((f) => f.level === 'error') ? 'fail' : 'pass'`. Do not mutate `snapshot` or any array it owns. Keep `downgradeMaintain` to exactly one call site: 04-02 Task 1 lifts this body into a shared function that `gateDone` also uses, and GATE-07 is written about the profile matrix as a whole, not about Ready alone, so a second call site is how one gate silently falls out of the matrix when GATE-12 flips a level.

`packages/core/src/lint/render.ts`: widen the parameter from `LintResult` to `{ findings: Finding[] }` and derive the two counts inside from `findings` instead of reading `result.errors` and `result.warnings`. Keep every other byte of the output shape, including the literal plural. This is the resolution of the D-87 open question: `GateResult` renders through `renderText` with no adapter and without adding two derivable fields to the JSON contract Phase 5 publishes. In `packages/core/test/render.test.ts`, the one call that passes a fresh object literal with `errors` and `warnings` now fails TypeScript's excess-property check; drop those two properties from that literal only and leave every other line of the file alone.

`packages/core/src/index.ts`: add `export { gateReady } from './gate/index.js';` and `export type { GateResult } from './gate/index.js';` next to the lint exports, and extend the file's opening comment so it names the gate exports the way it names `lintSnapshot`. Do not export `READY_RULES`, `DONE_RULES`, or any check.

`packages/core/test/gate.test.ts`: header comment saying the goldens are regenerated with `npm test -- --project core gate -u -t "<case name>"`. Declare a `CASES` table of `{ name, fixture, ticket, gate }` rows — gate cases are ticket-scoped, so the table is explicit rather than a directory scan. This task adds one row, `{ name: 'valid-build.LOGIN-1.ready', fixture: 'valid-build', ticket: 'LOGIN-1', gate: 'ready' }`. Loop the table with `describe('gate ' + row.gate + ' ' + row.fixture + '/' + row.ticket)`, reading the fixture inside `beforeAll` so a `-t` filter never touches a sibling fixture, and assert `await expect(stableJson(result)).toMatchFileSnapshot('./__golden__/' + row.name + '.json')`. Add the pinned `valid-build` block for behaviours 4 to 7 and the purity, sort, and `renderText` checks.

Run `npm test -- --project core hash`, then generate the one golden with a scoped `-u`, read it, rerun without `-u`, then `npm run lint && npm run typecheck`. Leave everything uncommitted.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core hash &amp;&amp; npm test -- --project core gate &amp;&amp; npm test -- --project core render &amp;&amp; npm test -- --project core lint</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or any golden reported obsolete or mismatched; or a `lint` golden changing (this task must not alter any Phase 3 lint output)</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/gate/hash.ts` contains `export function acHash(` and `0xcbf29ce484222325n`; `grep -cE "^import .*'node:" packages/core/src/gate/hash.ts` prints 0
    - `packages/core/src/gate/index.ts` contains `export function gateReady(` and `export interface GateResult`; `grep -c "lintSnapshot" packages/core/src/gate/index.ts` prints at least 1
    - `grep -cE "^\s*export const (READY_RULES|DONE_RULES|READY_PROMOTE|MAINTAIN_DOWNGRADE)" packages/core/src/gate/rules.ts` prints 4
    - `packages/core/src/index.ts` contains `gateReady` and `GateResult`; `grep -c "READY_RULES" packages/core/src/index.ts` prints 0
    - `packages/core/test/__golden__/valid-build.LOGIN-1.ready.json` exists, contains `"verdict": "pass"`, contains `fnv1a64:`, and contains `lint.plan-empty`
    - `npm test -- --project core hash` reports at least 3 passing tests and 0 failures
    - `git status --porcelain -- packages/core/test/__golden__` lists no path ending `.lint.json`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>`gateReady` runs a real ticket from a real fixture through the rule table to a `GateResult` with a real `ac_hash`, and that result prints through the existing renderer with no adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Ready failure coverage — the two missing GATE-01 clauses, the hygiene promotion, the design reference on both profiles, and the maintain downgrade</name>
  <read_first>
    - packages/core/src/gate/index.ts, rules.ts, ready.ts (Task 1) and packages/core/test/gate.test.ts (the `CASES` table this task extends)
    - packages/core/src/lint/ears.ts: `classifyEars`, which is already exported and returns `{ pattern }` or `{ reason }` — `gate.ears-missing` reuses it rather than restating what an EARS line is
    - packages/core/src/lint/ticket.ts lines 34-44: the module-private `section` and `stripped` helpers built from `headingKey` and `stripHtmlComments`, which `ready.ts` reproduces in three lines rather than exporting them out of a lint module
    - docs/design.md §5, the Ready table: four rows today, and the two GATE-01 clauses it does not list
    - packages/core/src/lint/ticket.ts lines 16-31 and 60-130: which conditions raise `lint.sentinel`, `lint.open-question`, `lint.assumption-unconfirmed`, and `lint.vague-wording`, so each fixture ticket triggers exactly the intended rule
    - packages/core/test/fixtures/lint-hygiene/accord/tickets/HYGIENE.md and CLEAN.md: the working shapes for a hygiene-dirty and a hygiene-clean ticket
    - packages/core/test/fixtures/valid-build/accord/config.yml (profile `build`) and packages/core/test/fixtures/verification-edges/accord/config.yml (the empty `design.tokens` shape)
    - packages/core/test/fixtures/lint-no-tokens/accord/assets/A/prototype.html and packages/core/templates/prototype-header.html: a prototype whose header keeps `lint.prototype-derivation` silent
    - packages/core/test/lint.test.ts lines 12-17 and packages/core/test/snapshot.test.ts (the directory-scan discovery): adding a fixture directory with an `accord/` folder creates a new `.lint.json` and a new `.snapshot.json` golden automatically
    - packages/core/test/fixtures/frontmatter-errors/accord/tickets/SCHEMA.md: the shape of a ticket that fails the schema
  </read_first>
  <files>packages/core/src/gate/ready.ts, packages/core/src/gate/rules.ts, docs/design.md, packages/core/test/fixtures/gate-ready/accord/config.yml, packages/core/test/fixtures/gate-ready/accord/tickets/CLEAN.md, packages/core/test/fixtures/gate-ready/accord/tickets/HYGIENE.md, packages/core/test/fixtures/gate-ready/accord/tickets/NOUI.md, packages/core/test/fixtures/gate-ready/accord/tickets/UINOREF.md, packages/core/test/fixtures/gate-ready/accord/tickets/SCHEMA.md, packages/core/test/fixtures/gate-ready/accord/tickets/THIN.md, packages/core/test/fixtures/gate-maintain/accord/config.yml, packages/core/test/fixtures/gate-maintain/accord/tickets/UIPROTO.md, packages/core/test/fixtures/gate-maintain/accord/tickets/UILINK.md, packages/core/test/fixtures/gate-maintain/accord/assets/UIPROTO/prototype.html, packages/core/test/gate.test.ts, packages/core/test/__golden__/gate-ready.CLEAN.ready.json, packages/core/test/__golden__/gate-ready.HYGIENE.ready.json, packages/core/test/__golden__/gate-ready.NOUI.ready.json, packages/core/test/__golden__/gate-ready.UINOREF.ready.json, packages/core/test/__golden__/gate-ready.SCHEMA.ready.json, packages/core/test/__golden__/gate-ready.THIN.ready.json, packages/core/test/__golden__/gate-maintain.UIPROTO.ready.json, packages/core/test/__golden__/gate-maintain.UILINK.ready.json, packages/core/test/__golden__/gate-ready.snapshot.json, packages/core/test/__golden__/gate-ready.lint.json, packages/core/test/__golden__/gate-maintain.snapshot.json, packages/core/test/__golden__/gate-maintain.lint.json</files>
  <action>
**File count, accepted deliberately.** This task lists 27 paths. Twelve are goldens that no one authors — each is produced by one scoped `vitest -u` and read as a diff, so the review unit is the diff, not the file. Nine are fixture tickets of roughly twenty lines each, differing from one another in one respect apiece. The hand-written source surface is three files: `ready.ts`, `rules.ts`, and `docs/design.md`. Splitting the fixture build away from the rule work was considered and rejected: it would produce one plan whose entire content is Markdown fixtures whose expected findings cannot be stated without the rules, and a second plan that cannot verify itself. Keep them together and review by diff.


**The two missing GATE-01 clauses.** GATE-01 requires Ready to pass only with "valid frontmatter, intent, at least one EARS line, at least one tagged scenario, no LINT-06 findings, and a design reference when the profile requires it". Five of those six are already enforced by rules the Ready path evaluates — frontmatter by the ticket-scoped loader findings, the tagged scenario by `lint.no-scenarios` and `lint.ac-tag-missing` (both `error`), the hygiene set by `READY_PROMOTE`, and the design reference by `gate.design-missing`. Intent and the EARS line are not: `lint.heading-missing` catches an **absent** heading, and `lint.ears-unclassified` is a warning that fires per unclassifiable line and so emits nothing at all for a section holding no lines. A ticket with both headings present and both sections empty therefore passes Ready today. Close it with two rows in `READY_RULES`, not with two new `lint.*` rules at `error`: the gate table is already ticket-scoped by signature (`(snapshot, id)`), so a `gate.*` row runs only where Ready runs, whereas an `error`-level lint rule would make `accord lint` exit 1 on every half-written ticket in the repository and would move a lint golden for every existing fixture. That is the same reasoning Phase 3 used to keep `lint.vague-wording` a warning: lint is the always-on advisor, the gate is the thing that blocks.

Add to `packages/core/src/gate/ready.ts`, importing `classifyEars` from `../lint/ears.js` and `headingKey`, `stripHtmlComments` from `../load/sections.js`:
- `intentEmpty` (GATE-01): find the ticket section whose `headingKey` is `intent`; return `[]` when there is none, because `lint.heading-missing` at `error` already fails Ready for that. Otherwise run `stripHtmlComments` over its lines and, when every remaining line is blank after trimming, emit one draft on the ticket file at that heading's line, reason saying `## Intent` is empty and Ready needs the intent written.
- `earsMissing` (GATE-01): return `[]` when there is no section whose `headingKey` is `requirements`. Otherwise, when no entry of `ticket.requirements` yields a `pattern` from `classifyEars(l.text)`, emit one draft on the ticket file at that heading's line, reason saying `## Requirements` holds no line in one of the six EARS patterns and naming `the system shall` as the subject every pattern needs. Reuse `classifyEars` verbatim; do not restate what an EARS line is.

Add the two rows to `READY_RULES`, both `error`, both profiles: `gate.intent-empty`, `gate.ears-missing`. Then add the two rows they correspond to to the Ready table in `docs/design.md` §5, which lists four checks today and omits both — one row reading that `## Intent` says what the story is for, owner BA, and one reading that `## Requirements` carries at least one EARS line, owner BA. This is a documentation sync, not a new decision: `REQUIREMENTS.md` GATE-01 already requires both, and without the rows the table and the rule table name different sets of Ready reasons.

**Fixture `gate-ready`** — `accord/config.yml` copied from `valid-build`'s shape with `profile: build`, `design.tokens: ""`, and no `tests:` key. Six tickets, each `type: story`, `status: open`, with the five D-07 headings and one `@ac-1` scenario carrying two steps. Every one of them except `THIN` carries a non-empty `## Intent` and at least one `## Requirements` line that `classifyEars` classifies (for example `WHEN the user submits valid credentials the system SHALL open the dashboard`), so the two new rules stay silent everywhere they are not the point:
- `CLEAN.md`: `ui: false`, nothing else. Expected Ready verdict `pass`, `findings` empty apart from any warning the lint table already raises for a scenario with no `@test:` tag — pin whatever that is in the golden. `CLEAN` is also the negative proof for the two new rules: a ticket with real intent and one EARS line raises neither.
- `HYGIENE.md`: `ui: false`, one uppercase sentinel word in `## Intent`, one unchecked `- [ ]` item under `## Open questions`, one `assumptions:` entry with `confirmed: false`, and one of the seven vague words inside an otherwise valid EARS line in `## Requirements` (for example `WHEN the payment maybe fails the system SHALL retry once`), so `lint.vague-wording` fires without `gate.ears-missing` also firing and muddying the golden. Expected: `lint.sentinel`, `lint.open-question`, and `lint.assumption-unconfirmed` present at `level: 'error'`, `lint.vague-wording` present at `level: 'warning'`, verdict `fail`.
- `NOUI.md`: `ui: false` and no `design:` key. Expected: no `gate.design-missing`.
- `UINOREF.md`: `ui: true`, no `design:` key, no prototype under `accord/assets/UINOREF/`. Expected: one `gate.design-missing` at `level: 'error'`, verdict `fail`.
- `SCHEMA.md`: frontmatter missing the required `title`, body otherwise complete. Expected: the ticket-scoped `schema.required` loader finding at `level: 'error'`, verdict `fail`, and no gate rule beyond `gate.design-missing` staying silent because `frontmatter` is undefined. `gate.intent-empty` and `gate.ears-missing` read sections, not frontmatter, so they run on this ticket and stay silent because its body is complete — that is the point of giving it a complete body.
- `THIN.md`: `ui: false`, a `## Intent` heading whose only content is a blank line and an HTML comment, and a `## Requirements` heading holding two lines of prose that name no `the system shall` subject. Everything else is as `CLEAN`. Expected: exactly one `gate.intent-empty` and exactly one `gate.ears-missing`, both at `level: 'error'`, verdict `fail`, alongside the two `lint.ears-unclassified` warnings the prose lines already earn.

**Fixture `gate-maintain`** — the same config with `profile: maintain`. Two tickets, both with a non-empty `## Intent` and an EARS line:
- `UIPROTO.md`: `ui: true`, no `design:` key, with `accord/assets/UIPROTO/prototype.html` present. Give that prototype the shipped header with `TICKET-ID` replaced by `UIPROTO` and a `Derived from:` line naming a path that exists in the fixture, so `lint.prototype-derivation` stays silent. Expected: no `gate.design-missing`, verdict `pass`.
- `UILINK.md`: `ui: true` with a `design:` URL and no prototype. Expected: one `gate.design-missing` at `level: 'error'` — on `maintain` a link does not substitute for a prototype (`docs/design.md` §5). The ticket is named for what it carries, a design link, and not for any vendor.

**`gate.test.ts`** — add the eight rows to `CASES` (`gate-ready.CLEAN.ready`, `.HYGIENE.ready`, `.NOUI.ready`, `.UINOREF.ready`, `.SCHEMA.ready`, `.THIN.ready`, `gate-maintain.UIPROTO.ready`, `gate-maintain.UILINK.ready`), so the golden loop covers them. Add a pinned `describe` asserting, by rule id and level: the three promoted hygiene rules are `error` on `HYGIENE` while `lint.vague-wording` is `warning`; `NOUI` and `UIPROTO` carry no `gate.design-missing`; `UINOREF` and `UILINK` carry exactly one each; `SCHEMA`'s findings all have `file` equal to `accord/tickets/SCHEMA.md`. Add a scope test proving isolation: `gateReady(gate-ready snapshot, 'CLEAN')` contains no finding whose `file` names `HYGIENE`.

Add the GATE-01 pinned block: `THIN` carries exactly one `gate.intent-empty` and exactly one `gate.ears-missing`, both `error`, and its verdict is `fail`; `CLEAN` carries neither and its verdict is `pass`; and every other ticket in both fixtures carries neither, so the two rules fire on the ticket built for them and nowhere else. Add the GATE-01 coverage assertion itself: for each of the six GATE-01 clauses, name the rule id that enforces it (`schema.*` loader findings, `gate.intent-empty`, `gate.ears-missing`, `lint.no-scenarios`, the three `READY_PROMOTE` ids, `gate.design-missing`) and assert each of the four `gate.*` ids among them is a row of `READY_RULES` at `level: 'error'` — so a later refactor that drops a row fails a test rather than quietly re-opening the gate.

Add the `downgradeMaintain` unit test with synthetic rows (ROADMAP criterion 5, D-88): four hand-built `Finding` objects, one per id in `MAINTAIN_DOWNGRADE`, each at `level: 'error'`, plus one `lint.id-mismatch` at `level: 'error'` as a control. On `maintain` the four become `warning` and the control stays `error`; on `build` all five are returned unchanged and the input array is not mutated. Add a second assertion that every id in `MAINTAIN_DOWNGRADE` is a real row of the lint `RULES` table and is already `level: 'warning'` there, which is why no v0.1 fixture distinguishes the two profiles on these rules. Add a third: `downgradeMaintain` is called from exactly one place in `packages/core/src/gate/index.ts`, asserted by reading nothing — state it as a `describe` name and prove it with the shared-body structure — because GATE-07 covers both gates and a second call site is how one of them silently falls out of the matrix. Give this `describe` a stable name; 04-02 Task 1 extends it with the same synthetic rows driven through `gateDone` once the shared body exists.

Add the table test: every `READY_RULES` and `DONE_RULES` id matches `/^gate\.[a-z-]+$/`, ids are unique across both tables, and `READY_PROMOTE` entries are all real lint rule ids.

Generate the new goldens one fixture at a time with a scoped `-u` (`npm test -- --project core gate -u -t "<case name>"`, then `npm test -- --project core lint -u -t "fixture gate-ready"`, then the same for `gate-maintain` and for the snapshot project), read each diff, and rerun without `-u`. Confirm no golden belonging to an existing fixture changed; any that did is a finding for the SUMMARY. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core gate &amp;&amp; npm test -- --project core lint &amp;&amp; npm test -- --project core snapshot</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched; or `git status --porcelain packages/core/test/__golden__` listing a modified golden whose name starts with a Phase 1-3 fixture name; or `gate-ready.CLEAN.ready.json` or `gate-maintain.UIPROTO.ready.json` reporting a verdict other than `pass`, which means one of the two new GATE-01 rules fired outside `THIN`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/gate-ready.HYGIENE.ready.json` contains `"rule": "lint.sentinel"` on a line whose sibling `level` is `error`, and contains `"rule": "lint.vague-wording"` with `level` `warning`, and contains `"verdict": "fail"`
    - `packages/core/test/__golden__/gate-ready.UINOREF.ready.json` contains `gate.design-missing`; `grep -c "gate.design-missing" packages/core/test/__golden__/gate-ready.NOUI.ready.json` prints 0
    - `packages/core/test/__golden__/gate-maintain.UILINK.ready.json` contains `gate.design-missing`; `grep -c "gate.design-missing" packages/core/test/__golden__/gate-maintain.UIPROTO.ready.json` prints 0
    - `packages/core/test/__golden__/gate-ready.SCHEMA.ready.json` contains `schema.required` and `"verdict": "fail"`
    - `grep -cE "'gate\.(intent-empty|ears-missing)'" packages/core/src/gate/rules.ts` prints 2, and both rows carry `level: 'error'`
    - `grep -c "gate.intent-empty" packages/core/test/__golden__/gate-ready.THIN.ready.json` prints 1; `grep -c "gate.ears-missing" packages/core/test/__golden__/gate-ready.THIN.ready.json` prints 1; that golden contains `"verdict": "fail"`
    - `grep -cE "gate\.(intent-empty|ears-missing)" packages/core/test/__golden__/gate-ready.CLEAN.ready.json` prints 0, and that golden contains `"verdict": "pass"`
    - `grep -c "classifyEars" packages/core/src/gate/ready.ts` prints at least 1 (the EARS definition is reused, not restated)
    - `grep -c "at least one EARS line" docs/design.md` prints 1 and `grep -c "^| .*## Intent" docs/design.md` prints 1 — the two rows are in the §5 Ready table, whose four existing rows are unchanged
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' packages/core/src/gate/index.ts | grep -cF 'downgradeMaintain('` prints 1 — exactly one live **call site**. The trailing `(` is load-bearing and is what makes this criterion reachable: `index.ts` must import the symbol from `rules.ts`, and an import line carries the bare name with no parenthesis, so a correct file holds one import plus one call and a count of bare mentions would read 2. Counting call sites also makes the import style irrelevant — a named import and a namespace import both leave one `downgradeMaintain(`. Comments are filtered first because this plan's own wording about the single call site is the most likely thing to end up in a header comment
    - `packages/core/test/gate.test.ts` contains `downgradeMaintain` and `MAINTAIN_DOWNGRADE`
    - `ls packages/core/test/__golden__/gate-ready.snapshot.json packages/core/test/__golden__/gate-ready.lint.json packages/core/test/__golden__/gate-maintain.snapshot.json packages/core/test/__golden__/gate-maintain.lint.json` lists four files
    - `npm test -- --project core gate` reports 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Every clause of GATE-01 is enforced by a rule the Ready path evaluates, every Ready reason in `docs/design.md` §5 has a fixture ticket and a golden and the table lists every reason the rules enforce, both profiles are covered for the design reference, and the `maintain` downgrade is proved on synthetic rows from a single call site.</done>
</task>

<task type="auto">
  <name>Task 3: The tick-binding frontmatter contract — model, schema, and template guidance</name>
  <reversibility rating="costly">D-76: `verified_hash` and `verified_commit` enter `ticket.schema.json` under `additionalProperties: false` and the shipped templates, so once a real ticket carries them the key names are a published contract.</reversibility>
  <read_first>
    - packages/core/src/model/snapshot.ts lines 1-8 (the plain-records-only header constraint), lines 29-42 (`TicketFrontmatter`), and lines 85-99 (`RepoSnapshot`, especially the D-72 `tests?` line that is the precedent for `git?`)
    - packages/core/schemas/ticket.schema.json (whole file): `additionalProperties: false`, and the `ac_hash` / `verified` property shapes to copy
    - packages/core/test/schemas.test.ts lines 29-60 and 211-245: the `ticket({...})` helper, the `has(findings, pointer, rule)` helper, and the D-25 / D-03 tests the two new keys sit beside
    - packages/core/templates/ticket-build.md lines 1-22 and packages/core/templates/ticket-maintain.md: the commented guidance block, especially the `# ac_hash:` and `# verified: []` lines and the note that `verified` is always the last key
    - packages/core/test/templates.test.ts lines 64-135: the D-04 key-order test, the "build and maintain differ only inside body HTML comments" test, and the generated-module drift test
    - packages/core/test/write.test.ts lines 45-60: the `ticket-build.verified-empty.md` golden that snapshots the whole template after a `setFrontmatterKey` call
    - .planning/phases/03-lint/03-04-SUMMARY.md: how the previous template change was regenerated and which goldens moved
  </read_first>
  <files>packages/core/src/model/snapshot.ts, packages/core/schemas/ticket.schema.json, packages/core/templates/ticket-build.md, packages/core/templates/ticket-maintain.md, packages/core/src/generated/templates.ts, packages/core/test/schemas.test.ts, packages/core/test/__golden__/ticket-build.verified-empty.md</files>
  <action>
**Model (D-78, D-76).** In `packages/core/src/model/snapshot.ts`, add to `TicketFrontmatter`, immediately after `verified`, the two optional scalars `verified_hash?: string` and `verified_commit?: string`, each with a trailing comment citing D-76 and giving the shapes `fnv1a64:<16 hex>` and a commit sha of at least 7 hex characters. Add to both `SnapshotInput` and `RepoSnapshot` the optional field `git?: { commit: string; authors: Record<string, string> }` with a trailing comment citing D-78 and stating that it is host-supplied, that `authors` is keyed by repo-relative posix path plus one entry keyed by the value of `commit` carrying the gated commit's author, and that the whole field is absent — not `undefined` — when the host cannot supply it, so the D-54 goldens are unchanged. Keep the file type-only and keep every value a plain record or array.

**Schema (D-76, D-75, D-81).** In `packages/core/schemas/ticket.schema.json`, add two properties after `ac_hash`: `verified_hash` as `{ "type": "string", "pattern": "^fnv1a64:[0-9a-f]{16}$" }` and `verified_commit` as `{ "type": "string", "pattern": "^[0-9a-fA-F]+$", "minLength": 7 }`. Neither goes in `required`. Use `pattern`, never `format`, so the schema stays self-contained for a non-ajv consumer. Extend `packages/core/test/schemas.test.ts` beside the existing D-25 test with a D-76 case: a ticket carrying neither key validates clean; `verified_hash: 'fnv1a64:0123456789abcdef'` validates; `verified_hash: 'abc'` raises `schema.pattern` at pointer `/verified_hash`; `verified_hash: 'FNV1A64:0123456789ABCDEF'` raises `schema.pattern` (the prefix and the digits are lowercase); `verified_commit: '1234567'` validates; `verified_commit: '123456'` raises `schema.minLength` at `/verified_commit`; `verified_commit: 'zzzzzzz'` raises `schema.pattern`; and an unknown sibling key still raises `schema.additionalProperties`.

**Templates.** In `packages/core/templates/ticket-build.md` and `packages/core/templates/ticket-maintain.md`, add two commented guidance lines directly under the existing `# verified: []` line, keeping the same `# ` comment style and staying commented so no new frontmatter key is introduced and the D-04 key-order test is unaffected: one naming `verified_hash` as the AC hash the ticks were made against, copied from `ac_hash` at the moment of ticking, and one naming `verified_commit` as the commit sha those ticks were made against. Say in the guidance that Done fails unless both match the current values, and that both are written by hand in v0.1. Do not name any tool. Then run `npm run gen` to refresh `packages/core/src/generated/templates.ts`, regenerate `packages/core/test/__golden__/ticket-build.verified-empty.md` with a scoped `-u`, read the diff to confirm it is exactly the two added comment lines, and rerun without `-u`.

Finally run the whole suite so the model and schema additions are proved not to move any Phase 1-3 golden. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run gen &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core schemas &amp;&amp; npm test -- --project core templates &amp;&amp; npm test -- --project core write &amp;&amp; npm test</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched; or the generated-module drift test reporting that `packages/core/src/generated/templates.ts` differs from `packages/core/templates/`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "verified_hash" packages/core/src/model/snapshot.ts` prints at least 1; `grep -c "verified_commit" packages/core/src/model/snapshot.ts` prints at least 1; `grep -c "git?:" packages/core/src/model/snapshot.ts` prints 2
    - `packages/core/schemas/ticket.schema.json` contains `^fnv1a64:[0-9a-f]{16}$` and `"minLength": 7`
    - `grep -c "verified_hash" packages/core/templates/ticket-build.md packages/core/templates/ticket-maintain.md packages/core/src/generated/templates.ts` prints a non-zero count for each of the three files
    - `packages/core/test/__golden__/ticket-build.verified-empty.md` contains `verified_hash`
    - `npm test` reports 0 failures across both projects
    - `git status --porcelain -- packages/core/test/__golden__` lists no modified golden other than `ticket-build.verified-empty.md` and the goldens 04-01 Tasks 1 and 2 created
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The two tick-binding keys exist in the type, the schema, and the shipped templates, and no existing snapshot, lint, or write golden moved.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 04-01)

**New files:** `packages/core/src/gate/hash.ts`, `packages/core/src/gate/rules.ts`, `packages/core/src/gate/ready.ts`, `packages/core/src/gate/index.ts`, `packages/core/test/hash.test.ts`, `packages/core/test/gate.test.ts`.

**New exported values:** `acHash`, `hashInput`, `READY_RULES`, `DONE_RULES`, `READY_PROMOTE`, `MAINTAIN_DOWNGRADE`, `downgradeMaintain`, `designMissing`, `ticketUnknown`, `intentEmpty`, `earsMissing`, `gateReady`.

**New exported types:** `GateResult`, `GateRule`, `GateDraft`.

**Module-internal symbols:** `scoped`, `byFileLineRule`, `cmp` (in `gate/index.ts`).

**Barrel additions (`packages/core/src/index.ts`):** value `gateReady`, type `GateResult`.

**Changed signature:** `renderText(result: { findings: Finding[] })` — widened from `LintResult`; output byte-identical.

**New rule ids:** `gate.ticket-unknown`, `gate.design-missing`, `gate.intent-empty`, `gate.ears-missing`.

**New model fields:** `SnapshotInput.git`, `RepoSnapshot.git`, `TicketFrontmatter.verified_hash`, `TicketFrontmatter.verified_commit`.

**New schema properties:** `verified_hash`, `verified_commit` in `packages/core/schemas/ticket.schema.json`.

**New fixtures:** `gate-ready` (tickets `CLEAN`, `HYGIENE`, `NOUI`, `UINOREF`, `SCHEMA`, `THIN`), `gate-maintain` (tickets `UIPROTO`, `UILINK`; asset `accord/assets/UIPROTO/prototype.html`).

**New goldens:** `valid-build.LOGIN-1.ready.json`, `gate-ready.{CLEAN,HYGIENE,NOUI,UINOREF,SCHEMA,THIN}.ready.json`, `gate-maintain.{UIPROTO,UILINK}.ready.json`, `gate-ready.{snapshot,lint}.json`, `gate-maintain.{snapshot,lint}.json`.

**New vitest test-name filters:** `--project core hash`, `--project core gate`, and the `gate <gate> <fixture>/<ticket>` describe names.

**Changed shipped document:** two rows added to the Ready table in `docs/design.md` §5.

**New CLI flags, subcommands, config keys:** none. Phase 4 adds no process surface; ROADMAP criterion 5's exit-code clause is Phase 5's, and CONTEXT locks it out of this phase.
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

Six of the 26 probe-surfaced edge items arrived `unclassified`; three of those belong to this plan's requirements and are listed below. All three remain `category: unclassified, status: unresolved` in `coverage.json` — the GATE-01 row records that the gap it pointed at is closed in Task 2, which is a fact about the plan and not a change to the probe's own status. Count for this plan: 0 probe items authored into `must_haves.truths` + 3 surfaced here = 3 probe items for GATE-01, GATE-06, GATE-07. An unclassified probe item carries no edge predicate to author, so nothing moves between the two columns; the phase total stays 20 + 6 = 26.

| Source | Item | Disposition |
|---|---|---|
| Edge probe, GATE-01 (unclassified) | The probe could not classify GATE-01, so no edge predicate was derived for it | Still unclassified in the probe; the gap it pointed at is closed in Task 2. The question this row asked — whether any Ready condition was missing from the fixture set — turned out to have the answer yes, twice: GATE-01's "intent" and "at least one EARS line" clauses had no enforcing rule, and `docs/design.md` §5's Ready table omitted both, so the design doc could not surface the gap either. Closed in Task 2 by `gate.intent-empty` and `gate.ears-missing` in `READY_RULES`, the `THIN` fixture, and two rows added to the §5 table. Ready's remaining edges stay covered by the `must_haves.truths` written from §5 and D-89 |
| Edge probe, GATE-06 (unclassified) | The probe could not classify GATE-06 | Unresolved. Exit codes are Phase 5; this plan covers "reasons listed by rule" and "no bypass" through the rule table and the prohibitions block |
| Edge probe, GATE-07 (unclassified) | The probe could not classify GATE-07 | Unresolved. D-88 already answers it: the mechanism ships with inert data and is proved by a unit test on synthetic rows |
| Open question 1 (pattern mapper), D-87 vs `renderText` | `renderText` takes `LintResult` (`{ findings, errors, warnings }`); `GateResult` has none of the three count fields | **Resolved by widening `renderText`'s parameter to `{ findings: Finding[] }` and deriving the counts inside.** Output is byte-identical, every existing caller keeps compiling, and one object literal in `render.test.ts` drops two now-excess properties. Alternative rejected: adding `errors` and `warnings` to `GateResult`, because D-87 enumerates that shape, rates it `costly`, and Phase 5 publishes it under `--json`; duplicating derivable counts in a published JSON contract is the cost D-87's rating warns about |
| D-77 (separator not pinned) | The join separator for the hash input is not specified. Chosen: a single `U+0000` between every field (tag and each step) | Chosen because it cannot occur in Markdown source, so the encoding is injective; a `\n` separator would let a whitespace-collapsed step whose text is literally `ac-2` collide with a tag line. **The resulting hash value is a published contract (D-75 costly), so overturn this before the first real ticket records an `ac_hash`, not after** |
| D-89 (findings set not pinned) | Ready returns *every* ticket-scoped lint finding, warnings included, with only the three LINT-06 hygiene rules promoted to `error` | Chosen from D-87 ("every warning leaves the verdict alone") and D-89 ("`lint.vague-wording` must never block Ready"), both of which presuppose warnings are present in the result. Alternative: return only the blocking findings, which would make the result terser but would drop the vague-wording signal the BA is meant to read |
| D-89 / GATE-06 (exit-2 branch) | Core does not model the config-error branch. `gateReady` still runs when `snapshot.config` is undefined, defaulting the profile to `build`, exactly as `lintSnapshot` does | Chosen. Phase 5 already loads the snapshot and can branch on `snapshot.config === undefined` for exit 2; adding a third verdict value or an error channel to `GateResult` would change the shape D-87 pins for one caller that does not exist yet |
| D-86 (Done not mentioned) | `acHash` is returned by `gateDone` as well as `gateReady`, whenever the ticket has a tagged scenario | Chosen; the value is already computed for the D-76 comparison and Phase 5 benefits under `--json`. D-86 only mentions Ready |
| `docs/design.md` §5 ("a Figma link") | On profile `build`, the design reference is satisfied by **any** non-empty `design:` URL, not by a host check. The `gate-maintain` fixture ticket for that case is named `UILINK`, not for the vendor | Chosen. The schema already forces `^https://`, and a shipped rule that matches a vendor host would name another tool in code and in the reason text, which `.claude/CLAUDE.md` forbids. Test fixtures are outside the published `files:` list, but the name is avoidable and the policy's spirit is broad, so the fixture is named for what it carries. Alternative: require the URL host to be that vendor's |
| GATE-01 vs `docs/design.md` §5 (mechanism choice) | The two missing clauses are closed with `gate.*` rows in `READY_RULES`, not with `lint.*` rules promoted to `error` | Chosen on the same simplicity grounds Phase 3 used to keep `lint.vague-wording` a warning: lint is the always-on advisor and the gate is the thing that blocks. A gate row is ticket-scoped by signature and runs only where Ready runs; an `error`-level lint rule would make `accord lint` exit 1 on every half-written ticket in the repository and would move a lint golden for every existing fixture. Alternative: `lint.intent-empty` and `lint.ears-missing` at `error`, which is the same enforcement at the cost of a noisier linter and a wider golden diff |
| `docs/design.md` §5 Ready table (two rows added) | The table gained a `## Intent` row and an at-least-one-EARS-line row | Treated as a documentation sync rather than a decision, because `REQUIREMENTS.md` GATE-01 already requires both and the table was the thing out of step. **The owner should confirm the two rows read right**, since §5 is normative for the Ready checks and this is the only place in the phase where a shipped document gains content rather than code |
| CONTEXT discretion (`valid-build` and GATE-04) | `valid-build` is **not** given `test/login.spec.ts` and is not otherwise edited | Chosen. GATE-04 runs only at Done, and `valid-build` can never pass Done (`@ac-2` records `Result: fail`, no `@test:` tags, no report configured, no `## Verification notes`). Leaving it untouched keeps every Phase 1-3 snapshot, lint, CLI, and write golden stable; the Done end-to-end pass path gets its own `gate-done` fixture in 04-02 |
| CONTEXT discretion (templates) | The ticket templates **do** gain commented `verified_hash` / `verified_commit` guidance | Chosen because a human must hand-write both keys in v0.1 and there is no command to write them, so the template is the only place the key names are discoverable at authoring time. Cost is one `npm run gen` and one write golden. Alternative: leave the templates alone and rely on the Phase 6 skill text |
| ROADMAP `Mode: mvp` on Phase 4 | The ROADMAP Goal line for Phase 4 is not in `As a / I want / so that` form, and Phase 4 is core-library work with no user-facing surface until Phase 5 | Following the Phase 1, 2, and 3 UAT precedent (each cleared `Mode: mvp` as an infrastructure phase), Phase 4 looks like the same case. **No user story was invented and the ROADMAP was not edited** — the plans carry per-plan `As a` framing only, as the Phase 3 plans did. Raise this at UAT |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket Markdown and frontmatter → the gate rule table | Repository content authored by anyone with commit access, possibly large, possibly crafted |
| ticket id (from the caller) → the scope filter and the constructed file paths | An id that reached the gate from a CLI argument in Phase 5 |
| scenario steps → the FNV-1a input and `TextEncoder` | Free text, including Vietnamese, that decides a value written back into a file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | a ticket id containing `.`, `-`, or `_` used to widen the scope filter and pull another ticket's clean findings into this result | high | mitigate | `scoped` compares strings with equality and `startsWith` only; no regular expression is constructed from `id` (Task 1 action; the Task 2 isolation test proves `CLEAN` never sees a `HYGIENE` finding) |
| T-04-02 | Tampering | crafted scenario steps chosen to collide two different acceptance-criteria sets onto one `ac_hash`, so an AC edit survives Done | high | mitigate | Fields are joined with `U+0000`, which cannot appear in the Markdown source, making the encoding injective; the algorithm prefix `fnv1a64:` makes a future change fail loudly rather than compare wrong (D-75) |
| T-04-03 | Denial of service | a very large `## Acceptance criteria` block driving the hash and the scope filter | low | mitigate | Both are single linear passes over already-parsed data; no backtracking regular expression is introduced, and the gate reads `ticket.scenarios` rather than re-scanning Markdown |
| T-04-04 | Tampering | a `gate.*` finding constructed with a `level` literal inside a check body, so a rule silently self-downgrades | medium | mitigate | Checks return `GateDraft` (`level` omitted from the type) and the engine stamps `level` from the table row; the Task 2 table test asserts every id and level comes from `READY_RULES` or `DONE_RULES` |
| T-04-05 | Elevation of privilege | core reaching a Node built-in to read the filesystem or spawn git, making the gate non-deterministic and non-isomorphic | high | mitigate | The existing ESLint `no-restricted-imports` block globs `packages/core/src/**/*.ts` and `types: []` removes the ambient declarations, so `gate/` is covered the moment the folder exists; `npm run lint && npm run typecheck` is in every verify command |
| T-04-06 | Information disclosure | a reason string echoing a full absolute path from a caller's machine | low | accept | Every path in a gate reason is constructed from the repo-relative `id` or read from `Finding.file`, which the loader has already made repo-relative and posix (D-51) |
| T-04-SC | Tampering | npm, pip, or cargo installs | high | mitigate | No package is added by this plan; `package.json` dependency blocks are untouched, so no package-legitimacy checkpoint is required |
</threat_model>

<verification>
- Task 1's gate is `lint`, `typecheck`, and the four scoped vitest runs; the `lint` run is there specifically to prove the `renderText` widening moved no Phase 3 lint golden.
- Task 3's gate is the full suite in both projects, which is where a model or schema regression against a Phase 1-3 golden would surface.
- ROADMAP criterion 1 is proved by `gate-ready` and `gate-maintain`, with `THIN` covering the two GATE-01 clauses no rule enforced before this task; criterion 5's profile half by the `downgradeMaintain` unit test on synthetic rows, from a single call site so both gates inherit it (D-88, GATE-07).
</verification>

<success_criteria>
- `gateReady` and `GateResult` are exported from the core barrel and a real fixture ticket returns a `pass` verdict with an `ac_hash`.
- Every clause of GATE-01 is enforced by a rule the Ready path evaluates, and every Ready reason in `docs/design.md` §5 has a fixture ticket, a golden, and a rule id — in both directions, because §5's table gained the two rows it was missing.
- `verified_hash` and `verified_commit` exist in the type, the schema, and both templates.
- Nothing committed; the working tree is left for the author to review.
</success_criteria>

<output>
Create `.planning/phases/04-gates/04-01-SUMMARY.md` when done. Do not commit.
</output>
