---
phase: 02-core-model-and-loading
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/load/sections.ts
  - packages/core/test/sections.test.ts
  - packages/core/test/__golden__/body-edges.snapshot.json
  - packages/core/test/fixtures/body-edges/accord/config.yml
  - packages/core/test/fixtures/body-edges/accord/tickets/DUP.md
  - packages/core/test/fixtures/body-edges/accord/tickets/CASE.md
  - packages/core/test/fixtures/body-edges/accord/tickets/FENCE.md
  - packages/core/test/fixtures/body-edges/accord/tickets/UNTERMINATED.md
  - packages/core/test/fixtures/body-edges/accord/tickets/TILDE.md
  - packages/core/test/fixtures/body-edges/accord/tickets/NOREQ.md
autonomous: true
requirements: [FMT-05, FMT-04, CORE-06]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `## Acceptance criteria` heading and a `- WHEN ...` line written inside a fenced block (backtick or tilde, terminated or not) are never a section and never a requirement (D-39, D-41, STACK Decision 4; ROADMAP criterion 3)"
    - "`## Acceptance Criteria ##`, `##   requirements`, and `## INTENT` match the D-07 headings: matching is case-insensitive, whitespace-collapsed, and ignores trailing `#` characters (D-39)"
    - "A second `## Requirements` heading yields `load.heading-duplicate` at the line of the second occurrence, the first section's lines are the requirements, and nothing is merged (D-40)"
    - "EARS lines are every non-blank line under `## Requirements` after removing single-line and multi-line HTML comments and fenced blocks, with one leading `-`, `*`, `+`, or `1.` marker stripped; plain lines and bullets are both requirements; each carries its Markdown line; `###` sub-heading lines are not requirements (D-41, FMT-05)"
    - "A `gherkin` fence under `## Plan` (or any heading other than `## Acceptance criteria`) contributes no scenario (D-36)"
    - "A ticket with no `## Requirements` section has `requirements: []` and no loader finding; missing headings are Phase 3's (D-07, D-35)"
  artifacts:
    - path: "packages/core/test/__golden__/body-edges.snapshot.json"
      provides: "golden for six body-edge tickets"
      contains: "load.heading-duplicate"
    - path: "packages/core/test/sections.test.ts"
      provides: "unit tests for scan, headingKey, requirementLines, stripHtmlComments, duplicateHeadings"
      min_lines: 60
  key_links:
    - from: "packages/core/src/load/snapshot.ts"
      to: "packages/core/src/load/sections.ts"
      via: "scan(body, bodyOffset), requirementLines, duplicateHeadings (wired in 02-01)"
      pattern: "from '\\./sections\\.js'"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "The scanner never parses Markdown it was not asked to parse: no setext headings, no indented code blocks, no HTML blocks, no CommonMark parser dependency; the fixed template is the contract (STACK Decision 4)"
      status: resolved
      verification: judgment
    - statement: "The loader never classifies an EARS line (WHEN/IF/WHILE/WHERE/SHALL) or reports a missing heading; it extracts and Phase 3 judges (D-41, D-07)"
      status: resolved
      verification: judgment
---

<objective>
**As a** BA who pastes a code sample, a comment, or a tilde fence into a ticket, **I want to** be sure that nothing inside a fence or a comment is mistaken for a heading or a requirement, and that a duplicated heading is pointed at, **so that** gates never pass or fail on layout accidents (PITFALLS §10).

Purpose: 02-01's scanner handles the template layout. This slice proves ROADMAP criterion 3 and D-36/D-39/D-40/D-41 against adversarial bodies and hardens `load/sections.ts` where they fail.

Output: the `body-edges` fixture and golden, `sections.test.ts`, and any corrections to `load/sections.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-model-and-loading/02-CONTEXT.md
@.planning/phases/02-core-model-and-loading/02-01-SUMMARY.md
@.planning/research/STACK.md
@packages/core/src/load/sections.ts
@packages/core/src/load/snapshot.ts
@packages/core/test/helpers/fixture.ts
@packages/core/templates/ticket-build.md

<interfaces>
<!-- From 02-01. -->
```typescript
import { scan, headingKey, requirementLines, stripHtmlComments, duplicateHeadings, d07Key } from '../src/load/sections.js';
import type { Fence } from '../src/load/sections.js';   // { info: string; open: number; content: Line[] }
// Section = { heading: string; line: number; lines: Line[] }; Line = { line: number; text: string }
// scan(body, bodyOffset) -> { sections, fences }; requirementLines(section, fences) -> Line[]
// duplicateHeadings(file, sections, keyOf: (section: Section) => string | undefined) -> Finding[]
//   (rule 'load.heading-duplicate', line = second occurrence). keyOf returns the identity to dedupe on, or undefined for an
//   untracked section: tickets pass d07Key (normalised D-07 heading key); the verification parser passes the ac-n tag (02-01 step 6).
// snapshot.test.ts wraps each fixture in describe('fixture <name>'); scope golden creation with -t "fixture body-edges".
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scanner unit tests — fences, heading normalisation, comment stripping, duplicates</name>
  <read_first>
    - packages/core/src/load/sections.ts (code under test)
    - .planning/research/STACK.md Decision 4 (fence rules: ``` or ~~~, length >= 3, closing at least as long, info string, headings at column 0 only outside a fence)
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-39, D-40, D-41 and Claude's Discretion (`###` inside a `##` section belongs to it)
  </read_first>
  <files>packages/core/src/load/sections.ts, packages/core/test/sections.test.ts</files>
  <action>
Write `packages/core/test/sections.test.ts` with `describe` blocks that call the scanner on small inline bodies (build them with `[...].join('\n')`, `bodyOffset` 0 unless stated) and assert:

1. `headingKey`: `'Acceptance Criteria'` → `'acceptance criteria'`; `'  Open   questions '` → `'open questions'`; `scan` strips a trailing ` ##` from `## Plan ##` so the heading is `Plan`.
2. Fences: a body `## Intent`, three backticks, `## Acceptance criteria`, `- WHEN fake`, three backticks, `## Plan` → sections are exactly Intent (line 1) and Plan (line 6); one fence with `open` 2 and two content lines. The same with `~~~` delimiters. A `~~~` line inside a backtick fence does not close it. A closing line of four backticks closes a fence opened with three; a closing line of two backticks does not. A fence opened and never closed swallows every later line (no later section). Headings indented by one space are not headings (column 0 rule).
3. Offsets: `scan(body, 17)` numbers the first body line 18.
4. `stripHtmlComments`: a single-line comment is removed leaving the surrounding text on the same line; a comment spanning three lines removes the inner line entirely and keeps text before `<!--` and after `-->`; line numbers of kept lines are unchanged.
5. `requirementLines`: `- a`, `* b`, `+ c`, `1. d`, `e` → five lines `a`..`e` with their numbers; a `### sub` line is dropped; a line inside a fence within the section is dropped; blank lines dropped; a bullet whose text is only whitespace is dropped.
6. `duplicateHeadings` with `d07Key`: two `## Requirements` → one finding at the second line with rule `load.heading-duplicate`; two `## Notes` (untracked: `d07Key` returns `undefined`) → none; `## Requirements` and `## requirements` → one finding (keys equal). With a tag `keyOf` (`s => /^@(ac-[1-9][0-9]*)\b/.exec(s.heading)?.[1]`): `## @ac-1 Một` followed by `## @ac-1 Một lần nữa` → one finding at the second line (identity is the tag, not the heading text — D-42), while `## @ac-1 A` and `## @ac-2 A` → none.

Fix `load/sections.ts` for any failing expectation; the expectations above are the contract, the code is not. Run `npm test -- --project core sections`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core sections</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary reports fewer than 6 passing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/sections.test.ts` contains `headingKey`, `stripHtmlComments`, `requirementLines`, `duplicateHeadings`, `d07Key`, `~~~`, and `load.heading-duplicate`
    - `npm test -- --project core sections` reports at least 6 passing tests and 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The scanner's fence state machine, heading normalisation, comment stripping, EARS extraction, and duplicate detection are each pinned by a unit test independent of any fixture.</done>
</task>

<task type="auto">
  <name>Task 2: The `body-edges` fixture — fenced fakes, casing, duplicates, fences under other headings</name>
  <read_first>
    - packages/core/test/snapshot.test.ts and packages/core/test/helpers/fixture.ts (discovery loop; new fixture folders are picked up automatically)
    - packages/core/src/load/snapshot.ts `parseTicket` (which section feeds `requirements`, which fences feed `scenarios`)
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-36, D-39, D-40, D-41
  </read_first>
  <files>packages/core/test/sections.test.ts, packages/core/test/__golden__/body-edges.snapshot.json, packages/core/test/fixtures/body-edges/accord/config.yml, packages/core/test/fixtures/body-edges/accord/tickets/DUP.md, packages/core/test/fixtures/body-edges/accord/tickets/CASE.md, packages/core/test/fixtures/body-edges/accord/tickets/FENCE.md, packages/core/test/fixtures/body-edges/accord/tickets/UNTERMINATED.md, packages/core/test/fixtures/body-edges/accord/tickets/TILDE.md, packages/core/test/fixtures/body-edges/accord/tickets/NOREQ.md</files>
  <action>
Create `packages/core/test/fixtures/body-edges/accord/` with a valid `config.yml` (D-16 default, `tokens: ""`) and six story tickets. Every ticket uses the same six-line valid frontmatter (`---`, `id: <stem>`, `title: t`, `type: story`, `status: open`, `---`) so body line 1 is Markdown line 7. Write each body so the expectations below hold, and record the exact line numbers you wrote in the test:

- `DUP.md`: `## Intent`, one line, `## Requirements` with `- WHEN a the system SHALL b`, then a second `## Requirements` with `- WHEN c the system SHALL d`, then `## Acceptance criteria` with a one-scenario `@ac-1` gherkin fence → one `load.heading-duplicate` finding at the second `## Requirements` line; `requirements` has exactly one entry (`WHEN a the system SHALL b`); the ticket keeps its frontmatter and its scenario.
- `CASE.md`: `## INTENT`, `##   requirements` (three spaces) with one plain EARS line, `## Acceptance Criteria ##` with a one-scenario fence → no findings; one requirement; one scenario; `sections[0].heading` is `INTENT` (raw text preserved) while matching used the key.
- `FENCE.md`: `## Requirements` containing, in order: a single-line HTML comment; a comment opened on one line, holding `- WHEN inside a comment the system SHALL not count` on the next line, and closed on a third; a `~~~` fence containing `## Acceptance criteria` and `- WHEN inside a fence the system SHALL not count`, closed by `~~~`; then `- WHEN one`, `* WHEN two`, `+ WHEN three`, `1. WHEN four`, `WHEN five`, and a `### Sub heading` line; then the real `## Acceptance criteria` with a one-scenario `@ac-1` fence; then `## Plan` containing a gherkin fence with a `@ac-9` scenario → `requirements` is exactly the five `WHEN one`..`WHEN five` lines with markers stripped and their exact lines; `sections` has exactly three entries (Requirements, Acceptance criteria, Plan); `scenarios` has exactly one entry with `acTag` `ac-1` (the Plan fence is ignored, D-36); no findings.
- `UNTERMINATED.md`: `## Intent`, one line, then a three-backtick fence that is never closed, containing `## Requirements` and `- WHEN swallowed the system SHALL not count` → `sections` is exactly one entry (Intent); `requirements` is `[]`; `scenarios` `[]`; no findings (flagged assumption: an unterminated fence is content, not an error).
- `TILDE.md`: `## Acceptance criteria` with a three-backtick `gherkin` fence whose content includes a line that is exactly `~~~` before the scenario, closed with four backticks → one scenario; no findings.
- `NOREQ.md`: `## Intent` only → `requirements` `[]`, `scenarios` `[]`, no findings.

Append `describe('body-edges fixture')` to `sections.test.ts` loading `loadSnapshot(readFixture('body-edges'))` and asserting each expectation above with exact lines. Record `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` first. Run `npm test -- --project core snapshot -u -t "fixture body-edges"` once to create `body-edges.snapshot.json` — never `-u` without the `-t` filter in this wave, because the discovery loop walks every fixture folder and an unscoped `-u` would rewrite the tracer golden and sibling plans' half-written goldens. Read it, verify, then run `npm test -- --project core snapshot -t "fixture (body-edges|valid-build)"` without `-u`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core sections && npm test -- --project core snapshot -t "fixture (body-edges|valid-build)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a `|core|` summary reports 0 tests; or a golden is reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/body-edges.snapshot.json` parses; `errors` is exactly one object with `file` `accord/tickets/DUP.md` and `rule` `load.heading-duplicate`; `tickets.DUP.requirements` has length 1
    - In the golden, `tickets.FENCE.requirements.map(r => r.text)` equals `["WHEN one","WHEN two","WHEN three","WHEN four","WHEN five"]`, `tickets.FENCE.sections.length` is 3, `tickets.FENCE.scenarios.length` is 1 with `acTag` `ac-1`
    - In the golden, `tickets.UNTERMINATED.sections.length` is 1, `tickets.CASE.scenarios.length` is 1, `tickets.CASE.requirements.length` is 1, `tickets.TILDE.scenarios.length` is 1, `tickets.NOREQ.requirements` is `[]`
    - `grep -c 'inside a fence' packages/core/test/__golden__/body-edges.snapshot.json` prints at least 1 only inside `sections[].lines[].text` entries — i.e. `node -e` over the golden confirms no `requirements[].text` and no `scenarios[]` entry contains `inside a fence` or `inside a comment`
    - `npm test -- --project core sections` and `npm test -- --project core snapshot -t "fixture (body-edges|valid-build)"` report 0 failures; `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` is unchanged from the start of the plan (the tracer golden was compared, never rewritten)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Text inside fences and comments is never a section or a requirement, heading matching is forgiving, duplicates are pointed at, and only fences under the AC heading produce scenarios.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Fixture: `packages/core/test/fixtures/body-edges/` (config + 6 tickets)
- Golden: `packages/core/test/__golden__/body-edges.snapshot.json`
- Test: `packages/core/test/sections.test.ts`
- Hardened internal module (no new exports): `packages/core/src/load/sections.ts`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions

| Source | Item | Disposition |
|---|---|---|
| FMT-05 | unclassified probe row — review manually | Flagged: FMT-05 says "one requirement per line"; the loader treats every remaining non-blank line as a requirement without classifying it, and excludes `###` heading lines. Whether a wrapped requirement (one sentence over two lines) should be joined is not decided; the loader keeps two lines and Phase 3 will warn on the unclassifiable second line. |
| D-41 | unterminated fence | Assumed: content to end of body, no finding (CommonMark semantics). Alternative: a `load.fence-unterminated` error. |
| D-40 | duplicate of an extra (non-D-07) heading | Assumed: not a finding; extra headings are free-form (D-07). |
| D-39 | H1 (`# Title`) or H3+ headings as section boundaries | Assumed: only `##` starts a section; `#` and `###` lines are ordinary lines of the enclosing section. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket body → `scan` / `requirementLines` | Untrusted Markdown with arbitrary fences and comments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | a fence or comment used to hide a heading or smuggle a fake requirement past a reader | medium | mitigate | Fence-state and comment-state scanners; the FENCE and UNTERMINATED fixtures pin that hidden text never becomes a section, requirement, or scenario |
| T-02-10 | Denial of service | very long bodies | low | accept | Linear single-pass scanner; no backtracking regexes on whole bodies |
| T-02-SC | Tampering | npm installs | high | mitigate | No packages added |
</threat_model>

<verification>
- Per-plan gates are the scoped commands in each `<verify>`; `body-edges.snapshot.json` reviewed against the expectations table; the tracer golden's hash unchanged.
- Wave-end check, not a per-plan gate: once every Wave 2 plan (02-02..02-05, 02-07) has finished, `npm test -- --project core` must report 0 failures. While a sibling plan's fixture is still being written, a failure inside that sibling's `fixture <name>` describe is not this plan's failure.
</verification>

<success_criteria>
- ROADMAP criterion 3 pinned adversarially; D-36 (heading filter), D-39, D-40, D-41 each have executable assertions.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/02-core-model-and-loading/02-03-SUMMARY.md` when done. Do not commit.
</output>
