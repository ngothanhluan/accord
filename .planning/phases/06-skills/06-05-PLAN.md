---
phase: 06-skills
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - packages/core/src/skills/targets.ts
  - packages/core/src/index.ts
  - packages/core/skills/dev/review.md
  - packages/core/skills/shared/prototype.md
  - packages/core/test/skills.test.ts
  - packages/cli/src/commands/skills.ts
  - packages/cli/test/skills-sync.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLI-08, SKILL-09]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After a runtime is dropped from `runtimes:`, `accord skills sync` names every `accord-*` directory under the abandoned target directory on stderr, together with the command to remove it, deletes nothing, and still exits 0 (D-113, CLI-08)"
    - "A config declaring a single runtime reports every `accord-*` directory under the other target directory, and reports nothing under the declared directory whose role is still declared (SKILL-09 probe: empty)"
    - "When the declared directory set is exactly equal to the full `DIRS` union — `runtimes: [cursor]`, which maps to both directories — `skills sync` prints nothing on stderr (SKILL-09 probe: adjacency)"
    - "Orphan lines are printed sorted by directory path, so two runs over the same disk state print the same lines in the same order regardless of directory-read order (SKILL-09 probe: ordering)"
    - "No rendered skill text sends its reader to a file that exists only inside accord's own repository"
    - "A reader following `accord-dev/review.md` step 2 can write `accord/tickets/<id>/verification.md` from that step alone, with no second file to open"
    - "A reader following `prototype.md` step 2 can start the prototype HTML file from that step alone, with no second file to open"
    - "REQUIREMENTS.md records CLI-08 as delivered in both the checklist and the traceability table, and SKILL-09's text names the directory the definitions actually live in"
  prohibitions:
    - "`skills sync` must not read, count, report on, or delete any directory it did not generate — widening the scan widens what is opened, never what is inspected or acted on"
    - "The new inline prose must not restate what `accord lint`, `accord gate ready`, or `accord gate done` check; it names the command and lets the command print its own reasons (D-126: naming a format slot such as `Derived from:` is the folder convention and passes; saying what happens when that slot is absent, empty, or names a path outside the repository reproduces `lint.prototype-derivation`'s three reasons and fails)"
    - "The new inline prose must not depend on a file or a command that does not ship in this phase — it must be followable in a repository that has only what Phase 6 delivers"
    - "No requirement checkbox or traceability row may be ticked ahead of the evidence that closes it; SKILL-04 stays open in this plan"
  artifacts:
    - "packages/core/src/skills/targets.ts — exports `allSkillDirs()`, the full union of the `DIRS` table"
    - "packages/core/src/index.ts — re-exports `allSkillDirs` from the skills barrel line"
    - "packages/cli/src/commands/skills.ts — `orphans()` scans the full union and judges directory and role independently"
    - "packages/cli/test/skills-sync.test.ts — a dropped-runtime case, an equal-sets case, and the no-unreported-directory invariant"
    - "packages/core/test/skills.test.ts — the `allSkillDirs` superset assertion and the no-repository-only-path assertion"
    - "packages/core/skills/dev/review.md — step 2 describes the verification.md structure inline"
    - "packages/core/skills/shared/prototype.md — step 2 describes the prototype HTML file inline, naming format slots only (D-126)"
    - "packages/core/src/generated/skills.ts — REGENERATED OUTPUT, never an edit target: produced by `npm run gen` from `packages/core/skills/**`, which is why it is absent from `files_modified`"
    - ".planning/REQUIREMENTS.md — CLI-08 ticked and Complete; SKILL-09's trailing clause amended"
  key_links:
    - "`orphans()` -> `allSkillDirs()` — the scan set, promoted from the declared subset"
    - "`skillTargets()` -> `skillDirs(config)` — the write set, deliberately unchanged"
    - "`packages/core/skills/**` -> `npm run gen` -> `packages/core/src/generated/skills.ts` -> `renderSkill` -> the copies under `.claude/skills/accord-dev/`"
    - "Task 1's widened scan -> Task 3's CLI-08 tick — the tick is only honest once the scan is whole"
---

<objective>
Close the three gaps 06-VERIFICATION.md records against Phase 6, and nothing else.

Gap 1 — `orphans()` in `packages/cli/src/commands/skills.ts` iterates `skillDirs(config)`, the
directories the config currently declares, so a directory orphaned by dropping a **runtime** is
never opened. The roles half of D-113 works; the runtimes half does not.

Gap 2 — two rendered files send the reader to a file they do not have: `dev/review.md` names a
path inside accord's own monorepo, and `shared/prototype.md` names a file no shipped command
emits into a user repository. Both step 2s become self-contained.

Gap 3 — REQUIREMENTS.md does not match what shipped: CLI-08 reads Pending while `skills sync`
is implemented and covered by 17 passing tests, and SKILL-09's text still names the drafts
directory this phase's graduation deleted.

Purpose: every numbered ROADMAP criterion this phase claims is already verified; these three are
the difference between "the tests pass" and "a user can follow the workflows and the tracker tells
the truth". None of them blocks Phase 7; all three should close before the milestone.

Output: a whole orphan scan with its regressions, two followable briefs with the regression that
would have caught the leak, and a tracker that matches the code.

**Plans 06-01 through 06-04 are the producer of record for everything already delivered and are
not re-authored, superseded, or modified by this plan.**
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills/06-VERIFICATION.md
@.claude/CLAUDE.md
</context>

<assumption_delta_decision>
The assumption-delta detector fired on this phase (`detected: true`, two `pluralization` signals:
"second run is a no-op" and "Two techniques render alongside the roles"). The orchestrator resolved
it before planning. Recorded here verbatim — **do not re-ask the user**:

- **Primary noun:** *the set of directories the orphan scan opens*.
- **Decision: `promote`.** The primary set becomes "every directory the `DIRS` table in
  `packages/core/src/skills/targets.ts` can produce". `skillDirs(config)` is demoted to "the subset
  the current config declares", which remains correct for the **write** path (`skillTargets`) and is
  no longer the scan set.
- **Rationale:** the two sets were identical only while `runtimes:` never shrank. Once a runtime can
  be dropped they diverge, and the scan that used the declared subset silently stopped covering the
  case D-113 exists for. 06-VERIFICATION.md's own `missing` entry prescribes exactly this promotion.
- **Not `add-alongside`:** a second parallel scan for undeclared runtimes would leave two code paths
  answering "which directories might hold an orphan", which is the divergence that produced this gap.
- **Invariant test required by the checkpoint:** dropping any runtime from `config.yml` leaves no
  `accord-*` directory unreported. It goes red the instant a future change reintroduces the
  declared-subset assumption. Task 1 carries it.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scan every directory the DIRS table can produce, not the declared subset</name>

  <read_first>
    - `packages/core/src/skills/targets.ts` — the whole file. The `DIRS` table, the `cmp`
      comparator, `skillDirs()`, `skillTargets()`, and the T-06-01 header comment explaining why
      the table is a literal in source and never read from config.
    - `packages/cli/src/commands/skills.ts` — the whole file. `orphans()` (the D-113/D-123 doc
      comment and the loop), `assertNoLink()`, and `skills()` including the point where `orphans()`
      is called after the writes.
    - `packages/core/src/index.ts` lines 34-41 — the skills re-export block, to see the house form
      for adding a named export.
    - `packages/cli/test/skills-sync.test.ts` lines 1-60 (the `sandbox()`, `setConfig()`, `at()`,
      `bytes()` and `listing()` helpers) and the whole
      `describe('accord skills sync — the orphan report (D-113, D-123)')` block.
    - `packages/core/test/skills.test.ts` lines 44-77 — the `rendered skills` describe, where a
      `skillTargets`/`skillDirs` assertion belongs.
  </read_first>

  <files>packages/core/src/skills/targets.ts, packages/core/src/index.ts, packages/cli/src/commands/skills.ts, packages/core/test/skills.test.ts, packages/cli/test/skills-sync.test.ts</files>

  <behavior>
    - `allSkillDirs()` returns `['.agents/skills', '.claude/skills']` — the deduped, code-point
      sorted union of every array in `DIRS` — and is a superset of `skillDirs(config)` for each of
      the four runtimes taken alone (`claude`, `codex`, `cursor`, `copilot`).
    - Roster synced at `roles: [ba, dev, designer]` / `runtimes: [claude, codex]` — the state the
      existing `sandbox()` helper produces — then `config.yml` changed to `runtimes: [claude]` and
      synced again: stderr carries **three** orphan lines, one each for
      `.agents/skills/accord-ba`, `.agents/skills/accord-designer` and `.agents/skills/accord-dev`.
      All three directories are still on disk with unchanged bytes, exit code is 0, and stdout names
      no `.agents/` path.
    - Those three lines are printed in exactly that order — code-point sorted, so `accord-ba`,
      then `accord-designer`, then `accord-dev`.
    - Roster `runtimes: [cursor]` — the declared set is exactly equal to the full union: stderr is
      empty.
    - A directory whose name does not start with `accord-`, planted under the now-undeclared
      `.agents/skills`, is named on neither stream and is not touched (D-123).
    - Invariant: after synching at `[claude, codex]` and then narrowing to each single-runtime
      roster in turn, every `accord-*` directory present under either target directory is either
      still declared (declared directory AND declared role) or named on stderr. No directory falls
      through.
  </behavior>

  <action>
    In `packages/core/src/skills/targets.ts`, add an exported `allSkillDirs(): string[]` returning
    the deduped, `cmp`-sorted union of every array in the `DIRS` table — derived from `DIRS` itself,
    never from an `AccordConfig`, so it takes no argument. Give it a doc comment in the file's
    existing voice saying what it is for: the orphan scan (D-113) must open every directory the
    table can produce, because a directory orphaned by dropping a runtime is by definition no longer
    in the declared subset. State in the same comment that `skillDirs(config)` stays the **write**
    set used by `skillTargets`, and that the T-06-01 property is strengthened rather than weakened —
    `allSkillDirs()` reads no config value at all, so no user-supplied string can enter a path.

    In `packages/core/src/index.ts`, add `allSkillDirs` to the existing named re-export from
    `./skills/targets.js` (the line that already exports `skillDirs` and `skillTargets`).

    In `packages/cli/src/commands/skills.ts`, import `allSkillDirs` alongside the existing
    `skillDirs` import from `@accord-dev/accord-core`, and rewrite the body of `orphans()` so the
    scan set and the declared set are two different things:
    - keep the existing role set, renamed to make the pair readable (`declaredRoles`);
    - add `const declaredDirs = new Set(skillDirs(config));`
    - iterate `allSkillDirs()` instead of `skillDirs(config)`;
    - inside the loop, compute whether the current directory is declared, and push an entry when
      **either** the directory is undeclared **or** the role name is undeclared.
    Everything else in `orphans()` stays exactly as it is: the `accord-` prefix filter (D-123), the
    `statSync(..., { throwIfNoEntry: false })?.isDirectory()` guard that keeps a first run off the
    error path, the `dir + '/' + entry.name` string composition, and the trailing `found.sort()`.
    The printed line is unchanged — a directory orphaned by a dropped runtime reads exactly like one
    orphaned by a dropped role, which is what 06-VERIFICATION.md asks for.

    Update the D-113/D-123 doc comment above `orphans()` so it states the promoted set: the scan
    opens every directory the table can produce, and a directory is an orphan when the config no
    longer declares the runtime that puts it on disk OR no longer declares the role that names it.
    Keep the existing sentences about never deleting and about not touching another tool's files.

    In `packages/core/test/skills.test.ts`, add one case to the `rendered skills` describe: assert
    `allSkillDirs()` equals `['.agents/skills', '.claude/skills']`, and loop the four runtime names
    asserting every element of `skillDirs({ ...config, runtimes: [r] })` is in `allSkillDirs()`.
    That superset loop is the structural half of the invariant; it goes red the moment a runtime is
    added to the table without the scan seeing it.

    In `packages/cli/test/skills-sync.test.ts`, add three cases inside the existing
    `describe('accord skills sync — the orphan report (D-113, D-123)')`:
    1. **a dropped runtime.** Sync the sandbox. `sandbox()` rosters `roles: [ba, dev, designer]`
       and `runtimes: [claude, codex]`, so that first sync writes **three** role directories under
       each of the two target directories — the existing case at `skills-sync.test.ts:158` already
       relies on `accord-designer` being present under both. Dropping `codex` therefore orphans
       three directories, not two: write the expected set as all three and assert it exactly.
       Snapshot the bytes of `.agents/skills/accord-ba/SKILL.md`,
       `.agents/skills/accord-designer/SKILL.md` and `.agents/skills/accord-dev/SKILL.md` with the
       existing `bytes()` helper, then `setConfig(repo, /^runtimes: .*$/m, 'runtimes: [claude]')`
       and sync again. Assert exit 0; assert the stderr orphan lines, in order, are for
       `.agents/skills/accord-ba`, `.agents/skills/accord-designer` and `.agents/skills/accord-dev`
       and no others — an exact list comparison on the parsed directory names, not a `toContain`
       per line, so the sorted-order claim is actually asserted; assert all three files still exist
       with the same bytes; assert stdout contains no `.agents/` path.
    2. **equal sets.** A sandbox at `runtimes: [cursor]` — which maps to both directories, so the
       declared set is exactly the full union — synced twice: stderr is empty on the second run.
    3. **the invariant.** Sync at `[claude, codex]`, then for each of `runtimes: [claude]` and
       `runtimes: [codex]` re-run sync and assert that every `accord-*` directory present under
       `.claude/skills` or `.agents/skills` is either (declared directory AND declared role) or
       named on stderr — a set difference that must be empty. Reuse the existing `listing()` and
       `at()` helpers rather than adding new ones. Name the assumption in a comment: this is the
       case that was silently uncovered while the scan used the declared subset.
    Extend the existing D-123 case (`never names a directory it did not generate`) by planting its
    non-`accord-` directory under `.agents/skills` as well, and dropping `codex`, so the prefix
    filter is proven on the newly-scanned directory too.
  </action>

  <verify>
    <automated>npm test -- --project core skills</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the reporter names skills.test.ts with a failing assertion on `allSkillDirs`</fails_when>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project cli skills-sync</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or eslint reports `no-restricted-imports` against packages/core/src/skills/targets.ts</fails_when>
  </verify>

  <acceptance_criteria>
    - `packages/core/src/skills/targets.ts` declares `export function allSkillDirs(): string[]`, it
      takes no parameter, and its body reads only the `DIRS` literal — no `AccordConfig` value
      reaches it.
    - `packages/core/src/index.ts` exports `allSkillDirs` from `./skills/targets.js`.
    - `packages/cli/src/commands/skills.ts` `orphans()` iterates `allSkillDirs()`, and separately
      holds a set built from `skillDirs(config)` used only to decide whether a scanned directory is
      declared.
    - `orphans()` still filters on the `accord-` prefix, still returns `found.sort()`, and
      `skills()` still calls it after the write loop.
    - `skillTargets()` is unchanged and still composes its paths from `skillDirs(config)`.
    - `npm test -- --project core skills` passes with a case asserting `allSkillDirs()` equals
      `['.agents/skills', '.claude/skills']` and a superset assertion over all four runtimes.
    - `npm test -- --project cli skills-sync` passes with three added orphan cases, including one
      that sets `runtimes: [claude]` and asserts the orphan set is exactly
      `['.agents/skills/accord-ba', '.agents/skills/accord-designer', '.agents/skills/accord-dev']`
      in that order — three directories, because `sandbox()` rosters all three roles.
    - Mutation check, stated and performed by hand once: temporarily restoring `skillDirs(config)`
      as the loop set makes the dropped-runtime case fail. Restore the fix immediately; record the
      observed failure message in the SUMMARY.
    - No file under `packages/core/src/` gains an import of `node:fs` or `node:path`.
  </acceptance_criteria>

  <reversibility rating="reversible">Three small edits to one function, one new pure export, and three tests. Reverting is a `git checkout` of five files; nothing is written to a user's disk that a revert would strand.</reversibility>

  <done>`accord skills sync` reports an `accord-*` directory left behind by a dropped runtime exactly as it reports one left behind by a dropped role, deletes nothing, exits 0, and the case is covered by a test that goes red if the scan narrows back to the declared subset.</done>
</task>

<!-- planner-discipline-allow: packages/ -->
<!-- planner-discipline-allow: prototype-header.html -->
<task type="auto" tdd="true">
  <name>Task 2: Make both step 2s followable from the brief alone, and add the regression</name>

  <read_first>
    - `packages/core/skills/dev/review.md` — the whole file, especially step 2 at lines 26-37 and
      the voice of the surrounding steps (short declarative sentences, backticked keys, em-dash
      bullets).
    - `packages/core/templates/verification.md` — the whole file (15 lines). The three frontmatter
      keys, the HTML comment explaining who writes the file, the `## @ac-1 <scenario name>` block,
      and the `Result:` / `Evidence:` lines. The inline description must be faithful to this.
    - `packages/core/skills/shared/prototype.md` — the whole file, especially step 2 at lines 30-35.
    - `packages/core/templates/prototype-header.html` — the whole file (15 lines). The four comment
      lines (ticket, `Derived from:`, the rule, the owner) and the empty-body HTML skeleton.
    - `packages/core/test/skills.test.ts` lines 160-175 (`const output = skillTargets(config())` and
      the `render output invariants` describe opening with its guard-the-guard `has files to check`
      case), lines 237-244 (the backslash scan, the shape the new assertion copies), and lines
      376-385 (the `names exactly one accord/-rooted path across both briefs` case whose comment
      carries the carve-out that must go).
    - `packages/core/scripts/gen-skills.mjs` lines 1-30 — to confirm the generated module is
      produced from `skills/**/*.md` and must be regenerated, never hand-edited.
  </read_first>

  <files>packages/core/skills/dev/review.md, packages/core/skills/shared/prototype.md, packages/core/test/skills.test.ts</files>

  <behavior>
    - A scan over every rendered file returned by `skillTargets(config())` finds no occurrence of
      the monorepo path prefix and no occurrence of the shipped header template's filename; the
      scan has more than zero files and reports the offending path and line when it fails.
    - Before the prose edit, that scan is red on `.claude/skills/accord-dev/review.md`. Write the
      assertion first, watch it fail, then edit the prose. Record the red message in the SUMMARY.
    - `accord-dev/review.md` step 2 names the three frontmatter keys, the per-scenario block shape
      in tag order including the `<scenario name>` placeholder, and the two value lines, and names
      no other file.
    - `prototype.md` step 2 describes one HTML file with a comment block and an empty body, names
      three format slots the comment block carries — the ticket, a `Derived from:` line, the owner —
      says `accord lint` reports anything the header is missing, and names no other file and no
      rule's content.
    - The two rendered `prototype.md` copies stay byte-identical (the existing D-119 case still
      passes).
    - `node packages/cli/dist/cli.js skills sync` run twice after `npm run gen && npm run build`
      prints only `unchanged` lines on the second run.
  </behavior>

  <action>
    Write the regression FIRST, in `packages/core/test/skills.test.ts`, inside the
    `render output invariants` describe and beside the existing backslash case: one `it` that scans
    every `{ path, text }` in `output` for two literals and reports the path and the offending line
    when either is found — the monorepo directory prefix `packages/`, and the shipped header
    template's filename `prototype-header.html`. Both are the same defect in two forms: a rendered
    file telling its reader to open something that is not in their repository. Guard the guard by
    asserting `output.length` is greater than zero (the describe's existing `has files to check`
    case covers this; a comment pointing at it is enough). No split literal is needed here — the
    scan reads `output`, the render result, not this test file. Add a comment recording that if a
    future phase's `accord init` scaffolds the header template into a user repository, the second
    literal comes off this list in the same change that makes the reference true, not before.

    Run the suite and confirm the new case is RED on `.claude/skills/accord-dev/review.md`.

    Then rewrite `packages/core/skills/dev/review.md` step 2 so it describes the structure inline
    instead of pointing at the template. Keep the heading
    `## 2. Write \`accord/tickets/<id>/verification.md\`` exactly as it is. Replace the sentence
    that names the template with a line naming the frontmatter keys — `ticket:`, `commit:`,
    `reviewed_on:`. Keep the two `Result:` / `Evidence:` bullets and the closing "Set `commit:` in
    the frontmatter to the commit you reviewed." sentence verbatim — the last says *which* commit
    and is not implied by the key name.

    The lead-in sentence changes, and this is deliberate rather than incidental: the file today
    reads "One `## @ac-n` block per scenario, in tag order, each carrying:", with no `<scenario
    name>`. The placeholder lives in `packages/core/templates/verification.md` line 8, which is the
    file the reader will no longer open. Add it — the amended lead-in reads
    "One `## @ac-n <scenario name>` block per scenario, in tag order, each carrying:" — because the
    whole point of this edit is that the block shape must be learnable from the brief alone, and the
    human-readable name is part of that shape. Do not describe this line as unchanged; it is the one
    existing sentence in step 2 that gains text.

    Match the file's voice throughout: short declarative sentences, backticked keys, no new heading
    levels, wrapped at the same column as the rest of the file.

    Then rewrite `packages/core/skills/shared/prototype.md` step 2 to the shape the owner approved.
    Change the heading to `## 2. Start from a single HTML file`. The body describes the artifact
    rather than a file to copy: one HTML file, a comment block at the top, an empty body; the
    comment block names the ticket, a `Derived from:` line for what step 1 found, and the owner;
    build the page underneath it. Then one sentence: `accord lint` reports anything the header is
    missing. Keep the existing closing clause "no build step, no assets directory — a reviewer opens
    it in a browser and it works." — it describes the artifact, restates no rule, and the owner's
    resolution did not ask for it to go.

    **Name the format slot; never explain the rule. This is D-126 and it is load-bearing.** D-126
    rejected padding the designer definition with "token-file and `Derived from:` guidance" on the
    ground that "those are lint rules the CLI already enforces (LINT-04), and SKILL-04 forbids a
    skill from restating an enforced rule". `lint.prototype-derivation` is registered in
    `packages/core/src/lint/rules.ts`, and its three checks in `packages/core/src/lint/tokens.ts`
    are precisely "no \"Derived from:\" line in its header comment", "\"Derived from:\" lists no
    path", and "\"Derived from:\" path is not in the repository". The line D-126 draws is between
    **naming what the artifact contains** and **reproducing what the command checks**:
    - naming the slot `Derived from:` is the same class of statement as naming `## @ac-n` or
      `## Open questions` — it is the folder and file convention, which is the product, and its
      source is `docs/design.md` section 2, not a gate table. It passes.
    - saying what happens when the slot is absent, empty, or names a path outside the repository
      reproduces `lint.prototype-derivation`'s three reasons verbatim, and saying that hard-coded
      colours or spacing values are warned about reproduces `lint.token-hardcoded` — the rule behind
      the template's `Rule:` line, and the axis D-126 named first. Both drift silently. Either
      fails.
    So: name the slot, and hand the reader `accord lint` for everything else. Concretely, the
    template's comment block carries four lines (ticket, `Derived from:`, `Rule:`, `Owner:`) and the
    inline description names three of them — the `Rule:` line is deliberately not described, because
    describing it restates `lint.token-hardcoded`. `packages/core/templates/prototype-header.html`
    keeps its own `Rule:` line and is NOT edited: a template is not a skill and SKILL-04 does not
    reach it.

    Neither new passage may name another tool, plugin, harness, or planning system (the existing
    deny-list scan covers this), and neither may introduce a backslash (the existing D-51 scan
    covers that).

    Then update the carve-out. The comment inside
    `it('names exactly one accord/-rooted path across both briefs, and it is verification.md')`
    currently explains the assertion by saying the briefs' other paths are the template source under
    the monorepo prefix or a relative `./` reference. The first half of that sentence is the leak
    this task removes; rewrite the comment so it describes only what is still true — every other
    path these two texts mention is a relative `./` reference, which is not `accord/`-rooted. Do not
    leave the old clause standing beside the new assertion.

    Finally regenerate and re-sync, in this order: `npm run gen` (rewrites
    `packages/core/src/generated/skills.ts` from `packages/core/skills/**`), `npm run build`, then
    `node packages/cli/dist/cli.js skills sync` twice. Never hand-edit the generated module — the
    drift test exists to catch exactly that. The second sync run must print `unchanged` for every
    line; the first will print `updated` for this repository's own
    `.claude/skills/accord-dev/review.md` and `.claude/skills/accord-dev/prototype.md`, which is the
    correct D-112 state for an intact copy that is behind the definition. Those two files are
    generated output: they are rewritten by the command, never edited by hand, and are not listed in
    this plan's `files_modified`.
  </action>

  <verify>
    <automated>npm run gen &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core skills</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the generated-module drift case fails with a message naming `npm run gen`</fails_when>
    <automated>! grep -rq 'packages/' packages/core/skills/</automated>
    <fails_when>non-zero exit — a definition under the skills source directory still names a path from accord's own repository layout</fails_when>
    <automated>! grep -rq 'prototype-header' packages/core/skills/</automated>
    <fails_when>non-zero exit — a definition still tells its reader to start from the shipped header template</fails_when>
    <automated>node packages/cli/dist/cli.js skills sync &amp;&amp; node packages/cli/dist/cli.js skills sync</automated>
    <fails_when>non-zero exit, or any line of the SECOND run's stdout does not begin with `unchanged `</fails_when>
  </verify>

  <acceptance_criteria>
    - `packages/core/test/skills.test.ts` contains a case in the `render output invariants` describe
      that scans every rendered text for the two literals and fails with the offending path and
      line.
    - That case was observed RED before the prose edit, and the red message is quoted in the
      SUMMARY.
    - `packages/core/skills/dev/review.md` step 2 contains the literal names `ticket:`, `commit:`,
      and `reviewed_on:`; contains the amended lead-in literal
      "One `## @ac-n <scenario name>` block per scenario, in" — note this differs from the text the
      file carries today, which has no `<scenario name>`, and the SUMMARY records that one-line
      change as intentional; retains both `Result:` and `Evidence:` bullets verbatim; and retains
      "Set `commit:` in the frontmatter to the commit you reviewed."
    - `packages/core/skills/dev/review.md` names no file other than `./code-review.md` and
      `accord/tickets/<id>/verification.md`.
    - `packages/core/skills/shared/prototype.md` step 2's heading is
      `## 2. Start from a single HTML file`; its body contains the literal `Derived from:` as a
      named slot, names the ticket and the owner as the other two slots, contains the sentence that
      `accord lint` reports anything the header is missing, and retains the clause "no build step,
      no assets directory — a reviewer opens it in a browser and it works."
    - `packages/core/skills/shared/prototype.md` step 2 carries none of these clauses (D-126):
      what the token rule warns about — hard-coded colour or spacing values; what happens when the
      `Derived from:` line is missing or empty; what happens when it names a path that is not in the
      repository. Naming the slot, plus the generic "`accord lint` reports anything the header is
      missing" sentence the criterion above mandates, is the whole of what step 2 says about the
      rules — that sentence names the command and stays true however the rule table changes, which
      is the drift test D-126 applies.
    - `packages/core/templates/verification.md` and `packages/core/templates/prototype-header.html`
      are both byte-unchanged — they still ship, Phase 7 still scaffolds them, and the template
      keeps the `Rule:` line the skill deliberately does not describe.
    - The comment inside the `names exactly one accord/-rooted path` case no longer describes a
      template source under the monorepo prefix.
    - `packages/core/src/generated/skills.ts` differs from its previous content only in the
      `dev/review.md` and `shared/prototype.md` string values, and was produced by `npm run gen`.
      It is regenerated output, not an edit target — which is why it appears in neither this task's
      `<files>` nor the plan's `files_modified`.
    - A second `node packages/cli/dist/cli.js skills sync` prints `unchanged` on every line.
  </acceptance_criteria>

  <reversibility rating="reversible">Two prose passages, one test case, one comment, and a regenerated module. The templates they used to point at are untouched, so reverting restores the previous text exactly.</reversibility>

  <done>A reader who follows `accord-dev/review.md` step 2 or `prototype.md` step 2 can produce the artifact from the brief alone, and a test goes red if any rendered skill starts naming a file that exists only in accord's own repository.</done>
</task>

<!-- planner-discipline-allow: docs/skills/ -->
<task type="auto">
  <name>Task 3: Make REQUIREMENTS.md say what shipped</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` line 67 (the CLI-08 checklist entry), line 74 (SKILL-04, which
      stays open), line 79 (the SKILL-09 entry and its trailing clause), and the traceability rows
      at lines 186 (CLI-08), 190 (SKILL-04) and 195 (SKILL-09).
    - `.planning/ROADMAP.md`, the `### Phase 6: Skills` section — success criterion 5, whose
      amended wording ("The definitions live in `packages/core/skills/`") is the sentence SKILL-09's
      trailing clause is being brought into line with.
    - `.planning/phases/06-skills/06-VERIFICATION.md` — the Requirements Coverage table, which is
      the evidence behind the CLI-08 tick (four D-112 states, pin refusal, no-accord-folder refusal,
      link refusal, orphan report, printed-path invariant — 19 tests).
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
    Three edits in `.planning/REQUIREMENTS.md`, and no others.

    1. Line 67: change the CLI-08 checklist entry from unticked to ticked — `- [ ] **CLI-08**`
       becomes `- [x] **CLI-08**`. Leave the requirement text itself exactly as written. This is
       honest only because Task 1 of this plan completed the orphan half of the command; run Task 1
       first.
    2. Line 186 of the traceability table: change the CLI-08 row's third column from `Pending` to
       `Complete`, so it reads `| CLI-08 | Phase 6 | Complete |`.
    3. Line 79: the SKILL-09 entry ends with the clause `Drafts live in \`docs/skills/\`` — a
       directory this phase's graduation commit deleted. Replace that trailing clause with
       `The definitions live in \`packages/core/skills/\``, the exact wording ROADMAP criterion 5
       already carries. Change nothing else in that line.

    Do NOT touch SKILL-04. It stays `- [ ]` on line 74 and `Pending` on line 190; it is blocked on
    the eleven-file rule read, which is plan 06-06's second task, and the tick is that task's
    consequence — not this one's.

    Do not touch any other requirement row, any other traceability row, or the out-of-scope notes
    for SKILL-10 and SKILL-11.

    Leave the change uncommitted for review. Do not `git commit` and do not `git push` — the
    project's standing rule in `.claude/CLAUDE.md` overrides any workflow step that says to commit.
  </action>

  <verify>
    <automated>grep -q '^- \[x\] \*\*CLI-08\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| CLI-08 | Phase 6 | Complete |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — the CLI-08 checklist entry is still unticked, or its traceability row still reads Pending</fails_when>
    <automated>! grep -v '^#' .planning/REQUIREMENTS.md | grep -q 'docs/skills'</automated>
    <fails_when>non-zero exit — the deleted drafts directory is still named somewhere in REQUIREMENTS.md</fails_when>
    <automated>grep -q '^- \[ \] \*\*SKILL-04\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| SKILL-04 | Phase 6 | Pending |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — SKILL-04 was ticked in this task, ahead of the human read that closes it</fails_when>
    <automated>grep -q 'The definitions live in `packages/core/skills/`$' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — the SKILL-09 entry does not end with the amended clause</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line 67 reads `- [x] **CLI-08**: ...` with the requirement text
      unchanged.
    - The traceability row for CLI-08 reads `| CLI-08 | Phase 6 | Complete |`.
    - The SKILL-09 entry ends with `The definitions live in \`packages/core/skills/\`` and the rest
      of that line is byte-identical to what it was.
    - SKILL-04 is still `- [ ]` in the checklist and still `Pending` in the traceability table.
    - Exactly three lines of `.planning/REQUIREMENTS.md` differ from what they were before this
      task. Note that the file already carries 18 uncommitted changed lines from earlier Phase 6
      work, so a whole-file diff against the last commit cannot isolate this task's edit — compare against a
      copy of the file taken before editing, or read the three lines back directly.
    - Nothing is committed; `git status` still shows the change in the working tree.
  </acceptance_criteria>

  <reversibility rating="reversible">Three lines in a tracking document.</reversibility>

  <done>REQUIREMENTS.md records CLI-08 as delivered in both places, SKILL-09's text names the directory the definitions actually live in, SKILL-04 is still open, and the change sits uncommitted for the owner to review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `accord/config.yml` -> path composition | `roles:` and `runtimes:` are user-editable text that decides which paths the CLI opens and writes |
| repository working tree -> stderr | the orphan report prints directory names read off the user's disk |
| `packages/core/skills/**` -> the shipped generated module | authored prose becomes text installed into every user repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Tampering | `orphans()` in `packages/cli/src/commands/skills.ts` | high | mitigate | The widened scan iterates `allSkillDirs()`, derived only from the `DIRS` literal in `packages/core/src/skills/targets.ts`. No config-supplied string enters path composition — widening **drops** the config input rather than adding one, so T-06-01's "the table is fixed in source and never read from config" holds a fortiori. Task 1's core test pins `allSkillDirs()` to the exact two-element literal set, which goes red if a config value ever reaches it. |
| T-06-05-02 | Information disclosure | the stderr orphan report | medium | mitigate | The `accord-` prefix filter (D-123) stays on the entry loop, so the scan opens two directories but still names only accord's own. Task 1 extends the existing D-123 case to plant a non-`accord-` directory under the newly-scanned `.agents/skills` and assert it is named on neither stream. |
| T-06-05-03 | Elevation of privilege | `assertNoLink()` pre-pass | medium | accept | `assertNoLink` guards the **write** targets, which are unchanged by this plan. The orphan scan only `statSync`/`readdirSync`s directories it does not write to, and follows no leaf. Accepted: the widened scan adds no write destination. |
| T-06-05-04 | Denial of service | the orphan scan | low | accept | One extra shallow `readdirSync` over a directory that is usually absent (`throwIfNoEntry: false` keeps that off the error path). No recursion, no unbounded walk. |
| T-06-05-05 | Tampering | `packages/core/skills/**` -> `src/generated/skills.ts` | medium | mitigate | The prose edit reaches the shipped artifact only through `npm run gen`; the existing drift test in `skills.test.ts` fails with a message naming `npm run gen` if the generated module is hand-edited. Task 2 regenerates and re-syncs rather than editing the module, and its acceptance criteria pin the diff to the two changed string values. |
| T-06-05-06 | Spoofing | the generated marker | low | accept | Unchanged by this plan. FNV-1a is a drift detector and the marker claims nothing more; `skills.test.ts` forbids the two words that would make it an integrity claim. |

**Supply chain:** no task in this plan runs `npm install`, `pip install`, or `cargo add`, and no
dependency is added to any `package.json`. No `T-06-05-SC` row is minted and no package-legitimacy
checkpoint is required. Path traversal is **canon** — covered by `/gsd-secure-phase` and by the
T-06-01 fixed-table property above; not minted as a bespoke prohibition here.
</threat_model>

<verification>
Run from the repository root, in this order:

1. `npm run gen` — the generated module matches `packages/core/skills/**`.
2. `npm run check` — build, lint, typecheck, and the full 781+ test suite, green.
3. `node packages/cli/dist/cli.js skills sync` twice — the second run prints `unchanged` on every
   line and exits 0.
4. `! grep -rq 'packages/' packages/core/skills/` — no definition names accord's own repository
   layout.
5. `git status --porcelain` — the working tree carries the changes and nothing is committed.
</verification>

<success_criteria>
- Dropping a runtime from `runtimes:` produces an orphan line for every `accord-*` directory under
  the abandoned target directory, and the case is covered by a test that goes red if the scan
  narrows back to the declared subset.
- `runtimes: [cursor]`, where the declared set equals the full table, produces no orphan line.
- No rendered skill text names a path that exists only inside accord's own repository, and a test
  fails if one is reintroduced.
- Both amended step 2s are followable with nothing but the file in front of the reader.
- REQUIREMENTS.md ticks CLI-08 in both places, amends SKILL-09's trailing clause, and leaves
  SKILL-04 open.
- Nothing is committed.
</success_criteria>

## Multi-Source Coverage Audit

Gap-closure scope. The four standard sources are audited against the gap contract rather than the
whole phase: 06-01..06-04 are the producer of record for everything else and are not re-authored.

| Source | Item | Covered by |
|--------|------|------------|
| GOAL (ROADMAP criterion 2, D-113 clause) | an orphan under a no-longer-declared **runtime** directory is reported | Task 1 |
| GOAL (phase goal, "complete enough to follow end to end") | `review.md` step 2 followable | Task 2 |
| GOAL (phase goal, same) | `prototype.md` step 2 followable | Task 2 |
| REQ · CLI-08 | orphan scan whole; checklist and traceability row corrected | Task 1, Task 3 |
| REQ · SKILL-09 | trailing clause names `packages/core/skills/` | Task 3 |
| REQ · SKILL-04 | deliberately NOT closed here — blocked on 06-06 Task 2 | plan 06-06 |
| RESEARCH (06-RESEARCH.md Pitfall 1, normalisation before compare) | unchanged; the read-back path is not touched | already covered by 06-01 |
| CONTEXT · D-113 | never deletes; reports and returns | Task 1 (message and exit code unchanged) |
| CONTEXT · D-123 | `accord-*` only, on the newly-scanned directory too | Task 1 |
| CONTEXT · D-105 | the generated module is produced, never hand-edited | Task 2 |
| CONTEXT · D-126 | no skill restates a rule the CLI enforces; the `Derived from:` slot is named, never explained | Task 2 (and carried into plan 06-06 Task 2's pass criteria) |
| CONTEXT · D-119 | the two `prototype.md` copies stay byte-identical | Task 2 (existing case still green) |
| CONTEXT · D-115 | the manual half of criterion 7 | plan 06-06 Task 1 |

**No item is MISSING.** Excluded as out of scope by explicit user decision, not by omission:
06-VERIFICATION.md human items 3 (the three fresh-context reviews read as one shape — editorial,
blocks no criterion) and 4 (Cursor/Copilot duplicate-name observation — informational, no design
change either way); and review findings WR-01, WR-03, WR-05, IN-01..IN-04, which
06-VERIFICATION.md's `advisory` block already dispositions as defeating no Phase 6 criterion.

## Flagged Assumptions

The spec-less edge probe surfaced 17 rows over the eleven phase requirement IDs, all `unresolved`.
Six are authored into `must_haves` (SKILL-09's three here; SKILL-04's three in plan 06-06). The
eleven below belong to requirements this gap-closure plan does not touch. Each is surfaced, not
dropped — **17 = 6 authored + 11 flagged.**

| Probe row | Disposition |
|-----------|-------------|
| CLI-08 / unclassified | already covered by plans 06-01 and 06-03; not re-authored in gap closure |
| SKILL-01 / unclassified | already covered by plans 06-01, 06-02 and 06-04; not re-authored in gap closure |
| SKILL-02 / unclassified | already covered by plan 06-01; not re-authored in gap closure |
| SKILL-03 / unclassified | already covered by plan 06-01; not re-authored in gap closure |
| SKILL-05 / unclassified | already covered by plan 06-04; not re-authored in gap closure |
| SKILL-06 / unclassified | already covered by plan 06-02; not re-authored in gap closure |
| SKILL-07 / unclassified | already covered by plan 06-02; not re-authored in gap closure |
| SKILL-08 / adjacency | already covered by plan 06-03; not re-authored in gap closure |
| SKILL-08 / empty | already covered by plan 06-03; not re-authored in gap closure |
| SKILL-08 / ordering | already covered by plan 06-03; not re-authored in gap closure |
| SKILL-12 / unclassified | already covered by plan 06-02; not re-authored in gap closure |

Two further assumptions this plan makes explicit:

- **Nothing in Phase 6 is committed.** `packages/core/skills/**`, `packages/cli/src/commands/skills.ts`,
  `packages/core/src/skills/targets.ts`, `packages/core/test/skills.test.ts`,
  `packages/cli/test/skills-sync.test.ts` and `packages/core/src/generated/skills.ts` exist on disk
  as uncommitted working-tree state, per the owner's standing rule; `git ls-files` returns nothing
  for them. Every path in this plan was verified to exist on disk before being written here. The
  tracked files this plan touches are `.planning/REQUIREMENTS.md`, `packages/core/src/index.ts`,
  `packages/core/templates/verification.md` and `packages/core/templates/prototype-header.html`
  (the last two read-only).
- **06-VERIFICATION.md's `missing` entry claims one assertion would have caught both gap-2
  occurrences.** It would not: `shared/prototype.md` contains no monorepo path, only a bare
  filename. Task 2 therefore scans for both literals in one case rather than one, which is why the
  assertion is written as two forms of a single defect.

## Artifacts this phase produces

New symbols, files and test names created by this plan — excluded from drift verification because
they do not exist yet:

**Exported functions**
- `allSkillDirs()` — `packages/core/src/skills/targets.ts`, re-exported from
  `packages/core/src/index.ts`

**Local identifiers**
- `declaredRoles`, `declaredDirs` — inside `orphans()` in `packages/cli/src/commands/skills.ts`

**New test cases** (names are indicative; the executor may phrase them in the house voice)
- `packages/core/test/skills.test.ts` — an `allSkillDirs` union + superset case in the
  `rendered skills` describe
- `packages/core/test/skills.test.ts` — a no-repository-only-path case in the
  `render output invariants` describe
- `packages/cli/test/skills-sync.test.ts` — a dropped-runtime orphan case
- `packages/cli/test/skills-sync.test.ts` — an equal-declared-and-full-set case
- `packages/cli/test/skills-sync.test.ts` — a no-unreported-directory invariant case

**No new files.** Every path this plan writes to already exists on disk.

<output>
Create `.planning/phases/06-skills/06-05-SUMMARY.md` when done.

Record in it: the mutation-check failure message from Task 1, the RED message from Task 2's
regression before the prose edit, the exact before/after of both amended step 2s, and the three
changed lines of REQUIREMENTS.md. State explicitly that nothing was committed.
</output>
