---
phase: 01-workspace-and-formats
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/test/schemas.test.ts
  - packages/core/test/__golden__/config.invalid.json
  - packages/core/test/__golden__/verification.invalid.json
  - packages/core/test/__golden__/ticket.tracker.invalid.json
autonomous: true
requirements: [FMT-02, FMT-03, FMT-06]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The D-16 default `config.yml` document validates to `[]`; `tracker.adapter: github-issues` without `repo` yields a `schema.required` finding at `/tracker`; an undeclared top-level key (`sprint`) yields `schema.additionalProperties` at `''`; `roles: [ba]` yields `schema.contains` at `/roles` (FMT-06, D-13, D-16)"
    - "`design` accepts only `{ tokens: string }`; a `design.source` key is rejected at `/design` (D-15)"
    - "`tracker` is a map keyed by adapter name: `{ shortcut: '1234' }` and `{ 'github-issues': '42' }` validate; `{ Shortcut: '1234' }` (capitalised key), `{ shortcut: 1234 }` (numeric value), and `{ shortcut: '' }` (empty value) each yield a finding under `/tracker` (FMT-03, D-04, D-05)"
    - "`tracker: {}` is valid (FMT-03 empty edge, D-22, verification: explicit)"
    - "Tracker keys are matched case-sensitively against `^[a-z][a-z0-9-]*$` (ASCII lowercase kebab) and values must satisfy `minLength: 1`, which JSON Schema measures in Unicode code points — a one-character non-ASCII value such as `'é'` is accepted (FMT-03 encoding edge, verification: explicit)"
    - "A `verification.md` frontmatter with `commit: 'g'`, `reviewed_on: '2026-9-5'`, and an extra `reviewer` key yields findings at `''`, `/commit`, `/reviewed_on` (D-09)"
    - "`design` is accepted on `type: epic` (D-23); `ui` is optional (D-21); `ac_hash: ''` is rejected with `schema.minLength` at `/ac_hash` (D-25); `verified: ['ac-0']` and `verified: ['ac-1', 'ac-1']` are rejected (D-03); `type: feature` and `status: todo` are rejected (D-01, D-02); an `owner` key is rejected at `''` (D-04)"
    - "`validate` returns findings in ajv schema-evaluation order and that order is identical across two consecutive runs — the goldens pin it (CORE-01 ordering edge, verification: explicit)"
  artifacts:
    - path: "packages/core/test/schemas.test.ts"
      provides: "acceptance tests for the three schemas and their edges"
      min_lines: 90
    - path: "packages/core/test/__golden__/config.invalid.json"
      provides: "5 findings for the research probe config document"
      contains: "must have required property 'repo'"
    - path: "packages/core/test/__golden__/verification.invalid.json"
      provides: "findings at '', /commit, /reviewed_on"
      contains: "/reviewed_on"
    - path: "packages/core/test/__golden__/ticket.tracker.invalid.json"
      provides: "tracker map findings (capitalised key, numeric value, empty value)"
      contains: "/tracker"
  key_links:
    - from: "packages/core/test/schemas.test.ts"
      to: "packages/core/src/index.ts"
      via: "import { validate } from '../src/index.js'"
      pattern: "from '../src/index\\.js'"
    - from: "packages/core/test/schemas.test.ts"
      to: "packages/core/test/__golden__/config.invalid.json"
      via: "toMatchFileSnapshot"
      pattern: "config\\.invalid\\.json"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "Tracker-owned work data (sprint, priority, estimate, assignee, work status) must never be storable in ticket frontmatter or `config.yml`; a test proves an undeclared key is rejected (D-02, D-05)"
      status: resolved
      verification: test
    - statement: "`config.yml` must never gain a key that holds an API key, token, or secret; the schema's top-level `properties` are exactly `accord`, `profile`, `tracker`, `design`, `roles`, `runtimes`"
      status: resolved
      verification: test
    - statement: "Gate results (Ready/Done PASS or FAIL) must never be storable in the ticket file; `status` accepts only the document-lifecycle values `draft|open|archived` (D-02)"
      status: resolved
      verification: test
---

<objective>
**As a** BA or developer editing a ticket or `config.yml`, **I want** a misspelt key, a wrong enum value, or a badly shaped tracker link to be reported with the exact JSON path, **so that** the file can be fixed before any gate runs and tracker-owned data can never leak into git.

Purpose: Plan 01-01 shipped the three schemas and one ticket golden. This plan pins the rest of the contract as executable acceptance criteria: the config schema (`if/then`, roles, design), the verification schema, the tracker map (FMT-03), and every D-21..D-25 planning-time decision, plus the empty/encoding/ordering edges from the spec-less probe. Success criterion 2 ("an invalid document produces a schema error naming the path") is proven for all three schemas.

Output: an extended `packages/core/test/schemas.test.ts` and three new goldens.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-formats/01-CONTEXT.md
@.planning/phases/01-workspace-and-formats/01-RESEARCH.md
@packages/core/schemas/ticket.schema.json
@packages/core/schemas/verification.schema.json
@packages/core/schemas/config.schema.json
@packages/core/test/schemas.test.ts

<interfaces>
<!-- From Plan 01-01. -->
```typescript
import { validate, schemaIds } from '../src/index.js';
import type { Finding, SchemaId } from '../src/index.js';
// validate(schemaId: 'ticket' | 'verification' | 'config', doc: unknown): Finding[]
// Finding = { path: string /* ajv instancePath */, rule: string /* 'schema.' + keyword */, reason: string }
```
Existing tests in schemas.test.ts (01-01 Task 2): "schemas compile and accept a minimal valid document" (3 cases), "a full D-04 ticket document is valid", "an invalid ticket produces findings naming the path" (golden `ticket.invalid.json`, 8 findings), "empty input yields one required finding per missing key and never throws". Append to this file; do not rename or remove existing tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.yml and verification.md acceptance goldens</name>
  <read_first>
    - packages/core/test/schemas.test.ts (existing tests and the golden pattern to extend)
    - packages/core/schemas/config.schema.json and packages/core/schemas/verification.schema.json (the contracts under test)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md — Code Example 6 "Verified" line and Code Example 9 golden (the 5-finding `config.invalid.json` content is already known), Code Example 8 "Verified" line, Pattern 5 "Error order"
    - .planning/phases/01-workspace-and-formats/01-CONTEXT.md D-09, D-12, D-13, D-15, D-16
  </read_first>
  <files>packages/core/test/schemas.test.ts, packages/core/test/__golden__/config.invalid.json, packages/core/test/__golden__/verification.invalid.json</files>
  <action>
Append a `describe('config.schema.json')` block:
  1. `it('the D-16 default document is valid')` — already covered by the compile test; here assert additionally that `profile: 'maintain'` and `runtimes: ['claude']` are also valid, and that `roles: ['ba', 'dev', 'designer']` is valid (D-13: designer optional).
  2. `it('github-issues requires repo; extra keys and a lonely role are named by path (golden)')` — the RESEARCH Code Example 9 probe: `{ accord: '0.1.0', profile: 'build', tracker: { adapter: 'github-issues' }, design: { tokens: '' }, roles: ['ba'], runtimes: ['claude'], sprint: 3 }` → `toMatchFileSnapshot('./__golden__/config.invalid.json')`. The research run produced exactly 5 findings: `''` `schema.additionalProperties`, `/tracker` `schema.required` (`must have required property 'repo'`), `/tracker` `schema.if`, `/roles/0` `schema.const`, `/roles` `schema.contains`. Confirm the written golden matches before finishing.
  3. `it('github-issues with repo is valid; repo shape is checked')` — `tracker: { adapter: 'github-issues', repo: 'owner/name' }` → `[]`; `repo: 'owner name'` and `repo: 'owner/name/extra'` → a finding at `/tracker/repo` with rule `schema.pattern`.
  4. `it('design accepts only tokens')` — `design: { tokens: '', source: 'figma' }` → finding at `/design` rule `schema.additionalProperties` (D-15); `design: {}` → finding at `/design` rule `schema.required`.
  5. `it('roles are ba|dev|designer, ba and dev required, no duplicates')` — `roles: ['ba', 'dev', 'qa']` → finding at `/roles/2` rule `schema.enum`; `roles: ['dev', 'designer']` → finding at `/roles` rule `schema.contains`; `roles: ['ba', 'dev', 'dev']` → finding rule `schema.uniqueItems` (D-13: `qa` and `lead` are not roster roles).
  6. `it('top-level properties are exactly the six D-16 keys')` — import the schema JSON directly in the test (`with { type: 'json' }`) and assert `Object.keys(schema.properties)` deep-equals `['accord', 'profile', 'tracker', 'design', 'roles', 'runtimes']`; assert `schema.required` deep-equals the same six; assert no property name matches `/key|token|secret|password/i` (accord never holds credentials).
  7. `it('accord is a quoted semver string')` — `accord: '0.1.0-rc.1'` valid; `accord: 1` → finding rule `schema.type` at `/accord`; `accord: 'v0.1.0'` → `schema.pattern`.

Append a `describe('verification.schema.json')` block:
  1. `it('a minimal D-09 record is valid; full SHA and short SHA both pass')` — `commit: '0000000'` and a 40-hex value → `[]`.
  2. `it('bad commit, bad date, extra reviewer are named by path (golden)')` — `{ ticket: 'TICKET-ID', commit: 'g', reviewed_on: '2026-9-5', reviewer: 'x' }` → `toMatchFileSnapshot('./__golden__/verification.invalid.json')`; research observed findings at `''`, `/commit`, `/reviewed_on`. Confirm.
  3. `it('reviewer is not a field; the git author is the proof')` — assert `Object.keys(schema.properties)` deep-equals `['ticket', 'commit', 'reviewed_on']` (D-09).

Append `describe('finding order is deterministic')`: run `validate('config', probe)` twice on the config probe object and assert `JSON.stringify` of both results is identical (CORE-01 ordering edge; RESEARCH Pattern 5 verified stability).

Run `npm test -- --project core schemas`; inspect both new goldens by eye. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core schemas</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary shows fewer than 17 tests passed (6 from 01-01 Task 2 + 7 config + 3 verification + 1 ordering); or a golden write is reported as `obsolete`/`mismatch`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/config.invalid.json` parses as an array of exactly 5 objects whose `path` values in order are `''`, `/tracker`, `/tracker`, `/roles/0`, `/roles` and whose `rule` values in order are `schema.additionalProperties`, `schema.required`, `schema.if`, `schema.const`, `schema.contains`
    - `packages/core/test/__golden__/verification.invalid.json` parses as an array whose `path` set is exactly `''`, `/commit`, `/reviewed_on`
    - `packages/core/test/schemas.test.ts` contains `describe('config.schema.json'`, `describe('verification.schema.json'`, `'owner/name'`, `source: 'figma'`, `['accord', 'profile', 'tracker', 'design', 'roles', 'runtimes']`, `['ticket', 'commit', 'reviewed_on']`, and `/key|token|secret|password/i`
    - `npm test -- --project core schemas` reports 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The config and verification contracts are pinned by goldens and targeted assertions covering D-09, D-13, D-15, D-16, and the no-credentials prohibition.</done>
</task>

<task type="auto">
  <name>Task 2: Ticket edges — tracker map (FMT-03) and the D-21..D-25 decisions</name>
  <read_first>
    - packages/core/test/schemas.test.ts (as extended by Task 1)
    - packages/core/schemas/ticket.schema.json (`tracker` patternProperties, `verified` pattern, `ac_hash` minLength, `design` pattern)
    - .planning/phases/01-workspace-and-formats/01-CONTEXT.md D-01, D-02, D-03, D-04, D-05, D-21, D-22, D-23, D-25; Claude's Discretion (`id` pattern)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md — Assumptions A4 (`design` `^https://`), A5 (tracker key pattern), Validation Architecture row FMT-03
  </read_first>
  <files>packages/core/test/schemas.test.ts, packages/core/test/__golden__/ticket.tracker.invalid.json</files>
  <action>
Append `describe('ticket.schema.json tracker map (FMT-03)')` using a base valid ticket `{ id: 'TICKET-ID', title: 'Short title', type: 'story', status: 'open' }` spread with a `tracker` value per case:
  1. valid: `{ shortcut: '1234' }`, `{ 'github-issues': '42' }`, `{ shortcut: '1234', 'github-issues': '42' }` → `[]` each (map keyed by adapter name, string values).
  2. `it('an empty map is allowed (D-22)')` — `tracker: {}` → `[]`.
  3. `it('capitalised key, numeric value, empty value are named by path (golden)')` — `{ Shortcut: '1234', shortcut: 1234, jira: '' }` → `toMatchFileSnapshot('./__golden__/ticket.tracker.invalid.json')`; expect findings at `/tracker` (`schema.additionalProperties` for `Shortcut`), `/tracker/shortcut` (`schema.type`), `/tracker/jira` (`schema.minLength`). Confirm the golden.
  4. `it('keys are ASCII lowercase kebab, case-sensitive; values are measured in code points')` — key `'SHORTCUT'` rejected; key `'short_cut'` rejected (underscore); key `'a1-b2'` accepted; value `'é'` (one code point, two UTF-8 bytes) accepted (encoding edge: `minLength` counts code points, not bytes).

Append `describe('ticket.schema.json decisions D-01..D-05, D-21..D-25')`:
  1. D-01/D-02: `type: 'feature'` → `/type` `schema.enum`; `status: 'todo'` → `/status` `schema.enum`; each of `epic|story|bug` × `draft|open|archived` is valid (loop, 9 cases in one `it`).
  2. D-21: omitting `type` → `''` `schema.required`; omitting `status` → `''` `schema.required`; omitting `ui` → valid (default applied by the loader in Phase 2, not by the schema).
  3. D-23: `{ ...base, type: 'epic', design: 'https://www.figma.com/file/abc' }` → `[]`; `design: 'figma.com/abc'` → `/design` `schema.pattern` (A4).
  4. D-25: `ac_hash: 'a'` valid; `ac_hash: ''` → `/ac_hash` `schema.minLength`; `ac_hash: 123` → `schema.type`.
  5. D-03: `verified: ['ac-1', 'ac-12']` valid; `verified: ['ac-0']` → `/verified/0` `schema.pattern`; `verified: ['ac-1', 'ac-1']` → `/verified` `schema.uniqueItems`; `verified: 'ac-1'` → `/verified` `schema.type`.
  6. D-04/D-05: `owner: 'x'` → `''` `schema.additionalProperties`; `qa: { ticks: [] }` → `''` `schema.additionalProperties`; `feature: 'x'` → `''` `schema.additionalProperties`; `sprint: 3` and `priority: 'high'` → `''` `schema.additionalProperties` (tracker-owned data cannot leak into git); `assumptions: [{ text: 'a' }]` → `/assumptions/0` `schema.required`; `assumptions: [{ text: 'a', confirmed: 'yes' }]` → `/assumptions/0/confirmed` `schema.type` (booleans are the only non-string scalars).
  7. `id`/`parent` pattern: `id: '-bad'` → `/id` `schema.pattern`; `id: 'EPIC-1.2_a'` valid; `parent: 'EPIC-1'` valid; `parent: ''` → `/parent` `schema.pattern`.

Keep assertions on `(path, rule)` pairs via a small local helper `has(findings, path, rule)` rather than on message text. Run `npm test -- --project core schemas`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core schemas</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary shows fewer than 28 tests passed (17 after Task 1 + 4 tracker + 7 decisions)</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/ticket.tracker.invalid.json` parses as an array whose `path` set is exactly `/tracker`, `/tracker/shortcut`, `/tracker/jira`
    - `packages/core/test/schemas.test.ts` contains `tracker: {}`, `'github-issues': '42'`, `Shortcut`, `'é'`, `type: 'feature'`, `status: 'todo'`, `ac_hash: ''`, `'ac-0'`, `owner: 'x'`, `sprint: 3`, `priority: 'high'`, and `confirmed: 'yes'`
    - `npm test -- --project core schemas` reports 0 failures and at least 28 passing tests
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Every ticket-frontmatter decision from CONTEXT.md is an executable assertion; the tracker map's shape, case-sensitivity, empty case, and code-point length rule are pinned.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `packages/core/test/schemas.test.ts` (extended): `describe` blocks `config.schema.json`, `verification.schema.json`, `finding order is deterministic`, `ticket.schema.json tracker map (FMT-03)`, `ticket.schema.json decisions D-01..D-05, D-21..D-25`
- Goldens: `packages/core/test/__golden__/config.invalid.json`, `verification.invalid.json`, `ticket.tracker.invalid.json`

No new exported symbols.
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (spec-less probe rows surfaced, not silently dropped)

| Requirement | Probe | Disposition |
|---|---|---|
| FMT-03 | empty — What is the result for empty, single-element, or null input? | Resolved explicit (Task 2 case 2): `tracker: {}` is valid per D-22; Phase 3 lint may warn. A single-entry map is the normal case. `tracker: null` is rejected (`schema.type`) — covered implicitly by `type: object`. |
| FMT-03 | encoding — Whose definition of length/equality applies? | Resolved explicit (Task 2 case 4): keys are matched byte-for-byte against an ASCII pattern (case-sensitive); values use JSON Schema `minLength`, which ajv measures in Unicode code points (`unicode: true` default), so `'é'` has length 1. |
| FMT-06 | unclassified — review manually | Flagged: the `accord` pin is checked for semver **shape** only; whether the CLI requires an exact match or a range against its own version is Phase 5 (CLI-06) and is not decided here. Also flagged (RESEARCH A5): the tracker key pattern `^[a-z][a-z0-9-]*$` assumes all future adapter names are lowercase kebab (`shortcut`, `github-issues`, `jira`, `linear`). |
| CORE-01 | ordering — When elements compare equal, is output order specified and stable? | Resolved explicit (Task 1 last describe): ajv emits findings in schema-evaluation order, identical across runs; goldens pin it. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter / config document → `validate()` | Untrusted user-authored objects reach the compiled validators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information disclosure | ticket frontmatter / `config.yml` undeclared keys | medium | mitigate | Task 2 case 6 and Task 1 case 6 prove `sprint`, `priority`, `owner`, `qa`, and any credential-looking key are rejected (D-05) |
| T-01-05 | Denial of service | `pattern` keywords on user input | low | mitigate | Patterns are anchored and linear; Task 2 exercises them with short inputs only; no user-supplied patterns |
| T-01-10 | Repudiation | `verification.md` `reviewer` field could claim authorship without proof | low | mitigate | Task 1 verification case 3 asserts `reviewer` is not a field; the git author is the proof (D-09) |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages; pins and lockfile unchanged |
</threat_model>

<verification>
- `npm test -- --project core schemas` green with the three new goldens present and reviewed.
- Each golden names paths, never just messages.
</verification>

<success_criteria>
- FMT-02, FMT-03, FMT-06: valid documents pass, invalid ones fail with JSON-pointer paths, for all three schemas; every CONTEXT decision about the formats has a corresponding assertion.
- Probe edges (empty, encoding, ordering) resolved explicitly; FMT-06 pin-semantics flagged for Phase 5.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-and-formats/01-03-SUMMARY.md` when done. Do not commit.
</output>
