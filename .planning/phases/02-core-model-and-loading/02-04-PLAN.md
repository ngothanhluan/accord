---
phase: 02-core-model-and-loading
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/load/gherkin.ts
  - packages/core/test/gherkin.test.ts
  - packages/core/test/__golden__/gherkin-shapes.snapshot.json
  - packages/core/test/fixtures/gherkin-shapes/accord/config.yml
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/OUTLINE.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/VI.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/RULE.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/PARSE-ERROR.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/TAGS.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/STEPS.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/MULTI.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/EMPTY-AC.md
  - packages/core/test/fixtures/gherkin-shapes/accord/tickets/BADLANG.md
autonomous: true
requirements: [FMT-04, CORE-03, CORE-06]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Scenario Outline with two example rows is exactly one `ScenarioRef` with `keyword` `Scenario Outline`, and its `steps` end with one `Examples:` string carrying the header and both rows (FMT-04, D-48, D-50)"
    - "A fence beginning `# language: vi` parses with the Vietnamese dialect: `Kịch bản`/`Tình huống` become `keyword` `Scenario`, `Khung kịch bản` becomes `Scenario Outline`, `line` is still the Markdown line of the keyword, and step strings keep the Vietnamese step keywords (D-50)"
    - "Background steps (feature-level, and rule-level after them) are prepended to every scenario's `steps`; scenarios nested under `Rule:` are collected and the rule name is absent from the result (D-47, D-49)"
    - "A Gherkin parse error yields `load.gherkin-parse` at the Markdown line of the offending fence line, the ticket keeps its frontmatter, and `scenarios` is empty for that fence (D-35, CORE-03)"
    - "An untagged scenario is kept with `acTag` absent and `tags` `[]`; a scenario tagged `@ac-1 @ac-2 @smoke` has `acTag` `ac-1` and all three in `tags`; a feature-level tag never becomes a scenario tag; two scenarios sharing `@ac-1` are both kept with no loader finding (D-46; FMT-04 adjacency edge — equal tags collide only in Phase 3, the loader keeps both, verification: explicit)"
    - "Two `gherkin` fences under `## Acceptance criteria` are parsed separately and their scenarios concatenated in document order; a fence without a `Feature:` line gets one prepended and its lines still map to the Markdown file (D-36; FMT-04 ordering edge — document order, stable, verification: explicit)"
    - "An `## Acceptance criteria` section with no fence, or a fence holding only `Feature: X`, yields `scenarios: []` and no loader finding (FMT-04 empty edge, verification: explicit)"
    - "Extracting the same fence twice yields identical output; each extraction uses a fresh parser and no module-level state (CORE-03 concurrency edge, verification: explicit)"
  artifacts:
    - path: "packages/core/test/__golden__/gherkin-shapes.snapshot.json"
      provides: "golden for nine Gherkin-shape tickets"
      contains: "Scenario Outline"
    - path: "packages/core/test/gherkin.test.ts"
      provides: "steps normalisation contract, line remap, determinism, dialect keywords"
      min_lines: 60
  key_links:
    - from: "packages/core/src/load/gherkin.ts"
      to: "@cucumber/gherkin"
      via: "dialects[lang].scenarioOutline decides keyword; Errors.CompositeParserException carries errors[].location"
      pattern: "CompositeParserException"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "Scenarios are never parsed by regex: every fence goes through `@cucumber/gherkin` and a file the parser rejects yields a finding, never a partial scenario list (PITFALLS §5, ARCHITECTURE anti-pattern 5)"
      status: resolved
      verification: judgment
    - statement: "The loader never keys a scenario by its name and never invents a tag for an untagged scenario; identity is the `@ac-n` tag the BA wrote or nothing (D-46, PITFALLS §5)"
      status: resolved
      verification: test
---

<objective>
**As a** BA writing acceptance criteria — in English or in Vietnamese, with a Background, an Outline, a Rule, or two fences — **I want** every scenario found with its tag and its exact line, and a typo reported at that line, **so that** the Done gate and the AC hash stand on complete, stable step content.

Purpose: 02-01 extracted two plain tagged scenarios. This slice proves D-46 to D-50, D-35, and D-36 against every Gherkin shape the team will write, documents the `steps` normalisation contract in a test (Claude's Discretion), and hardens `load/gherkin.ts`.

Output: the `gherkin-shapes` fixture and golden, `gherkin.test.ts`, and corrections to `load/gherkin.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-model-and-loading/02-CONTEXT.md
@.planning/phases/02-core-model-and-loading/02-PATTERNS.md
@.planning/phases/02-core-model-and-loading/02-01-SUMMARY.md
@packages/core/src/load/gherkin.ts
@packages/core/src/load/snapshot.ts
@packages/core/test/helpers/fixture.ts

<interfaces>
<!-- From 02-01. -->
```typescript
import { extractScenarios } from '../src/load/gherkin.js';
import type { Fence } from '../src/load/sections.js';
// extractScenarios(file, ticketId, fence: { info, open, content: Line[] }) -> { scenarios: ScenarioRef[]; findings: Finding[] }
// ScenarioRef = { name, keyword: 'Scenario' | 'Scenario Outline', line, tags: string[], acTag?: string, steps: string[] }
// Markdown line = fence.open + parserLine - prepended (prepended = 1 when a Feature line was inserted)
```
Verified dialect keywords for `vi` (from `dialects.vi` in @cucumber/gherkin 42.0.1 — use these, do not guess): feature `Tính năng`; background `Bối cảnh`; scenario `Tình huống` or `Kịch bản`; scenarioOutline `Khung tình huống` or `Khung kịch bản`; examples `Dữ liệu`; rule `Quy tắc`; given `Biết ` or `Cho `; when `Khi `; then `Thì `. English `Example:` is a Scenario; `Scenario Template:` is a Scenario Outline. An unknown language such as `# language: xx` throws `CompositeParserException` with one error at (1:1) `Language not supported: xx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steps contract, line remap, and determinism as unit tests over `extractScenarios`</name>
  <read_first>
    - packages/core/src/load/gherkin.ts (code under test — the prepend rule, the remap formula, the steps builder)
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-46, D-47, D-48, D-49, D-50, D-35; Claude's Discretion ("exact normalisation of steps ... documented in a test")
    - .planning/phases/02-core-model-and-loading/02-PATTERNS.md `load/gherkin.ts` section (AST shape, `location.line`, `errors[].location`)
  </read_first>
  <files>packages/core/src/load/gherkin.ts, packages/core/test/gherkin.test.ts</files>
  <action>
Write `packages/core/test/gherkin.test.ts` with a small `fence(lines: string[], open = 10)` helper building a `Fence` (`info: 'gherkin'`, `content` numbered `open + 1 ...`). Assert with `extractScenarios('accord/tickets/T.md', 'T', fence(...))`:

1. Steps contract (D-48, the documented normalisation): a scenario with `Given  a   thing` (extra spaces), a `When` step carrying a doc string of two lines (`hello`, `  world`), and a `Then` step carrying a two-row data table `| x | y |` / `| 1 | 2 |` produces `steps` exactly `['Given a thing', 'When <text> """hello world"""', 'Then <text> | x | y | | 1 | 2 |']` (whitespace collapsed to single spaces; doc string wrapped in triple quotes; table rows as pipe-delimited cells). Write the expected strings literally in the test so the contract is readable.
2. Outline (D-48, D-50): `Scenario Outline: O` with `Given <a>` and `Examples:` header `| a |` rows `| 1 |`, `| 2 |` → one ref, `keyword` `Scenario Outline`, `steps` `['Given <a>', 'Examples: | a | | 1 | | 2 |']`.
3. Background + Rule (D-47, D-49): feature `Background: Given fb`, `Rule: R` with `Background: Given rb` and `Scenario: S` `Given s` → one ref, `steps` `['Given fb', 'Given rb', 'Given s']`, `name` `S`; no `R` anywhere in the JSON of the result.
4. Line remap without prepend: a fence at `open` 30 whose content line 7 is the `Scenario:` line → `line` 37. With prepend: content starting `@ac-1` then `Scenario: S` (no Feature line) at `open` 30 → `line` 32 (content line 2), and `findings` is `[]`.
5. Tags (D-46): `@ac-2 @ac-1 @smoke` → `acTag` `ac-2` (first in order), `tags` `['@ac-2','@ac-1','@smoke']`; untagged → `acTag` undefined and absent from `JSON.stringify` output, `tags` `[]`; a feature-level `@feat` tag does not appear in any scenario's `tags`.
6. Dialect (D-50): content `# language: vi`, `Tính năng: T`, `@ac-1`, `Kịch bản: S`, `Cho a` → `keyword` `Scenario`, `steps` `['Cho a']`; `Khung kịch bản:` with `Dữ liệu:` → `Scenario Outline`; a fence `# language: vi` followed directly by `@ac-1` and `Kịch bản: S` (no feature line) parses because the prepended line uses `Tính năng:`.
7. Errors (D-35): a fence with a stray line `Bogus` after a step at content line 4 → `scenarios` `[]`, one finding with `rule` `load.gherkin-parse`, `line` `open + 4`, `reason` not starting with `(`; `# language: xx` → one finding at `open + 1` whose reason contains `Language not supported`.
8. Determinism (CORE-03 concurrency edge): calling `extractScenarios` twice on the same fence gives `JSON.stringify`-equal results; the fence object is not mutated.

Fix `load/gherkin.ts` for every failing expectation. Run `npm test -- --project core gherkin`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core gherkin</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary reports fewer than 8 passing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/gherkin.test.ts` contains the literal strings `Examples: | a | | 1 | | 2 |`, `Khung kịch bản`, `Language not supported`, `load.gherkin-parse`, and `Given fb`
    - `npm test -- --project core gherkin` reports at least 8 passing tests and 0 failures
    - `grep -c 'new Parser(' packages/core/src/load/gherkin.ts` prints at least 1 inside the function body (a parser per call), and `grep -c '^const parser' packages/core/src/load/gherkin.ts` prints 0 (no module-level parser)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The `steps` identity contract, the Markdown line remap with and without a synthetic Feature line, tag rules, dialect handling, and parse-error reporting are pinned by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: The `gherkin-shapes` fixture — nine tickets, one golden</name>
  <read_first>
    - packages/core/test/snapshot.test.ts and packages/core/test/helpers/fixture.ts (discovery loop)
    - packages/core/src/load/snapshot.ts `parseTicket` (which fences reach `extractScenarios`)
    - The `<interfaces>` block above for the exact `vi` keywords
  </read_first>
  <files>packages/core/test/gherkin.test.ts, packages/core/test/__golden__/gherkin-shapes.snapshot.json, packages/core/test/fixtures/gherkin-shapes/accord/config.yml, packages/core/test/fixtures/gherkin-shapes/accord/tickets/OUTLINE.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/VI.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/RULE.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/PARSE-ERROR.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/TAGS.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/STEPS.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/MULTI.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/EMPTY-AC.md, packages/core/test/fixtures/gherkin-shapes/accord/tickets/BADLANG.md</files>
  <action>
Create `packages/core/test/fixtures/gherkin-shapes/accord/` with a valid `config.yml` and nine story tickets sharing the six-line valid frontmatter (`---`, `id: <stem>`, `title: t`, `type: story`, `status: open`, `---`; body line 1 = Markdown line 7). Each body is `## Acceptance criteria` followed by the fence(s) described; record the exact `Scenario:` keyword lines you write and assert them:

- `OUTLINE.md`: one fence, `Feature: OUTLINE`, `@ac-1`, `Scenario Outline: Nhiều giá trị` with `Given <a>`, `Examples:` `| a |` `| 1 |` `| 2 |` → one ref, keyword `Scenario Outline`.
- `VI.md`: `# language: vi`, `Tính năng: VI`, `Bối cảnh:` with `Cho một người dùng`, `@ac-1` `Kịch bản: Đăng nhập` with `Khi họ đăng nhập` / `Thì thấy bảng điều khiển`, `@ac-2` `Khung kịch bản: Nhiều` with `Cho <a>` and `Dữ liệu:` `| a |` `| 1 |` → two refs with English `keyword` values, Vietnamese step keywords in `steps`, Background first in both.
- `RULE.md`: `Feature: RULE`, `Background:` `Given fb`, `Quy tắc`-free English `Rule: Thanh toán` with `Background:` `Given rb`, `@ac-1` `Scenario: Trong rule` `Given s` → one ref, steps `['Given fb','Given rb','Given s']`, no `Thanh toán` in the golden.
- `PARSE-ERROR.md`: `Feature: PARSE-ERROR`, `@ac-1`, `Scenario: S`, `Given a`, then a line `Đây là dòng sai` (not a step) → `scenarios` `[]`, one `load.gherkin-parse` finding at that line's Markdown number; `frontmatter` present.
- `TAGS.md`: `@feature-tag` above `Feature: TAGS`; scenario 1 untagged; scenario 2 tagged `@ac-1 @ac-2 @smoke`; scenario 3 tagged `@smoke @ac-3`; scenario 4 tagged `@ac-1` again → four refs: `acTag` absent / `ac-1` / `ac-3` / `ac-1`; no findings; no `@feature-tag` in any `tags`.
- `STEPS.md`: `@ac-1` scenario with a doc string and a data table (as in Task 1 case 1) → steps exactly as the Task 1 contract.
- `MULTI.md`: fence 1 `Feature: MULTI`, `@ac-1` `Scenario: One`; fence 2 with no Feature line: `@ac-2` `Scenario: Two` `Given b` → two refs in that order with exact lines; no findings.
- `EMPTY-AC.md`: `## Acceptance criteria` with only a guidance comment, then `## Plan` → `scenarios` `[]`, no findings. Add a second section? No — keep one ticket per shape; the "fence holding only `Feature: X`" case goes into `MULTI.md` as a third fence at the end (adds no scenario).
- `BADLANG.md`: fence `# language: xx`, `Feature: X` → one `load.gherkin-parse` finding at the fence's first content line with reason containing `Language not supported`; `scenarios` `[]`.

Append `describe('gherkin-shapes fixture')` to `gherkin.test.ts` asserting the above with exact `line` values. Record `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` first. Run `npm test -- --project core snapshot -u -t "fixture gherkin-shapes"` once for the golden — never `-u` without the `-t` filter in this wave: `snapshot.test.ts` wraps every fixture folder in `describe('fixture <name>')` and an unscoped `-u` would rewrite the tracer golden and sibling plans' half-written goldens. Read it, verify each ticket, then run `npm test -- --project core snapshot -t "fixture (gherkin-shapes|valid-build)"` without `-u`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core gherkin && npm test -- --project core snapshot -t "fixture (gherkin-shapes|valid-build)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a `|core|` summary reports 0 tests; or a golden is reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/gherkin-shapes.snapshot.json` parses; `tickets.OUTLINE.scenarios` has length 1 with `keyword` `"Scenario Outline"`; `tickets.VI.scenarios.map(s => s.keyword)` equals `["Scenario","Scenario Outline"]` and `tickets.VI.scenarios[0].steps[0]` starts with `Cho `
    - In the golden, `tickets.RULE.scenarios[0].steps` equals `["Given fb","Given rb","Given s"]` and the string `Thanh toán` does not appear anywhere in the golden
    - In the golden, `tickets.TAGS.scenarios.map(s => s.acTag ?? null)` equals `[null,"ac-1","ac-3","ac-1"]` and no `tags` array contains `@feature-tag`
    - In the golden, `errors` has exactly two entries: one for `accord/tickets/PARSE-ERROR.md` with rule `load.gherkin-parse` and a `line` inside that file's fence, one for `accord/tickets/BADLANG.md` with reason containing `Language not supported`; `tickets["PARSE-ERROR"].frontmatter.id` is `"PARSE-ERROR"`
    - In the golden, `tickets.MULTI.scenarios.map(s => s.acTag)` equals `["ac-1","ac-2"]` and `tickets["EMPTY-AC"].scenarios` is `[]`
    - `npm test -- --project core gherkin` and `npm test -- --project core snapshot -t "fixture (gherkin-shapes|valid-build)"` report 0 failures; `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` is unchanged from the start of the plan (the tracer golden was compared, never rewritten)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Every Gherkin shape in the D-53 fixture list (Outline, `vi`, Background, Rule, parse error, tag variants, doc strings and tables, multi-fence, empty AC) has an executable expectation and a golden.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Fixture: `packages/core/test/fixtures/gherkin-shapes/` (config + 9 tickets)
- Golden: `packages/core/test/__golden__/gherkin-shapes.snapshot.json`
- Test: `packages/core/test/gherkin.test.ts` (the `steps` normalisation contract lives here)
- Hardened internal module (no new exports): `packages/core/src/load/gherkin.ts`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions

| Source | Item | Disposition |
|---|---|---|
| D-48 | `steps` string format | Chosen (Claude's Discretion): `keyword text` with doc strings as `"""content"""` and tables as `| a | b |` cells, all whitespace collapsed; Outline `Examples:` appended as one string per table. Deterministic and documented in `gherkin.test.ts`. Any later change to this format changes every `ac_hash` (Phase 4), so it should be treated as frozen after Phase 4 ships. |
| D-49 | Rule-level `Background:` | Assumed: appended after the feature Background for scenarios inside that Rule (Gherkin semantics). |
| D-50 | unknown `# language:` | Assumed: reported as `load.gherkin-parse` (the parser's own error), not a distinct rule. |
| D-46 | scenario `name` trailing whitespace | The parser trims; not normalised further. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fence text → `@cucumber/gherkin` parser | Untrusted Gherkin from any contributor, possibly in any dialect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | scenario identity keyed on mutable names | medium | mitigate | Identity is the `@ac-n` tag; names are display only; TAGS fixture pins untagged scenarios carry no invented id (PITFALLS §5) |
| T-02-12 | Tampering | hash input omitting Background or Examples so an AC edit goes unnoticed | medium | mitigate | RULE, OUTLINE, STEPS fixtures pin that Background, Examples, doc strings, and tables are all inside `steps` (D-47, D-48) |
| T-02-13 | Denial of service | pathological fence content | low | accept | Upstream parser is linear over tokens; fences are bounded by ticket size discipline |
| T-02-SC | Tampering | npm installs | high | mitigate | No packages added |
</threat_model>

<verification>
- Per-plan gates are the scoped commands in each `<verify>`; golden reviewed against the nine-ticket table; the tracer golden's hash unchanged.
- Wave-end check, not a per-plan gate: once every Wave 2 plan (02-02..02-05, 02-07) has finished, `npm test -- --project core` must report 0 failures. While a sibling plan's fixture is still being written, a failure inside that sibling's `fixture <name>` describe is not this plan's failure.
</verification>

<success_criteria>
- ROADMAP criterion 2 (line numbers, tags, Outline counts as one) pinned for every dialect and shape; D-35 (Gherkin side), D-36, D-46, D-47, D-48, D-49, D-50 each have executable assertions.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/02-core-model-and-loading/02-04-SUMMARY.md` when done. Do not commit.
</output>
