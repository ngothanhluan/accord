---
phase: 02-core-model-and-loading
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/load/yaml.ts
  - packages/core/src/load/frontmatter.ts
  - packages/core/src/load/config.ts
  - packages/core/test/frontmatter.test.ts
  - packages/core/test/__golden__/frontmatter-errors.snapshot.json
  - packages/core/test/__golden__/no-config.snapshot.json
  - packages/core/test/__golden__/bad-config.snapshot.json
  - packages/core/test/__golden__/config-syntax.snapshot.json
  - packages/core/test/fixtures/frontmatter-errors/accord/config.yml
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/NOFM.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/UNTERMINATED.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/SYNTAX.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/NOTMAP.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/EMPTYFM.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/SCHEMA.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/TYPING.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/MISMATCH.md
  - packages/core/test/fixtures/frontmatter-errors/accord/tickets/EMPTY.md
  - packages/core/test/fixtures/no-config/accord/tickets/ONLY.md
  - packages/core/test/fixtures/bad-config/accord/config.yml
  - packages/core/test/fixtures/bad-config/accord/tickets/T.md
  - packages/core/test/fixtures/config-syntax/accord/config.yml
  - packages/core/test/fixtures/config-syntax/accord/tickets/T.md
autonomous: true
requirements: [CORE-02, CORE-06, FMT-08]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A ticket with no frontmatter, an unterminated block, a YAML syntax error, a non-map document, an empty document, or a schema violation still has an entry in `snapshot.tickets` keyed by its file stem, with `frontmatter` absent and its body sections parsed; no partial frontmatter object is ever kept (D-32, D-35)"
    - "Finding lines follow D-33 exactly: no frontmatter → 1; YAML syntax error → the line `yaml` reports (+1 for the opening `---`); non-map or empty YAML → 2; `schema.additionalProperties` → the line of the unknown key; `schema.enum`/`schema.type`/`schema.pattern` → the line of the offending value; `schema.required` at the root → 2 (flagged assumption)"
    - "`frontmatter.id` that differs from the file name is kept verbatim with no loader finding (D-34)"
    - "Unquoted `007`, `0x1F`, `0123`, `1e3`, and `2026-01-01` load as the strings `'007'`, `'0x1F'`, `'0123'`, `'1e3'`, `'2026-01-01'`; unquoted `no` loads as the string `'no'` and is reported by the schema as a type error rather than coerced to `false` (CORE-02, PITFALLS §8; CORE-02 boundary edge — each YAML implicit-typing boundary value, verification: explicit; CORE-02 precision edge — no numeric conversion can lose precision because no conversion happens, and every line number in the golden is exact, verification: explicit)"
    - "A zero-byte ticket file yields one `load.frontmatter-missing` finding at line 1 and an empty `sections` array; an empty `config.yml` yields `load.yaml-not-map` at line 1 (FMT-08 empty edge, verification: explicit)"
    - "When `accord/config.yml` is absent `snapshot.config` is `undefined` and `errors` contains `load.config-missing` for file `accord/config.yml`; when it is invalid, `config` is `undefined` and every schema finding names `accord/config.yml`, its pointer, and its file line (no `---` offset) (D-31)"
  artifacts:
    - path: "packages/core/test/__golden__/frontmatter-errors.snapshot.json"
      provides: "one golden covering nine broken and typing-edge tickets"
      contains: "load.frontmatter-missing"
    - path: "packages/core/test/__golden__/bad-config.snapshot.json"
      provides: "config schema findings with lines"
      contains: "\"file\": \"accord/config.yml\""
    - path: "packages/core/test/frontmatter.test.ts"
      provides: "exact (file, rule, line, pointer) assertions and unit tests for splitFrontmatter/normaliseText"
      min_lines: 60
  key_links:
    - from: "packages/core/src/load/yaml.ts"
      to: "yaml"
      via: "parseDocument with LineCounter; linePos(range[0]) gives the line of a node"
      pattern: "LineCounter"
    - from: "packages/core/src/load/frontmatter.ts"
      to: "packages/core/src/validate/index.ts"
      via: "validate(schemaId, map) then schemaFindings(file, doc, lines, 1, found)"
      pattern: "schemaFindings"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "The loader never silently coerces, quotes, or repairs a frontmatter value to make it validate; `no` stays `'no'` and is reported, `0123` stays `'0123'` (PITFALLS §8, D-32)"
      status: resolved
      verification: test
    - statement: "BOM and CRLF are never reported as findings; a Windows-authored file produces the same findings as its LF twin (D-38)"
      status: resolved
      verification: test
---

<objective>
**As a** BA or developer who mistyped a frontmatter key, forgot the closing `---`, or wrote `confirmed: no`, **I want to** see the file and the exact line of the problem, **so that** I can fix the ticket without a developer reading YAML for me — and the rest of the ticket still loads.

Purpose: 02-01 wired `loadFrontmatter`, `parseYamlMap`, `schemaFindings`, and `loadConfig` on the happy path. This slice proves and hardens every D-32/D-33 error convention and the D-31 config boundary with fixtures whose expected lines are written down before the golden exists.

Output: the `frontmatter-errors`, `no-config`, `bad-config`, and `config-syntax` fixtures with goldens, `frontmatter.test.ts`, and any line-mapping corrections in `load/yaml.ts`, `load/frontmatter.ts`, `load/config.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-model-and-loading/02-CONTEXT.md
@.planning/phases/02-core-model-and-loading/02-01-SUMMARY.md
@packages/core/src/load/yaml.ts
@packages/core/src/load/frontmatter.ts
@packages/core/src/load/config.ts
@packages/core/test/helpers/fixture.ts
@packages/core/test/snapshot.test.ts
@packages/core/schemas/ticket.schema.json
@packages/core/schemas/config.schema.json

<interfaces>
<!-- From 02-01. Do not change these signatures; extend behaviour only. -->
```typescript
import { loadSnapshot } from '../src/index.js';
import type { Finding, RepoSnapshot } from '../src/index.js';
import { normaliseText, splitFrontmatter, loadFrontmatter } from '../src/load/frontmatter.js';
import { parseYamlMap, schemaFindings } from '../src/load/yaml.js';
import { loadConfig } from '../src/load/config.js';
import { readFixture, variants, stableJson } from './helpers/fixture.js';
// Finding = { file, line?, rule, reason, pointer? }; SchemaFinding = { pointer, rule, reason, param? }
// parseYamlMap -> { map?, doc, lines, findings }; schemaFindings(file, doc, lines, lineOffset, found).
// The D-33 line rule is defined ONCE in 02-01 step 4 and only pinned here: additionalProperties -> the unknown key's line;
// required -> the key line of the pointer's path (root -> first YAML line); any other rule -> the value's line when the
// value is a scalar, the key line when the value is a map or sequence (so /tracker and /roles report their key line).
// The generic loop in snapshot.test.ts wraps every fixture folder that has accord/ in describe('fixture <name>');
// in Wave 2 always scope golden creation with -t "fixture <name>" so -u never touches another plan's golden.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broken and typing-edge ticket frontmatter is reported at the right line and never partially kept</name>
  <read_first>
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-32, D-33, D-34, D-35, D-38; Phase 1 D-04/D-05/D-21 (which keys exist and which are required)
    - packages/core/src/load/frontmatter.ts and packages/core/src/load/yaml.ts (the code under test; adjust only where an expected line below is not produced)
    - packages/core/test/snapshot.test.ts and packages/core/test/helpers/fixture.ts (the discovery loop that will pick up the new fixture; the `stableJson` helper)
    - packages/core/schemas/ticket.schema.json (required keys, enums, `assumptions` item shape, `verified` pattern)
    - .planning/research/STACK.md Decision 2 table (which unquoted scalars stay strings under core schema + the numerics filter)
  </read_first>
  <files>packages/core/src/load/yaml.ts, packages/core/src/load/frontmatter.ts, packages/core/test/frontmatter.test.ts, packages/core/test/__golden__/frontmatter-errors.snapshot.json, packages/core/test/fixtures/frontmatter-errors/accord/config.yml, packages/core/test/fixtures/frontmatter-errors/accord/tickets/NOFM.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/UNTERMINATED.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/SYNTAX.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/NOTMAP.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/EMPTYFM.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/SCHEMA.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/TYPING.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/MISMATCH.md, packages/core/test/fixtures/frontmatter-errors/accord/tickets/EMPTY.md</files>
  <action>
Create the fixture `packages/core/test/fixtures/frontmatter-errors/accord/` (LF, UTF-8, no BOM) with a valid `config.yml` (the eight D-16 lines from the `valid-build` fixture with `tokens: ""`) and these tickets, each line numbered as written:

- `NOFM.md`: L1 `## Intent`, L2 `Không có frontmatter.` → expect `load.frontmatter-missing` at line 1; the ticket entry has `sections[0]` = Intent at line 1.
- `UNTERMINATED.md`: L1 `---`, L2 `id: UNTERMINATED`, L3 `title: x`, L4 `## Intent` (never closed) → `load.frontmatter-missing` at line 1; the whole text is the body, so `sections[0]` is Intent at line 4.
- `SYNTAX.md`: L1 `---`, L2 `id: SYNTAX`, L3 `title: a`, L4 `title: b`, L5 `type: story`, L6 `status: open`, L7 `---` → `yaml` reports the duplicate `title` key; expect one `load.yaml-syntax` finding whose `line` is the reported YAML line + 1 (expected 4 — confirm from yaml's `linePos` and pin whatever yaml reports; D-33 says "the line `yaml` reports"); frontmatter absent.
- `NOTMAP.md`: L1 `---`, L2 `- just a list`, L3 `---` → `load.yaml-not-map` at line 2.
- `EMPTYFM.md`: L1 `---`, L2 empty, L3 `---`, L4 `## Intent` → `load.yaml-not-map` at line 2; Intent section at line 4.
- `SCHEMA.md`: L1 `---`, L2 `id: SCHEMA`, L3 `type: feature`, L4 `status: open`, L5 `owner: someone`, L6 `assumptions:`, L7 `  - { text: "x", confirmed: no }`, L8 `verified:`, L9 `  - ac-0`, L10 `---`, L11 empty, L12 `## Intent` → expect exactly these five schema findings (any order): `schema.required` pointer `''` line 2 (title missing; root convention); `schema.additionalProperties` pointer `''` line 5 (the `owner` key via `param`); `schema.enum` pointer `/type` line 3; `schema.type` pointer `/assumptions/0/confirmed` line 7 (the Norway value `no` is a string, not `false`); `schema.pattern` pointer `/verified/0` line 9. `frontmatter` absent; `sections[0]` Intent at line 12 (D-35: body survives).
- `TYPING.md`: L1 `---`, L2 `id: TYPING`, L3 `title: 007`, L4 `type: story`, L5 `status: open`, L6 `tracker:`, L7 `  hex: 0x1F`, L8 `  oct: 0123`, L9 `  exp: 1e3`, L10 `  date: 2026-01-01`, L11 `---` → no findings; `frontmatter.title` is the string `007`; `tracker` is `{ hex: '0x1F', oct: '0123', exp: '1e3', date: '2026-01-01' }`.
- `MISMATCH.md`: L1 `---`, L2 `id: OTHER`, L3 `title: t`, L4 `type: bug`, L5 `status: draft`, L6 `---` → no findings; `tickets['MISMATCH'].frontmatter.id` is `'OTHER'` (D-34).
- `EMPTY.md`: zero bytes → `load.frontmatter-missing` at line 1; `sections` is `[]`, `requirements` `[]`, `scenarios` `[]`.

Write `packages/core/test/frontmatter.test.ts`: a `has(errors, file, rule, line, pointer?)` helper; `loadSnapshot(readFixture('frontmatter-errors'))` once; one `it` per ticket above asserting the exact findings and the `frontmatter` presence/absence; an `it` asserting every ticket above has an entry in `snapshot.tickets` (nine keys); unit tests: `splitFrontmatter` returns `bodyOffset` 17 for the `valid-build` LOGIN-1 text and 0 when there is no block; `normaliseText` strips only a leading U+FEFF (a U+FEFF at offset 5 is kept) and turns `\r\n` into `\n` while leaving a lone `\r` alone; `loadSnapshot` over `variants(readFixture('frontmatter-errors')).bomCrlf` produces findings deep-equal to the LF run (D-38: never reported).

Where an expected line is not produced, fix `load/yaml.ts` (the D-33 line rule in 02-01 step 4 — scalar value → value line; collection value or `schema.required` → key line; `additionalProperties` → the unknown key's line) or `load/frontmatter.ts`, not the fixture. Before touching anything record `git hash-object packages/core/test/__golden__/valid-build.snapshot.json`. Run `npm test -- --project core snapshot -u -t "fixture frontmatter-errors"` once to create `frontmatter-errors.snapshot.json` — never run `-u` without the `-t` filter in this wave: the discovery loop walks every fixture folder, so an unscoped `-u` would rewrite the tracer golden (hiding a regression caused by this plan's hardening) and any sibling plan's half-written fixture golden. Read the golden, confirm every line above, then run `npm test -- --project core frontmatter` and `npm test -- --project core snapshot -t "fixture (frontmatter-errors|valid-build)"` without `-u`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core frontmatter && npm test -- --project core snapshot -t "fixture (frontmatter-errors|valid-build)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a `|core|` summary reports 0 tests; or vitest reports a golden as obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` prints the same hash as recorded before the task (the tracer golden was compared, never rewritten)
    - `packages/core/test/__golden__/frontmatter-errors.snapshot.json` parses; `Object.keys(tickets)` sorted equals `["EMPTY","EMPTYFM","MISMATCH","NOFM","NOTMAP","SCHEMA","SYNTAX","TYPING","UNTERMINATED"]`; `tickets.TYPING.frontmatter.tracker` is `{"date":"2026-01-01","exp":"1e3","hex":"0x1F","oct":"0123"}` and `tickets.TYPING.frontmatter.title` is `"007"`; `tickets.MISMATCH.frontmatter.id` is `"OTHER"`
    - In the golden, `errors` contains objects with (`file`, `rule`, `line`) = (`accord/tickets/NOFM.md`, `load.frontmatter-missing`, 1), (`accord/tickets/UNTERMINATED.md`, `load.frontmatter-missing`, 1), (`accord/tickets/EMPTY.md`, `load.frontmatter-missing`, 1), (`accord/tickets/NOTMAP.md`, `load.yaml-not-map`, 2), (`accord/tickets/EMPTYFM.md`, `load.yaml-not-map`, 2), and one `load.yaml-syntax` for `accord/tickets/SYNTAX.md`
    - In the golden, the five `accord/tickets/SCHEMA.md` findings have (`rule`, `pointer`, `line`) = (`schema.required`, ``, 2), (`schema.additionalProperties`, ``, 5), (`schema.enum`, `/type`, 3), (`schema.type`, `/assumptions/0/confirmed`, 7), (`schema.pattern`, `/verified/0`, 9); `tickets.SCHEMA` has no `frontmatter` key and `tickets.SCHEMA.sections[0].line` is 12
    - `grep -c '"frontmatter"' packages/core/test/__golden__/frontmatter-errors.snapshot.json` prints 2 (TYPING and MISMATCH only)
    - `packages/core/test/frontmatter.test.ts` contains `bomCrlf`, `0x1F`, `confirmed: no` or `/assumptions/0/confirmed`, and `load.yaml-not-map`
    - `npm test -- --project core frontmatter` reports at least 12 passing tests and 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Every D-32 failure class and every YAML typing boundary from PITFALLS §8 is pinned with an exact file and line, and a broken frontmatter never hides the body.</done>
</task>

<task type="auto">
  <name>Task 2: config.yml missing, invalid, or unparsable is a finding, never an exception, and `snapshot.config` is absent</name>
  <read_first>
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-31; Phase 1 D-16 (config shape) and D-13/D-15
    - packages/core/src/load/config.ts (code under test) and packages/core/src/load/yaml.ts (line mapping with `lineOffset` 0)
    - packages/core/schemas/config.schema.json (`if/then` on `github-issues`, `roles` contains, enum values)
    - packages/core/test/__golden__/config.invalid.json (the same probe document's findings without lines — the new golden adds `file` and `line`)
  </read_first>
  <files>packages/core/src/load/config.ts, packages/core/test/frontmatter.test.ts, packages/core/test/__golden__/no-config.snapshot.json, packages/core/test/__golden__/bad-config.snapshot.json, packages/core/test/__golden__/config-syntax.snapshot.json, packages/core/test/fixtures/no-config/accord/tickets/ONLY.md, packages/core/test/fixtures/bad-config/accord/config.yml, packages/core/test/fixtures/bad-config/accord/tickets/T.md, packages/core/test/fixtures/config-syntax/accord/config.yml, packages/core/test/fixtures/config-syntax/accord/tickets/T.md</files>
  <action>
Create three fixtures (LF, no BOM). `ONLY.md` and both `T.md` are the same minimal valid story: L1 `---`, L2 `id: <stem>`, L3 `title: t`, L4 `type: story`, L5 `status: open`, L6 `---`, L7 empty, L8 `## Intent`.

- `no-config/accord/tickets/ONLY.md` only → expect `snapshot.config` undefined, one finding `{ file: 'accord/config.yml', rule: 'load.config-missing' }` with no `line` key, and `tickets.ONLY.frontmatter` present (the ticket still loads).
- `bad-config/accord/config.yml`: L1 `accord: "0.1.0"`, L2 `profile: staging`, L3 `tracker:`, L4 `  adapter: github-issues`, L5 `design:`, L6 `  tokens: ""`, L7 `roles: [ba]`, L8 `runtimes: [claude]`, L9 `sprint: 3` → `config` undefined; findings all with `file: 'accord/config.yml'`: `schema.additionalProperties` pointer `''` line 9 (`sprint`); `schema.enum` pointer `/profile` line 2; `schema.required` pointer `/tracker` line 3 (`repo` missing under `github-issues`: the parent key line); `schema.if` pointer `/tracker` line 3 (ajv's wrapper finding, kept as in Phase 1; its value node is the `tracker` block map, so the 02-01 step 4 collection rule reports the `tracker:` key line — the same line as the `schema.required`, not line 4 where the map's first child sits); the `roles` findings (`/roles/0` `schema.const` — a scalar on line 7 — and `/roles` `schema.contains` — a flow sequence whose key is on line 7, as the Phase 1 golden shows) both at line 7.
- `config-syntax/accord/config.yml`: L1 `accord: [` → one `load.yaml-syntax` finding for `accord/config.yml` at the line `yaml` reports (expected 1; pin what yaml reports); `config` undefined.

Append to `packages/core/test/frontmatter.test.ts` a `describe('config.yml (D-31)')` with one `it` per fixture asserting the findings above, that `snapshot.config` is `undefined` in all three, and — using `loadConfig` directly on the empty string — that an empty config yields `load.yaml-not-map` at line 1 (FMT-08 empty edge; config lines carry no `---` offset). Fix `load/config.ts` (or the shared mapping in `load/yaml.ts`) where an expected line is not produced. Run `npm test -- --project core snapshot -u -t "fixture (no-config|bad-config|config-syntax)"` once for the three new goldens (scoped `-u` only, as in Task 1), read them, then run `npm test -- --project core snapshot -t "fixture (no-config|bad-config|config-syntax|frontmatter-errors|valid-build)"` without `-u`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core frontmatter && npm test -- --project core snapshot -t "fixture (no-config|bad-config|config-syntax|frontmatter-errors|valid-build)" && npm run typecheck</automated>
    <fails_when>non-zero exit; or `No test files found`; or a `|core|` summary reports 0 tests; or a golden is reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/__golden__/no-config.snapshot.json` has no `"config"` key, its `errors` is exactly one object `{"file":"accord/config.yml","reason":<string>,"rule":"load.config-missing"}` (no `line`), and `tickets.ONLY.frontmatter.id` is `"ONLY"`
    - `packages/core/test/__golden__/bad-config.snapshot.json` has no `"config"` key; every entry in `errors` has `"file": "accord/config.yml"`; it contains findings with (`pointer`, `rule`, `line`) = (``, `schema.additionalProperties`, 9), (`/profile`, `schema.enum`, 2), (`/tracker`, `schema.required`, 3), (`/roles`, `schema.contains`, 7)
    - `packages/core/test/__golden__/config-syntax.snapshot.json` contains exactly one `load.yaml-syntax` finding for `accord/config.yml` and no `"config"` key
    - `grep -c "describe('config.yml (D-31)'" packages/core/test/frontmatter.test.ts` prints 1
    - `npm test -- --project core frontmatter` and `npm test -- --project core snapshot -t "fixture (no-config|bad-config|config-syntax|frontmatter-errors|valid-build)"` report 0 failures; `git hash-object packages/core/test/__golden__/valid-build.snapshot.json` is unchanged from the start of the plan
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>A missing, invalid, or unparsable `config.yml` is a finding the host can map to exit 2, the config object is never partially present, and every config finding carries the file line.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Fixtures: `packages/core/test/fixtures/frontmatter-errors/` (config + 9 tickets), `no-config/`, `bad-config/`, `config-syntax/`
- Goldens: `frontmatter-errors.snapshot.json`, `no-config.snapshot.json`, `bad-config.snapshot.json`, `config-syntax.snapshot.json`
- Tests: `packages/core/test/frontmatter.test.ts`
- Hardened internals (no new exports): `load/yaml.ts`, `load/frontmatter.ts`, `load/config.ts`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions

| Source | Item | Disposition |
|---|---|---|
| D-33 | `schema.required` at the document root | Assumed line 2 for frontmatter (line 1 for config.yml): the first YAML line. Alternative: the `---` line or the last frontmatter line. The golden pins the choice; changing it is a one-line edit in `load/yaml.ts`. |
| D-32 | unterminated frontmatter | Assumed: treated like "no frontmatter" — one `load.frontmatter-missing` at line 1 and the entire text becomes the body (so YAML lines may appear as body lines). Alternative: a distinct rule id `load.frontmatter-unterminated`. |
| D-33 | `schema.if` wrapper finding | Kept as Phase 1 kept it (STATE.md decision); Phase 3 decides whether to suppress it. It carries the parent key line. |
| FMT-08 | lone `\r` (classic Mac line endings) | Not normalised; D-38 names CRLF only. Pinned by a unit test so the behaviour is explicit. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter / config.yml text → `yaml.parseDocument` | Untrusted YAML from any contributor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | YAML implicit typing turning `no`/`0123`/dates into booleans/numbers/Dates | medium | mitigate | Core schema + numerics filter; the TYPING and SCHEMA fixtures pin string preservation and the surfaced type error |
| T-02-07 | Denial of service | pathological YAML (deep nesting, aliases) | low | accept | `yaml` 2.x has no alias-expansion bomb (no merge-key expansion by default); ticket frontmatter is bounded by the strict schema; not exercised in v0.1 |
| T-02-08 | Information disclosure | `Finding.reason` from yaml/ajv | low | accept | Messages name keys, positions, and allowed values; no file content beyond the offending key |
| T-02-SC | Tampering | npm installs | high | mitigate | No packages added; lockfile unchanged |
</threat_model>

<verification>
- Per-plan gates are the scoped commands in each `<verify>`; four new goldens reviewed line by line against the tables above; the tracer golden's hash unchanged.
- The same goldens are produced from the `bomCrlf` variant (the generic loop asserts it).
- Wave-end check, not a per-plan gate: once every Wave 2 plan (02-02..02-05, 02-07) has finished, `npm test -- --project core` must report 0 failures. While a sibling plan's fixture is still being written, a failure inside that sibling's `fixture <name>` describe is not this plan's failure.
</verification>

<success_criteria>
- D-31, D-32, D-33, D-34, D-35 (frontmatter side), D-38 are each pinned by an executable assertion with exact lines.
- ROADMAP criterion 1 (numerics and dates stay strings) is pinned beyond the tracer with the full PITFALLS §8 boundary set.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/02-core-model-and-loading/02-02-SUMMARY.md` when done. Do not commit.
</output>
