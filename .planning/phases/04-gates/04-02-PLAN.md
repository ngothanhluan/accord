---
phase: 04-gates
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/core/src/gate/done.ts
  - packages/core/src/gate/rules.ts
  - packages/core/src/gate/index.ts
  - packages/core/src/index.ts
  - packages/core/test/gate.test.ts
  - packages/core/test/fixtures/gate-done/accord/config.yml
  - packages/core/test/fixtures/gate-done/accord/tickets/PASS.md
  - packages/core/test/fixtures/gate-done/accord/tickets/PASS/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/SETS.md
  - packages/core/test/fixtures/gate-done/accord/tickets/SETS/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/STALE.md
  - packages/core/test/fixtures/gate-done/accord/tickets/STALE/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/TICKS.md
  - packages/core/test/fixtures/gate-done/accord/tickets/TICKS/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/COMMIT.md
  - packages/core/test/fixtures/gate-done/accord/tickets/COMMIT/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/BLOCKED.md
  - packages/core/test/fixtures/gate-done/accord/tickets/BLOCKED/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/REVIEW.md
  - packages/core/test/fixtures/gate-done/accord/tickets/REVIEW/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/NOVERIF.md
  - packages/core/test/fixtures/gate-done/accord/tickets/EMPTY.md
  - packages/core/test/fixtures/gate-done/reports/junit.xml
  - packages/core/test/fixtures/gate-done/src/auth/login.ts
  - packages/core/test/fixtures/gate-done/test/login.spec.ts
  - packages/core/test/__golden__/gate-done.PASS.done.json
  - packages/core/test/__golden__/gate-done.SETS.done.json
  - packages/core/test/__golden__/gate-done.STALE.done.json
  - packages/core/test/__golden__/gate-done.TICKS.done.json
  - packages/core/test/__golden__/gate-done.COMMIT.done.json
  - packages/core/test/__golden__/gate-done.BLOCKED.done.json
  - packages/core/test/__golden__/gate-done.REVIEW.done.json
  - packages/core/test/__golden__/gate-done.NOVERIF.done.json
  - packages/core/test/__golden__/gate-done.EMPTY.done.json
  - packages/core/test/__golden__/gate-done.PASS.ready.json
  - packages/core/test/__golden__/verification-edges.A.done.json
  - packages/core/test/__golden__/gate-done.snapshot.json
  - packages/core/test/__golden__/gate-done.lint.json
autonomous: true
requirements: [GATE-02, GATE-03, GATE-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`gateDone(snapshot, id)` returns `{ gate: 'done', ticket: id, verdict, findings, acHash }` built from the same scope filter, the same stamping, and the same sort as `gateReady`, and is pure by the same two-call and structuredClone checks (D-87)"
    - "`downgradeMaintain` is applied inside the shared engine body, so `gateDone` inherits the `maintain` matrix from the same single call site `gateReady` uses; four synthetic `error`-level rows, one per `MAINTAIN_DOWNGRADE` id, come back `warning` through `gateDone` on a `maintain` snapshot and unchanged through `gateDone` on `build`, which is what keeps GATE-12 a one-cell table edit rather than a change that leaves one gate behind (D-88, GATE-07)"
    - "`gateDone` on fixture `gate-done` ticket `PASS`, with the case table's `git` facts layered on, is `verdict: 'pass'` with no `error`-level finding, and stays `pass` after 04-03 and 04-04 add their rules"
    - "Done passes only when the scenario `@ac-n` set, the `## @ac-n` evidence-block set in `verification.md`, and `verified` are the same set; any difference yields exactly one `gate.tags-differ` error whose reason names, for each of the three sets, the tags that set lacks (GATE-02, ROADMAP criterion 2)"
    - "Two tags that are exactly equal across all three sets merge into one union member and raise nothing; a tag present in two sets and absent from the third is named against that third set only (GATE-02 adjacency)"
    - "A ticket with zero `@ac-n`-tagged scenarios fails Done with `gate.no-scenarios` regardless of `type`, so three empty sets are never treated as equal; a `story` with no scenarios additionally carries the existing ticket-scoped `lint.no-scenarios` error, and both findings are kept rather than merged (GATE-02 empty, D-58)"
    - "The `@ac-n` lists inside a `gate.tags-differ` reason are sorted by the numeric part of the tag, and the reason names the three sets in the fixed order scenarios, evidence, verified, so the reason text is identical on every host and in every run (GATE-02 ordering)"
    - "`gate.ac-hash-missing` (error) fires when `frontmatter.ac_hash` is absent, and `gate.ac-changed` (error) fires when it is present and differs by exact string comparison from the hash computed now, its reason naming both the recorded value and the computed one so a change of hash algorithm fails loudly (GATE-03, D-75)"
    - "A non-empty `verified` with `verified_hash` or `verified_commit` absent fails Done with `gate.tick-unbound`; an empty `verified` with both keys absent raises no tick-binding finding, because the set mismatch is what fails such a ticket (GATE-11 empty, D-76)"
    - "`gate.tick-stale-hash` (error) fires when `verified_hash` differs from the hash computed now, and `gate.tick-stale-commit` (error) fires when `verified_commit` does not match the gated commit by the D-81 rule, so a tick never survives a code or acceptance-criteria change (GATE-11)"
    - "Two shas are equal when, compared after lower-casing, the shorter is a prefix of the longer and both are at least 7 hexadecimal characters; a 7-character prefix of a 40-character sha matches, a 6-character value raises `gate.sha-too-short` naming the offending value, and a value differing at the seventh character does not match (GATE-11 adjacency, D-81)"
    - "The order of entries in `verified` never changes any verdict or reason, because the comparison is by set and every emitted tag list is sorted numerically (GATE-11 ordering)"
    - "A missing `git.commit` fails Done with `gate.commit-missing` rather than reporting the check skipped (D-81, D-80 reasoning)"
    - "`gate.stale-review` (error) fires when `verification.md` frontmatter `commit:` does not match the gated commit by the D-81 rule (ROADMAP criterion 2, design.md §5)"
    - "`gate.verification-missing` (error) fires when `snapshot.verifications` has no own entry for the ticket id"
    - "An evidence block whose `result` is `fail` or `blocked` fails Done with `gate.result-not-pass`, naming the tag and the result; a block whose `Result:` is missing or invalid already carries the loader's `load.result-invalid` at `error` in ticket scope, so Done fails with no extra rule (design.md §5, D-58)"
    - "`gateDone` reads `verification.blocks` and never re-scans the Markdown, so duplicate `## @ac-n` headings are resolved once by the loader (D-40, D-42); fixture `verification-edges` ticket `A` produces a Done result whose evidence tag set is exactly the four distinct tags the loader kept"
    - "`packages/core/src/index.ts` exports `gateDone` as a value; the `DONE_RULES` table and the check functions stay unexported (D-55)"
  artifacts:
    - path: "packages/core/src/gate/done.ts"
      provides: "tagsDiffer, noScenarios, acHashMissing, acChanged, tickUnbound, tickStaleHash, tickStaleCommit, commitMissing, shaTooShort, staleReview, verificationMissing, resultNotPass, and shaEqual"
      contains: "export const tagsDiffer"
    - path: "packages/core/src/gate/index.ts"
      provides: "gateDone alongside gateReady, sharing scoped and byFileLineRule"
      contains: "export function gateDone("
    - path: "packages/core/test/fixtures/gate-done/accord/tickets/PASS.md"
      provides: "the ticket that passes Done end to end, carrying ac_hash, verified, verified_hash, verified_commit, and its Verification notes"
      contains: "verified_hash"
    - path: "packages/core/test/__golden__/gate-done.PASS.done.json"
      provides: "the Done end-to-end pass golden"
      contains: "\"verdict\": \"pass\""
  key_links:
    - from: "packages/core/src/gate/done.ts"
      to: "packages/core/src/model/snapshot.ts"
      via: "snapshot.verifications[id].blocks for the evidence set and the Result value; frontmatter.verified, verified_hash, verified_commit for the tick binding; snapshot.git.commit for the gated commit"
      pattern: "verifications"
    - from: "packages/core/src/gate/done.ts"
      to: "packages/core/src/gate/hash.ts"
      via: "acHash over the same scenario list Ready hashed, compared against both ac_hash and verified_hash"
      pattern: "acHash"
    - from: "packages/core/test/gate.test.ts"
      to: "packages/core/test/helpers/fixture.ts"
      via: "the case table layers git facts onto the SnapshotInput readFixture returns, because a fixture on disk cannot carry them"
      pattern: "readFixture"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`, and no attribution trailer is written; the working tree is left dirty for the author to review"
      status: flagged
      verification: unverified
    - statement: "No gate function writes `ac_hash`, `verified_hash`, or `verified_commit` into any file or returns rewritten file text; the gate never binds a tick on the developer's behalf, because a gate that ticks its own work verifies nothing (D-86)"
      status: flagged
      verification: unverified
    - statement: "No flag, option, environment variable, or config key added by this plan skips a Done check or forces a `pass` verdict, and no check reports itself skipped merely because its input is absent (GATE-06, D-80)"
      status: flagged
      verification: unverified
    - statement: "The three-set comparison is never softened to a subset test, a case-insensitive match, or a numeric tolerance; equality of the three sets is exact (GATE-02)"
      status: flagged
      verification: unverified
    - statement: "No Done check swallows an internal error and returns an empty draft list; a check that cannot evaluate emits a finding rather than silence, so a crash never reads as a pass (PITFALLS §9)"
      status: flagged
      verification: unverified
    - statement: "This plan adds no per-tick storage shape and does not change `verified` from a flat string array; the residual D-76 records is accepted, not quietly closed by widening the schema (D-76, deferred)"
      status: flagged
      verification: unverified
---

<objective>
**As a** developer about to send a story to QA, **I want** Done to refuse unless every scenario has an evidence block and a tick, and unless my ticks were made against the acceptance criteria and the commit that are in front of me now, **so that** a tick cannot survive the change it was supposed to be checking.

This is the Done vertical slice: one fixture ticket that passes Done end to end, plus the identity layer that decides whether the three records agree with each other and with the code. 04-03 and 04-04 add rules on top of this slice and must leave the `PASS` ticket passing.

Purpose: ROADMAP criterion 2 (the three-set match, `Result: blocked`, the stale-review commit) and criterion 3 (an AC edit after Ready fails Done) become executable.
Output: `packages/core/src/gate/done.ts`, `gateDone` in the engine and the barrel, and the `gate-done` fixture with nine tickets and their goldens.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-gates/04-CONTEXT.md
@.planning/phases/04-gates/04-PATTERNS.md
@.planning/phases/04-gates/04-01-SUMMARY.md
@packages/core/src/gate/index.ts
@packages/core/src/gate/rules.ts
@packages/core/src/load/verification.ts

<interfaces>
```typescript
// packages/core/src/gate/done.ts
export function shaEqual(a: string, b: string): boolean;   // D-81: case-insensitive prefix match, both >= 7 hex chars
export function isSha(v: string): boolean;                 // >= 7 characters and every character hexadecimal
export const noScenarios: GateRule['check'];
export const tagsDiffer: GateRule['check'];
export const verificationMissing: GateRule['check'];
export const resultNotPass: GateRule['check'];
export const staleReview: GateRule['check'];
export const commitMissing: GateRule['check'];
export const shaTooShort: GateRule['check'];
export const acHashMissing: GateRule['check'];
export const acChanged: GateRule['check'];
export const tickUnbound: GateRule['check'];
export const tickStaleHash: GateRule['check'];
export const tickStaleCommit: GateRule['check'];

// packages/core/src/gate/index.ts
export function gateDone(snapshot: RepoSnapshot, id: string): GateResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end "gate done on a fully verified ticket" — the three-set match and the evidence layer</name>
  <read_first>
    - packages/core/src/gate/index.ts and rules.ts (from 04-01): `scoped`, `byFileLineRule`, the stamping loop, `GateRule`, `DONE_RULES` (still empty)
    - packages/core/src/load/verification.ts (whole file, 66 lines): `EvidenceBlock` construction, the duplicate-by-tag rule, and where `load.result-invalid` comes from — Done must not re-do any of it
    - packages/core/src/model/snapshot.ts lines 44-84: `Verification`, `EvidenceBlock`, `VerificationFrontmatter` (`commit` is always a string)
    - packages/core/src/lint/ticket.ts lines 138-160 or wherever `planTagsDiffer` lives: the `plan lacks nothing; scenarios lack @ac-2` reason shape this task mirrors
    - packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1/verification.md and packages/core/templates/verification.md: the shipped evidence-block shape
    - packages/core/test/fixtures/verification-edges/accord/tickets/A.md and A/verification.md: the duplicate `## @ac-1`, `Result: PASS`, missing `Result:`, and non-block `## Notes` edges this task reuses instead of building a new fixture
    - packages/core/test/fixtures/lint-report/reports/junit.xml and accord/config.yml: the JUnit report and `tests.report` shape the `gate-done` fixture copies
    - packages/core/test/helpers/fixture.ts lines 10-19 and 40-51: `readFixture` derives `tree` from files actually on disk, and `variants` rebuilds inputs from `files` and `tree` only — neither can carry `git`
  </read_first>
  <files>packages/core/src/gate/done.ts, packages/core/src/gate/rules.ts, packages/core/src/gate/index.ts, packages/core/src/index.ts, packages/core/test/gate.test.ts, packages/core/test/fixtures/gate-done/accord/config.yml, packages/core/test/fixtures/gate-done/accord/tickets/PASS.md, packages/core/test/fixtures/gate-done/accord/tickets/PASS/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/SETS.md, packages/core/test/fixtures/gate-done/accord/tickets/SETS/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/BLOCKED.md, packages/core/test/fixtures/gate-done/accord/tickets/BLOCKED/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/NOVERIF.md, packages/core/test/fixtures/gate-done/accord/tickets/EMPTY.md, packages/core/test/fixtures/gate-done/reports/junit.xml, packages/core/test/fixtures/gate-done/src/auth/login.ts, packages/core/test/fixtures/gate-done/test/login.spec.ts, packages/core/test/__golden__/gate-done.PASS.done.json, packages/core/test/__golden__/gate-done.PASS.ready.json, packages/core/test/__golden__/gate-done.SETS.done.json, packages/core/test/__golden__/gate-done.BLOCKED.done.json, packages/core/test/__golden__/gate-done.NOVERIF.done.json, packages/core/test/__golden__/gate-done.EMPTY.done.json, packages/core/test/__golden__/gate-done.snapshot.json, packages/core/test/__golden__/gate-done.lint.json</files>
  <behavior>
    - Test 1 (`gateDone` on `gate-done`/`PASS` with the table's `git` facts): `gate` is `done`, `ticket` is `PASS`, `verdict` is `pass`, no finding has `level: 'error'`, and `acHash` equals the ticket's `ac_hash`.
    - Test 2 (`gateReady` on the same ticket): also `pass`, so the fixture is a ticket that legitimately reached Done, not one that skipped Ready.
    - Test 3 (`tagsDiffer` on `SETS`): exactly one `gate.tags-differ` error whose reason names, in the fixed order, what scenarios lack, what evidence lacks, and what verified lacks, with each list sorted numerically and the literal `nothing` where a set lacks none.
    - Test 4 (three-set adjacency and ordering): with all three sets equal there is no finding; reversing the order of `verified` in a ticket that otherwise passes changes no byte of the result; a tag present in scenarios and evidence but absent from `verified` is named against `verified` only.
    - Test 5 (`EMPTY`, an `epic` with no tagged scenario): exactly one `gate.no-scenarios` error, verdict `fail`, so three empty sets never read as equal.
    - Test 6 (`BLOCKED`): one `gate.result-not-pass` error naming the tag and the value `blocked`, verdict `fail`. A block recording `fail` produces the same rule with `fail` in the reason.
    - Test 7 (`NOVERIF`): `gate.verification-missing` and `gate.ac-hash-missing` both present at `error`, verdict `fail`.
    - Test 8 (`verification-edges`/`A`): the evidence tag set is exactly the four distinct tags the loader kept from the duplicated headings, the block with the missing `Result:` contributes the loader's `load.result-invalid` rather than a new gate rule, and the `blocked` block contributes `gate.result-not-pass`.
    - Test 9 (purity and determinism): `gateDone` called twice returns identical `stableJson`, the snapshot deep-equals a `structuredClone` taken before the call, and `findings` is already sorted by `byFileLineRule`.
    - Test 10 (the `maintain` downgrade reaches Done, GATE-07, D-88): the four synthetic `error`-level rows from the 04-01 downgrade test, injected as ticket-scoped lint findings, come back at `level: 'warning'` from `gateDone` on a `maintain` snapshot and unchanged from `gateDone` on `build`; the `lint.id-mismatch` control stays `error` on both. This is asserted through `gateDone`, not through `downgradeMaintain` alone, because the defect it guards against is the shared body being written without the call.
  </behavior>
  <action>
**File count, accepted deliberately.** Task 1 lists 24 paths. Twelve are goldens produced by one scoped `vitest -u` each and reviewed as diffs, not authored. Nine are fixture ticket and verification files of roughly twenty lines apiece, each differing from `PASS` in one respect so its golden isolates one reason. The hand-written source surface is four files: `done.ts`, `rules.ts`, `index.ts`, and the barrel. Splitting the nine-ticket fixture build away from the `done.ts` rule work was considered and rejected: the expected findings for each ticket cannot be stated without the rules, and the fixture-only plan would have nothing to verify itself against. Keep them together and review by diff.


**Fixture `gate-done`.** `accord/config.yml` with `accord: "0.1.0"`, `profile: build`, `tracker.adapter: none`, `design.tokens: ""`, the usual `roles` and `runtimes`, and `tests: { report: reports/junit.xml }`. Add `reports/junit.xml` as a single `<testsuite>` with two passing `<testcase>` elements whose ids resolve to `test/login.spec.ts#ok1` and `test/login.spec.ts#ok2` under the Phase 3 `classname#name` rule (spaces as `-`) — read `packages/core/src/load/junit.ts` if the id derivation is not obvious from the `lint-report` fixture. Add `src/auth/login.ts` and `test/login.spec.ts` as one-line real files, because `readFixture` derives `tree` from what is on disk and 04-03 resolves evidence references against that tree.

`PASS.md`: `type: story`, `status: open`, `ui: false`, two scenarios `@ac-1 @test:test/login.spec.ts#ok1` and `@ac-2 @test:test/login.spec.ts#ok2`, the five D-07 headings — with a non-empty `## Intent` and at least one `## Requirements` line that `classifyEars` classifies, so 04-01's `gate.intent-empty` and `gate.ears-missing` stay silent and `gate-done.PASS.ready.json` really is `pass` — plus a final `## Verification notes` section with one `### @ac-1` and one `### @ac-2` block. Each note names `src/auth/login.ts` and then says something in Vietnamese that is not the scenario text — 04-03 will enforce that, and writing it correctly now keeps this ticket passing when it does. Frontmatter carries `verified: [ac-1, ac-2]`, `ac_hash`, `verified_hash`, and `verified_commit: "1234567"`. Obtain the two hash values by writing a placeholder, running the gate once, reading the `acHash` the result reports, and pasting that exact value into both `ac_hash` and `verified_hash`; do not hand-compute it. `PASS/verification.md`: frontmatter `ticket: PASS`, `commit: 1234567`, a `reviewed_on` date, and two `## @ac-n` blocks each with `Result: pass` and an `Evidence:` line citing `test/login.spec.ts`.

`SETS.md` + `SETS/verification.md`: scenarios `@ac-1` and `@ac-2`; evidence blocks for `@ac-1` only; `verified: [ac-1, ac-3]`. `BLOCKED.md` + its verification: two scenarios, two blocks, the second recording `Result: blocked`, everything else complete so `gate.result-not-pass` is the only new reason. `NOVERIF.md`: one scenario, no `NOVERIF/` folder at all, no `ac_hash`. `EMPTY.md`: `type: epic`, the three epic headings, no `## Acceptance criteria` and no scenario. Every ticket except `EMPTY` is `type: story`.

**`packages/core/src/gate/done.ts`** — header comment naming GATE-02, GATE-03, GATE-11, D-76, D-81, D-87, and `docs/design.md` §5. Module-private helpers: `tagsOf(list)` returning a sorted-by-number array of distinct `ac-n` strings; `fmt(tags)` rendering `nothing` for an empty list and otherwise the `@ac-n` tokens space-joined in numeric order; `scenarioTags`, `evidenceTags`, `tickTags` reading `snapshot.tickets[id].scenarios`, `snapshot.verifications[id].blocks`, and `frontmatter.verified` respectively. Export `isSha(v)` as at least 7 characters and every character in `0-9a-fA-F`, and `shaEqual(a, b)` as: both `isSha`, then lower-case both, then the shorter is a prefix of the longer.

Checks in this task, each `export const <name>: GateRule['check'] = (snapshot, id) => {...}`, each returning `[]` when the ticket is absent:
- `noScenarios`: the scenario tag set is empty → one draft on `accord/tickets/<id>.md` at the `## Acceptance criteria` heading line when that section exists, otherwise with no line, reason saying the ticket has no scenario carrying an `@ac-n` tag and so has nothing Done can be claimed about.
- `tagsDiffer`: build the union of the three sets; when any set differs from the union, one draft on `accord/tickets/<id>.md` at the `## Acceptance criteria` heading line, reason `scenarios lack <fmt>; evidence lacks <fmt>; verified lacks <fmt>` in that fixed order. Return `[]` when the union is empty, because `noScenarios` already owns that case.
- `verificationMissing`: `id` is not an own key of `snapshot.verifications` → one draft on `accord/tickets/<id>/verification.md` naming the expected path.
- `resultNotPass`: for each block whose `result` is `fail` or `blocked`, one draft on the verification file at the block's `line`, reason naming the tag and the recorded value.

**`rules.ts`** — add the four rows to `DONE_RULES`, all `error`, both profiles: `gate.no-scenarios`, `gate.tags-differ`, `gate.verification-missing`, `gate.result-not-pass`.

**`index.ts`** — add `gateDone(snapshot, id)` next to `gateReady`, sharing `scoped`, `byFileLineRule`, and the stamping loop; factor the shared body into one module-private function taking the gate name and the rule table rather than copying it. Move the single `downgradeMaintain` call into that shared body, so `gateDone` inherits the `maintain` matrix rather than being written without it: GATE-07 is about the profile matrix as a whole, not about Ready alone, and in v0.1 nothing differs because all four ids are already `warning` — the moment GATE-12 flips `lint.token-hardcoded` to `error`, one hard-coded colour in a prototype would block Done on `maintain`, which is exactly the "cries wolf" failure D-88 cites `PITFALLS.md` §13 to avoid. No golden can catch that omission today, so the assertion below is the only guard. After this edit the file must still hold exactly one call to it in code. The hygiene promotion stays Ready-only and stays in `gateReady`, above the shared call. Done therefore includes every ticket-scoped lint finding at the level lint stamped and then downgraded by profile, with no hygiene promotion and no design rule, plus the `DONE_RULES` rows for the profile. Compute `acHash` the same way and spread it in when defined. Add `export { gateDone }` to `packages/core/src/index.ts`.

**`gate.test.ts`** — give the `CASES` row type an optional `git: { commit: string; authors: Record<string, string> }` field and build every case input as `readFixture(row.fixture)` spread with `git` when the row supplies it; apply the same spread after `variants()` if a variant assertion is added, because `variants` rebuilds inputs from `files` and `tree` alone. Declare one shared constant `GIT` whose `commit` is a 40-character lowercase hex string beginning `1234567` and whose `authors` is `{}` for now (04-04 fills it). Add the rows `gate-done.PASS.done`, `gate-done.PASS.ready`, `gate-done.SETS.done`, `gate-done.BLOCKED.done`, `gate-done.NOVERIF.done`, `gate-done.EMPTY.done`, and `verification-edges.A.done` (the last with the same `GIT`), then the pinned `describe` blocks for behaviours 1 to 10.

Extend the 04-01 `downgradeMaintain` `describe` rather than writing a second one: keep its synthetic-row assertions and add behaviour 10, which drives the same four rows through `gateDone`. That is the only way the shared-body placement is observable, because every id in `MAINTAIN_DOWNGRADE` is already `warning` in the real lint table.

Generate the new goldens one at a time with a scoped `-u`, read each diff, rerun without `-u`. Regenerate `gate-done.snapshot.json` and `gate-done.lint.json`, which the directory-scanning snapshot and lint suites create automatically for the new fixture. Confirm no golden belonging to an existing fixture changed. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core gate &amp;&amp; npm test -- --project core lint &amp;&amp; npm test -- --project core snapshot</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched; or `gate-done.PASS.done.json` containing a finding whose `level` is `error`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/gate/done.ts` contains `export const tagsDiffer`, `export const noScenarios`, `export const verificationMissing`, `export const resultNotPass`, `export function shaEqual(`, `export function isSha(`
    - `grep -cE "'gate\.(no-scenarios|tags-differ|verification-missing|result-not-pass)'" packages/core/src/gate/rules.ts` prints 4
    - `packages/core/test/__golden__/gate-done.PASS.done.json` contains `"verdict": "pass"` and `fnv1a64:`; `grep -c '"level": "error"' packages/core/test/__golden__/gate-done.PASS.done.json` prints 0
    - `packages/core/test/__golden__/gate-done.PASS.ready.json` contains `"verdict": "pass"`
    - `packages/core/test/__golden__/gate-done.SETS.done.json` contains `gate.tags-differ` and contains the literal `nothing`
    - `packages/core/test/__golden__/gate-done.EMPTY.done.json` contains `gate.no-scenarios`
    - `packages/core/test/__golden__/gate-done.BLOCKED.done.json` contains `gate.result-not-pass` and `blocked`
    - `packages/core/test/__golden__/verification-edges.A.done.json` contains `load.result-invalid` and `gate.result-not-pass`
    - `packages/core/src/index.ts` contains `gateDone`
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' packages/core/src/gate/index.ts | grep -cF 'downgradeMaintain('` prints 1 — exactly one live **call site**, and it is inside the shared body, so both gates inherit the `maintain` matrix. The trailing `(` is what keeps this reachable: the file must also import the symbol from `rules.ts`, and that import line carries the bare name with no parenthesis, so counting bare mentions would read 2 on a correct file. Comments are filtered first, or a header comment restating this rule would add a third
    - `grep -c "maintain downgrade reaches Done" packages/core/test/gate.test.ts` prints 1 — behaviour 10 exists under that exact `it` name, inside the 04-01 `downgradeMaintain` describe
    - `npm test -- --project core gate` reports 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>One fixture ticket goes through Done end to end and passes; the three-set match, the empty-set hole, `Result: blocked`, and a missing review file each produce a named reason; and the `maintain` downgrade reaches Done from the same single call site Ready uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The identity layer — AC-hash staleness, the tick binding, and the D-81 sha comparison</name>
  <reversibility rating="costly">D-76: `verified_hash` and `verified_commit` become the recorded shape of a tick the moment a real ticket carries one, and D-75's `ac_hash` value is compared against them, so changing either the key names or the comparison rule invalidates every recorded tick.</reversibility>
  <read_first>
    - packages/core/src/gate/done.ts and rules.ts (Task 1) and packages/core/src/gate/hash.ts (04-01)
    - packages/core/src/model/snapshot.ts: the `verified_hash`, `verified_commit`, and `git` fields added in 04-01 Task 3, and `VerificationFrontmatter.commit`
    - packages/core/schemas/ticket.schema.json: the `verified_commit` `minLength: 7` and hex `pattern`, which is why a too-short tick sha cannot be built as a fixture and is unit-tested on `shaEqual` instead
    - .planning/phases/04-gates/04-CONTEXT.md, the "Tick binding (GATE-11)" and "Host-supplied facts" sections: D-76's exact failure conditions and D-81's three sources
    - packages/core/test/gate.test.ts (Task 1): the `CASES` table, the `GIT` constant, and the golden loop this task extends
  </read_first>
  <files>packages/core/src/gate/done.ts, packages/core/src/gate/rules.ts, packages/core/test/gate.test.ts, packages/core/test/fixtures/gate-done/accord/tickets/STALE.md, packages/core/test/fixtures/gate-done/accord/tickets/STALE/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/TICKS.md, packages/core/test/fixtures/gate-done/accord/tickets/TICKS/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/COMMIT.md, packages/core/test/fixtures/gate-done/accord/tickets/COMMIT/verification.md, packages/core/test/fixtures/gate-done/accord/tickets/REVIEW.md, packages/core/test/fixtures/gate-done/accord/tickets/REVIEW/verification.md, packages/core/test/__golden__/gate-done.STALE.done.json, packages/core/test/__golden__/gate-done.TICKS.done.json, packages/core/test/__golden__/gate-done.COMMIT.done.json, packages/core/test/__golden__/gate-done.REVIEW.done.json, packages/core/test/__golden__/gate-done.snapshot.json, packages/core/test/__golden__/gate-done.lint.json</files>
  <behavior>
    - Test 1 (`shaEqual`, D-81, unit, no fixture): `1234567` and a 40-character sha beginning `1234567` are equal; `1234567` and `1234568...` are not; `ABCDEF0` and `abcdef0123` are equal (case-insensitive); `123456` against anything is not equal because it fails `isSha`; a 7-character value containing `g` is not a sha; two identical 40-character shas are equal; the relation is symmetric for every pair asserted.
    - Test 2 (`STALE`, GATE-03): `ac_hash` recorded but not equal to the hash computed now yields `gate.ac-changed` at `error`, its reason containing both the recorded value and the computed value; `verified_hash` equal to that same stale value additionally yields `gate.tick-stale-hash`; verdict `fail`.
    - Test 3 (`TICKS`, GATE-11): `verified: [ac-1, ac-2]` with `verified_hash` and `verified_commit` both absent yields exactly one `gate.tick-unbound` error naming both missing keys; a ticket with an empty `verified` and both keys absent yields no tick-binding finding at all.
    - Test 4 (`COMMIT`, GATE-11): `verified_commit` that is a valid sha but does not match `git.commit` under `shaEqual` yields `gate.tick-stale-commit`, its reason naming both values; verdict `fail`.
    - Test 5 (`REVIEW`, ROADMAP criterion 2): `verification.md` frontmatter `commit:` not matching `git.commit` yields `gate.stale-review` on the verification file; the same ticket run with a `git.commit` that does match yields no such finding.
    - Test 6 (`gate.commit-missing`): the same `PASS` ticket run through a case row that supplies no `git` yields `gate.commit-missing` at `error` and verdict `fail`, and does not report any check as skipped.
    - Test 7 (`gate.sha-too-short`): a case row supplying `git: { commit: 'abc123', authors: {} }` yields `gate.sha-too-short` naming `abc123`, and does not also yield a spurious commit-mismatch finding for the same value.
    - Test 8 (tick order and set semantics): reversing the order of `verified` on the `PASS` ticket, via an in-test edit to the frontmatter text before `loadSnapshot`, produces a byte-identical `stableJson` result.
  </behavior>
  <action>
Write the `shaEqual` and `isSha` unit tests first (Test 1), then the four fixture tickets, then the checks.

**Fixture tickets**, each a complete `type: story` copy of `PASS` differing in exactly one respect so the golden isolates one reason:
- `STALE`: `ac_hash` and `verified_hash` both set to a syntactically valid but wrong value (`fnv1a64:` followed by 16 hex digits that are not the computed hash).
- `TICKS`: `verified: [ac-1, ac-2]` with no `verified_hash` and no `verified_commit` key.
- `COMMIT`: `verified_commit: "9999999"` while the case row's `git.commit` begins `1234567`.
- `REVIEW`: `REVIEW/verification.md` frontmatter `commit: 9999999` while the ticket's `verified_commit` and the case row's `git.commit` agree.
For each, obtain the correct `ac_hash`/`verified_hash` the same way Task 1 did — run the gate once and paste the reported `acHash` — except on `STALE`, where the point is that they are wrong.

**Checks added to `done.ts`**, each returning `[]` when the ticket or its frontmatter is absent:
- `commitMissing`: `snapshot.git?.commit` is undefined → one draft on `accord/config.yml`... no: on `accord/tickets/<id>.md` with no line, reason saying the host supplied no commit for the gated tree and Done cannot bind a tick without one. Do not report it as skipped.
- `shaTooShort`: for each of `snapshot.git?.commit`, `frontmatter.verified_commit`, and `snapshot.verifications[id]?.frontmatter?.commit` that is present and fails `isSha`, one draft naming the value and where it came from, on the file that carries it (the config-supplied commit is reported on the ticket file with no line). Emit at most one draft per source.
- `acHashMissing`: `frontmatter.ac_hash` is undefined and the scenario tag set is non-empty → one draft on the ticket file with pointer `/ac_hash`, reason saying Ready has not recorded a hash.
- `acChanged`: `frontmatter.ac_hash` is defined and not exactly equal to the computed hash → one draft with pointer `/ac_hash`, reason naming the recorded value and the computed value so a change of algorithm prefix fails loudly.
- `tickUnbound`: `frontmatter.verified` is non-empty and either `verified_hash` or `verified_commit` is undefined → one draft with pointer `/verified`, reason naming which of the two keys are missing.
- `tickStaleHash`: `verified_hash` is defined and not exactly equal to the computed hash → one draft with pointer `/verified_hash`, reason naming both values.
- `tickStaleCommit`: `verified_commit` is defined, passes `isSha`, `snapshot.git?.commit` is defined and passes `isSha`, and `shaEqual` is false → one draft with pointer `/verified_commit`, reason naming both values. Guard on `isSha` for both so a too-short value produces only `gate.sha-too-short`, never two reasons for one typo.
- `staleReview`: the verification frontmatter `commit` is defined, both it and `snapshot.git?.commit` pass `isSha`, and `shaEqual` is false → one draft on `accord/tickets/<id>/verification.md` with pointer `/commit`, reason naming both values and saying the review was written against a different commit.

**`rules.ts`** — add eight rows to `DONE_RULES`, all `error`, both profiles: `gate.commit-missing`, `gate.sha-too-short`, `gate.ac-hash-missing`, `gate.ac-changed`, `gate.tick-unbound`, `gate.tick-stale-hash`, `gate.tick-stale-commit`, `gate.stale-review`.

**`gate.test.ts`** — add the case rows `gate-done.STALE.done`, `gate-done.TICKS.done`, `gate-done.COMMIT.done`, `gate-done.REVIEW.done`, plus two rows reusing the `PASS` ticket with a different `git`: one with no `git` at all and one with a six-character commit. Add the pinned assertions for behaviours 1 to 8.

Regenerate the affected goldens with scoped `-u`, read each diff, rerun without `-u`, and refresh `gate-done.snapshot.json` and `gate-done.lint.json` for the four new tickets. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core gate &amp;&amp; npm test -- --project core lint &amp;&amp; npm test -- --project core snapshot &amp;&amp; npm test -- --project core schemas</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched; or `gate-done.PASS.done.json` gaining a finding whose `level` is `error`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'gate\.(commit-missing|sha-too-short|ac-hash-missing|ac-changed|tick-unbound|tick-stale-hash|tick-stale-commit|stale-review)'" packages/core/src/gate/rules.ts` prints 8
    - `packages/core/test/__golden__/gate-done.STALE.done.json` contains `gate.ac-changed` and `gate.tick-stale-hash`
    - `packages/core/test/__golden__/gate-done.TICKS.done.json` contains `gate.tick-unbound`; `grep -c "gate.tick-stale" packages/core/test/__golden__/gate-done.TICKS.done.json` prints 0
    - `packages/core/test/__golden__/gate-done.COMMIT.done.json` contains `gate.tick-stale-commit`
    - `packages/core/test/__golden__/gate-done.REVIEW.done.json` contains `gate.stale-review`
    - `grep -c '"level": "error"' packages/core/test/__golden__/gate-done.PASS.done.json` still prints 0
    - `packages/core/test/gate.test.ts` contains `shaEqual` and `gate.commit-missing` and `gate.sha-too-short`
    - `npm test -- --project core gate` reports 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>An acceptance-criteria edit after Ready, a tick made against a different hash or a different commit, a review of an older commit, and a missing host commit each fail Done with their own reason, and the D-81 prefix rule is pinned on both sides.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 04-02)

**New files:** `packages/core/src/gate/done.ts`.

**New exported values:** `shaEqual`, `isSha`, `noScenarios`, `tagsDiffer`, `verificationMissing`, `resultNotPass`, `commitMissing`, `shaTooShort`, `acHashMissing`, `acChanged`, `tickUnbound`, `tickStaleHash`, `tickStaleCommit`, `staleReview`, `gateDone`.

**Barrel additions (`packages/core/src/index.ts`):** value `gateDone`.

**New rule ids:** `gate.no-scenarios`, `gate.tags-differ`, `gate.verification-missing`, `gate.result-not-pass`, `gate.commit-missing`, `gate.sha-too-short`, `gate.ac-hash-missing`, `gate.ac-changed`, `gate.tick-unbound`, `gate.tick-stale-hash`, `gate.tick-stale-commit`, `gate.stale-review`.

**Module-internal symbols in `done.ts`:** `tagsOf`, `fmt`, `scenarioTags`, `evidenceTags`, `tickTags`.

**New fixture:** `gate-done` — tickets `PASS`, `SETS`, `STALE`, `TICKS`, `COMMIT`, `BLOCKED`, `REVIEW`, `NOVERIF`, `EMPTY`; verification folders for all but `NOVERIF` and `EMPTY`; `reports/junit.xml`; `src/auth/login.ts`; `test/login.spec.ts`.

**New goldens:** `gate-done.{PASS,SETS,STALE,TICKS,COMMIT,BLOCKED,REVIEW,NOVERIF,EMPTY}.done.json`, `gate-done.PASS.ready.json`, `verification-edges.A.done.json`, `gate-done.{snapshot,lint}.json`.

**New test-file symbols:** the `CASES` row `git` field and the shared `GIT` constant in `packages/core/test/gate.test.ts`.

**New CLI flags, subcommands, config keys:** none.
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

Seven of the 26 probe-surfaced edge items belong to this plan's requirements. Count for this plan: 6 authored into `must_haves.truths` (GATE-02 adjacency, empty, ordering; GATE-11 adjacency, empty, ordering) + 1 surfaced here (GATE-03 unclassified) = 7.

| Source | Item | Disposition |
|---|---|---|
| Edge probe, GATE-03 (unclassified) | The probe could not classify GATE-03, so no edge predicate was derived for it | Unresolved. GATE-03's edges are covered instead by the `STALE` ticket, the `gate.ac-hash-missing` case on `NOVERIF`, and the D-77 stability tests in 04-01 |
| Open question 2 (pattern mapper), git facts in fixtures | `readFixture` derives `tree` from files on disk and cannot produce `git.commit` or `git.authors` at all | **Resolved by carrying `git` on the `CASES` row in `gate.test.ts` and spreading it onto the `SnapshotInput` after `readFixture` (and after `variants`, which rebuilds inputs from `files` and `tree` only).** Alternative rejected: a `git.json` file inside the fixture, because `readFixture` would then load it into `snapshot.files` and it would appear in the D-54 snapshot golden |
| Open question 2 (pattern mapper), `test/login.spec.ts` in the tree | The `gate-done` fixture contains real `src/auth/login.ts` and `test/login.spec.ts` files on disk | Chosen so the tree genuinely holds them for the 04-03 reference resolution, instead of editing `valid-build` and moving four Phase 1-3 goldens |
| GATE-02 empty set (no decision covers it) | Three empty sets are equal, which would let a ticket with no scenarios pass Done. Closed with a dedicated `gate.no-scenarios` error that fires on any ticket type | Chosen because a gate bypass is not a place to be economical. A `story` with no scenarios therefore carries two findings, `lint.no-scenarios` and `gate.no-scenarios`; D-58's precedent is to keep both rather than merge. Alternative: rely on `lint.no-scenarios` alone, which leaves an `epic` passing Done vacuously |
| D-89 scope, applied to Done (not decided) | Done includes every ticket-scoped lint finding at the level lint stamped, with no hygiene promotion and no design-reference rule; it does **not** re-run Ready | Chosen. A schema-invalid ticket therefore still fails Done through the loader errors already in scope, with no extra rule. Alternative: make Done a superset of Ready, which would restate every Ready reason on a ticket the developer already got past Ready |
| GATE-02 reason text (CONTEXT discretion) | The mismatch reason is one finding reading `scenarios lack <list>; evidence lacks <list>; verified lacks <list>`, mirroring the existing `lint.plan-tags-differ` shape, with `nothing` for an empty list | Chosen for consistency with a reason the project already ships. Alternative: one finding per missing tag per set, which reads better in a long list but multiplies the golden |
| D-81 applied to a too-short value | A value that fails `isSha` produces `gate.sha-too-short` only, never an additional mismatch finding for the same typo | Chosen so one bad character produces one reason. This is why `tickStaleCommit` and `staleReview` both guard on `isSha` before comparing |
| Schema vs fixture (`verified_commit`) | A too-short `verified_commit` cannot be built as a fixture, because `ticket.schema.json` enforces `minLength: 7` and the loader would raise `schema.minLength` first | Accepted. The `gate.sha-too-short` path is therefore triggered through the host-supplied `git.commit`, and the D-81 prefix rule is pinned directly on `shaEqual` in a unit test |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `verification.md` content → the evidence tag set and the `Result:` reading | Written by an agent context, then committed by a human; the file the Fresh-context layer exists to check |
| ticket frontmatter `verified`, `verified_hash`, `verified_commit` → the tick binding | Hand-written by the developer whose work is being gated |
| host-supplied `snapshot.git` → every commit comparison | Supplied by the CLI in 04-04; core cannot verify it and must fail closed when it is absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Repudiation | a developer relabelling an older tick by editing one shared `verified_hash` value instead of overwriting a per-tick entry | medium | accept | Recorded and accepted in D-76 as inside the ceiling `docs/design.md` §5 states for itself; closing it needs a `verified` shape change and a human hand-typing 16 hex characters per scenario with no command to help |
| T-04-08 | Spoofing | a short or truncated commit value crafted so the prefix rule matches a different commit | high | mitigate | D-81 requires both values to be at least 7 hexadecimal characters and compares by prefix in both directions; anything shorter raises `gate.sha-too-short` and never compares equal (Task 2 Test 1 and Test 7) |
| T-04-09 | Tampering | deleting `git` from the host input, or running the gate through a host that supplies none, to dodge every commit check | high | mitigate | A missing `git.commit` raises `gate.commit-missing` at `error` rather than reporting the check skipped, by the same no-bypass reasoning as D-80 (Task 2 Test 6) |
| T-04-10 | Tampering | duplicate `## @ac-n` headings in `verification.md` used to make one tag appear satisfied twice or to hide a `Result: fail` behind an earlier `Result: pass` | medium | mitigate | Done reads `verification.blocks`, which the loader already de-duplicates by tag with the first block winning and a duplicate-heading finding raised (D-40, D-42); Task 1 Test 8 pins this on the `verification-edges` fixture |
| T-04-11 | Denial of service | a `verification.md` with very many `## @ac-n` blocks, or a `verified` list with very many entries | low | mitigate | Every comparison is a single linear pass building small arrays of `ac-n` strings; no nested scan over blocks times scenarios is introduced |
| T-04-12 | Elevation of privilege | a `Result:` value cased or spelled differently (`PASS`, `Passed`) accepted as a pass | high | mitigate | The loader accepts only lowercase `pass`, `fail`, `blocked` and raises `load.result-invalid` otherwise (Phase 2, `load/verification.ts`); that finding is an `error` in ticket scope, so Done fails with no new rule |
| T-04-SC | Tampering | npm, pip, or cargo installs | high | mitigate | No package is added by this plan; `package.json` dependency blocks are untouched |
</threat_model>

<verification>
- Task 1's `gate-done.PASS.done.json` is the phase's end-to-end proof and must keep zero `error`-level findings through 04-03 and 04-04; every later verify command re-checks it.
- ROADMAP criterion 2 is proved by `SETS`, `BLOCKED`, and `REVIEW`; criterion 3 by `STALE`.
- The `shaEqual` unit test is the only place the D-81 rule is pinned in both directions, because the schema prevents a too-short `verified_commit` fixture.
</verification>

<success_criteria>
- `gateDone` is exported and one fixture ticket passes Done end to end with a real hash and a real commit binding.
- Every Done failure named in ROADMAP criterion 2 and criterion 3 has a fixture ticket, a golden, and a rule id.
- Nothing committed; the working tree is left for the author to review.
</success_criteria>

<output>
Create `.planning/phases/04-gates/04-02-SUMMARY.md` when done. Do not commit.
</output>
