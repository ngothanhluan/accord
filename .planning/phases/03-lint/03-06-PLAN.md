---
phase: 03-lint
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - packages/core/src/lint/tokens.ts
  - packages/core/src/lint/rules.ts
  - packages/core/test/tokens.test.ts
  - packages/core/test/lint.test.ts
  - packages/core/test/snapshot.test.ts
  - packages/cli/test/load.test.ts
  - packages/core/test/fixtures/lint-tokens/accord/config.yml
  - packages/core/test/fixtures/lint-tokens/accord/assets/PROTO-1/prototype.html
  - packages/core/test/fixtures/lint-tokens/src/tokens.css
  - packages/core/test/fixtures/lint-no-tokens/accord/config.yml
  - packages/core/test/fixtures/lint-no-tokens/accord/assets/A/prototype.html
  - packages/core/test/fixtures/lint-no-tokens/accord/assets/B/prototype.html
  - packages/core/test/fixtures/lint-no-tokens/accord/assets/C/prototype.html
  - packages/core/test/fixtures/lint-no-tokens/accord/assets/D/prototype.html
  - packages/core/test/fixtures/lint-no-tokens/src/base.css
  - packages/core/test/fixtures/valid-build/accord/assets/LOGIN-1/prototype.html
  - packages/core/test/fixtures/valid-build/src/styles/tokens.css
  - packages/core/test/__golden__/lint-tokens.snapshot.json
  - packages/core/test/__golden__/lint-tokens.lint.json
  - packages/core/test/__golden__/lint-no-tokens.snapshot.json
  - packages/core/test/__golden__/lint-no-tokens.lint.json
  - packages/core/test/__golden__/lint-missing-files.lint.json
  - packages/core/test/__golden__/valid-build.snapshot.json
  - packages/core/test/__golden__/valid-build.lint.json
  - packages/core/test/__golden__/verification-edges.lint.json
autonomous: true
requirements: [LINT-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`tokenNames(css)` returns every `--name` declared at any nesting (plain `:root`, `@theme`, `@theme inline`, attribute blocks) after CSS comments are removed, skipping wildcard names and `initial` values; the research sample yields exactly `--acme-canvas`, `--color-canvas`, `--color-primary`, `--spacing` (D-66, Tailwind v4 extraction)"
    - "`scanPrototype(html, known)` reports a literal colour on a colour property, a literal length on a spacing property, and `var(--x)` with `--x` not in `known` (that reason ends `; tokens are read from config.design.tokens only`, RESEARCH Open Question 6), across `<style>` blocks, `style` attributes in both quote styles, and arbitrary-value utility classes with variants and opacity modifiers, each at the line of the offending token; the research prototype yields exactly its eight findings, and the listed non-findings stay silent (D-66)"
    - "HTML comments and `<script>` blocks are blanked before scanning; a hex colour in the header comment is never reported; the exemptions `transparent`, `currentColor`, `inherit`, `0`, `1px`, `100%` are case-insensitive (D-66)"
    - "`lint.token-hardcoded` is a warning on the prototype file with a line, for every `accord/assets/<id>/prototype.html` in `snapshot.files`, whether or not `tickets/<id>.md` exists or is `ui: true`; it never errors (D-66, D-68, criterion 4)"
    - "`lint.tokens-missing` (warning, `accord/config.yml`, pointer `/design/tokens`) fires when `design.tokens` is non-empty and its key is absent from `snapshot.files`, its reason says tokens are read from config.design.tokens only, and the allowlist check is skipped for that snapshot (D-67, RESEARCH Open Question 6)"
    - "`lint.prototype-derivation` (warning, prototype file) fires when `design.tokens` is empty and the first comment has no `Derived from:` line (line 1), or its list is empty or only a `<...>` placeholder (that line), or a listed path is not in `snapshot.tree` (that line, naming the path); a header listing an existing path is silent (D-67, RESEARCH A4)"
    - "`valid-build` gains a token-clean prototype and lints with no `lint.token-hardcoded`; its snapshot golden, tree pin, CLI file-list pin, and the 03-01 `snapshot.files` pin in `lint.test.ts` include `accord/assets/LOGIN-1/prototype.html`, the last with the two-token `tokens.css` text (D-65, Pitfall 11)"
    - "The Phase 2 `verification-edges` fixture (empty `design.tokens`, a prototype with no header comment) records `lint.prototype-derivation` at line 1 of `accord/assets/A/prototype.html` in its lint golden; no other existing golden changes in this plan"
  artifacts:
    - path: "packages/core/src/lint/tokens.ts"
      provides: "tokenNames, offending, scanPrototype, derivedFrom, and the three rule checks"
      contains: "export function scanPrototype("
    - path: "packages/core/test/tokens.test.ts"
      provides: "extraction, value classification, eight-finding prototype, header parsing"
      min_lines: 60
    - path: "packages/core/test/__golden__/lint-tokens.lint.json"
      provides: "the eight research findings as a golden"
      contains: "lint.token-hardcoded"
  key_links:
    - from: "packages/core/src/lint/tokens.ts"
      to: "packages/core/src/model/snapshot.ts"
      via: "snapshot.files keys matching assets/<id>/prototype.html; snapshot.files[design.tokens]; snapshot.tree for Derived from paths"
      pattern: "snapshot.tree"
    - from: "packages/core/src/lint/rules.ts"
      to: "packages/core/src/lint/tokens.ts"
      via: "three rows, all warning"
      pattern: "lint.token-hardcoded"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; no attribution trailer is written"
      status: flagged
      verification: unverified
    - statement: "No CSS, HTML, or utility-class library is added; `tokens.ts` uses `String`, `RegExp`, `Set`, and arrays only, and imports nothing from `node:` (D-66, PITFALLS §13)"
      status: flagged
      verification: unverified
    - statement: "`lint.token-hardcoded` is never an error in v0.1; no task promotes it (D-66, GATE-12 deferred)"
      status: flagged
      verification: unverified
    - statement: "`tokens.ts` never normalises BOM or CRLF itself and never imports `load/frontmatter.ts`; text arrives normalised from the loader (D-65)"
      status: flagged
      verification: unverified
    - statement: "No shipped reason string names a CSS framework or another tool; the reasons say 'hard-coded colour', 'hard-coded spacing', 'unknown token', 'class <name>', and 'tokens are read from config.design.tokens only'"
      status: flagged
      verification: unverified
    - statement: "`LOGIN-1.md` in `valid-build` is not edited (the `LOGIN-1.*.md` write goldens derive from it)"
      status: flagged
      verification: unverified
---

<objective>
**As a** designer handing a prototype to a developer, **I want** lint to warn at the exact line where the prototype uses a colour or spacing value that is not a project token, and to warn when a prototype without a tokens file does not say what it was derived from, **so that** the prototype and the codebase share one palette before code is written.

This is the last rule group: the LINT-04 token rule (D-66 to D-68) with its three research questions answered by RESEARCH.md (property lists, exemptions, `@theme` extraction, line attribution), plus the `valid-build` extension CONTEXT.md asked for.

Purpose: ROADMAP criterion 4 (token half) becomes executable; the table reaches its 30 rows (31 rule ids counting the loader's `load.report-invalid`).
Output: `lint/tokens.ts`, three rows, `tokens.test.ts`, fixtures `lint-tokens` and `lint-no-tokens`, the `valid-build` prototype, regenerated goldens and pins.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-lint/03-CONTEXT.md
@.planning/phases/03-lint/03-RESEARCH.md
@.planning/phases/03-lint/03-PATTERNS.md
@.planning/phases/03-lint/03-01-SUMMARY.md
@.planning/phases/03-lint/03-05-SUMMARY.md
@packages/core/src/lint/rules.ts
@packages/core/templates/prototype-header.html

<interfaces>
```typescript
// packages/core/src/lint/tokens.ts
export function tokenNames(css: string): Set<string>;
export function offending(prop: string, value: string, known: Set<string>): { token: string; why: string }[];
export function scanPrototype(html: string, known: Set<string>): { line: number; reason: string }[];   // sorted by line, then reason
export function derivedFrom(html: string): { line: number; paths: string[] } | undefined;             // undefined when the first comment has no `Derived from:` line
export const tokenHardcoded: Rule['check'];
export const tokensMissing: Rule['check'];
export const prototypeDerivation: Rule['check'];
// module-private: COLOR_PROPS, SPACING_PROPS, COLOR_UTIL, SPACING_UTIL, EXEMPT, NAMED_COLORS, isColor, isLength, PROTOTYPE key regex
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token extraction, value classification, prototype scanner, header parser</name>
  <read_first>
    - .planning/phases/03-lint/03-RESEARCH.md "Token Rule (D-66 to D-68)": extraction code, property lists, value classification, surfaces and line attribution, the verified `offending`/`scanPrototype` code with its eight expected findings and the not-flagged list, the `Derived from:` header section (A4), Pitfall 10
    - packages/core/src/load/sections.ts lines 102-131 (line-preserving stripping style) and packages/core/src/load/verification.ts lines 23-39 (find a labelled line, exec the remainder)
    - packages/core/templates/prototype-header.html (the header the parser must read; `<list ...>` is the placeholder)
    - packages/core/test/sections.test.ts lines 1-26 (unit table style)
  </read_first>
  <files>packages/core/src/lint/tokens.ts, packages/core/test/tokens.test.ts</files>
  <behavior>
    - Test 1 (`tokenNames`): the research sample mixing `@theme { --color-primary: #0055ff; --spacing: 0.25rem; --color-*: initial; }`, `@theme inline { --color-canvas: var(--acme-canvas); }`, `:root { --acme-canvas: #fff; }`, `[data-theme="dark"] { --acme-canvas: #000; }`, and a commented-out `/* --color-old: red; */` yields exactly the set `--acme-canvas`, `--color-canvas`, `--color-primary`, `--spacing`
    - Test 2 (`offending`, `known = {--color-primary, --space-2}`): on `color`: `#fff` → hard-coded colour; `white` → hard-coded colour; `oklch(60% 0.15 50)` → hard-coded colour; `var(--color-primary)` → none; `var(--color-primary, #fff)` → none (fallback blanked); `var(--nope)` → unknown token `var(--nope)`; `transparent`, `CurrentColor`, `inherit` → none; `linear-gradient(#fff, var(--color-primary))` → hard-coded colour. On `padding`: `12px` → hard-coded spacing; `0 auto` → none; `1px` → none; `100%` → none; `calc(var(--space-2) * 2)` → none; `2rem !important` → one finding `2rem`. On `border`: `1px solid var(--color-primary)` → none; `1px solid #000` → one colour finding. On `width` (not listed): `13px` → none. On `font-size`: `14px` → none
    - Test 3 (`scanPrototype` on the research prototype, verbatim from RESEARCH.md's comment block): exactly the eight findings with the listed lines and reasons (`padding: hard-coded spacing 12px` at line 10, `background: hard-coded colour #fff` at 11, `border: unknown token var(--nope); tokens are read from config.design.tokens only` at 12, `class bg-[#fff]: hard-coded colour #fff` at 16, `class p-[13px]: hard-coded spacing 13px` at 16, `class hover:bg-[oklch(60%_0.15_50)]/50: hard-coded colour oklch(60% 0.15 50)` at 16, `color: hard-coded colour white` at 17, `padding: hard-coded spacing 2rem` at 18), sorted by line then reason; the header comment's `#123456` and the `<script>` block's `#000` are absent
    - Test 4 (`derivedFrom`): the shipped `prototype-header.html` text yields `paths: []` at line 3 (the `<list ...>` placeholder counts as none); a header with `Derived from: src/a.css` on one line yields `['src/a.css']`; a header with `Derived from:` followed by two `- ` lines and then `Rule: ...` yields both paths with backslashes converted and `./` stripped; an HTML file with no comment yields `undefined`
    - Test 5 (linear time): a 1 MB `<style>` block of repeated valid declarations and a `class="` attribute of 20 000 tokens scan in under 2 s (`performance.now()` from the global, no `node:` import in the test either)
  </behavior>
  <action>
Write `packages/core/test/tokens.test.ts` first with the five behaviours (import from `../src/lint/tokens.js`; build the research prototype as a `body(...lines)` literal so the line numbers are explicit). Create `packages/core/src/lint/tokens.ts`: header comment citing D-66, D-67, D-68, A4, A5, A6, A10. Port `tokenNames`, `offending`, and `scanPrototype` from RESEARCH.md verbatim, removing the BOM/CRLF replace (loader normalises), and sort `scanPrototype`'s result by `line` then `reason` (code-point compare). `scanPrototype` composes each reason as `<prop>: <why> <token>` (or `class <name>: <why> <token>`); when `why` is `unknown token` it appends `; tokens are read from config.design.tokens only` (RESEARCH Open Question 6, owner-resolved: a default-theme token absent from the configured file is unknown, and the reason says where tokens come from). `offending` is unchanged (Test 2 still sees `why` = `unknown token`). Define the constants: `COLOR_PROPS` and `SPACING_PROPS` as the research property lists; `COLOR_UTIL` (`bg`, `text`, `border`, `border-t`, `border-r`, `border-b`, `border-l`, `border-x`, `border-y`, `border-s`, `border-e`, `outline`, `ring`, `ring-offset`, `shadow`, `inset-shadow`, `fill`, `stroke`, `accent`, `caret`, `decoration`, `divide`, `placeholder`, `from`, `via`, `to`) and `SPACING_UTIL` (`p`, `px`, `py`, `pt`, `pr`, `pb`, `pl`, `ps`, `pe`, `m`, `mx`, `my`, `mt`, `mr`, `mb`, `ml`, `ms`, `me`, `gap`, `gap-x`, `gap-y`, `space-x`, `space-y`, `inset`, `inset-x`, `inset-y`, `top`, `right`, `bottom`, `left`, `start`, `end`); `EXEMPT` lower-cased `transparent`, `currentcolor`, `inherit`, `0`, `1px`, `100%`; `NAMED_COLORS` as the full CSS Color Level 4 named-colour table (148 names, lower-case, as one string split on whitespace); `isColor(p)` matching `#` hex of 3, 4, 6, or 8 digits, a call to one of `rgb rgba hsl hsla hwb lab lch oklab oklch color color-mix light-dark device-cmyk contrast-color`, or a named colour (case-insensitive); `isLength(p)` matching a number followed by one of the MDN length units listed in RESEARCH.md or `%`, or the bare `0`. Add arbitrary-property classes `[prop:value]` as a declaration (A10). Add `derivedFrom(html)`: first `<!-- ... -->` match; inside it find the line matching `/^\s*Derived from:\s*(.*)$/i`; collect the remainder and each following line until one matches `/^\s*[A-Za-z][A-Za-z ]*:/` or the comment ends; each non-empty entry with a leading `- ` stripped, backslashes to `/`, leading `./` removed is a path; an entry starting with `<` and ending with `>` is dropped; return `{ line, paths }` with `line` the 1-based line of the `Derived from:` text, or `undefined` when no comment or no such line. Run `npm test -- --project core tokens`, then `npm run lint && npm run typecheck`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core tokens</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|core|` summary reports fewer than 5 passing tests; or any test exceeds 2 s</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/lint/tokens.ts` contains `export function tokenNames(`, `export function offending(`, `export function scanPrototype(`, `export function derivedFrom(`; `grep -c "node:" packages/core/src/lint/tokens.ts` prints 0; `grep -c "frontmatter" packages/core/src/lint/tokens.ts` prints 0
    - `packages/core/test/tokens.test.ts` contains the literal reasons `padding: hard-coded spacing 12px`, `border: unknown token var(--nope); tokens are read from config.design.tokens only`, `class hover:bg-[oklch(60%_0.15_50)]/50: hard-coded colour oklch(60% 0.15 50)`, and the token set entry `--acme-canvas`
    - `npm test -- --project core tokens` reports at least 5 passing tests and 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The three research questions of D-66 (property list, exemptions, `@theme` extraction) and the line-attribution method are implemented as pure functions with the research probe pinned as unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: The three token rules, fixtures `lint-tokens` and `lint-no-tokens`, the `valid-build` prototype, goldens and pins</name>
  <read_first>
    - packages/core/src/lint/tokens.ts (Task 1) and packages/core/src/lint/rules.ts (27 rows)
    - packages/core/src/load/snapshot.ts (`PROTOTYPE` and `normaliseKey` are exported since 03-01; import both, do not re-declare)
    - packages/core/test/fixtures/verification-edges/accord/assets/A/prototype.html and its config.yml (the Phase 2 prototype with no header comment and empty `design.tokens`; its lint golden gains one finding here)
    - packages/core/test/snapshot.test.ts lines 60-76 (tree pin) and packages/cli/test/load.test.ts lines 51-68 (file and tree pins)
    - packages/core/test/lint.test.ts, the `describe('fixture valid-build: pinned lint values')` block from 03-01 (the `snapshot.files` pin this task updates)
    - packages/core/test/fixtures/valid-build/src/styles/tokens.css and packages/core/templates/prototype-header.html
    - packages/core/test/__golden__/lint-missing-files.lint.json (from 03-05; gains one finding here)
    - .planning/phases/03-lint/03-RESEARCH.md Pitfall 11 and Open Questions (RESOLVED) 6
  </read_first>
  <files>packages/core/src/lint/tokens.ts, packages/core/src/lint/rules.ts, packages/core/test/lint.test.ts, packages/core/test/snapshot.test.ts, packages/cli/test/load.test.ts, packages/core/test/fixtures/lint-tokens/accord/config.yml, packages/core/test/fixtures/lint-tokens/accord/assets/PROTO-1/prototype.html, packages/core/test/fixtures/lint-tokens/src/tokens.css, packages/core/test/fixtures/lint-no-tokens/accord/config.yml, packages/core/test/fixtures/lint-no-tokens/accord/assets/A/prototype.html, packages/core/test/fixtures/lint-no-tokens/accord/assets/B/prototype.html, packages/core/test/fixtures/lint-no-tokens/accord/assets/C/prototype.html, packages/core/test/fixtures/lint-no-tokens/accord/assets/D/prototype.html, packages/core/test/fixtures/lint-no-tokens/src/base.css, packages/core/test/fixtures/valid-build/accord/assets/LOGIN-1/prototype.html, packages/core/test/fixtures/valid-build/src/styles/tokens.css, packages/core/test/__golden__/lint-tokens.snapshot.json, packages/core/test/__golden__/lint-tokens.lint.json, packages/core/test/__golden__/lint-no-tokens.snapshot.json, packages/core/test/__golden__/lint-no-tokens.lint.json, packages/core/test/__golden__/lint-missing-files.lint.json, packages/core/test/__golden__/valid-build.snapshot.json, packages/core/test/__golden__/valid-build.lint.json, packages/core/test/__golden__/verification-edges.lint.json</files>
  <action>
**Rules in `lint/tokens.ts`:** import `PROTOTYPE` and `normaliseKey` from `../load/snapshot.js` (exported by 03-01; one definition each, per RESEARCH "Don't Hand-Roll"). A private `prototypes(snapshot)` returns the sorted `files` keys matching `PROTOTYPE`; a private `tokensKey(snapshot)` returns `normaliseKey(config.design.tokens)` or `undefined` when unset or empty.
- `tokensMissing` (D-67): `tokensKey` defined and not `in snapshot.files` → one draft `{ file: 'accord/config.yml', pointer: '/design/tokens', reason: \`design.tokens "${tokens}" is not in the snapshot; tokens are read from config.design.tokens only, so the token rule is skipped\` }` (RESEARCH Open Question 6: the reason names the configured file as the only token source).
- `tokenHardcoded` (D-66, D-68): return `[]` when `tokensKey` is undefined (no tokens file: D-67 applies instead) or the key is absent (`tokensMissing` reported it); else `known = tokenNames(files[key])` and for each prototype key every `scanPrototype(files[key], known)` entry becomes `{ file: key, line, reason }`.
- `prototypeDerivation` (D-67): only when `tokensKey` is undefined; for each prototype key: `d = derivedFrom(text)`; `d === undefined` → draft at line 1, reason `prototype has no "Derived from:" line in its header comment`; `d.paths.length === 0` → draft at `d.line`, reason `"Derived from:" lists no path`; each path not in `snapshot.tree` → draft at `d.line`, reason `"Derived from:" path "${p}" is not in the repository`.

**`rules.ts`:** append `lint.token-hardcoded`, `lint.tokens-missing`, `lint.prototype-derivation`, all `warning`, both profiles. The table has 30 rows.

**Fixture `lint-tokens`:** config with `design:\n  tokens: src/tokens.css`; `src/tokens.css` = the Test 1 sample; `accord/assets/PROTO-1/prototype.html` = the research prototype from Test 3 (no ticket file, proving D-68). Expected lint: the eight `lint.token-hardcoded` warnings at their lines with the Test 3 reasons (the line-12 one ending `; tokens are read from config.design.tokens only`), nothing else (`known` from this tokens file includes `--color-primary` and `--spacing`; adjust the prototype's `var(--space-2)` use to `var(--spacing)` so the not-flagged list holds).

**Fixture `lint-no-tokens`:** config with `design:\n  tokens: ""`; `src/base.css` (one line); four prototypes: `A` with the shipped header verbatim (placeholder) → `lint.prototype-derivation` at line 3 reason `"Derived from:" lists no path`; `B` with `Derived from: src/base.css` → silent; `C` with `Derived from: src/nope.css` → warning naming `src/nope.css`; `D` with no comment at all → warning at line 1. Each prototype may use hard-coded colours freely (the allowlist check is skipped without tokens: pin this by giving `B` a `color: #fff` and asserting no `lint.token-hardcoded` in the golden).

**`lint-missing-files`:** no file change; its lint golden gains `lint.tokens-missing` with pointer `/design/tokens` next to `lint.report-missing`.

**`verification-edges` (Phase 2 fixture, no file change):** its config has `design.tokens: ""` and it ships `accord/assets/A/prototype.html` with no comment at all, so `prototypeDerivation` yields one `lint.prototype-derivation` at line 1 on that file with reason `prototype has no "Derived from:" line in its header comment`. Regenerate `verification-edges.lint.json` with `npm test -- --project core lint -u -t "fixture verification-edges"`, read the diff, and confirm it is exactly that one warning (the two `lint.plan-empty` from 03-02 stay) before running without `-u`. Every other Phase 2 fixture has empty `design.tokens` and no prototype, so `tokens-missing`, `token-hardcoded`, and `prototype-derivation` are silent there; `valid-build` is handled below. Any other change to an existing golden is a finding for the SUMMARY.

**`valid-build` extension (CONTEXT.md discretion):** extend `src/styles/tokens.css` to `:root { --color-primary: #0055ff; --space-2: 0.5rem; }` (one line still); add `accord/assets/LOGIN-1/prototype.html` = the shipped header with `TICKET-ID` replaced by `LOGIN-1` and `Derived from: src/styles/tokens.css`, then a document whose `<style>` uses only `color: var(--color-primary)`, `padding: var(--space-2)`, `border: 1px solid transparent`, and a `class="p-[var(--space-2)] bg-(--color-primary)"`. Do not edit `LOGIN-1.md`. Update the tree pin in `snapshot.test.ts` (insert `accord/assets/LOGIN-1/prototype.html` after `accord/config.yml`), both pins in `cli/test/load.test.ts` (files list and tree list), and the `snapshot.files` pin that 03-01 Task 1 added in `lint.test.ts` (the `describe('fixture valid-build: pinned lint values')` block): it now equals exactly the two keys `accord/assets/LOGIN-1/prototype.html` (the prototype text written above) and `src/styles/tokens.css` with the new tokens text `:root { --color-primary: #0055ff; --space-2: 0.5rem; }\n`. Regenerate `valid-build.snapshot.json` (scoped `-u`; `files` now has two keys and `tokens.css` has the new text) and `valid-build.lint.json` (findings unchanged: no `lint.token-hardcoded`, no `lint.prototype-derivation`).

Extend the pinned `describe`s in `lint.test.ts`: `lint-tokens` has exactly eight `lint.token-hardcoded` with the Test 3 reasons; `lint-no-tokens` has three `lint.prototype-derivation` on `A`, `C`, `D` and nothing on `B`; `lint-missing-files` has both `*-missing` rules; `valid-build` has no rule starting `lint.token` or `lint.prototype`; `verification-edges` has exactly one `lint.prototype-derivation`, on `accord/assets/A/prototype.html` at line 1. Generate goldens with scoped `-u` per fixture, read them, rerun without `-u`, then the full `npm run check`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run typecheck && npm test</automated>
    <fails_when>non-zero exit; or `No test files found`; or a golden reported obsolete or mismatched in either project; or the cli `load` test reports a file-list mismatch</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/lint/rules.ts` contains 30 `id:` entries including `lint.token-hardcoded`, `lint.tokens-missing`, `lint.prototype-derivation`; `grep -c "level: '" packages/core/src/lint/tokens.ts` prints 0
    - `packages/core/test/__golden__/lint-tokens.lint.json` contains exactly 8 findings, all `"rule": "lint.token-hardcoded"` and `"level": "warning"`, on `accord/assets/PROTO-1/prototype.html`, and `"errors": 0`
    - `packages/core/test/__golden__/lint-no-tokens.lint.json` contains exactly 3 `lint.prototype-derivation`, one with `"line": 1`, one whose reason contains `src/nope.css`, and no `lint.token-hardcoded`
    - `packages/core/test/__golden__/lint-missing-files.lint.json` contains both `"pointer": "/design/tokens"` and `"pointer": "/tests/report"`
    - `grep -c "tokens are read from config.design.tokens only" packages/core/test/__golden__/lint-tokens.lint.json` prints 1 (the line-12 `var(--nope)` finding), and the same grep prints 1 for `lint-missing-files.lint.json` (the `lint.tokens-missing` reason)
    - `grep -c "lint.prototype-derivation" packages/core/test/__golden__/verification-edges.lint.json` prints 1, on `accord/assets/A/prototype.html` with `"line": 1`; `grep -c "lint.prototype-derivation\|lint.token-hardcoded\|lint.tokens-missing"` prints 0 for `gherkin-shapes`, `body-edges`, `frontmatter-errors`, `lint-hygiene`, `lint-ears`, `lint-gherkin`, `lint-report` `.lint.json`
    - `packages/core/src/lint/tokens.ts` contains `import { normaliseKey, PROTOTYPE } from '../load/snapshot.js'` (in either order) and declares no regex literal of its own that matches `prototype.html` (the key regex is imported, not re-declared)
    - `packages/core/test/__golden__/valid-build.snapshot.json` `files` has exactly the keys `accord/assets/LOGIN-1/prototype.html` and `src/styles/tokens.css`; `valid-build.lint.json` contains no `lint.token-hardcoded` and no `lint.prototype-derivation`
    - `packages/core/test/snapshot.test.ts` and `packages/cli/test/load.test.ts` both contain `accord/assets/LOGIN-1/prototype.html`
    - `git diff --stat -- packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1.md` is empty; `git diff --stat -- package-lock.json` is empty; `git log -1 --format=%H` is unchanged
  </acceptance_criteria>
  <done>ROADMAP criterion 4 (token half) is pinned: a prototype outside the token file warns at the line and never errors, a missing tokens file is reported once on config, a prototype without a tokens file must name its source files, and the reference fixture proves no false positives.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Functions: `tokenNames`, `offending`, `scanPrototype`, `derivedFrom`, `tokenHardcoded`, `tokensMissing`, `prototypeDerivation` (`lint/tokens.ts`); private constants `COLOR_PROPS`, `SPACING_PROPS`, `COLOR_UTIL`, `SPACING_UTIL`, `EXEMPT`, `NAMED_COLORS`
- Rule ids: `lint.token-hardcoded`, `lint.tokens-missing`, `lint.prototype-derivation`
- Tests: `packages/core/test/tokens.test.ts`; pinned describes in `lint.test.ts`; updated pins in `snapshot.test.ts` and `cli/test/load.test.ts`
- Fixtures: `lint-tokens/` (config, `src/tokens.css`, `assets/PROTO-1/prototype.html`), `lint-no-tokens/` (config, `src/base.css`, prototypes A to D); `valid-build/accord/assets/LOGIN-1/prototype.html`; extended `valid-build/src/styles/tokens.css`
- Goldens: `lint-tokens.*.json`, `lint-no-tokens.*.json`; regenerated `lint-missing-files.lint.json`, `valid-build.snapshot.json`, `valid-build.lint.json`, `verification-edges.lint.json` (one `lint.prototype-derivation`)
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

| Source | Item | Disposition |
|---|---|---|
| RESEARCH A5 | Colour and spacing property lists and the utility prefix sets as listed; `width`, `height`, `font-size`, `border-radius`, `border-width` are not spacing | Adopted; a miss, never a false positive; extend the constant |
| RESEARCH A6 | Full CSS Color Level 4 named-colour table embedded | Adopted so `color: white` is caught |
| RESEARCH A4 | `Derived from:` list ends at the next `Key:` line or the comment end; `<...>` counts as no path; paths compared exactly against `snapshot.tree` | Adopted |
| RESEARCH A10 | Arbitrary-property classes `[color:#fff]` scanned as declarations | Adopted (cheap) |
| Owner decision (RESEARCH Open Question 6, resolved 2026-09-14) | A prototype using a framework default token not declared in the project tokens file warns "unknown token"; the reason text says tokens are read from the configured file only | Owner-confirmed; the unknown-token `lint.token-hardcoded` reason and the `lint.tokens-missing` reason both carry `tokens are read from config.design.tokens only` (Task 1 Test 3, Task 2), naming the config key rather than any framework |
| D-66 / D-67 | With `design.tokens` empty the allowlist check is skipped entirely and only the derivation rule runs | Chosen; there is no allowlist to check against |
| LINT-04 (unclassified edge) | `var(--x)` with a fallback is accepted when `--x` is known even if the fallback is a literal; a `var()` used on a non-listed property is still checked for unknown tokens | Chosen per the verified scanner |
| CONTEXT discretion | `valid-build` gains the prototype and a two-token `tokens.css`; `LOGIN-1.md` is not given `@test:` tags or a report, so its lint golden keeps `lint.test-tag-missing` × 2 and `lint.plan-empty` (a ticket at Ready without tests) | Chosen to keep the `LOGIN-1.*.md` write goldens stable; the owner may ask for a fully clean reference |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prototype HTML and tokens CSS → scanner regexes | Designer-authored files, possibly large, possibly hostile |
| `Derived from:` paths → `snapshot.tree` lookup | Contributor-written paths checked for existence only, never read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | prototype declaring its own `--x: #fff` in a local `<style>` to launder a literal | medium | mitigate | Declarations of `--*` are skipped; every `var(--x)` use is checked against the tokens file only, so the use site warns (Task 1 Test 2 `var(--nope)`, RESEARCH "Value classification") |
| T-03-01 | Denial of service | megabyte prototype or a 20 000-token class attribute against the scanner | medium | mitigate | Per-token anchored `CLASS` regex, lazy fixed terminators, offset-to-line binary search; Task 1 Test 5 bounds a 1 MB input under 2 s |
| T-03-14 | Tampering | `<script>` or comment content scanned as CSS to create noise or hide a literal | low | mitigate | Comments and scripts blanked with newlines preserved before any matching (Task 1 Test 3) |
| T-03-15 | Information disclosure | `Derived from:` path probing the filesystem | low | mitigate | Existence is a membership test on `snapshot.tree`; core never reads a path |
| T-03-SC | Tampering | npm installs | high | mitigate | No package added |
</threat_model>

<verification>
- Task 2's gate is the full `npm run check` equivalent (`build`, `lint`, `typecheck`, `test`), which is the phase gate: 30 lint rules, fifteen lint goldens, both projects green on Windows.
</verification>

<success_criteria>
- ROADMAP criterion 4 token half pinned; D-65 (goldens include `files`), D-66, D-67, D-68 each have a task step, a fixture, and a golden.
- `npm run check` green; nothing committed.
</success_criteria>

<output>
Create `.planning/phases/03-lint/03-06-SUMMARY.md` when done. Do not commit.
</output>
