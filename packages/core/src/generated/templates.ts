// Generated by scripts/gen-templates.mjs from templates/*. Do not edit.
export const templates = {
  "business-rules.md": "# Business rules\n\n<!-- BA. Rules, thresholds, tolerances, rounding, and edge cases already decided; one rule per bullet.\nWhen a story depends on a rule, the ticket cites it instead of restating it. At handover, epic requirements are folded in here. -->\n\n- Sample rule: amounts are rounded to two decimal places, half away from zero\n- Sample rule: a request older than 30 days is treated as expired\n",
  "epic.md": "---\nid: \"TICKET-ID\"                            # equals the file name: tickets/<id>.md\ntitle: \"Short title in business language\"\ntype: epic                                 # an epic groups stories and bugs, holds intent and requirements for its children, is linted but never gated\nstatus: draft                              # draft | open | archived. Document lifecycle only; work status stays in the tracker\n# The epic this ticket belongs to. Grouping is by parent; there is no per-epic folder.\n# parent: \"EPIC-ID\"\n# Ids in the tracker, a map keyed by adapter name; values are strings.\n# tracker: { shortcut: \"1234\" }\n# When true, Ready requires a design reference; the Intent note below says which one this profile expects.\nui: false\n# Optional design reference URL. Allowed on epics too.\n# design: \"https://www.figma.com/...\"\n# Unconfirmed assumptions block Ready.\n# assumptions:\n#   - { text: \"...\", confirmed: false }\n---\n\n## Intent\n<!-- BA. Why, for whom, how success is measured, and explicit non-goals. About five lines.\nDescribe observable behaviour in business language; never name tables, endpoints, libraries, or screens.\nTerms go in product/glossary.md; rules already decided go in product/business-rules.md.\nAn epic is never gated; design: may hold the epic-level design link for its child stories. -->\n\n## Requirements\n<!-- BA. One EARS line per requirement: WHEN <trigger> the system SHALL <response>, and its siblings. 5 to 15 lines.\nOrdering of child stories, when needed, is prose here in v0.1 (ticket dependencies are deferred). -->\n\n## Open questions\n<!-- BA. `- [ ]` items; Ready stays blocked while any is unchecked. -->\n",
  "glossary.md": "# Glossary\n\n<!-- BA. One term per entry: the word the team actually uses and its business meaning. Agents and tickets use these terms verbatim. -->\n\n- **Sample term** — what the term means to the business, in one sentence\n- **Another sample term** — replace both entries with the words your team actually uses\n",
  "prototype-header.html": "<!--\naccord prototype for ticket TICKET-ID\nDerived from: <list the stylesheets or token files this prototype was built from, one per line>\nRule: use only colours and spacing from those files; the token rule (lint) warns on hard-coded values\nOwner: designer (assets/<id>/ is designer-owned)\n-->\n<!doctype html>\n<html lang=\"en\">\n<head>\n<meta charset=\"utf-8\">\n<title>TICKET-ID prototype</title>\n</head>\n<body>\n</body>\n</html>\n",
  "ticket-build.md": "---\nid: \"TICKET-ID\"                            # equals the file name: tickets/<id>.md\ntitle: \"Short title in business language\"\ntype: story                                # epic | story | bug. story and bug touch code and need at least one tagged scenario\nstatus: draft                              # draft | open | archived. Document lifecycle only; work status stays in the tracker. draft blocks Ready\n# The epic this ticket belongs to. Grouping is by parent; there is no per-epic folder.\n# parent: \"EPIC-ID\"\n# Ids in the tracker, a map keyed by adapter name; values are strings.\n# tracker: { shortcut: \"1234\" }\n# When true, Ready requires a design reference; the Intent note below says which one this profile expects.\nui: false\n# Optional design reference URL. Allowed on epics too.\n# design: \"https://www.figma.com/...\"\n# Unconfirmed assumptions block Ready.\n# assumptions:\n#   - { text: \"...\", confirmed: false }\n# Written by `gate ready`; never edit by hand.\n# ac_hash:\n# Developer self-test ticks, e.g. [ac-1, ac-2]. Only the developer writes it.\n# Always the last key so developer hunks stay far from BA hunks.\n# verified: []\n---\n\n## Intent\n<!-- BA. Why, for whom, how success is measured, and explicit non-goals. About five lines.\nDescribe observable behaviour in business language; never name tables, endpoints, libraries, or screens.\nTerms go in product/glossary.md; rules already decided go in product/business-rules.md.\nIf the story has a screen, set ui: true in the frontmatter. Ready then requires a Figma link in design:. -->\n\n## Requirements\n<!-- BA. One EARS line per requirement: WHEN <trigger> the system SHALL <response>, and its siblings. 5 to 15 lines. -->\n\n## Acceptance criteria\n<!-- BA. One fenced gherkin block; every scenario tagged @ac-n with a unique n; 2 to 5 scenarios.\nA bug is written as Given the situation, When the action, Then the correct result. -->\n```gherkin\nFeature: TICKET-ID\n\n  @ac-1\n  Scenario: <observable outcome>\n    Given ...\n    When ...\n    Then ...\n```\n\n## Open questions\n<!-- BA. `- [ ]` items; Ready stays blocked while any is unchecked. -->\n\n## Plan\n<!-- Developer fills this in. BA leaves it empty.\nA short plan only. Evidence goes to tickets/<id>/verification.md, written by the fresh review context, not by the agent that wrote the code. -->\n",
  "ticket-maintain.md": "---\nid: \"TICKET-ID\"                            # equals the file name: tickets/<id>.md\ntitle: \"Short title in business language\"\ntype: story                                # epic | story | bug. story and bug touch code and need at least one tagged scenario\nstatus: draft                              # draft | open | archived. Document lifecycle only; work status stays in the tracker. draft blocks Ready\n# The epic this ticket belongs to. Grouping is by parent; there is no per-epic folder.\n# parent: \"EPIC-ID\"\n# Ids in the tracker, a map keyed by adapter name; values are strings.\n# tracker: { shortcut: \"1234\" }\n# When true, Ready requires a design reference; the Intent note below says which one this profile expects.\nui: false\n# Optional design reference URL. Allowed on epics too.\n# design: \"https://www.figma.com/...\"\n# Unconfirmed assumptions block Ready.\n# assumptions:\n#   - { text: \"...\", confirmed: false }\n# Written by `gate ready`; never edit by hand.\n# ac_hash:\n# Developer self-test ticks, e.g. [ac-1, ac-2]. Only the developer writes it.\n# Always the last key so developer hunks stay far from BA hunks.\n# verified: []\n---\n\n## Intent\n<!-- BA. Why, for whom, how success is measured, and explicit non-goals. About five lines.\nDescribe observable behaviour in business language; never name tables, endpoints, libraries, or screens.\nTerms go in product/glossary.md; rules already decided go in product/business-rules.md.\nIf the story has a screen, set ui: true in the frontmatter. Ready then requires a prototype at assets/<id>/prototype.html derived from the project's existing styles.\nA Figma link in design: is optional in maintain; the prototype is the primary reference. -->\n\n## Requirements\n<!-- BA. One EARS line per requirement: WHEN <trigger> the system SHALL <response>, and its siblings. 5 to 15 lines. -->\n\n## Acceptance criteria\n<!-- BA. One fenced gherkin block; every scenario tagged @ac-n with a unique n; 2 to 5 scenarios.\nA bug is written as Given the situation, When the action, Then the correct result. A bug fix still needs at least one scenario. -->\n```gherkin\nFeature: TICKET-ID\n\n  @ac-1\n  Scenario: <observable outcome>\n    Given ...\n    When ...\n    Then ...\n```\n\n## Open questions\n<!-- BA. `- [ ]` items; Ready stays blocked while any is unchecked. -->\n\n## Plan\n<!-- Developer fills this in. BA leaves it empty.\nA short plan only. Evidence goes to tickets/<id>/verification.md, written by the fresh review context, not by the agent that wrote the code. -->\n",
  "verification.md": "---\nticket: \"TICKET-ID\"\ncommit: \"0000000\"\nreviewed_on: \"2026-01-01\"\n---\n\n<!-- This file is tickets/<id>/verification.md. It is written only by the fresh review context the dev workflow opens at the end (review.md), never by the agent that wrote the code.\nOne `## @ac-n <scenario name>` block per scenario. Matching is by tag; the name is for humans. -->\n\n## @ac-1 <scenario name>\n\nResult: pass\n<!-- pass | fail | blocked -->\n\nEvidence: <what was run or inspected, may span several lines>\n<!-- The reference syntax `gate done` checks is decided in Phase 4. -->\n",
} as const;
export type TemplateName = keyof typeof templates;
