---
phase: 06-skills
plan: 06
type: execute
wave: 2
depends_on: ["06-05"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SKILL-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The wrong-plan fixture has been run through the plan-review brief once, in a fresh context, and the `## Plan` it came back with is recorded beside the one it went in with"
    - "The returned plan differs from the fixture's: the three technical-layer steps come back as vertical slices carrying their own `@ac-n` tags, and the endpoint and serialiser steps come back ahead of the step that consumes their output"
    - "All eleven rendered files have been read against the Ready and Done tables in `docs/design.md` section 5 and against `packages/core/src/lint/rules.ts`, with one recorded line per file"
    - "Every rule mention in those eleven files points at `accord lint`, `accord gate ready`, or `accord gate done` and lets the command print its own reasons"
    - statement: "Each of the eleven files gets a record line; a file that mentions no rule at all is recorded as `no rule mention` and counts as a pass, not as unrecorded (SKILL-04 probe: empty)"
      verification: backstop
    - statement: "A sentence that names the command AND also paraphrases what the command checks is recorded as a restatement, not a pass — naming and restating touching is the boundary case (SKILL-04 probe: adjacency)"
      verification: backstop
    - statement: "The eleven records are written in the order the task lists the files, so the record reads line-for-line against that list (SKILL-04 probe: ordering)"
      verification: backstop
    - "SKILL-04 is ticked in REQUIREMENTS.md only after the eleven-file record exists and every line reads as a pass"
  prohibitions:
    - "The wrong-plan fixture must not be edited, regenerated, or re-tagged to make the review come back changed — a review that returns the plan unchanged is recorded as a fail, and the fixture is the thing being tested against, not the thing being fixed"
    - "The review context must not be given the skill body, the ticket's `## Requirements`, or any source file — it receives the five-line brief plus `## Intent`, `## Acceptance criteria`, and `## Plan`, and nothing else"
    - "A record line must not claim a read that did not happen; a file that could not be read is recorded as `not read` and SKILL-04 stays open"
    - "No requirement checkbox or traceability row may be ticked ahead of the evidence that closes it — SKILL-04's tick is a consequence of Task 2's record and of nothing else"
  artifacts:
    - ".planning/phases/06-skills/06-06-SUMMARY.md — carries the before/after `## Plan`, the eleven record lines, and exactly one `SKILL-04 verdict: pass|fail` line; this plan's whole evidence trail and the only input to Task 3's branch"
    - ".planning/REQUIREMENTS.md — SKILL-04 ticked and Complete, conditional on Task 2"
  key_links:
    - "`accord-dev/SKILL.md` step 4's five-line brief -> the wrong-plan fixture -> the recorded before/after plan (ROADMAP criterion 7, manual half)"
    - "the eleven rendered files -> `docs/design.md` section 5 + `packages/core/src/lint/rules.ts` -> the eleven record lines -> SKILL-04's tick"
---

<objective>
Perform the two human-verification items 06-VERIFICATION.md left open, record their evidence, and
tick SKILL-04 only if the second one passes.

Both are judgement work a test suite cannot do. The project's `No API keys` constraint forbids
running a model inside the test suite, and D-115 accepts the split explicitly: CI proves the
structure, a human proves the behaviour, once.

Purpose: ROADMAP criterion 7 and the second clause of criterion 3 are each verified on their
automated half and blocked only on a manual step the phase itself designated manual. SKILL-04 is
correctly still `[ ]` in REQUIREMENTS.md for exactly this reason.

Output: a recorded before/after `## Plan`, eleven recorded rule-mention lines, and — if and only if
those eleven read as passes — SKILL-04 closed.

**Items 3 (the three fresh-context reviews read as one shape) and 4 (Cursor/Copilot duplicate-name
behaviour) from 06-VERIFICATION.md are out of scope by the owner's decision. Neither blocks a
numbered criterion. Do not perform them and do not plan them.**

`workflow.human_verify_mode` is `end-of-phase` in `.planning/config.json`. These two are authored
as mid-flight checkpoints anyway, and as `gate="blocking-human"`, for two reasons: item 2 gates the
SKILL-04 tick in Task 3 of this same plan, so batching it to the end-of-phase harvest would leave
the tick either ungated or undone; and auto-approving "did you read eleven files and judge them?"
would defeat the entire purpose of the check.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills/06-VERIFICATION.md
@.planning/phases/06-skills/06-VALIDATION.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Run the wrong-plan fixture through the plan review</name>

  <read_first>
    - `packages/core/skills/dev/SKILL.md` step 4 (lines 32-48) — the five-line brief, verbatim. This
      is what the fresh context receives.
    - `packages/core/test/fixtures/wrong-plan/accord/tickets/TCK-1.md` — the whole file, including
      the HTML comment that names the two defects it was built to carry.
    - `.planning/phases/06-skills/06-VERIFICATION.md` — the first entry under
      `behavior_unverified_items`, which states the test and the expected result this task is
      measured against.
  </read_first>

  <precondition>`packages/core/test/fixtures/wrong-plan/accord/tickets/TCK-1.md` exists on disk, is 1917 bytes, and hashes to `bc5139f867319f08aeb7856797debaff0fc9ae566558be7fbc05dc7e9bd9c653` (sha256). It is untracked working-tree state, so `git status` cannot detect a change to it — the hash is the only guard.</precondition>

  <what-built>
    Plans 06-01 to 06-04 shipped the dev workflow, its plan-review step, and the deliberately-wrong
    fixture the step is meant to fix. CI already asserts the structure: the plan-review step exists,
    it states that it reads no code and edits only `## Plan`, and it precedes the implementation
    step positionally (`packages/core/test/skills.test.ts:317` and `:325`). Nothing has run the
    brief against the fixture. This is ROADMAP criterion 7's second clause and it is the last
    unverified half of that criterion.

    The fixture carries both designed defects. Its `## Plan` reads:

        - Wire the export control to the endpoint and show the empty-list message @ac-1 @ac-2
        - Add the row serialiser that turns a ticket into a CSV line
        - Add the export endpoint that gathers the listed tickets and calls the serialiser

    Step 1 holds every `@ac-n` tag while steps 2 and 3 hold none (technical layers, not vertical
    slices), and step 1 consumes the output of steps 2 and 3 (wrong order).
  </what-built>

  <how-to-verify>
    1. Record the fixture's `## Plan` verbatim, as the BEFORE. Confirm the sha256 above first.
    2. Open a fresh context — a subagent where the runtime provides one, otherwise a new session.
       It must have no memory of this plan, of the fixture's HTML comment, or of what the defects
       are. Do not tell it what to look for beyond the brief.
    3. Hand it exactly two things and nothing else:
       - the five lines of `accord-dev/SKILL.md` step 4 ("Read no code" / "Edit only `## Plan`" /
         the wrong layer / the wrong order / an approach the acceptance criteria will not survive),
         plus the sentence that follows them ("Whether every scenario is covered is `accord lint`'s
         question, not this one's");
       - the fixture's `## Intent`, `## Acceptance criteria`, and `## Plan` sections only. Not the
         frontmatter, not `## Requirements`, not `## Open questions`, not the HTML comment, and no
         source file.
    4. Record what it returns as the AFTER, verbatim.
    5. Re-hash the fixture. It must be byte-identical to step 1. If the fresh context wrote back
       into the file rather than returning text, that is itself a finding: the brief says edit only
       `## Plan`, and the review was handed text, not a path.

    **PASSES when the returned `## Plan` differs from the BEFORE in both of these ways:**
    - the three technical-layer steps come back as vertical slices, each carrying its own `@ac-n`
      tag, rather than one step holding both tags and two holding none;
    - the endpoint step and the serialiser step come back ahead of the step that consumes their
      output.

    **FAILS when** the plan comes back unchanged, or comes back changed in only one of the two ways,
    or comes back changed in some third way while leaving either defect in place. Record the failure
    and stop; do not re-run with a broader hint and do not edit the fixture. A fail is a real result
    about the workflow's wording, and the phase's own criterion is what it was measured against.
  </how-to-verify>

  <resume-signal>Type "pass" with the BEFORE and AFTER plans pasted, or "fail" with the returned plan pasted and one line saying which of the two defects survived.</resume-signal>

  <acceptance_criteria>
    - The SUMMARY carries the BEFORE `## Plan` and the AFTER `## Plan`, both verbatim, both labelled.
    - The SUMMARY states which of the two defects the returned plan fixed, quoting the step order
      and the `@ac-n` tag distribution it came back with.
    - `packages/core/test/fixtures/wrong-plan/accord/tickets/TCK-1.md` still hashes to
      `bc5139f867319f08aeb7856797debaff0fc9ae566558be7fbc05dc7e9bd9c653`.
    - The SUMMARY names what the fresh context was given, and confirms it was not given the skill
      body, `## Requirements`, or any source file.
    - A `fail` result is recorded as a fail. It does not block Task 2 or Task 3 — SKILL-12 is
      already `[x]` on its structural half and this item closes no requirement checkbox.
  </acceptance_criteria>

  <done>The wrong-plan fixture has been through the plan-review brief once, in a fresh context, and the before and after plans plus the pass/fail verdict are recorded in the SUMMARY with the fixture byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Read all eleven rendered files for restated rules</name>

  <read_first>
    - `docs/design.md` section 5 ("Gates", lines 84-114) — the Ready and Done tables. These are the
      rules a skill must not reproduce.
    - `packages/core/src/lint/rules.ts` lines 49-79 — the `RULES` table: thirty rule ids, each with
      its level. This is the other half of what must not be reproduced.
    - The nine rendered files this repository has on disk under `.claude/skills/`:
      `accord-ba/SKILL.md`, `accord-ba/setup.md`, `accord-ba/story.md`, `accord-ba/ready.md`,
      `accord-dev/SKILL.md`, `accord-dev/debug.md`, `accord-dev/review.md`,
      `accord-dev/code-review.md`, `accord-dev/prototype.md`.
    - `packages/core/skills/designer/SKILL.md` — the source of the tenth file. This repository's
      `accord/config.yml` declares `roles: [ba, dev]`, so `accord-designer/` is not rendered here;
      the source body is the rendered body minus the two stripped authoring keys, so reading the
      source is a faithful read of what a designer-rostered repository receives.
    - `packages/core/skills/shared/prototype.md` — the source of the eleventh file. The two rendered
      copies (`accord-designer/prototype.md` and `accord-dev/prototype.md`) are asserted
      byte-identical by `packages/core/test/skills.test.ts:389`, so the designer copy gets the same
      verdict as the dev copy and that must be stated rather than assumed silently.
  </read_first>

  <precondition>Plan 06-05 Task 2 has landed and the copies under `.claude/skills/` have been re-synced, so `accord-dev/review.md` and `accord-dev/prototype.md` carry the amended step 2s. Reading the pre-amendment text would judge prose that no longer ships. Confirm with a second `node packages/cli/dist/cli.js skills sync` printing `unchanged` on every line.</precondition>

  <what-built>
    ROADMAP criterion 3's second clause — "none restates a rule the CLI enforces" — is the one
    clause of that criterion with no automated half, and it is what holds SKILL-04 open. Clause 1
    (every SKILL.md opens on a gate command) and clause 3 (a test fails if a skill names a CLI
    command that does not exist) are both already verified in CI.

    06-VALIDATION.md designates this Manual-Only and gives the reason: the property is whether a
    sentence restates a gate row rather than pointing at the command, which is a judgement about
    intent. A keyword matcher would flag a skill for naming `## Open questions` at all.
  </what-built>

  <how-to-verify>
    Read each of the eleven files end to end, in the order listed below, against the two references.
    For each file write exactly one record line into the SUMMARY, in this order, with no file
    skipped:

    1. `accord-ba/SKILL.md`
    2. `accord-ba/setup.md`
    3. `accord-ba/story.md`
    4. `accord-ba/ready.md`
    5. `accord-dev/SKILL.md`
    6. `accord-dev/debug.md`
    7. `accord-dev/review.md`
    8. `accord-dev/code-review.md`
    9. `accord-dev/prototype.md`
    10. `accord-designer/SKILL.md` (read from `packages/core/skills/designer/SKILL.md`; say so in
        the line)
    11. `accord-designer/prototype.md` (byte-identical to line 9's file by
        `skills.test.ts:389`; say so in the line and carry line 9's verdict)

    Each record line is: the file name, the verdict (`pass` / `restates` / `no rule mention`), and
    the evidence — for a pass, which command the rule mention points at; for a `restates`, the
    sentence and the gate row or lint rule id it reproduces.

    **What counts as a pass:** the text names `accord lint`, `accord gate ready`, or
    `accord gate done` and stops, leaving the command to print its own reasons. Naming the section a
    finding lands in (`## Open questions`, `## Plan`, `## Verification notes`) is naming the folder
    convention, not restating a rule — the convention is the product, and `docs/design.md` section 2
    is its source, not the gate tables.

    **`Derived from:` in `accord-dev/prototype.md` and `accord-designer/prototype.md` is a PASS.**
    Record it as one, do not re-litigate it. The owner settled this while plan 06-05 was being
    revised, and the ground is D-126's own line: naming a format slot the artifact carries is the
    same class of statement as `## @ac-n` or `## Open questions`, even when a lint rule also checks
    that slot is present. What D-126 forbids is *explaining* the rule — and after plan 06-05 Task 2
    that prose says only that the comment block names the ticket, a `Derived from:` line for what
    step 1 found, and the owner, then hands the reader `accord lint` for anything missing. Two kinds
    of clause flip that verdict, and if you find either one *that* is a `restates` and plan 06-05
    Task 2 did not land as specified:
    - what happens when the slot is absent, empty, or names a path outside the repository — the
      three reasons `lint.prototype-derivation` prints;
    - that hard-coded colour or spacing values are warned about — `lint.token-hardcoded`, the rule
      behind the `Rule:` line the template keeps and the skill deliberately does not describe. This
      is the axis D-126 named first, so it is the one to look for hardest.

    **What counts as a restatement — the boundary case to watch for:** a sentence that names the
    command *and also* paraphrases what the command checks. "Run `accord gate ready <id>`" passes.
    "Run `accord gate ready <id>`; it fails if intent is empty or no scenario carries a tag" is a
    restatement, because the second half reproduces a Ready table row and will drift from it
    silently. Naming and restating touch here — when they touch, record `restates`.

    **A file that mentions no rule at all** is recorded as `no rule mention` and counts as a pass.
    It is not left unrecorded and it is not a skip.

    **A file you could not read** is recorded as `not read`, the reason is stated, and SKILL-04
    stays open. Do not infer a verdict from a source file when the rendered file is available.

    **PASSES when** all eleven lines exist and every one reads `pass` or `no rule mention`.
    **FAILS when** any line reads `restates` or `not read`. On a fail, record the offending
    sentences and stop — the fix is a prose change, which is a separate, explicitly-justified step
    the owner decides on, not something to make while reading.

    Close the eleven lines with the verdict on a line of its own, written in exactly one of these
    two forms and nothing else, because Task 3's check parses it:

        SKILL-04 verdict: pass
        SKILL-04 verdict: fail

    Prose either side of it is fine; the line itself carries no trailing text.
  </how-to-verify>

  <resume-signal>Type "pass" with the eleven record lines pasted, or "fail" with the eleven lines pasted and the offending sentences quoted.</resume-signal>

  <acceptance_criteria>
    - The SUMMARY carries exactly eleven record lines, in the listed order, one per file, none
      missing.
    - Every line carries a verdict from the set `pass` / `restates` / `no rule mention` / `not read`
      and its evidence.
    - Lines 10 and 11 state where they were read from and why that read is faithful.
    - Line 7 (`accord-dev/review.md`) and line 9 (`accord-dev/prototype.md`) were read AFTER plan
      06-05 Task 2's amendment, and the lines say so.
    - No rendered file was edited during this task; `node packages/cli/dist/cli.js skills sync`
      prints `unchanged` on every line afterwards.
    - `06-06-SUMMARY.md` carries exactly one line matching `^SKILL-04 verdict: (pass|fail)$`,
      written after the eleven record lines. Task 3's verify parses that line, so a paraphrase or a
      trailing clause breaks it.
  </acceptance_criteria>

  <done>Eleven record lines exist, one per rendered file, each with a verdict and its evidence, followed by a single `SKILL-04 verdict: pass` or `SKILL-04 verdict: fail` line that Task 3 acts on.</done>
</task>

<task type="auto">
  <name>Task 3: Close SKILL-04 — only if Task 2 passed</name>

  <read_first>
    - The SUMMARY section Task 2 wrote — specifically its `SKILL-04 verdict:` line and the eleven
      record lines above it. This task acts on that line and on nothing else.
    - `.planning/REQUIREMENTS.md` line 74 (the SKILL-04 checklist entry) and line 190 (its
      traceability row).
    - `.planning/phases/06-skills/06-VERIFICATION.md` — the Requirements Coverage row for SKILL-04,
      which records the first clause as verified and names the second as the blocker this task
      clears.
  </read_first>

  <precondition>Task 2 of this plan has been performed and `.planning/phases/06-skills/06-06-SUMMARY.md` carries exactly one line matching `^SKILL-04 verdict: (pass|fail)$`. If that line reads `fail`, or is absent, this task makes no edit to REQUIREMENTS.md — that is a branch of the task, not a blocker on it.</precondition>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
    Read the `SKILL-04 verdict:` line Task 2 wrote into
    `.planning/phases/06-skills/06-06-SUMMARY.md`. It is the only input to this task's branch.

    **If it reads `SKILL-04 verdict: pass`** — all eleven lines read `pass` or `no rule mention`,
    none reads `restates` or `not read` — make exactly two edits in `.planning/REQUIREMENTS.md`:
    1. Line 74: `- [ ] **SKILL-04**` becomes `- [x] **SKILL-04**`. Leave the requirement text
       unchanged.
    2. Line 190 of the traceability table: `| SKILL-04 | Phase 6 | Pending |` becomes
       `| SKILL-04 | Phase 6 | Complete |`.
    Then write one line into the SUMMARY recording that the tick is a consequence of Task 2's
    eleven-line record, naming the date it was performed.

    **If it reads `SKILL-04 verdict: fail`, or the line is absent** — change nothing in
    `.planning/REQUIREMENTS.md`. Write one line into the SUMMARY stating that SKILL-04 remains open,
    quoting the record line or lines that held it open. A `fail` here is the correct outcome of the
    check, not a blocked task.

    Touch no other requirement row and no other traceability row. CLI-08 and SKILL-09 were closed by
    plan 06-05 Task 3 and are not revisited here.

    Leave the change uncommitted for review. Do not `git commit` and do not `git push` — the
    project's standing rule in `.claude/CLAUDE.md` overrides any workflow step that says to commit.
  </action>

  <verify>
    <automated>if grep -q '^SKILL-04 verdict: pass$' .planning/phases/06-skills/06-06-SUMMARY.md; then grep -q '^- \[x\] \*\*SKILL-04\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| SKILL-04 | Phase 6 | Complete |' .planning/REQUIREMENTS.md; else grep -q '^- \[ \] \*\*SKILL-04\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| SKILL-04 | Phase 6 | Pending |' .planning/REQUIREMENTS.md; fi</automated>
    <fails_when>non-zero exit — SKILL-04's state in REQUIREMENTS.md disagrees with the verdict line recorded in the SUMMARY. One command, both branches: a recorded `pass` demands the ticked pair, anything else (a recorded `fail`, or no verdict line at all) demands the untouched pair.</fails_when>
    <automated>grep -q '^- \[x\] \*\*CLI-08\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^| CLI-08 | Phase 6 | Complete |' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — plan 06-05 Task 3's CLI-08 edits were reverted or overwritten by this task</fails_when>
    <automated>test "$(grep -c '^| SKILL-04 | Phase 6 | ' .planning/REQUIREMENTS.md)" -eq 1</automated>
    <fails_when>non-zero exit — the SKILL-04 traceability row appears zero times or more than once, meaning it was duplicated or dropped rather than edited in place</fails_when>
    <automated>test "$(grep -c '^SKILL-04 verdict: ' .planning/phases/06-skills/06-06-SUMMARY.md)" -eq 1</automated>
    <fails_when>non-zero exit — the SUMMARY carries no verdict line, or more than one, so the agreement check above had nothing unambiguous to read</fails_when>
  </verify>

  <acceptance_criteria>
    - After a `pass` verdict: `.planning/REQUIREMENTS.md` line 74 reads
      `- [x] **SKILL-04**: ...` with the requirement text unchanged, and the traceability row reads
      `| SKILL-04 | Phase 6 | Complete |`.
    - After a `fail` or absent verdict: both SKILL-04 lines are byte-unchanged and the SUMMARY says
      why.
    - Either way, the SUMMARY carries one sentence tying the outcome to Task 2's record, with the
      date.
    - CLI-08's checklist entry and traceability row are exactly as plan 06-05 left them.
    - No line of `.planning/REQUIREMENTS.md` other than the two SKILL-04 lines changed during this
      task. The file already carries uncommitted changes from earlier Phase 6 work and from plan
      06-05, so a whole-file diff against the last commit cannot isolate this edit — compare against a copy of
      the file taken before editing, or read the two lines back directly.
    - Nothing is committed; `git status` still shows the change in the working tree.
  </acceptance_criteria>

  <reversibility rating="reversible">Two lines in a tracking document, gated on a recorded human verdict.</reversibility>

  <done>SKILL-04 is ticked in both places if and only if Task 2's eleven-line record reads as a pass, and the SUMMARY states which outcome applied and why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fresh review context -> the recorded result | a human judgement is the only evidence; nothing downstream can re-derive it |
| the checked-in fixture -> the review that grades against it | the thing being tested and the thing being fixed are one directory apart |
| the eleven-line record -> a requirement checkbox | a tracker tick is a claim the project makes about itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Repudiation | the two human-verify records | medium | mitigate | Each checkpoint writes its evidence into `06-06-SUMMARY.md` — the before/after plan verbatim for Task 1, eleven labelled lines for Task 2 — so "done" is auditable rather than asserted. Task 3 reads that record and refuses to tick without it. |
| T-06-06-02 | Tampering | `packages/core/test/fixtures/wrong-plan/accord/tickets/TCK-1.md` | medium | mitigate | The fixture is untracked working-tree state, so `git status` cannot detect an edit to it. Task 1 carries the file's sha256 as a `<precondition>` and re-checks it after the review; a prohibition forbids editing it to make the review pass. |
| T-06-06-03 | Tampering | `.planning/REQUIREMENTS.md` | high | mitigate | Ticking a requirement ahead of its evidence is the failure this whole project exists to prevent. Task 3 is gated on Task 2's recorded verdict, its `<verify>` asserts the SKILL-04 state agrees with that verdict in both directions, and a third command asserts plan 06-05's CLI-08 edits were not clobbered. |
| T-06-06-04 | Information disclosure | the fresh context opened for Task 1 | low | accept | The fixture is a synthetic ticket about exporting a CSV, checked in for this purpose, carrying no real data. The brief hands it three sections and nothing else. |
| T-06-06-05 | Elevation of privilege | the fresh review context | low | mitigate | It is handed text, not a path, and writes nothing. If it writes back into the fixture, Task 1's post-hash catches it and records it as a finding. |

**Supply chain:** no task in this plan runs a package-manager install or adds a dependency. No
`T-06-06-SC` row is minted and no package-legitimacy checkpoint is required.
</threat_model>

<verification>
1. `06-06-SUMMARY.md` carries the BEFORE and AFTER `## Plan` from Task 1 and a pass/fail verdict.
2. `sha256sum packages/core/test/fixtures/wrong-plan/accord/tickets/TCK-1.md` still prints
   `bc5139f867319f08aeb7856797debaff0fc9ae566558be7fbc05dc7e9bd9c653`.
3. `06-06-SUMMARY.md` carries eleven record lines in the listed order plus exactly one line
   matching `^SKILL-04 verdict: (pass|fail)$`.
4. `node packages/cli/dist/cli.js skills sync` prints `unchanged` on every line — nothing under
   `.claude/skills/` was edited while being read.
5. `.planning/REQUIREMENTS.md` SKILL-04 state agrees with the Task 2 verdict, and CLI-08 is still
   ticked and Complete.
6. `git status --porcelain` — the working tree carries the changes and nothing is committed.
</verification>

<success_criteria>
- ROADMAP criterion 7's manual half has been performed once and its result recorded either way.
- ROADMAP criterion 3's second clause has been read across all eleven rendered files with one
  recorded line per file.
- SKILL-04 is closed if and only if that read passed, and the SUMMARY says which happened.
- The wrong-plan fixture is byte-unchanged.
- Nothing is committed.
</success_criteria>

## Flagged Assumptions

Three of the spec-less edge probe's 17 rows are authored into this plan's `must_haves.truths` as
structured backstop markers — SKILL-04's `empty`, `adjacency` and `ordering` rows. They are backstop
rather than explicit because the check is a human read: there is no green/red command that can
observe "this sentence points at the command rather than reproducing it". The remaining fourteen
rows are dispositioned in plan 06-05 (three authored, eleven flagged), and the no-silent-drop
equality — 17 = 6 authored + 11 flagged — holds across the two plans together.

Two further assumptions this plan makes explicit:

- **The eleven rendered files are not all on disk in this repository.** `accord/config.yml` declares
  `roles: [ba, dev]`, so `.claude/skills/accord-designer/` is never written here. Nine of the eleven
  are read as rendered files; the tenth is read from its source (`packages/core/skills/designer/SKILL.md`,
  whose body is the rendered body minus the two stripped authoring keys) and the eleventh inherits
  the dev copy's verdict under the byte-identity assertion at `skills.test.ts:389`. Task 2 requires
  both facts to be stated in the record lines rather than assumed silently. 06-VERIFICATION.md's
  `advisory` block already records the single-role roster as a dogfooding choice, not a defect.
- **Nothing in Phase 6 is committed.** Every path this plan reads exists as uncommitted
  working-tree state, per the owner's standing rule. The only tracked files it touches are
  `.planning/REQUIREMENTS.md` (written) and `docs/design.md` plus `packages/core/src/lint/rules.ts`
  (read-only).

## Artifacts this phase produces

This plan creates no source symbol and no source file. What it produces is evidence:

**New files**
- `.planning/phases/06-skills/06-06-SUMMARY.md` — the before/after `## Plan`, the eleven record
  lines, and the SKILL-04 outcome

**Modified**
- `.planning/REQUIREMENTS.md` — two lines, conditional on Task 2's verdict

**No new exported functions, no new test files, no new test cases.** Both verification items are
deliberately outside the test suite: the `No API keys` constraint forbids running a model inside it,
and 06-VALIDATION.md designates the eleven-file read Manual-Only because a keyword matcher would
flag a skill for naming `## Open questions` at all.

<output>
Create `.planning/phases/06-skills/06-06-SUMMARY.md` when done.

It must carry, in this order: Task 1's BEFORE and AFTER `## Plan` verbatim with the pass/fail
verdict and the post-run fixture hash; Task 2's eleven record lines in the listed order, followed by
exactly one line reading `SKILL-04 verdict: pass` or `SKILL-04 verdict: fail` and nothing else on
that line; and Task 3's one-line statement of whether SKILL-04 was ticked and why. State explicitly
that nothing was committed.
</output>
