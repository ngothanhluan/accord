---
phase: 01-workspace-and-formats
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .claude/CLAUDE.md
  - README.md
  - docs/design.md
  - packages/core/test/convention.test.ts
autonomous: true
requirements: [FMT-01, OPS-01, FMT-02, FMT-06, FMT-07]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README.md and docs/design.md §2 describe the fixed root `accord/` containing `config.yml`, `product/`, `tickets/<id>.md`, `tickets/<id>/verification.md`, `assets/<id>/prototype.html`; neither shows a per-epic folder; grouping is `parent:`; tracker links are a map keyed by adapter (FMT-01, D-06, D-12; success criterion 5)"
    - "REQUIREMENTS.md FMT-01, FMT-02, FMT-06, FMT-07, OPS-01, SKILL-01, SKILL-06, SKILL-07, GATE-02, GATE-05, LINT-05 read as specified in CONTEXT.md 'Requirement text updates': fixed root, D-04 field set with `verified`, `type` `epic|story|bug`, `status` `draft|open|archived`, no design source, roster `ba|dev|designer`, seven templates, three workspaces, three skill definitions, dev-run fresh-context review, developer-owned `verified`"
    - "PROJECT.md Key Decisions and Constraints say three workspaces (`core`, `cli`, `mcp`), `epic` instead of `feature`, developer self-test `verified` ticks with QA recording in the tracker, and three roster roles (D-01, D-03, D-13, D-14, D-18)"
    - "docs/design.md §4 lists roster roles `ba`, `dev`, `designer`; states QA verifies on the dev environment and records in the tracker, the lead's readiness review is a BA-skill step, and the reviewer is the dev skill's fresh-context `review.md` step that alone writes `tickets/<id>/verification.md` (D-13, D-14)"
    - "docs/design.md §5 Done describes the three-set match between scenario tags, evidence tags in `tickets/<id>/verification.md`, and `verified` (D-03, D-09)"
    - "ROADMAP.md Phase 1 goal says three-workspace and criterion 5 says fixed root `accord/`; no ROADMAP success criterion still names the retired tick key, six skill definitions, or a QA skill (D-03, D-13, D-14)"
    - "The convention test asserts the documentation truths above by grep and runs in `npm test` (FMT-01)"
  artifacts:
    - path: "README.md"
      provides: "public description of the folder convention, roles, gates"
      contains: "tickets/<id>/verification.md"
    - path: "docs/design.md"
      provides: "§2 folder, §3 profiles, §4 roles, §5 gates updated to the CONTEXT vocabulary"
      contains: "verified"
    - path: ".planning/REQUIREMENTS.md"
      provides: "requirement texts after this phase"
      contains: "OPS-01**: Monorepo with npm workspaces: `core`, `cli`, `mcp`"
    - path: ".planning/PROJECT.md"
      provides: "constraints and key decisions after this phase"
      contains: "`core` / `cli` / `mcp`"
    - path: "packages/core/test/convention.test.ts"
      provides: "grep assertions over README.md and docs/design.md §2"
      min_lines: 25
  key_links:
    - from: "packages/core/test/convention.test.ts"
      to: "README.md"
      via: "readFileSync of the repo-root README"
      pattern: "README\\.md"
    - from: "packages/core/test/convention.test.ts"
      to: "docs/design.md"
      via: "readFileSync + slice between `## 2.` and `## 3.`"
      pattern: "design\\.md"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "Documentation must never describe the agent that wrote the code as the author of `verification.md`; the fresh-context review is the only writer (D-14)"
      status: resolved
      verification: judgment
    - statement: "No documentation may describe gate results, work status, comments, or estimates as stored in git; those stay in the tracker (D-02)"
      status: resolved
      verification: judgment
    - statement: "Edits to ROADMAP.md, REQUIREMENTS.md, and PROJECT.md are scoped `Edit` replacements only; never a whole-file `Write` that could drop other phases' entries"
      status: resolved
      verification: judgment
---

<objective>
**As a** team member reading the repository, **I want** the README, the design document, and the planning requirements to describe the same contract the schemas and templates now enforce, **so that** nobody follows a folder layout, role list, or tick key that the tooling rejects.

Purpose: CONTEXT.md changed vocabulary and shape in ways that made README.md, docs/design.md §2–§5, PROJECT.md, REQUIREMENTS.md, and the ROADMAP's own wording stale: `epic` replaces `feature` (D-01), `verified` replaces `qa.ticks` (D-03), three roster roles (D-13), the reviewer is a dev-skill step (D-14), fixed root `accord/` (D-12), three workspaces (D-18), `tickets/<id>/verification.md` (D-06). Success criterion 5 requires the folder convention to be documented. This plan makes the documents agree with the code and pins the documentation side with a grep test.

Output: updated REQUIREMENTS.md, PROJECT.md, ROADMAP.md (scoped edits), `.claude/CLAUDE.md` constraint line, README.md, docs/design.md; `packages/core/test/convention.test.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-workspace-and-formats/01-CONTEXT.md
@.planning/phases/01-workspace-and-formats/01-SKELETON.md
@README.md
@docs/design.md
@.claude/CLAUDE.md

<assumption_delta_decision>
Detected signal: ROADMAP Phase 1 success criterion 5 says "configurable root" (a chosen/optional root). Now-primary noun: the **fixed root `accord/`** (D-12). Decision: **no-change** to the code (the root is fixed; nothing is made configurable) and the stale ROADMAP/PROJECT/design wording is corrected in Task 1 and Task 2. Rationale: D-12 is a locked user decision and adding a `.accord` pointer file later is additive. Accepted debt: none. Invariant test: Task 3 asserts README.md and docs/design.md §2 state the root is `accord/` and do not call it configurable.
</assumption_delta_decision>
</context>

<!-- planner-discipline-allow: features/ -->
<!-- planner-discipline-allow: qa.ticks -->

<tasks>

<task type="auto">
  <name>Task 1: Planning artifacts — REQUIREMENTS.md, PROJECT.md, ROADMAP.md, CLAUDE.md constraint line</name>
  <read_first>
    - .planning/phases/01-workspace-and-formats/01-CONTEXT.md — "Requirement text updates" table (the authoritative list), D-01, D-02, D-03, D-13, D-14, D-18
    - .planning/REQUIREMENTS.md (FMT-01, FMT-02, FMT-06, FMT-07, OPS-01, SKILL-01, SKILL-06, SKILL-07, GATE-02, GATE-05, LINT-05 lines; leave the Out of Scope row about the per-epic folder as it is — it correctly describes an exclusion)
    - .planning/PROJECT.md (Active requirements bullets, Constraints "Tech stack", Key Decisions rows: feature/ticket, QA ticks, workflow definition per role, monorepo)
    - .planning/ROADMAP.md (Phase 1 Goal and criterion 5; Phase 3 criterion 4; Phase 4 criterion 2; Phase 6 criteria 1 and 4; Phase 9 criterion 3)
    - .claude/CLAUDE.md (`## Project` → Constraints → "Tech stack" line; this file is regenerated from PROJECT.md by GSD, so keep the edit to that one line)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .claude/CLAUDE.md</files>
  <action>
Use scoped `Edit` calls only (never `Write`) on these four files.

REQUIREMENTS.md — replace the requirement text after the bold ID, keeping the `- [ ] **ID**:` prefix:
  - FMT-01: `Folder convention with fixed root `accord/` containing `product/`, `tickets/`, `assets/<id>/`; no per-epic folder, grouping is `parent:`` (D-12).
  - FMT-02: `Ticket frontmatter validated by a JSON Schema 2020-12 file with `additionalProperties: false`: `id`, `title`, `type` (`epic` | `story` | `bug`), `status` (`draft` | `open` | `archived`), optional `parent`, `tracker` map, `ui`, `design`, `assumptions`, `ac_hash`, `verified`` (D-01..D-05).
  - FMT-06: `config.yml` with pinned accord version, profile (`build` | `maintain`), tracker adapter (`none` default; `github-issues` requires `repo`), design-token path, role roster (`ba` and `dev` required, `designer` optional), runtimes; validated by its own schema (D-13, D-15, D-16).
  - FMT-07: `Templates: `ticket-build.md`, `ticket-maintain.md`, `epic.md`, `glossary.md`, `business-rules.md`, prototype header, `verification.md``.
  - OPS-01: `Monorepo with npm workspaces: `core`, `cli`, `mcp`` (D-18).
  - SKILL-01: `One workflow definition per role as data: `ba`, `dev` (with a `review.md` reference for the fresh-context review step), `designer`` (D-13, D-14).
  - SKILL-06: `The dev workflow's final step runs the review in a fresh agent context that writes only `verification.md`; the context that wrote the code never writes it`.
  - SKILL-07: `Only the developer writes `verified` in frontmatter, as a self-test checklist run on the dev environment before the card goes to QA` (D-03).
  - GATE-02: `gate done` passes only when the scenario tag set, the evidence set in `verification.md`, and `verified` are equal.
  - GATE-05: `Author-mismatch warning when implementation and evidence share a git author; the CLI host supplies authors, the MCP host reports the check as skipped` (a developer ticking their own work is expected).
  - LINT-05: `Orphaned tick warning when `verified` names a tag with no scenario` (same rename as D-03; not in the CONTEXT table by ID but required by "`verified` replaces the old tick key").
  Append one line at the bottom `*Last updated:*` note: `2026-09-05 after Phase 1 planning (CONTEXT text updates applied)`.

PROJECT.md — scoped edits:
  - Constraints "Tech stack" bullet: `TypeScript monorepo — `core` (pure), `cli`, `mcp`; skill definitions live in `core` as data; distributed via npx`.
  - Active → "Folder and formats" first bullet: `Folder convention with fixed root `accord/`, `product/`, `tickets/`, `assets/<id>/`; a story or bug names its epic with `parent:``.
  - Active → "Done gate mechanics": the "Reviewer agent writes `verification.md`" bullet becomes `The dev workflow's final step opens a fresh agent context that writes `tickets/<id>/verification.md` mapping every scenario to evidence`; the "QA records a per-scenario tick" bullet becomes `The developer records a per-scenario self-test tick in `verified:` (last frontmatter key); QA verifies on the dev environment and records the result in the tracker; the ticket body (AC) stays BA-only`; the three-set bullet names `verified` instead of the ticked set.
  - Active → "Role workflows": the roles list becomes `(BA interview including the readiness review, designer attach design, dev implement slice then fresh-context review)`; delete the separate "Independent reviewer workflow" bullet or fold it into the dev bullet.
  - Key Decisions rows: "Feature and ticket are one file type; `parent:` groups" → `Epic, story, and bug are one file type (`type:`); `parent:` groups`; the "QA ticks live in ticket frontmatter" row → `Developer self-test ticks (`verified`) live in ticket frontmatter; evidence in `tickets/<id>/verification.md` written by a fresh review context; QA records in the tracker` with rationale `Evidence and confirmation in separate files with separate owners; git author is the proof; QA works where the team already works`; "One workflow definition per role… 4 runtimes × 5 roles" → `3 roles`; "Monorepo `core` / `cli` / `skills` / `mcp`" → `Monorepo `core` / `cli` / `mcp`` with rationale adding `skill definitions are data both hosts read, so they live in core`.
  - Out of Scope bullet "A separate `features/` folder — feature and ticket are the same unit of work" → keep the exclusion but say `epic and story are the same file type`.
  - Update the trailing `*Last updated:*` line to `2026-09-05 after Phase 1 planning (vocabulary: epic, verified, three roles, three workspaces)`.

ROADMAP.md — scoped edits, this phase's own contract first: Phase 1 `**Goal**:` "four-package monorepo" → "three-workspace monorepo"; Phase 1 criterion 5 "configurable root containing" → "fixed root `accord/` containing". Then the same rename elsewhere so the roadmap does not contradict REQUIREMENTS: Phase 3 criterion 4 "a `qa.ticks` entry naming a tag" → "a `verified` entry naming a tag"; Phase 4 criterion 2 "and `qa.ticks` are equal" → "and `verified` are equal"; Phase 6 criterion 1 "Six definitions (BA, designer, dev, QA, lead, reviewer)" → "Three definitions (`ba`, `dev` with `review.md`, `designer`)"; Phase 6 criterion 4 "the reviewer skill runs in a fresh context and writes only `verification.md`; the QA skill writes only `qa.ticks`" → "the dev skill's review step runs in a fresh context and writes only `verification.md`; only the developer writes `verified`"; Phase 9 criterion 3 "an independent reviewer's `verification.md`, and QA ticks" → "a fresh-context review's `verification.md`, and the developer's `verified` ticks". Do not touch the Plans list under Phase 1 (the planner maintains it) or any other line.

.claude/CLAUDE.md — one scoped edit in `## Project` → Constraints: the "Tech stack" bullet becomes the same text as PROJECT.md's.

Leave everything uncommitted.
  </action>
  <verify>
    <automated>grep -q 'OPS-01\*\*: Monorepo with npm workspaces: `core`, `cli`, `mcp`' .planning/REQUIREMENTS.md && ! grep -q 'qa.ticks' .planning/REQUIREMENTS.md && ! grep -q 'qa.ticks' .planning/ROADMAP.md && ! grep -q 'qa.ticks' .planning/PROJECT.md && grep -q 'three-workspace monorepo' .planning/ROADMAP.md && grep -q 'fixed root `accord/`' .planning/ROADMAP.md && grep -q '`core` / `cli` / `mcp`' .planning/PROJECT.md && ! grep -q 'skills`, `mcp`' .claude/CLAUDE.md</automated>
    <fails_when>non-zero exit (a required phrase is missing or a retired phrase is still present in one of the four files)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `fixed root \`accord/\``, `\`verified\``, `\`epic\` | \`story\` | \`bug\``, `\`ba\` and \`dev\` required, \`designer\` optional`, `\`ticket-build.md\`, \`ticket-maintain.md\`, \`epic.md\``, and `with a \`review.md\` reference`; `grep -c 'qa.ticks' .planning/REQUIREMENTS.md` prints 0; `grep -c 'design source' .planning/REQUIREMENTS.md` prints 0; the Traceability table and every other requirement line are unchanged (`git diff --stat .planning/REQUIREMENTS.md` touches only the edited lines)
    - `.planning/PROJECT.md` contains `\`core\` / \`cli\` / \`mcp\``, `fixed root \`accord/\``, `\`verified\``, and `3 roles`; `grep -c 'qa.ticks' .planning/PROJECT.md` prints 0; `grep -c 'skills\` / \`mcp\`' .planning/PROJECT.md` prints 0
    - `.planning/ROADMAP.md` Phase 1 entry contains `three-workspace monorepo` and `fixed root \`accord/\``; `grep -c 'qa.ticks' .planning/ROADMAP.md` prints 0; `grep -c 'Six definitions' .planning/ROADMAP.md` prints 0; the `Plans:` list under Phase 1 and all `### Phase N:` headers are unchanged
    - `.claude/CLAUDE.md` Tech stack constraint names `core` (pure), `cli`, `mcp` and not `skills`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The planning artifacts state the contract Phase 1 built; no retired vocabulary remains in REQUIREMENTS, PROJECT, or ROADMAP.</done>
</task>

<task type="auto">
  <name>Task 2: README.md and docs/design.md §2–§5</name>
  <read_first>
    - README.md (whole file, 40 lines) and docs/design.md §2 "Folder convention", §3 "Profiles", §4 "Roles and ownership", §5 "Gates" (lines 24–88)
    - .planning/phases/01-workspace-and-formats/01-CONTEXT.md — D-01, D-02, D-03, D-06, D-07, D-09, D-11, D-12, D-13, D-14, D-16; "Requirement text updates" last row; "Specific Ideas" (QA on the dev environment, gate Done PASS moves the card to QA)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md — "Folder convention the templates must reflect" tree (copy this tree shape into README and design.md §2)
    - .planning/PROJECT.md (Remote MCP server decision — the README's "Not a server" bullet is stale against it)
    - packages/core/templates/ticket-build.md if it already exists (Plan 01-04 may run in parallel; do not depend on it — describe the headings from D-07)
  </read_first>
  <files>README.md, docs/design.md</files>
  <action>
Use scoped `Edit` calls. Keep both documents short; this is a vocabulary and shape correction, not a rewrite.

README.md:
  - "What it is" first bullet: `A folder convention: a fixed `accord/` folder at the repo root with `config.yml`, `product/` (glossary, business rules), `tickets/<id>.md` (epics, stories, and bugs are all tickets; a story names its epic with `parent:`), `tickets/<id>/verification.md` (the review record), and `assets/<id>/` (designer-owned prototypes). Tracker links are a `tracker:` map keyed by adapter, for example `tracker: { shortcut: "1234" }`.` Remove the per-epic folder from the list.
  - Second bullet stays (EARS, Gherkin, YAML frontmatter with a JSON schema) but add: `fixed headings per ticket: Intent, Requirements, Acceptance criteria, Open questions, Plan` (D-07).
  - "Two gates" bullet: `**Ready** before work starts, **Done** before the card goes to QA: a fresh agent context, not the one that wrote the code, writes `tickets/<id>/verification.md`; the developer ticks `verified:` after a self-test; Done passes only when scenario tags, evidence tags, and `verified` match` (D-03, D-09, D-14).
  - Role workflows bullet: `Role workflows (`ba`, `dev`, `designer`) shipped as skill files for Claude Code, Cursor, Copilot, and Codex; QA verifies on the dev environment and records results in the tracker` (D-13; four runtimes per PROJECT.md).
  - First paragraph "a business analyst, designer, developer, and QA write down" → `a business analyst, designer, and developer write down …; QA verifies against the same acceptance criteria`.
  - "What it is not" third bullet ("Not a server…") → `Not a database. Git is the only source of truth; a stateless remote MCP server lets non-technical members work the same tickets from their chat app over the GitHub API` (PROJECT.md decision "Remote MCP server is the non-tech frontend" — a factual correction beyond the CONTEXT table, noted in the SUMMARY).
  - Keep the Status line and License.

docs/design.md §2 "Folder convention":
  - First sentence → `The root folder is always `accord/`. It is not configurable; a `.accord` pointer file may be added later as an additive change.` (D-12)
  - Replace the tree with: `accord/` → `config.yml` (`# version pin, profile, tracker adapter, token path, roles, runtimes`), `product/glossary.md`, `product/business-rules.md`, `tickets/<id>.md` (`# one file per epic, story, or bug`), `tickets/<id>/verification.md` (`# review record, written by the fresh review context`), `assets/<id>/prototype.html` (`# designer-owned`). No per-epic folder line.
  - Paragraph after the tree → `Flat files, not nested folders. Epics, stories, and bugs share one file type (`type: epic | story | bug`); a story or bug names its epic with `parent:`. An epic holds intent and requirements for its children and is linted but never gated. This mirrors every tracker's model and keeps indexing trivial.` (D-01)
  - Frontmatter sentence → `Every ticket has YAML frontmatter validated against `ticket.schema.json`: `id`, `title`, `type`, `status` (`draft | open | archived`, document lifecycle only — work status stays in the tracker), optional `parent`, `tracker` (map keyed by adapter), `ui`, `design`, `assumptions`, `ac_hash`, and `verified` (always the last key). Unknown keys are errors, so tracker-owned data cannot leak into git.` (D-02, D-04, D-05)
  - Add one sentence on body structure: `The body has fixed headings in fixed order: `## Intent`, `## Requirements`, `## Acceptance criteria`, `## Open questions` (BA-owned), then `## Plan` (developer-owned, last). Epics omit Acceptance criteria and Plan.` (D-07)
  - Keep "Formats", "Size discipline", "No binary assets" as they are.

docs/design.md §3 "Profiles" (D-01 says `feature` → `epic` everywhere): "one feature file per epic (5 to 10)" → "one epic ticket per epic (5 to 10)"; "Feature files are optional; an epic tagged `feature` re-enables them. At handover, feature specs are folded into `product/business-rules.md` and archived." → "Epic tickets are optional. At handover, epic intent and requirements are folded into `product/business-rules.md` and the epics are set to `status: archived`."

docs/design.md §4 "Roles and ownership":
  - First line → `The roster is `roles:` in `config.yml`: `ba` and `dev` are required, `designer` is optional. QA and lead are not roster roles: QA verifies on the dev environment and records results in the tracker; the lead's readiness review is a step inside the BA workflow. The roster only decides which skills are rendered; it never changes a gate result.` (D-13)
  - Keep the BA, Designer, Dev bullets (Dev: "Never adds a requirement. A gap goes to `## Open questions` and work stops until the BA answers.").
  - QA bullet → `**QA** (outside the repo) tests against the acceptance criteria on the dev environment after Done passes and records the result in the tracker; suggests missing scenarios to the BA, never by editing.`
  - Lead bullet → fold into the BA bullet as `The lead's readiness review happens inside the BA workflow before Ready.`
  - Add a reviewer paragraph: `The reviewer is not a role. The dev workflow's last step opens a fresh agent context (a subagent in Claude Code, a new chat in Cursor or Codex) and hands it `review.md`; only that fresh context writes `tickets/<id>/verification.md`. The agent that wrote the code never does. This is procedural, not tool-enforced, so the skill states it plainly.` (D-14)
  - Last line → `Role workflows ship as skill files (Claude Code, Cursor, Copilot, Codex) …`.

docs/design.md §5 "Gates":
  - Ready table last row "Decisions without `confirmed_by`: warning, not a block | Lead" → `Unchecked `## Open questions` items or unconfirmed `assumptions:` entries block Ready | BA`.
  - Done paragraph → `**Done** (a story may go to QA). The dev workflow's final step opens a fresh agent context that produces `tickets/<id>/verification.md`, one `## @ac-n` block per scenario with `Result:` and `Evidence:`. The developer runs the scenarios on the dev environment and ticks `verified: [ac-1, ...]` in the ticket's frontmatter. Done passes only when the scenario tag set, the evidence tag set, and `verified` are the same set, and the acceptance criteria hash still matches the one recorded at Ready. QA then tests on the dev environment and records the outcome in the tracker. This is the part that addresses the 36% failure mode.` (D-03, D-09)

Leave everything uncommitted.
  </action>
  <verify>
    <automated>grep -q 'tickets/<id>/verification.md' README.md && ! grep -q 'features/' README.md && grep -q 'verified' README.md && grep -q 'It is not configurable' docs/design.md && ! sed -n '/^## 2\./,/^## 3\./p' docs/design.md | grep -q 'features/' && grep -q 'review.md' docs/design.md && grep -q 'one epic ticket per epic' docs/design.md</automated>
    <fails_when>non-zero exit (a required phrase is missing from README.md or docs/design.md, or the legacy folder name still appears in README.md or in design.md §2)</fails_when>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `accord/`, `product/`, `tickets/<id>.md`, `tickets/<id>/verification.md`, `assets/<id>/`, `parent:`, `tracker: { shortcut: "1234" }`, `verified`, `Codex`; `grep -c 'features/' README.md` prints 0; `grep -c 'Not a server' README.md` prints 0
    - `docs/design.md` §2 (between `## 2.` and `## 3.`) contains `always \`accord/\``, `It is not configurable`, `tickets/<id>/verification.md`, `type: epic | story | bug`, `parent:`, `## Plan`; that slice does not contain `features/`, `overridable`, `owner`, or `tracker_ids`
    - `docs/design.md` §3 contains `one epic ticket per epic` and `status: archived`; `grep -c 'feature file' docs/design.md` prints 0 within §2–§5
    - `docs/design.md` §4 contains `\`ba\` and \`dev\` are required`, `\`designer\` is optional`, `review.md`, `fresh agent context`, and no longer lists `Lead` as a roster role
    - `docs/design.md` §5 contains `verified: [ac-1`, `same set`, and `recorded at Ready`; `grep -c 'QA ticks each scenario' docs/design.md` prints 0
    - §1, §6, §7, §8, §9 of docs/design.md are unchanged (`git diff docs/design.md` shows hunks only between the `## 2.` and `## 6.` headings)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>README and design.md describe the fixed root, the ticket types, the three roster roles, the fresh-context review, and the `verified` three-set match; the historical decisions log is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Convention test over README.md and design.md §2</name>
  <read_first>
    - README.md and docs/design.md (as edited in Task 2 — the exact phrases to pin)
    - packages/core/tsconfig.test.json (tests may import `node:fs`, `node:url`)
    - .planning/phases/01-workspace-and-formats/01-RESEARCH.md Validation Architecture row FMT-01
  </read_first>
  <files>packages/core/test/convention.test.ts</files>
  <action>
Create `packages/core/test/convention.test.ts` (imports: `vitest`, `node:fs`, `node:url`). Resolve `repoRoot = new URL('../../../', import.meta.url)`; read `README.md` and `docs/design.md`; slice `section2` as the text between the first line starting `## 2.` and the first line starting `## 3.`; fail loudly (`expect(section2.length).toBeGreaterThan(200)`) if the slice is empty so a heading rename cannot make the test vacuously pass.

Tests (FMT-01, success criterion 5):
  1. `it('README documents the folder convention')` — README contains each of `accord/`, `product/`, `tickets/<id>.md`, `tickets/<id>/verification.md`, `assets/<id>/`, `parent:`, and a `tracker:` map example; and does NOT match the legacy per-epic folder regex built from `'features' + '/'`.
  2. `it('design.md §2 documents the fixed root and the ticket types')` — `section2` contains `always \`accord/\``, `It is not configurable`, `type: epic | story | bug`, `tickets/<id>/verification.md`, `parent:`, `verified`; and does NOT match the legacy folder regex nor the word `overridable`.
  3. `it('documentation names the developer tick key and the fresh-context review')` — README contains `verified`; `docs/design.md` contains `review.md` and `fresh agent context`.
  4. `it('no retired vocabulary in README or design §2–§5')` — build the retired tick key from `'qa' + '.ticks'` and assert neither README nor the §2–§5 slice contains it; assert the §4 slice does not contain `Lead**` as a bullet role.

Run `npm test -- --project core convention`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm test -- --project core convention</automated>
    <fails_when>non-zero exit; or `No test files found`; or fewer than 4 tests pass for `|core|`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/convention.test.ts` contains `README.md`, `docs/design.md`, `## 2.`, `## 3.`, `'features' + '/'`, `'qa' + '.ticks'`, `tickets/<id>/verification.md`, and `It is not configurable`
    - `npm test -- --project core convention` reports 4 passing tests and 0 failures
    - `npm run typecheck` and `npm run lint` still exit 0
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>Success criterion 5's documentation side is a green test that would go red if the README or design §2 regressed to the old layout or vocabulary.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Updated documents: `README.md`, `docs/design.md` (§2, §3, §4, §5), `.planning/REQUIREMENTS.md` (FMT-01, FMT-02, FMT-06, FMT-07, OPS-01, SKILL-01, SKILL-06, SKILL-07, GATE-02, GATE-05, LINT-05), `.planning/PROJECT.md` (Constraints, Active bullets, Key Decisions), `.planning/ROADMAP.md` (Phase 1 goal and criterion 5; rename in Phases 3, 4, 6, 9), `.claude/CLAUDE.md` (Tech stack constraint line)
- Test: `packages/core/test/convention.test.ts`

No new exported symbols.
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (spec-less probe rows surfaced, not silently dropped)

| Requirement | Probe | Disposition |
|---|---|---|
| FMT-01 | unclassified — review manually | Flagged: in Phase 1 the folder convention can only be asserted in documentation and templates (greps); a real repository layout is not loaded until the Phase 2 loader exists, so "the loader treats `tickets/<id>/` as appendices and `assets/<id>/` as designer-owned" (D-06) remains a documented rule, not a tested behaviour, until Phase 2. Also flagged: the ROADMAP rename in Phases 3, 4, 6, 9 goes beyond the CONTEXT table's file list; it is done so the roadmap does not contradict the updated REQUIREMENTS, and the author may revert it if they prefer to leave later phases' wording to their own discuss step. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → future executors | Stale or contradictory requirement text steers later phases wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Tampering | whole-file rewrite of ROADMAP.md / REQUIREMENTS.md dropping other phases' content | medium | mitigate | Scoped `Edit` only; acceptance criteria require `### Phase N:` headers, the Traceability table, and the Phase 1 Plans list to be unchanged |
| T-01-14 | Repudiation | documentation implying the code author may write `verification.md` | low | mitigate | Task 2 §4/§5 wording and Task 3 assertion on `fresh agent context` and `review.md` (D-14) |
| T-01-12 | Information disclosure | README describing internal employer details | low | accept | README changes name no employer, tracker account, or person; PROJECT.md context already lives under `.planning` |
| T-01-SC | Tampering | npm installs | high | mitigate | No packages touched |
</threat_model>

<verification>
- `npm test -- --project core convention` green.
- `git diff --stat` shows only the seven files in `files_modified`.
- Manual read-through of README.md renders the folder tree and role list correctly on GitHub (human-check at end of phase).
</verification>

<success_criteria>
- Success criterion 5: the folder convention is documented (README, design §2) with fixed root, no per-epic folder, `parent:` grouping, tracker map; pinned by a test.
- CONTEXT "Requirement text updates" applied to REQUIREMENTS.md, PROJECT.md, README.md, design.md §2/§4/§5 (§3 for the D-01 rename), and the ROADMAP's own Phase 1 wording.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-and-formats/01-05-SUMMARY.md` when done. Do not commit.
</output>
