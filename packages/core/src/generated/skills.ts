// Generated by scripts/gen-skills.mjs from skills/**/*.md (D-105). Do not edit.
export const skills = {
  "ba/SKILL.md": "---\nname: accord-ba\ndescription: Use when turning an idea into a ticket an agent can implement, or when standing up a new project's product documents. Walks from intent through EARS requirements to tagged Gherkin scenarios, and has a fresh context check that the criteria cover the intent before the ticket is called ready.\nlicense: MIT\nkind: role\nloads: [ba/setup.md, ba/story.md, ba/ready.md]\n---\n\nBA is a stage a ticket passes through, not a job title. The same person and\nagent move through intent, design, and implementation in turn. This stage ends\nwhen `accord gate ready <id>` passes.\n\n## Steps\n\n1. Run `accord gate ready <id>` for a ticket that already exists, or\n   `accord new ticket <id>` for one that does not. What the gate prints is the\n   list of what is still missing, and that list is the brief for this stage.\n\n2. Read `profile:` in `accord/config.yml` and take the branch it selects.\n\n   - `config.profile` is `build` and `accord/product/` is still empty: this is\n     week zero, and the work is project and epic setup. Read `./setup.md`.\n   - Anything else: this is per-story work. Read `./story.md`.\n\n3. A ticket is not started before `accord gate ready <id>` passes. Run it and\n   work from the list it prints rather than deciding for yourself what is\n   still outstanding.\n\n   A ticket you are still writing stays `status: draft`. Setting it is yours.\n\n4. `## Acceptance criteria` are yours and nobody else edits them. Editing them\n   after `accord gate ready <id>` has passed has a consequence\n   `accord gate done <id>` reports; let it report it rather than working\n   around it.\n\n5. To pick work back up, read the ticket and run `accord status`, which shows\n   where every ticket in the repository stands.\n\n## Boundaries\n\nYou write the intent, the requirements, the scenarios, and the questions.\n`## Plan` belongs to the stage that implements, `verified:` to the person who\nran the scenarios on the dev environment, and the design reference to the\nstage that produces it.\n",
  "ba/ready.md": "---\nkind: reference\n---\n\n# Reviewing a story for readiness\n\nYou are a fresh context. You did not write this ticket, and that is the whole\nreason you were opened: whoever wrote the acceptance criteria already believes\nthey cover the intent, or they would have written different ones.\n\nYou receive a ticket id. Everything else you read for yourself.\n\n## 1. Read, and read only this\n\n1. The ticket's `## Intent`.\n2. Its `## Requirements` — the EARS lines.\n3. Its `## Acceptance criteria` — the tagged scenarios.\n\nRead no code. No implementation exists yet, and a criterion judged against\ncode is a criterion bent to fit what someone already plans to build.\n\n## 2. Ask the one question a gate cannot\n\nDo the acceptance criteria cover the intent?\n\nWhether a scenario is present at all is `accord gate ready <id>`'s question\nand it prints its own reasons. Yours is narrower and harder. Look for:\n\n- intent the criteria never reach — a purpose stated in section 1 that no\n  scenario would demonstrate;\n- a requirement no scenario exercises;\n- an edge the criteria assume away — the empty case, the zero, the duplicate,\n  the second attempt, the thing that was already there;\n- business logic nobody decided — a threshold, a rounding rule, a tolerance,\n  or an ordering that the criteria commit to without the ticket ever saying\n  it was chosen;\n- a criterion that describes an implementation rather than an outcome.\n\n## 3. Write into `## Open questions`, and nowhere else\n\nOne unchecked `- [ ]` item per finding, phrased as the question you would ask\nif the author were in the room. Name the gap, not the fix.\n\nYou write only into `## Open questions`. Not the intent, not the requirements,\nnot the criteria — those belong to the person this review is for, and a\nreviewer that rewrites has rejoined the work it was brought in to judge.\n`No findings.` written as a sentence to the author is a valid result; padding\nthe list is not.\n\n## 4. Hand it back\n\nSay that the review is written and where. Answering the questions and re-running\n`accord gate ready <id>` is the author's, not yours.\n",
  "ba/setup.md": "---\nkind: reference\n---\n\n# Week zero: the product documents and the epics\n\nThis is the `build` profile branch, and it runs once. A new project starts\nwith nothing written down, so the first work is not a story — it is the\nlanguage the stories will be written in and the epics they will hang off.\nWhen this is done, every later ticket takes the `./story.md` branch.\n\n## 1. `accord/product/glossary.md`\n\nOne entry per term the project's language depends on: the word the team\nactually uses, and what it means to the business, in one sentence. Tickets and\nagents then use those words verbatim instead of each story redefining them.\n\nOnly the terms that are genuinely load-bearing. A glossary that defines\neverything is read by nobody.\n\n## 2. `accord/product/business-rules.md`\n\nOne bullet per rule, threshold, tolerance, rounding convention, or edge case\nthat is already decided. A story that depends on a rule cites it instead of\nrestating it, so the rule has one home and one wording.\n\nWhen a rule was chosen over an alternative, record the loser directly beneath\nthe rule it explains:\n\n- Amounts are rounded to two decimal places, half away from zero\n  - `Rejected: banker's rounding — the finance team reconciles against a\n    system that rounds half away from zero`\n\nWhoever meets the rule next then finds the argument instead of re-proposing the\noption that already lost. Nothing validates the line and no gate reads it; it\nis a convention, and its whole value is that the reasoning outlives the person\nwho had it.\n\n## 3. One epic ticket per epic\n\nRun `accord new ticket <id> --type epic` for each one, and fill in `## Intent`\nand `## Requirements` only.\n\nAcceptance criteria are not written here. They are written per story, just\nbefore that story enters the sprint, by the `./story.md` branch. An epic is\nnever gated, so criteria written now have nothing to be checked against, and\ncriteria written weeks before the work are criteria written against a guess.\n\n## 4. Hand back\n\nWeek zero is finished when every term the epics use is in the glossary, every\ndecided rule is in the business rules, and every epic has a ticket. From then\non this stage opens on `./story.md`.\n",
  "ba/story.md": "---\nkind: reference\n---\n\n# Writing one story\n\nThis is the per-story branch. It fills one ticket, in this order, and ends\nwhen `accord gate ready <id>` passes.\n\nSize discipline first, because it governs everything below: about five lines\nof intent, five to fifteen requirement lines, two to five scenarios. If the\nspec is longer than the code it describes, the spec is wrong.\n\n## 1. `## Intent`\n\nWhy this story exists, for whom, how anyone would know it worked, and what it\ndeliberately does not cover. In business language: never name tables,\nendpoints, libraries, or screens. A term the business uses belongs in\n`accord/product/glossary.md`, and a rule already decided belongs in\n`accord/product/business-rules.md`; cite them here rather than restating them.\n\n## 2. `## Requirements`\n\nOne EARS line per requirement, each in one of six shapes. Write the shape the\nrequirement actually is — the classifier reads the keywords, and a line that\nis none of the six is reported by `accord lint` at its line number.\n\n- Ubiquitous, always true:\n  `The system shall record the time every approval was granted.`\n- State-driven, true while something holds:\n  `WHILE an order is unpaid, the system shall show its outstanding balance.`\n- Event-driven, triggered by something happening:\n  `WHEN a customer submits a refund request, the system shall send them a\n  confirmation.`\n- Unwanted behaviour, an undesired condition and its response:\n  `IF the payment provider returns an error, THEN the system shall keep the\n  order unpaid and show the provider's message.`\n- Optional feature, true only where a feature is present:\n  `WHERE the tenant has multi-currency enabled, the system shall display\n  amounts in the tenant's default currency.`\n- Complex, more than one of the above on one line:\n  `WHILE a promotion is running, WHEN a customer applies a second discount\n  code, the system shall reject the second code.`\n\n## 3. `## Acceptance criteria`\n\nGherkin in one fenced block, every scenario tagged `@ac-1`, `@ac-2`, and so\non, with a unique number each. Two to five scenarios for a story.\n\nWrite each scenario as an outcome someone could watch happen, not as a\ndescription of how the code will do it. A bug fix is written as Given the\nsituation, When the action, Then the correct result. A Scenario Outline with\nthree examples is one scenario, not three.\n\n## 4. `## Open questions` and `assumptions:`\n\nAnything still undecided goes into one of these two and never into a guess: an\nunchecked `- [ ]` item under `## Open questions`, or an entry under\n`assumptions:` in the frontmatter with `confirmed: false`. Writing them down\nrather than resolving them in your head is the point.\n\nThis applies hardest to business logic: a threshold, a rounding rule, a\ntolerance, or what happens at zero. Whoever owns the business decides those.\nYou record the question.\n\n## 5. The readiness review\n\nHand a fresh context `./ready.md` and nothing else. Do not run it inline: you\nwould be grading acceptance criteria you had just written, which is the one\nthing the separate context exists to prevent.\n\nAnswer the questions it writes into `## Open questions`, then continue.\n\n## 6. The gate\n\nRun `accord gate ready <id>` and fix what it names. Then run `accord lint` and\nread what it reports.\n\nOnce `accord gate ready <id>` passes, the acceptance criteria are recorded as\nthey stand. Changing them afterwards has a consequence `accord gate done <id>`\nreports, so a scenario that turns out to be wrong is re-opened deliberately,\nnot edited in passing.\n",
  "designer/SKILL.md": "---\nname: accord-designer\ndescription: Use when a ticket is marked ui and the Ready gate is asking for a design reference. Establishes whether the ticket has an interface, picks a design link or a prototype from the profile, produces the missing one, and re-runs the gate.\nlicense: MIT\nkind: role\nloads: [shared/prototype.md]\n---\n\nDesigner is a stage a ticket passes through, not a job title. The same person\nand agent move through intent, design, and implementation in turn. This stage\nends when the ticket carries the design reference the gate was asking for.\n\n## Steps\n\n1. Run `accord gate ready <id>` and read the reasons it prints. Those reasons\n   are the whole brief for this stage. If none of them is about design, there\n   is nothing to do here — hand the ticket back to the stage that owns the\n   reason rather than inventing work.\n\n2. Read `ui:` in the frontmatter of `accord/tickets/<id>.md`.\n\n   - `ui: false` — stop. A ticket with no interface has no design reference to\n     produce.\n   - `ui: true` — continue.\n\n3. Read `profile:` in `accord/config.yml` and take the matching branch.\n\n   - `profile: build` — the ticket needs a design link or a prototype. When\n     the design already exists, put its link in `design:` in the ticket\n     frontmatter. When it does not, read `./prototype.md` and build one.\n   - `profile: maintain` — the ticket needs\n     `accord/assets/<id>/prototype.html`. Read `./prototype.md` and build it\n     from the styles the project already has. A prototype invented from new\n     styles is a redesign nobody asked for.\n\n4. Run `accord gate ready <id>` again. The reason you came here for is gone,\n   or the work is not finished.\n\n## Boundaries\n\nYou own the design reference and nothing else. Acceptance criteria belong to\nthe stage that wrote them: a scenario you disagree with goes to\n`## Open questions` in the ticket, never into an edit of\n`## Acceptance criteria`.\n",
  "dev/SKILL.md": "---\nname: accord-dev\ndescription: Use when implementing a ticket that has passed Ready. Plans the change as vertical slices, has the plan reviewed before any code is written, and hands the finished change to a context that never wrote it.\nlicense: MIT\nkind: role\nloads: [dev/debug.md, dev/review.md, dev/code-review.md, shared/prototype.md]\n---\n\nDev is a stage a ticket passes through, not a job title. The same person and\nagent move through intent, design, and implementation in turn. This stage ends\nwhen the ticket carries independent evidence that the change does what the\nacceptance criteria say.\n\n## Steps\n\n1. Run `accord gate ready <id>`. Do not start until it passes.\n   Each reason it prints belongs to the stage that owns it; hand the ticket\n   back rather than answering the reason yourself.\n\n2. If the ticket is `type: bug`, or if anything behaves unexpectedly at any\n   later step, read `./debug.md` before changing code. The ticket is the\n   session — no debugging artifact is created.\n\n3. Write `## Plan` as vertical slices. One step per `@ac-n` by default; merge\n   steps where one change serves several scenarios, and split a scenario into\n   several steps where it needs them. There is no ceiling on the number of\n   steps. Run `accord lint` and fix what it reports about the plan.\n\n   For a `ui: true` ticket, read `./prototype.md` before planning the steps\n   that touch the interface.\n\n4. Have the plan reviewed in a fresh context, before any code is written.\n\n   Open a fresh context — a subagent where the runtime provides one, otherwise\n   a new session — and hand it the ticket's `## Intent`, its\n   `## Acceptance criteria`, and its `## Plan`, and nothing else. Its brief is\n   five lines:\n\n   - Read no code. You are judging a plan, not an implementation.\n   - Edit only `## Plan`. Leave every other section of the ticket alone.\n   - Look for the wrong layer: steps that are technical layers when they\n     should be slices a scenario can be seen working through.\n   - Look for the wrong order: a step placed before the step whose output it\n     needs, and any other missing dependency.\n   - Look for an approach the acceptance criteria will not survive.\n\n   Whether every scenario is covered is `accord lint`'s question, not this\n   one's.\n\n5. Implement, one commit per plan step.\n\n6. If a gap appears — something the ticket does not answer — write the question\n   into `## Open questions` and stop. Never invent the answer.\n\n   Then run `accord gate ready <id>` again and work from what it prints.\n\n7. Have the code reviewed in a fresh context. Open a fresh context — a subagent\n   where the runtime provides one, otherwise a new session — and hand it\n   `./review.md`. The context that wrote the code never writes\n   `verification.md`.\n\n8. Run the scenarios yourself on the dev environment. Tick `verified:` in the\n   ticket frontmatter, and write one line per scenario under\n   `## Verification notes`. You tick `verified:` and nobody else does — not the\n   review context, not the person who wrote the acceptance criteria.\n\n9. Run `accord gate done <id>`.\n\n## Boundaries\n\nYou implement from the intent, the acceptance criteria, the design reference,\nand the plan. You never add a requirement and you never edit\n`## Acceptance criteria`: a scenario you disagree with is a question for\n`## Open questions`, and work stops there until it is answered.\n",
  "dev/code-review.md": "---\nkind: technique\n---\n\n# Technique: code review\n\nA technique, not a role. The fresh review context loads it after writing the\nper-scenario blocks of `tickets/<id>/verification.md`, and appends its findings\nto the same file under `## Review`.\n\nThe reviewer reads the ticket first and the diff second. A review anchored in\nthe acceptance criteria catches \"this ships the wrong feature\"; a review that\nstarts from the diff only ever catches taste.\n\n## Two different questions\n\nDo not merge these. They fail in different directions.\n\n| | Asks | Lands in |\n|---|---|---|\n| **Verification** | Does it do what the acceptance criteria say? | `## @ac-n` blocks |\n| **Review** | Is what it does sound? | `## Review` |\n\nCode can satisfy every scenario and still be wrong — a passing AC says nothing\nabout the case nobody wrote a scenario for. Code can also be immaculate and\nimplement the wrong thing. Both questions get asked, separately.\n\n## Who runs it\n\nThe context that wrote the code never reviews it. Not because a different model\nis better, but because a context cannot fairly audit a decision it does not\nremember making. This is procedural — nothing in the CLI enforces it — so the\n`dev` workflow states it plainly and opens a new context.\n\n## Scope\n\nOnly the diff for this ticket, plus whatever must be read to judge it. Reviewing\nuntouched code is how a review turns into a refactor.\n\n## The evidence rule\n\n**Every finding names a concrete failure: specific input or state, and the\nwrong output, crash, or corruption that follows. A finding without one is\ndeleted, not softened.**\n\nThis single rule is what separates a review from a list of opinions. \"This could\nbe more robust\" is not a finding. \"`parseAmount('1e3')` returns 1000 because the\nYAML core schema coerces it, so a ticket id of `1e3` silently becomes a number\"\nis a finding.\n\nIf a failure scenario cannot be constructed, the finding was a preference.\n\n## What to look for\n\nOrdered by how often it actually bites. Stop when the diff is genuinely covered;\ninventing findings to fill the list is its own failure mode.\n\n1. **Wrong behaviour at the edges the scenarios miss.** Empty, zero, one,\n   negative, null, absent, duplicate, out-of-order, maximum. The scenarios cover\n   the path someone thought about — the bugs live in the ones nobody did.\n2. **Business logic invented rather than specified.** A threshold, a rounding\n   rule, a tolerance, a retry count, or an edge-case behaviour that appears in\n   the code but in neither the requirements nor `product/business-rules.md`.\n   This is a finding every time, and it is raised, never quietly accepted: the\n   developer decides business logic.\n3. **State and lifetime.** Mutation of shared or caller-owned data, a resource\n   opened and not closed, an await missing, a race between two callers.\n4. **Error paths.** An error swallowed, a failure reported as success, an error\n   message that cannot be acted on, an empty catch.\n5. **Boundary trust.** Input from a file, a network call, or a user treated as\n   already validated. Never simplify away validation at a trust boundary.\n6. **Reuse missed.** The diff reimplements something that already exists in this\n   repo or in the standard library. Name the existing thing and its path.\n7. **The cheaper equivalent.** The same behaviour in materially less code, or a\n   dependency added for what a few lines do. Only worth raising when the\n   reduction is real, not stylistic.\n\n## What is not a finding\n\nNaming, formatting, and import order — the linter owns those or nobody does.\nPreferences phrased as principles. Speculative scale (\"this won't handle a\nmillion rows\") without a stated reason to expect a million rows. Anything\noutside the diff. \"While we're here\" improvements. Restating a rule the CLI\nalready enforces.\n\n## Severity\n\n| | Meaning | Effect |\n|---|---|---|\n| **blocking** | Data loss, a security hole, or a scenario that is wrong in a case the AC covers | The developer fixes it before ticking `verified` |\n| **finding** | A real defect outside AC coverage, or invented business logic | Recorded; the developer decides. Invented business logic is also raised in conversation |\n| **note** | A cheaper equivalent or a missed reuse | Recorded only |\n\nThe gate does not read severity. It cannot tell a real blocker from a\nmislabelled one, and a gate that trusts the reviewer's own grade of its own\noutput is not a gate. Severity is for the human.\n\n## Output\n\nAppend to `tickets/<id>/verification.md`:\n\n```markdown\n## Review\n\n### blocking — Rounding drops the final cent on odd totals\n`src/billing/split.ts:34` — `Math.round(total / n)` on `total: 1001, n: 2`\nreturns 500 and 500, losing one cent. Every odd total loses its remainder.\n\n### finding — Retry count not specified anywhere\n`src/sync/push.ts:12` — retries 3 times with no backoff. Neither the\nrequirements nor `product/business-rules.md` state a retry policy. Raised with\nthe developer; do not treat 3 as decided.\n```\n\nNothing found is a valid review. Write `No findings.` and stop — do not pad.\n\n## The reviewer does not fix\n\nIt reports. A reviewer that edits has re-entered the work it was brought in to\njudge, and there is then no independent context left to check the edit.\n",
  "dev/debug.md": "---\nkind: technique\n---\n\n# Technique: systematic debugging\n\nA technique, not a role. The `dev` workflow loads it whenever a ticket has\n`type: bug`, or whenever anything behaves unexpectedly mid-implementation.\nIt adds no artifact of its own: a bug is a ticket, and the ticket is the session.\n\n## The iron law\n\n```\nNO FIX WITHOUT A ROOT CAUSE FIRST\n```\n\nA symptom fix is a failure, even when the symptom goes away. Following the\nletter of this process while violating its spirit is also a failure.\n\n## Where this sits in the ticket\n\nA bug is a ticket with `type: bug`. Its acceptance criteria are Gherkin in the\nform *Given the situation, When the action, Then the correct result* — which\nmakes the reproduction and the regression test the same artifact. The first\nreal work on a bug is writing the scenario that reproduces it, not reading\ncode; `accord gate ready <id>` is what says whether the ticket is there yet.\n\nDebugging state lives in `## Plan` in the ticket. Resuming is `accord status`\nplus the ticket — that is the whole picture. When a context is lost, re-read\nthe ticket.\n\n## Phase 1 — Root cause\n\nComplete this before proposing any fix.\n\n1. **Read the error completely.** Full stack trace, line numbers, error codes.\n   The answer is in there more often than not.\n2. **Reproduce reliably.** Exact steps. Every time, or intermittently? Not\n   reproducible means gather more data — never guess.\n3. **Check what changed.** `git diff`, recent commits, new dependencies,\n   config, environment differences.\n4. **Instrument every boundary in a multi-component system.** For each\n   component boundary log what enters, what exits, and whether config and\n   environment propagated. Run once to find *where* it breaks, then investigate\n   only that component. Do not propose a fix during this step.\n5. **Trace backward to the source.** When the error is deep in the stack: where\n   did the bad value originate, what passed it, what called that. Keep going up\n   until you reach the original trigger. Fix there, not where it surfaced —\n   the guard belongs in the one function every caller routes through, not in\n   each caller.\n\n## Phase 2 — Pattern\n\n1. Find similar code in this repo that works.\n2. Read the reference implementation completely. Not skimmed — every line.\n3. List every difference between working and broken, however small. \"That\n   can't matter\" is how this phase fails.\n4. Name the dependencies, settings, and assumptions the broken path needs.\n\n## Phase 3 — Hypothesis\n\n1. **One hypothesis, written down:** \"X is the root cause because Y.\" Specific.\n2. **Test it with the smallest possible change.** One variable.\n3. Confirmed → Phase 4. Not confirmed → a *new* hypothesis, never another fix\n   stacked on the last one.\n4. When you do not know, say \"I do not understand X\". Do not narrate a guess\n   as an explanation.\n\n## Phase 4 — Fix\n\n1. **Write the failing test first.** For a bug ticket this is the `@ac-n`\n   scenario's test, tagged `@test:<id>`, and `accord gate done <id>` is what\n   reports on it. Confirm it fails for the right reason before touching the fix.\n2. **One fix, at the root cause.** No \"while I'm here\", no bundled refactor.\n3. **Verify.** The new test passes, nothing else broke, the reported symptom\n   is actually gone.\n4. **If the fix did not work:** stop and count attempts. Under three, return to\n   Phase 1 with what you learned. Three or more, go to step 5 — do not attempt\n   a fourth fix.\n5. **Three failed fixes means the architecture is wrong, not the hypothesis.**\n   The tell: each fix uncovers a new problem somewhere else, or the next fix\n   needs \"a big refactor\". Stop and put the architectural question to the\n   developer. This is not a failed hypothesis; do not treat it as one.\n\n## Stop signals\n\nCatching yourself mid-sentence on any of these means return to Phase 1:\n\n- \"Quick fix now, investigate later\"\n- \"Just try changing X and see\"\n- \"It's probably X\"\n- \"I don't fully understand it but this might work\"\n- \"Here are the main problems:\" — a list of fixes, no investigation\n- \"One more attempt\" after two failures\n- Each fix revealing a new problem elsewhere\n\nFrom the developer, these mean the same thing: *\"Is that actually happening?\"*\n(assumed without verifying), *\"Stop guessing\"*, *\"Are we stuck?\"*.\n\n## Rationalizations\n\n| Excuse | Reality |\n|---|---|\n| Simple issue, process is overkill | Simple bugs have root causes too, and the process is fast on them |\n| Emergency, no time | Systematic is faster than guess-and-check thrashing. Thrashing only feels faster |\n| Try one fix first, then investigate | The first fix sets the pattern for the rest |\n| Test after confirming the fix | Untested fixes do not stick, and `accord gate done <id>` runs either way |\n| Several fixes at once saves time | You cannot tell which one worked, and you have added new bugs |\n| Reference is long, I'll adapt the pattern | Partial understanding guarantees a bug |\n| I can see the problem | Seeing a symptom is not understanding a cause |\n| One more attempt (after two) | Three failures is an architecture problem. Stop fixing |\n\n## When there really is no root cause\n\nGenuinely environmental, timing-dependent, or external causes exist. If the\ninvestigation lands there: document what was ruled out, implement the\nappropriate handling (retry, timeout, a clear error), and add enough logging\nthat the next occurrence is diagnosable. Then say so plainly in\n`verification.md`.\n\nNote that most \"no root cause\" conclusions are incomplete investigations. Treat\nyour own as suspect until Phase 1 is genuinely complete.\n",
  "dev/review.md": "---\nkind: reference\n---\n\n# Reviewing a finished change\n\nYou are a fresh context. You did not write this change, and that is the whole\nreason you were opened: a context cannot fairly audit a decision it does not\nremember making.\n\nYou receive a ticket id and the commit being reviewed. Everything else you read\nfor yourself.\n\n## 1. Read, in this order\n\n1. The ticket's `## Intent` — what the change is for.\n2. Its `## Requirements` — the EARS lines.\n3. Its `## Acceptance criteria` — the tagged scenarios. These are the standard\n   you judge against; nothing else is.\n4. The diff for the commit, and whatever you have to read to understand it.\n\nThe ticket first and the diff second. A review anchored in the acceptance\ncriteria catches \"this ships the wrong feature\"; a review that starts from the\ndiff only ever catches taste.\n\n## 2. Write `accord/tickets/<id>/verification.md`\n\nThe frontmatter carries `ticket:`, `commit:`, and `reviewed_on:`.\nOne `## @ac-n <scenario name>` block per scenario, in tag order, each carrying\ntwo lines that look exactly like this:\n\n```markdown\nResult: pass\n\nEvidence: <what you ran or inspected, and what you saw>\n```\n\nEach label begins the line it is on — it is not a list item, and nothing is\nindented in front of it.\n\n`Result:` is `pass`, `fail`, or `blocked`. A scenario you could not check is\n`blocked`, never `pass`. `Evidence:` names the command, the path, or the\noutput. \"Looks correct\" is not evidence.\n\nSet `commit:` in the frontmatter to the commit you reviewed.\n\n## 3. Append `## Review` to the same file\n\nRead `./code-review.md` and follow it. It carries the rule that decides what\nbelongs in this section: every finding names a concrete failure — the input or\nstate, and the wrong result that follows. A finding that cannot produce one is\ndeleted, not softened.\n\n`No findings.` is a valid review. Padding it is not.\n\n## 4. Report on the change; do not change it\n\nA reviewer that fixes has rejoined the work it was brought in to judge, and\nthere is then no independent context left to check the fix. A defect goes into\nyour findings and stops there — including one you could repair in a line, and\nincluding a test you would rather see passing.\n\nThe only file you write is the one named in step 2. You edit nothing else.\n\n## 5. Hand it back\n\nSay that the review is written, and that `accord gate done <id>` is the\ndeveloper's to run. Do not run it yourself and do not restate what it checks; it\nprints its own reasons.\n",
  "shared/prototype.md": "---\nkind: reference\n---\n\n# Producing a prototype\n\nA `ui: true` ticket needs something a reviewer can look at before the code\nexists. In the `maintain` profile that something is\n`accord/assets/<id>/prototype.html`, and it is derived from what the project\nalready looks like — not from a fresh idea of what it could look like.\n\n## 1. Find the styles the project already has\n\nBefore writing any markup, find where the look of this project is defined.\nLook for, in roughly this order:\n\n- the path named under `design.tokens` in `accord/config.yml`, if one is set;\n- a stylesheet or variables file the application loads on every page;\n- a utility-class configuration, if the project styles that way;\n- a components or design-system folder.\n\nRead what you find. You are looking for the names the project uses for its\ncolours, spacing, radii, and type — those names are what the prototype spends,\nand inventing new ones is a redesign the ticket did not ask for.\n\nIf you find nothing, say so in the ticket's `## Open questions` and keep\ngoing: a prototype built from nothing has to record what it was built from\ninstead.\n\n## 2. Start from a single HTML file\n\nOne HTML file: a comment block at the top, an empty body. The comment block\nnames the ticket, a `Derived from:` line for what step 1 found, and the owner.\nBuild the page underneath it. One file, no build step, no assets directory —\na reviewer opens it in a browser and it works.\n\n## 3. Put it where the gate looks\n\n`accord/assets/<id>/prototype.html`, with `<id>` the ticket id. That folder is\nthis stage's own; nothing else writes into it.\n\n## 4. Run `accord lint` and fix what it reports\n\nDo not work from memory of the rules — run it, read the reasons, and fix what\nit names.\n\nA finding you disagree with is a question, not a licence: write it into the\nticket's `## Open questions` and stop, rather than working around the rule.\n\n## 5. Re-run the gate\n\n`accord gate ready <id>` is the only thing that says whether the ticket now\nhas what it was missing.\n",
} as const;
export type SkillKey = keyof typeof skills;
