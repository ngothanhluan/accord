---
phase: 03-lint
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - packages/core/src/lint/ears.ts
  - packages/core/src/lint/gherkin.ts
  - packages/core/src/lint/rules.ts
  - packages/core/src/load/gherkin.ts
  - packages/core/test/ears.test.ts
  - packages/core/test/lint.test.ts
  - packages/core/test/fixtures/lint-ears/accord/config.yml
  - packages/core/test/fixtures/lint-ears/accord/tickets/EARS.md
  - packages/core/test/fixtures/lint-gherkin/accord/config.yml
  - packages/core/test/fixtures/lint-gherkin/accord/tickets/STEPS.md
  - packages/core/test/fixtures/lint-gherkin/accord/tickets/ACTAGS.md
  - packages/core/test/fixtures/lint-gherkin/accord/tickets/TESTTAGS.md
  - packages/core/test/fixtures/lint-gherkin/accord/tickets/NOSCEN.md
  - packages/core/test/fixtures/lint-missing-files/accord/config.yml
  - packages/core/test/fixtures/lint-missing-files/accord/tickets/MISSING.md
  - packages/core/test/__golden__/lint-ears.snapshot.json
  - packages/core/test/__golden__/lint-ears.lint.json
  - packages/core/test/__golden__/lint-gherkin.snapshot.json
  - packages/core/test/__golden__/lint-gherkin.lint.json
  - packages/core/test/__golden__/lint-missing-files.snapshot.json
  - packages/core/test/__golden__/lint-missing-files.lint.json
  - packages/core/test/__golden__/lint-report.lint.json
  - packages/core/test/__golden__/gherkin-shapes.lint.json
  - packages/core/test/__golden__/body-edges.lint.json
  - packages/core/test/__golden__/frontmatter-errors.lint.json
  - packages/core/test/__golden__/no-config.lint.json
  - packages/core/test/__golden__/bad-config.lint.json
  - packages/core/test/__golden__/config-syntax.lint.json
autonomous: true
requirements: [LINT-02, LINT-03, FMT-09, FMT-11]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each of the six EARS patterns (ubiquitous, event-driven `WHEN`, state-driven `WHILE`, unwanted behaviour `IF ... THEN`, optional feature `WHERE`, complex) classifies with no finding, case-insensitively, with the subject `the system shall` and English keywords around content in any language; `should`, `must`, `will` do not match; the three `valid-build` requirement lines classify (D-61, D-62, D-63)"
    - "A line matching no pattern yields one `lint.ears-unclassified` warning at its Markdown line whose reason is one of the ten deterministic diagnoses from RESEARCH.md (for example `has WHEN but no 'the system shall'`); no nearest-pattern suggestion (D-64)"
    - "`lint.ac-tag-missing`, `lint.ac-tag-multiple`, `lint.ac-tag-duplicate`, `lint.step-empty`, `lint.no-scenarios` are errors, each with its own id, at the scenario line (or AC heading line, else ticket file, for `no-scenarios`); a parse error surfaces as `load.gherkin-parse` and `PARSE-ERROR.md` in `gherkin-shapes` gets both that and `lint.no-scenarios` (LINT-03, criterion 3, owner decision on RESEARCH Open Question 3 resolved 2026-09-14)"
    - "The Phase 2 fixtures record the new rows' effect: `FENCE.md` carries five `lint.ears-unclassified`; every typed story or bug with no scenario (`MISMATCH`, `TYPING`, `NOREQ`, `UNTERMINATED`, `ONLY`, both `T.md`, `BADLANG`, `EMPTY-AC`, `PARSE-ERROR`) carries `lint.no-scenarios`; `TAGS.md` carries the three `ac-tag` errors; no other existing golden changes in this plan"
    - "`lint.test-tag-duplicate` (error) fires when a scenario carries two or more `@test:` tags; `lint.test-tag-on-ui` (warning) when `@ui` and any `@test:` coexist; `lint.test-tag-missing` (03-01) stays silent on `@ui` scenarios (D-69)"
    - "`lint.test-id-unknown` (warning, scenario line) fires only when `snapshot.tests` is present and the id after `@test:` is not a key, with a reason that names the report path and the `classname#name` shape with spaces as `-`; with no report configured or the report absent, the rule is silent (D-72, Pitfall 6)"
    - "`lint.report-missing` (warning, `accord/config.yml`, pointer `/tests/report`) fires when `config.tests.report` is set and the key is absent from `snapshot.files` (D-72)"
    - "`lint.step-empty` fires for a `steps` entry with no whitespace (keyword only) and for a scenario with zero steps; a `Given ...` placeholder step is `lint.sentinel` (03-02), not `step-empty` (RESEARCH 'Empty steps')"
  artifacts:
    - path: "packages/core/src/lint/ears.ts"
      provides: "classifyEars"
      contains: "export function classifyEars("
    - path: "packages/core/test/ears.test.ts"
      provides: "six positive patterns, ten diagnoses, fixture lines"
      min_lines: 40
    - path: "packages/core/test/__golden__/lint-gherkin.lint.json"
      provides: "one golden per LINT-03 and FMT-09 rule"
      contains: "lint.test-tag-duplicate"
  key_links:
    - from: "packages/core/src/lint/gherkin.ts"
      to: "packages/core/src/load/gherkin.ts"
      via: "AC_TAG exported and reused for ac-tag-multiple"
      pattern: "AC_TAG"
    - from: "packages/core/src/lint/gherkin.ts"
      to: "packages/core/src/model/snapshot.ts"
      via: "snapshot.tests and config.tests.report read for test-id-unknown and report-missing"
      pattern: "snapshot.tests"
    - from: "packages/core/src/lint/rules.ts"
      to: "packages/core/src/lint/ears.ts"
      via: "lint.ears-unclassified row calls classifyEars per requirement line"
      pattern: "ears-unclassified"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; no attribution trailer is written"
      status: flagged
      verification: unverified
    - statement: "The EARS classifier never uses `\\b` word boundaries or a nearest-pattern heuristic; it tokenises on whitespace and returns exactly one deterministic diagnosis (D-64, Pitfall 2)"
      status: flagged
      verification: unverified
    - statement: "No `@test:` id is matched by basename, prefix, or case folding; the lookup is `id in snapshot.tests` (PROJECT.md 'core never guesses')"
      status: flagged
      verification: unverified
    - statement: "No level literal for a `lint.*` rule appears outside `rules.ts`; nothing under `packages/core/src/` imports `node:`"
      status: flagged
      verification: unverified
    - statement: "No reason string names a test runner or another tool; the reason for an unknown id says 'the report' and the path from config"
      status: flagged
      verification: unverified
---

<objective>
**As a** BA writing EARS requirements and Gherkin scenarios in Vietnamese around English keywords, **I want** every line that is not one of the six EARS templates flagged with a plain diagnosis, and every scenario tag mistake named by its own rule, **so that** I fix the exact word that is wrong instead of guessing.

This plan adds the EARS classifier (D-61 to D-64), the LINT-03 Gherkin rules, the D-69 `@test:` rules that the tracer did not cover, and the two report-consuming rules from D-72. It regenerates seven existing lint goldens (`lint-report`, `gherkin-shapes`, `body-edges`, `frontmatter-errors`, `no-config`, `bad-config`, `config-syntax`) whose Phase 2 tickets are deliberately partial and gain findings from the new rows; the expected delta is stated per ticket in each task.

Purpose: ROADMAP criteria 2 and 3 become executable; FMT-09 and FMT-11 are complete on the lint side.
Output: `lint/ears.ts`, `lint/gherkin.ts` extended, ten new `RULES` rows, `AC_TAG` exported, `ears.test.ts`, fixtures `lint-ears`, `lint-gherkin`, `lint-missing-files`, and their goldens.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-lint/03-CONTEXT.md
@.planning/phases/03-lint/03-RESEARCH.md
@.planning/phases/03-lint/03-PATTERNS.md
@.planning/phases/03-lint/03-01-SUMMARY.md
@.planning/phases/03-lint/03-03-SUMMARY.md
@packages/core/src/lint/gherkin.ts
@packages/core/src/lint/rules.ts
@packages/core/src/load/gherkin.ts

<interfaces>
```typescript
// packages/core/src/lint/ears.ts
export function classifyEars(text: string): { pattern: string } | { reason: string };   // RESEARCH.md verified code, verbatim

// packages/core/src/load/gherkin.ts
export const AC_TAG = /^@ac-[1-9][0-9]*$/;        // add `export`; no other change

// packages/core/src/lint/ears.ts second export: the rule check next to the classifier
export const earsUnclassified: Rule['check'];

// packages/core/src/lint/gherkin.ts new exports (one per rule id; testTagMissing already exists from 03-01)
// acTagMissing, acTagMultiple, acTagDuplicate, stepEmpty, noScenarios,
// testTagDuplicate, testTagOnUi, testIdUnknown, reportMissing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EARS classifier and `lint.ears-unclassified` with the `lint-ears` fixture</name>
  <read_first>
    - .planning/phases/03-lint/03-RESEARCH.md "EARS Classifier (D-61 to D-64)": grammar, algorithm steps 1-6, the diagnosis table, the verified code block (port verbatim), the list of lines proven to classify, Pitfall 2
    - packages/core/src/load/sections.ts lines 13-15 and 133-142 (`headingKey` style; `requirementLines` produces the `Line[]` the rule reads)
    - packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1.md lines 23-26 and EPIC-1.md (the three reference lines that must classify)
    - packages/core/src/lint/rules.ts (append rows; keep the shape)
    - packages/core/test/sections.test.ts lines 1-26 (unit table style with `it.each`)
  </read_first>
  <files>packages/core/src/lint/ears.ts, packages/core/src/lint/rules.ts, packages/core/test/ears.test.ts, packages/core/test/lint.test.ts, packages/core/test/fixtures/lint-ears/accord/config.yml, packages/core/test/fixtures/lint-ears/accord/tickets/EARS.md, packages/core/test/__golden__/lint-ears.snapshot.json, packages/core/test/__golden__/lint-ears.lint.json, packages/core/test/__golden__/body-edges.lint.json</files>
  <behavior>
    - Test 1 (`it.each`, six positives → `{ pattern }` with the named pattern): `The system shall log every request.` → `ubiquitous`; `WHEN the user submits the form, the system SHALL save the data` → `event-driven`; `While the aircraft is on ground, the system shall disable reverse thrust` → `state-driven`; `IF the password is wrong THEN the system SHALL show an error` → `unwanted-behaviour`; `Where the premium feature is enabled, the system shall show the export button` → `optional-feature`; `While the user is signed in, when the session expires, the system shall redirect to login` → `complex`
    - Test 2 (fixture lines classify): `WHEN người dùng gửi email và mật khẩu hợp lệ the system SHALL mở bảng điều khiển` → `event-driven`; `IF mật khẩu sai THEN the system SHALL hiển thị thông báo lỗi` → `unwanted-behaviour`; `WHEN người dùng có tài khoản the system SHALL cho phép đăng nhập` → `event-driven`
    - Test 3 (`it.each`, ten diagnoses → exact `reason`): `WHEN the user clicks, it saves` → `has WHEN but no 'the system shall'`; `The user logs in` → `no EARS keyword and no 'the system shall'`; `The system must log it` → `has 'the system must' but the verb must be 'shall'`; `WHEN x the system shall` → `has 'the system shall' but no response after it`; `Sau khi đăng nhập WHEN x the system shall y` → `has 'the system shall' but the line starts with prose and no keyword`; `WHEN a WHEN b the system shall c` → `has WHEN twice`; `WHEN the system shall save` → `has WHEN but no trigger before 'the system shall'`; `IF x the system shall y` → `has IF but no THEN before 'the system shall'`; `WHEN x THEN the system shall y` → `has THEN but no IF`; `IF x THEN y WHEN z the system shall w` → `THEN must come last before 'the system shall'`
    - Test 4: `the system should x`, `The System Shall x` (case), `the   system   shall x` (whitespace) → the first is a verb diagnosis, the other two classify as `ubiquitous`
  </behavior>
  <action>
Write `packages/core/test/ears.test.ts` with the four behaviours (import `classifyEars` from `../src/lint/ears.js`). Create `packages/core/src/lint/ears.ts`: header comment citing D-61 to D-64 and A1 (clause order not enforced); port the verified `classifyEars` from RESEARCH.md verbatim with its `KEYWORDS`, `tokens`, `part`, and `names` tables; add `export const earsUnclassified: Rule['check']` that, for each ticket, maps `t.requirements` through `classifyEars(l.text)` and emits a draft at `l.line` with the returned `reason` when present. Append the row `lint.ears-unclassified`, `warning`, both profiles, to `rules.ts`.

Fixture `lint-ears`: `accord/config.yml` as in `lint-hygiene`; `accord/tickets/EARS.md` (story, five headings, one `@ac-1 @test:t#a` scenario, Plan `- x @ac-1`) whose `## Requirements` holds, as `- ` list items, the six positive lines from Test 1, the three fixture lines from Test 2, then the ten diagnosis lines from Test 3. Expected: exactly ten `lint.ears-unclassified` findings at the ten diagnosis lines with the exact reasons, nothing on the nine positive lines; `lint.requirements-oversize` fires once (19 lines) and is expected. Add `describe('fixture lint-ears: pinned lint values')` to `lint.test.ts` asserting ten `lint.ears-unclassified` findings whose reasons equal the Test 3 list in file order, one `lint.requirements-oversize`, and no other rule. Generate the two goldens with scoped `-u` (`-t "fixture lint-ears"`), read, rerun without `-u`.

**Existing golden that changes in this task.** `body-edges/FENCE.md` holds five requirement lines with a keyword and no subject (`WHEN one`, `WHEN two`, `WHEN three`, `WHEN four`, `WHEN five`, lines 16 to 20), so `body-edges.lint.json` gains five `lint.ears-unclassified` warnings, one per line, each with reason `has WHEN but no 'the system shall'`. Regenerate it with `npm test -- --project core lint -u -t "fixture body-edges"`, read the diff, and confirm it is exactly those five before running without `-u`. Every other existing requirement line classifies and its golden must not change: `CASE.md` and `DUP.md` (`WHEN a the system SHALL b`), `valid-build` (the three reference lines), `verification-edges` A and B, `lint-hygiene` (HYGIENE, SIZE, EPIC, NOTES, PLAN-EMPTY, CLEAN all use `WHEN ... the system SHALL ...` or `IF ... THEN the system SHALL ...`), and `lint-report`. A mismatch on any of those is a finding for the SUMMARY, not a golden to accept.

`npm run lint && npm run typecheck`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core ears && npm test -- --project core lint -t "fixture (lint-ears|valid-build|body-edges|verification-edges|lint-hygiene|lint-report)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or the ears `|core|` summary reports fewer than 20 passing tests; or a golden reported obsolete or mismatched</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/lint/ears.ts` contains `export function classifyEars(` and `export const earsUnclassified`; `grep -c '\\b' packages/core/src/lint/ears.ts` prints 0
    - `packages/core/test/ears.test.ts` contains the literal reasons `has WHEN but no 'the system shall'`, `has THEN but no IF`, `THEN must come last before 'the system shall'`, and the pattern name `unwanted-behaviour`
    - `packages/core/test/__golden__/lint-ears.lint.json` contains exactly 10 findings with `"rule": "lint.ears-unclassified"`, all `"level": "warning"`, and `"errors": 0`
    - `packages/core/test/__golden__/valid-build.lint.json` contains no `lint.ears-unclassified` (the three reference lines classify)
    - `grep -c "lint.ears-unclassified" packages/core/test/__golden__/body-edges.lint.json` prints 5, all on `accord/tickets/FENCE.md`; `git diff --stat -- packages/core/test/__golden__/valid-build.lint.json packages/core/test/__golden__/verification-edges.lint.json packages/core/test/__golden__/lint-hygiene.lint.json packages/core/test/__golden__/lint-report.lint.json` is empty at the end of this task
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>ROADMAP criterion 2 has executable assertions: six templates classify silently, ten failure shapes warn at their line with a fixed diagnosis, and the Vietnamese reference lines pass.</done>
</task>

<task type="auto">
  <name>Task 2: LINT-03 Gherkin rules, `@test:` duplicate/on-ui, unknown id, and report-missing with the `lint-gherkin` and `lint-missing-files` fixtures</name>
  <read_first>
    - packages/core/src/lint/gherkin.ts (03-01 state with `testTagMissing`) and packages/core/src/lint/rules.ts
    - packages/core/src/load/gherkin.ts lines 15-46 (`AC_TAG`, `stepText`, how `acTag` is the first matching tag)
    - packages/core/test/fixtures/gherkin-shapes/accord/tickets/TAGS.md, PARSE-ERROR.md, EMPTY-AC.md (existing shapes whose lint golden gains findings)
    - packages/core/test/__golden__/lint-report.snapshot.json (the three known ids from 03-03)
    - .planning/phases/03-lint/03-RESEARCH.md "Rule Catalogue" rows for these rules, "Empty steps (LINT-03)", Pitfall 6, Open Questions (RESOLVED) 3 and 5
    - packages/core/src/load/snapshot.ts (`normaliseKey` and `PROTOTYPE` are exported since 03-01; import, do not re-declare)
  </read_first>
  <files>packages/core/src/lint/gherkin.ts, packages/core/src/lint/rules.ts, packages/core/src/load/gherkin.ts, packages/core/test/lint.test.ts, packages/core/test/fixtures/lint-gherkin/accord/config.yml, packages/core/test/fixtures/lint-gherkin/accord/tickets/STEPS.md, packages/core/test/fixtures/lint-gherkin/accord/tickets/ACTAGS.md, packages/core/test/fixtures/lint-gherkin/accord/tickets/TESTTAGS.md, packages/core/test/fixtures/lint-gherkin/accord/tickets/NOSCEN.md, packages/core/test/fixtures/lint-missing-files/accord/config.yml, packages/core/test/fixtures/lint-missing-files/accord/tickets/MISSING.md, packages/core/test/__golden__/lint-gherkin.snapshot.json, packages/core/test/__golden__/lint-gherkin.lint.json, packages/core/test/__golden__/lint-missing-files.snapshot.json, packages/core/test/__golden__/lint-missing-files.lint.json, packages/core/test/__golden__/lint-report.lint.json, packages/core/test/__golden__/gherkin-shapes.lint.json, packages/core/test/__golden__/body-edges.lint.json, packages/core/test/__golden__/frontmatter-errors.lint.json, packages/core/test/__golden__/no-config.lint.json, packages/core/test/__golden__/bad-config.lint.json, packages/core/test/__golden__/config-syntax.lint.json</files>
  <action>
**`load/gherkin.ts`:** add `export` to `const AC_TAG` (no other change).

**Rules in `lint/gherkin.ts`** (each iterates tickets then `t.scenarios`; drafts at `s.line` unless stated):
- `acTagMissing` (LINT-03): `s.acTag === undefined` → reason `scenario "${s.name}" has no @ac-n tag`.
- `acTagMultiple` (D-46): `s.tags.filter((t) => AC_TAG.test(t)).length > 1` → reason `scenario "${s.name}" carries more than one @ac-n tag`.
- `acTagDuplicate` (LINT-03): per ticket, walk scenarios in order; an `acTag` seen before → draft on the later scenario, reason `@${acTag} is already used by scenario "${firstName}"`.
- `stepEmpty` (LINT-03): `s.steps.length === 0` → reason `scenario "${s.name}" has no steps`; else for each index `i` where `!/\s/.test(s.steps[i])` → reason `scenario "${s.name}" step ${i + 1} "${s.steps[i]}" has no text`.
- `noScenarios` (LINT-03): `frontmatter?.type` is `story` or `bug` and `t.scenarios.length === 0` → draft at the `acceptance criteria` section line when that section exists, else no line; reason `a ${type} needs at least one tagged scenario`.
- `testTagDuplicate` (D-69): two or more tags starting `@test:` → reason `scenario "${s.name}" carries more than one @test: tag`.
- `testTagOnUi` (D-69): `@ui` present and any `@test:` → reason `scenario "${s.name}" is @ui and carries a @test: tag`.
- `testIdUnknown` (D-72): skip the whole rule when `snapshot.tests === undefined`; else for each `@test:` tag whose id (`tag.slice(6)`) is not `in snapshot.tests` → reason `@test:${id} not found in ${snapshot.config.tests.report}; ids are classname#name with spaces as '-'`.
- `reportMissing` (D-72): `snapshot.config?.tests?.report` set and `normaliseKey(report)` (imported from `../load/snapshot.js`, exported by 03-01; do not re-implement the backslash and `./` rule) not in `snapshot.files` → one draft `{ file: 'accord/config.yml', pointer: '/tests/report', reason: \`tests.report "${report}" is not in the snapshot\` }`.

**`rules.ts`:** append `lint.ac-tag-missing` error, `lint.ac-tag-multiple` error, `lint.ac-tag-duplicate` error, `lint.step-empty` error, `lint.no-scenarios` error, `lint.test-tag-duplicate` error, `lint.test-tag-on-ui` warning, `lint.test-id-unknown` warning, `lint.report-missing` warning; both profiles each. With Task 1 the table has 27 rows.

**Fixture `lint-gherkin`** (config as `lint-hygiene`; every ticket a story with five headings, one valid EARS line, Open questions empty, Plan tagged to match its scenarios so no plan finding fires):
- `STEPS.md`: fence with `@ac-1 @test:t#a Scenario: Trống` whose only line is `Given ` (keyword plus trailing space, an empty step kept by the parser), `@ac-2 @test:t#b Scenario: Không bước` with no step lines, `@ac-3 @test:t#c Scenario: Mẫu` with `Given ...` (sentinel from 03-02, not step-empty). Expected: `lint.step-empty` × 2 (reasons `step 1 "Given" has no text` and `has no steps`), `lint.sentinel` × 1 on the third.
- `ACTAGS.md`: scenarios: untagged `@test:t#a` only (ac-tag-missing), `@ac-1 @ac-2 @test:t#b` (ac-tag-multiple), `@ac-1 @test:t#c` again (ac-tag-duplicate naming the second scenario). Plan `- x @ac-1 @ac-2`. Expected: exactly those three errors.
- `TESTTAGS.md`: scenarios: `@ac-1 @test:t#a @test:t#b` (test-tag-duplicate), `@ac-2 @ui @test:t#c` (test-tag-on-ui), `@ac-3` alone (test-tag-missing from 03-01), `@ac-4 @ui` (silent). Expected: one error, two warnings.
- `NOSCEN.md`: `## Acceptance criteria` holding only a comment; Plan empty (silent: no scenarios). Expected: `lint.no-scenarios` at the AC heading line. No report configured in this fixture, so `test-id-unknown` is silent everywhere.

**Fixture `lint-missing-files`:** config with `design:\n  tokens: src/missing.css` and `tests:\n  report: reports/missing.xml`, neither file present; `MISSING.md` story with one `@ac-1 @test:t#a` scenario and a matching plan. Expected now: `lint.report-missing` on `accord/config.yml` with pointer `/tests/report`; plan 03-06 adds `lint.tokens-missing` and regenerates this golden.

**New goldens (scoped `-u`):** `lint-gherkin` and `lint-missing-files`, snapshot and lint.

**Existing goldens that change in this task.** Regenerate each with `npm test -- --project core lint -u -t "fixture <name>"`, one at a time, read the diff, and confirm it is exactly the delta below before running without `-u`; any other change is a finding for the SUMMARY. `lint.no-scenarios` needs `frontmatter.type` of `story` or `bug`, so untyped tickets gain nothing; its line is the `## Acceptance criteria` heading when that section exists, else none:
- `lint-report.lint.json`: one `lint.test-id-unknown` on the `@test:nope#missing` scenario with the reason naming `reports/junit.xml` (the golden was empty after 03-03).
- `gherkin-shapes.lint.json`: `TAGS.md` gains `lint.ac-tag-missing` at line 13, `lint.ac-tag-multiple` at line 17 (`Hai thẻ`), `lint.ac-tag-duplicate` at line 25 (reason names `Hai thẻ`, the first `@ac-1`); it already carries `lint.test-tag-missing` on all four scenarios from 03-01. `BADLANG.md`, `EMPTY-AC.md`, `PARSE-ERROR.md` (zero scenarios) each gain `lint.no-scenarios` at line 7; `PARSE-ERROR.md` keeps its `load.gherkin-parse` beside it. Six new errors in total; the `lint.heading-missing` findings from 03-02 stay.
- `frontmatter-errors.lint.json`: `MISMATCH.md` (bug) and `TYPING.md` (story) each gain one `lint.no-scenarios`, no line (no AC section), reasons `a bug needs at least one tagged scenario` and `a story needs at least one tagged scenario`.
- `body-edges.lint.json`: `NOREQ.md` and `UNTERMINATED.md` gain one `lint.no-scenarios` each, no line; `CASE`, `DUP`, `FENCE`, `TILDE` each have one tagged scenario with a step and gain nothing.
- `no-config.lint.json`, `bad-config.lint.json`, `config-syntax.lint.json`: the single story gains one `lint.no-scenarios`, no line.
- Unchanged, and the full run proves it: `lint-hygiene` (every scenario has an `@ac-n`, steps, and a `@test:` or `@ui`; `HEADINGS.md` is a bug with one scenario), `lint-ears`, `valid-build`, `verification-edges` (no report configured anywhere, so `test-id-unknown` and `report-missing` are silent outside `lint-report` and `lint-missing-files`; no existing scenario has two `@ac-n`, a duplicate, an empty step, or a `@test:` tag). A mismatch there is a finding to report, not a golden to regenerate.

Extend the pinned `describe`s in `lint.test.ts`: `lint-gherkin` per-file rule multisets as listed; `lint-report` has exactly one `lint.test-id-unknown` whose reason contains `reports/junit.xml` and `classname#name`; `gherkin-shapes` `PARSE-ERROR.md` has both `load.gherkin-parse` and `lint.no-scenarios` and `TAGS.md` has the three `ac-tag` errors. `npm run lint && npm run typecheck`, then the full `npm test -- --project core lint`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test -- --project core lint && npm test -- --project core snapshot -t "fixture (lint-gherkin|lint-missing-files)"</automated>
    <fails_when>non-zero exit; or `No test files found`; or a golden reported obsolete or mismatched; or the `|core|` summary for lint reports fewer than 12 fixture groups</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/load/gherkin.ts` contains `export const AC_TAG`
    - `packages/core/src/lint/rules.ts` contains 27 `id:` entries including `lint.ac-tag-missing`, `lint.ac-tag-multiple`, `lint.ac-tag-duplicate`, `lint.step-empty`, `lint.no-scenarios`, `lint.test-tag-duplicate`, `lint.test-tag-on-ui`, `lint.test-id-unknown`, `lint.report-missing`; `grep -c "level: '" packages/core/src/lint/gherkin.ts packages/core/src/lint/ears.ts` prints 0 for both
    - `grep -c "Given $" packages/core/test/fixtures/lint-gherkin/accord/tickets/STEPS.md` prints 1 (the keyword-only step keeps its trailing space; an editor that trims it turns the case into a zero-step scenario)
    - `packages/core/test/__golden__/lint-gherkin.lint.json` contains exactly 2 `lint.step-empty`, 1 `lint.ac-tag-missing`, 1 `lint.ac-tag-multiple`, 1 `lint.ac-tag-duplicate`, 1 `lint.test-tag-duplicate`, 1 `lint.test-tag-on-ui`, 1 `lint.no-scenarios`, and no `lint.test-id-unknown`
    - `packages/core/test/__golden__/lint-report.lint.json` contains exactly one finding, rule `lint.test-id-unknown`, `"level": "warning"`, reason containing `reports/junit.xml`
    - `packages/core/test/__golden__/gherkin-shapes.lint.json` contains for `accord/tickets/PARSE-ERROR.md` both `load.gherkin-parse` and `lint.no-scenarios`; `grep -c "lint.no-scenarios"` prints 3 for `gherkin-shapes.lint.json`, 2 for `frontmatter-errors.lint.json` and `body-edges.lint.json`, 1 for each of `no-config`, `bad-config`, `config-syntax` `.lint.json`, and 0 for `lint-hygiene`, `valid-build`, `verification-edges` `.lint.json`
    - `git diff --stat -- packages/core/test/__golden__/lint-hygiene.lint.json packages/core/test/__golden__/lint-ears.lint.json packages/core/test/__golden__/valid-build.lint.json packages/core/test/__golden__/verification-edges.lint.json` is empty at the end of this task
    - `packages/core/test/__golden__/lint-missing-files.lint.json` contains `"rule": "lint.report-missing"` with `"pointer": "/tests/report"`
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>ROADMAP criterion 3 (each Gherkin failure has its own rule id) and the lint half of FMT-09 and FMT-11 are pinned by fixtures and goldens; the report round trip from config to warning is proven end-to-end.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Functions: `classifyEars`, `earsUnclassified` (`lint/ears.ts`); `acTagMissing`, `acTagMultiple`, `acTagDuplicate`, `stepEmpty`, `noScenarios`, `testTagDuplicate`, `testTagOnUi`, `testIdUnknown`, `reportMissing` (`lint/gherkin.ts`)
- Export added: `AC_TAG` from `load/gherkin.ts`
- Rule ids: `lint.ears-unclassified`, `lint.ac-tag-missing`, `lint.ac-tag-multiple`, `lint.ac-tag-duplicate`, `lint.step-empty`, `lint.no-scenarios`, `lint.test-tag-duplicate`, `lint.test-tag-on-ui`, `lint.test-id-unknown`, `lint.report-missing`
- Tests: `packages/core/test/ears.test.ts`; pinned describes in `lint.test.ts`
- Fixtures: `lint-ears/` (EARS.md), `lint-gherkin/` (STEPS, ACTAGS, TESTTAGS, NOSCEN), `lint-missing-files/` (MISSING.md)
- Goldens: `lint-ears.*.json`, `lint-gherkin.*.json`, `lint-missing-files.*.json`; regenerated `lint-report`, `gherkin-shapes`, `body-edges`, `frontmatter-errors`, `no-config`, `bad-config`, `config-syntax` `.lint.json` (ears-unclassified, no-scenarios, and ac-tag deltas stated in the tasks)
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

| Source | Item | Disposition |
|---|---|---|
| RESEARCH A1 | `WHILE` / `WHEN` / `WHERE` clauses may appear in any order; only `THEN` must be last | Adopted (PITFALLS §9: a fixed order forces unnatural rewrites) |
| Owner decision (RESEARCH Open Question 3, resolved 2026-09-14) | `lint.no-scenarios` fires alongside `load.gherkin-parse` on the same ticket | Owner-confirmed; both true, each with its own id |
| Owner decision (RESEARCH Open Question 5 / A2, resolved 2026-09-14) | `@test:` ids in the `lint-gherkin` and `lint-report` fixtures and the `test-id-unknown` reason use `norm(classname)#norm(name)` with whitespace runs as `-` | Owner-confirmed shape; exact lookup, no basename fallback |
| LINT-03 (unclassified edge) | `lint.step-empty` covers both the keyword-only step and the zero-step scenario under one id, at the scenario line, reason naming the step index | Chosen; steps carry no line of their own |
| LINT-02 (unclassified edge) | A requirement line inside a comment or fence is not classified because `requirementLines` already excludes them (D-41); a `###` line under Requirements is excluded likewise | Flagged; Phase 2 behaviour |
| FMT-09 (unclassified edge) | `@test:` with an empty id (`@test:` alone) counts as a test tag for `missing`/`duplicate`/`on-ui` and is reported by `test-id-unknown` when a report exists; no separate empty-id rule | Chosen; keep the rule count down |
| RESEARCH Pitfall 6 | The `test-id-unknown` reason repeats the id shape so a pasted `auth > rejects` title is corrected without docs | Adopted |
| D-72 | With `tests.report` configured but absent, only `lint.report-missing` fires; `test-id-unknown` stays silent because `snapshot.tests` is absent | Chosen |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement and scenario text → classifier and tag rules | Untrusted text in any language |
| `@test:` tag → `snapshot.tests` lookup | A contributor-chosen id joined against a machine-produced report |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | a near-miss `@test:` id treated as known by a fuzzy match, later letting GATE-08 pass on the wrong test | medium | mitigate | `id in snapshot.tests` exact lookup; `lint-report` golden pins `nope#missing` as unknown; prohibition recorded |
| T-03-01 | Denial of service | pathological requirement lines against the classifier | low | mitigate | Whitespace tokenisation and linear scans; no regex with nested quantifiers (`tokens` uses `[,;.]+(\s|$)` and `\s+` only) |
| T-03-05 | Information disclosure | reasons echoing scenario names and ids | low | mitigate | Reasons echo the scenario name, tag, id, and configured report path only |
| T-03-SC | Tampering | npm installs | high | mitigate | No package added |
</threat_model>

<verification>
- Task gates are the `<automated>` commands; this wave has no sibling, so a full `npm test -- --project core lint` run is the plan gate.
</verification>

<success_criteria>
- ROADMAP criteria 2 and 3 pinned; D-61, D-62, D-63, D-64, D-69 (duplicate and on-ui), D-72 (lint side) each have a task step, a fixture line, and a golden.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/03-lint/03-05-SUMMARY.md` when done. Do not commit.
</output>
