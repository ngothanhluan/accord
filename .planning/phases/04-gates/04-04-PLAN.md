---
phase: 04-gates
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - packages/core/src/gate/done.ts
  - packages/core/src/gate/rules.ts
  - packages/core/test/gate.test.ts
  - packages/core/test/fixtures/gate-done/accord/tickets/MACHINE.md
  - packages/core/test/fixtures/gate-done/accord/tickets/MACHINE/verification.md
  - packages/core/test/fixtures/gate-done/accord/tickets/SOLO.md
  - packages/core/test/fixtures/gate-done/accord/tickets/SOLO/verification.md
  - packages/core/test/fixtures/gate-done/reports/junit.xml
  - packages/core/test/fixtures/gate-no-report/accord/config.yml
  - packages/core/test/fixtures/gate-no-report/accord/tickets/GONE.md
  - packages/core/test/fixtures/gate-no-report/accord/tickets/GONE/verification.md
  - packages/core/test/fixtures/gate-no-report/src/auth/login.ts
  - packages/core/test/fixtures/gate-no-report/test/login.spec.ts
  - packages/core/test/__golden__/gate-done.MACHINE.done.json
  - packages/core/test/__golden__/gate-done.SOLO.done.json
  - packages/core/test/__golden__/gate-done.PASS.noauthors.done.json
  - packages/core/test/__golden__/gate-no-report.GONE.done.json
  - packages/core/test/__golden__/gate-ready.CLEAN.done.json
  - packages/core/test/__golden__/verification-edges.A.done.json
  - packages/core/test/__golden__/gate-done.snapshot.json
  - packages/core/test/__golden__/gate-done.lint.json
  - packages/core/test/__golden__/gate-no-report.snapshot.json
  - packages/core/test/__golden__/gate-no-report.lint.json
  - packages/cli/src/load/fs.ts
  - packages/cli/test/load.test.ts
autonomous: true
requirements: [GATE-05, GATE-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every scenario not tagged `@ui` must carry a `@test:<id>` tag; one that does not fails Done with `gate.test-tag-missing` at the Scenario line, whether or not a report is configured, because that is a property of the ticket rather than of the report (GATE-08)"
    - "A `@test:<id>` whose reported status is `skipped` fails Done exactly as `failed` does, and only the status `passed` passes; the reason names the status verbatim so the two are distinguishable in the output (GATE-08 adjacency, design.md §5)"
    - "When `config.yml` declares no `tests.report`, Done fails with `gate.tests-unconfigured` on `accord/config.yml` at pointer `/tests` and does **not** report the check skipped; when `tests.report` is declared but its file is absent from the snapshot, Done fails with `gate.report-missing` at pointer `/tests/report` (D-80)"
    - "In either of those two cases the per-scenario test findings are suppressed, so one configuration problem yields one reason rather than one per scenario; the ticket-level `gate.test-tag-missing` still fires (GATE-08 empty, D-67 precedent)"
    - "A `@test:` id is looked up in `snapshot.tests` by exact code-point equality using `Object.hasOwn`, with no case folding, no basename fallback, no whitespace normalisation, and no prototype-chain consultation; an id named after an `Object.prototype` member is unknown unless the report really declares it (GATE-08 encoding, Phase 3 UAT fix)"
    - "One finding is emitted per failing scenario at that scenario's line, and the engine's `byFileLineRule` sort makes the order identical on every host; two scenarios failing for the same reason produce two findings with distinct lines (GATE-08 ordering)"
    - "A scenario tagged `@ui` is exempt from the whole machine layer: it needs no `@test:` tag and is never joined against the report, while still needing an evidence block, a bound tick, and a note (GATE-09, D-90)"
    - "`gate.author-match` (**warning**) fires when the author of `accord/tickets/<id>/verification.md` equals the author of the gated commit after lower-casing; it never changes the verdict (GATE-05, D-79)"
    - "`gate.author-skipped` (**warning**) fires when the comparison cannot be made because `snapshot.git` is absent or `git.authors` lacks either identity, and is suppressed when the verification file itself is missing, because `gate.verification-missing` already owns that (GATE-05, ROADMAP criterion 4, D-80 skipped-as-a-Finding rule)"
    - "`git.authors` is keyed by repo-relative posix path for file authors, plus one entry keyed by the exact value of `git.commit` carrying the gated commit's author; a sha and a repo-relative path can never collide, so one record serves both (D-78, D-79)"
    - "`loadFromFs` supplies `git` from two `git` invocations, `git show -s` for the commit and its author and one `git log` over `accord/tickets` for the per-file authors, spawning `git` by name and never a `.cmd` and never with a shell; every key in `authors` uses forward slashes (D-51, D-78)"
    - "When the repository has no commit yet, `loadFromFs` omits the `git` key entirely rather than emitting `undefined`, so the existing `valid-build` CLI golden comparison is byte-unchanged and Done fails cleanly with `gate.commit-missing` (D-54)"
    - "Every git invocation in the `packages/cli/test/load.test.ts` commit helper runs against the `mkdtemp` sandbox: there is exactly one `execFileSync('git'` site in the file, it carries `cwd: tmp`, the helper throws before writing unless `tmp` lies under the OS temp directory and differs from `process.cwd()`, and `git rev-parse --show-toplevel` inside the helper resolves to the sandbox (CLAUDE.md standing rule: nothing may commit the author's tree)"
    - "The accord repository's own `git rev-parse HEAD` is the same value after the full suite runs as before it, and `git diff --cached --quiet` exits 0 afterwards, so a green test run can have neither committed nor staged the author's working tree"
    - "The author checks move exactly two goldens, `gate-done.SOLO.done.json` and `gate-done.PASS.noauthors.done.json`, because `GIT.authors` carries a review identity for every verification file on disk in every fixture a git-carrying case row drives — including `accord/tickets/A/verification.md` in `verification-edges` and `accord/tickets/GONE/verification.md` in `gate-no-report` — so `gate.author-skipped` never fires on a hole in the test data (GATE-05, D-78)"
    - "`gate-done` ticket `PASS` still returns `verdict: 'pass'` with no `error`-level finding once the machine layer and the author check land, and `npm run check` is green on both projects"
  artifacts:
    - path: "packages/core/src/gate/done.ts"
      provides: "testTagMissing, testsUnconfigured, reportMissing, testUnknown, testNotPassed, authorMatch, authorSkipped"
      contains: "export const testNotPassed"
    - path: "packages/cli/src/load/fs.ts"
      provides: "gitFacts, feeding SnapshotInput.git"
      contains: "git.authors"
    - path: "packages/core/test/__golden__/gate-done.MACHINE.done.json"
      provides: "skipped, failed, unknown-id, and missing-tag in one golden"
      contains: "gate.test-not-passed"
    - path: "packages/core/test/__golden__/gate-no-report.GONE.done.json"
      provides: "the configured-but-absent report golden"
      contains: "gate.report-missing"
  key_links:
    - from: "packages/core/src/gate/done.ts"
      to: "packages/core/src/model/snapshot.ts"
      via: "snapshot.tests for the @test: join, snapshot.config.tests.report for the two configuration reasons, snapshot.git.authors for the author comparison"
      pattern: "snapshot.tests"
    - from: "packages/cli/src/load/fs.ts"
      to: "packages/core/src/model/snapshot.ts"
      via: "loadFromFs returns SnapshotInput.git, the only producer of git.commit and git.authors in v0.1"
      pattern: "SnapshotInput"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag` in the accord repository; the one commit this plan creates is made by a test inside an `fs.mkdtemp` sandbox that is deleted afterwards, and the working tree is left dirty for the author to review"
      status: flagged
      verification: unverified
    - statement: "Every `execFileSync` in the `packages/cli/test/load.test.ts` git helper passes `cwd: tmp` explicitly, routed through one wrapper so there is exactly one invocation site; the helper throws before writing anything unless `tmp` lies under the OS temp directory and differs from `process.cwd()`, and asserts its own repository root is the sandbox — `execFileSync` defaults to `process.cwd()`, which under vitest is the accord repository root"
      status: flagged
      verification: unverified
    - statement: "A check never reports itself skipped merely because its input is absent; only GATE-05, which is a warning either way, has a skipped form, and D-80 already closed the one door a skipped machine check left open"
      status: flagged
      verification: unverified
    - statement: "No environment variable, config key, or command-line surface is added that turns the machine layer off, downgrades `gate.test-not-passed` to a warning, or makes a skipped test count as passed; muting a test is the cheapest way past a gate (design.md §5)"
      status: flagged
      verification: unverified
    - statement: "The `@test:` id lookup is never loosened with a basename fallback, a case-insensitive match, or a fuzzy match; core never guesses which test a scenario meant"
      status: flagged
      verification: unverified
    - statement: "`gate.author-match` is never promoted to an error; D-79 records that in the workflow accord itself prescribes it fires on nearly every honest ticket, and an error there would block every solo developer"
      status: flagged
      verification: unverified
    - statement: "The CLI never spawns `npm`, `npx`, or a `.cmd`, and never passes `shell: true`; only `git` by name, exactly as `gitTree` does (STACK 'What NOT to Use', D-51)"
      status: flagged
      verification: unverified
    - statement: "`loadFromFs` does not read, parse, or transmit git history beyond the author identity of files under `accord/tickets` and the gated commit; no diff, no message body, no remote is fetched"
      status: flagged
      verification: unverified
---

<objective>
**As a** developer whose test suite is green on screen, **I want** Done to check the report rather than my word for it, and to refuse when the test that proves a scenario was muted, **so that** a decorative gate is not mistaken for a real one.

This closes Phase 4: the Machine layer of Done (GATE-08), the author-mismatch warning (GATE-05), and the only producer of the host git facts both of them need.

Purpose: ROADMAP criterion 4 is fully proved, and the whole phase runs green under `npm run check` on both projects.
Output: seven more rules in the gate table, the `MACHINE` and `SOLO` tickets, the `gate-no-report` fixture, and `git` facts in `packages/cli/src/load/fs.ts`.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-gates/04-CONTEXT.md
@.planning/phases/04-gates/04-PATTERNS.md
@.planning/phases/04-gates/04-03-SUMMARY.md
@packages/core/src/lint/gherkin.ts
@packages/core/src/gate/done.ts
@packages/cli/src/load/fs.ts

<interfaces>
```typescript
// packages/core/src/gate/done.ts (added)
export const testTagMissing: GateRule['check'];
export const testsUnconfigured: GateRule['check'];
export const reportMissing: GateRule['check'];
export const testUnknown: GateRule['check'];
export const testNotPassed: GateRule['check'];
export const authorMatch: GateRule['check'];
export const authorSkipped: GateRule['check'];

// packages/cli/src/load/fs.ts (added, module-private)
function gitFacts(root: string): { commit: string; authors: Record<string, string> } | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Machine layer — the JUnit join, the two configuration reasons, and the `@ui` exemption</name>
  <read_first>
    - packages/core/src/lint/gherkin.ts lines 60-100: `isTest`, `perScenario`, `testTagMissing`, `testIdUnknown`, and `reportMissing` — the scenario-by-`snapshot.tests` join GATE-08 turns from warnings into errors, and the `Object.hasOwn` lookup that must be copied
    - packages/core/src/load/junit.ts: how a `<testcase>` becomes a `classname#name` key and where `passed`, `failed`, `skipped` come from, plus the `Object.hasOwn` fix the Phase 3 UAT made at line 41
    - packages/core/test/fixtures/lint-report/reports/junit.xml: a report already containing a passing, a failing, and a skipped case, and the escaped `&gt;` in a name
    - packages/core/test/fixtures/gate-done/reports/junit.xml and accord/config.yml (04-02): the report this task extends
    - packages/core/src/lint/tokens.ts `tokensMissing` and the D-67 note that the allowlist check is skipped when the tokens file is absent: the precedent for suppressing per-item findings when one configuration reason already fired
    - packages/core/src/gate/done.ts and rules.ts (04-02, 04-03) and packages/core/test/gate.test.ts (the `CASES` table and `GIT`)
    - .planning/phases/04-gates/04-CONTEXT.md, the "Machine layer and skipped checks" section: D-80 in full, including why the middle reading was rejected
  </read_first>
  <files>packages/core/src/gate/done.ts, packages/core/src/gate/rules.ts, packages/core/test/gate.test.ts, packages/core/test/fixtures/gate-done/accord/tickets/MACHINE.md, packages/core/test/fixtures/gate-done/accord/tickets/MACHINE/verification.md, packages/core/test/fixtures/gate-done/reports/junit.xml, packages/core/test/fixtures/gate-no-report/accord/config.yml, packages/core/test/fixtures/gate-no-report/accord/tickets/GONE.md, packages/core/test/fixtures/gate-no-report/accord/tickets/GONE/verification.md, packages/core/test/fixtures/gate-no-report/src/auth/login.ts, packages/core/test/fixtures/gate-no-report/test/login.spec.ts, packages/core/test/__golden__/gate-done.MACHINE.done.json, packages/core/test/__golden__/gate-no-report.GONE.done.json, packages/core/test/__golden__/gate-ready.CLEAN.done.json, packages/core/test/__golden__/verification-edges.A.done.json, packages/core/test/__golden__/gate-done.snapshot.json, packages/core/test/__golden__/gate-done.lint.json, packages/core/test/__golden__/gate-no-report.snapshot.json, packages/core/test/__golden__/gate-no-report.lint.json</files>
  <behavior>
    - Test 1 (`MACHINE`, the join): `@ac-1` names a case the report records as `skipped` and yields `gate.test-not-passed` at `error` with `skipped` in the reason; `@ac-2` names a case recorded `failed` and yields the same rule with `failed` in the reason; `@ac-3` names an id the report does not declare and yields `gate.test-unknown` at `error`; `@ac-4` carries no `@test:` tag and is not `@ui`, and yields `gate.test-tag-missing` at `error`. Each finding sits on its own Scenario line. Verdict `fail`.
    - Test 2 (`@ui` exemption, GATE-09): the `UINOTE` ticket's two `@ui` scenarios produce no machine-layer finding at all, and its Done result gains nothing from this task beyond what 04-03 pinned.
    - Test 3 (`gate-no-report`/`GONE`, D-80 second branch): `tests.report` is declared, its file is absent from the snapshot, and the result carries exactly one `gate.report-missing` at `error` on `accord/config.yml` with pointer `/tests/report`, and **no** per-scenario `gate.test-unknown`, and still carries `gate.test-tag-missing` for any untagged non-`@ui` scenario.
    - Test 4 (`gate-ready`/`CLEAN` run through Done, D-80 first branch): the config declares no `tests:` key, and the result carries exactly one `gate.tests-unconfigured` at `error` on `accord/config.yml` with pointer `/tests`, and no per-scenario test finding. The reason does not use the word skipped.
    - Test 5 (encoding, GATE-08): an id differing from a real key only in case yields `gate.test-unknown`; an id equal to `toString` yields `gate.test-unknown` unless the report declares it, proving the lookup does not walk the prototype chain; an id whose spaces were not converted to hyphens is unknown.
    - Test 6 (`PASS` unchanged): `gate-done`/`PASS` still has zero `error`-level findings.
  </behavior>
  <action>
**Report.** Extend `packages/core/test/fixtures/gate-done/reports/junit.xml` with two more cases in the same `<testsuite>`: one carrying a `<skipped/>` child and one carrying a `<failure>` child, whose derived ids are `test/login.spec.ts#skip1` and `test/login.spec.ts#fail1`. Leave the two existing passing cases untouched so `PASS` keeps passing. If the id derivation is not obvious, read `packages/core/src/load/junit.ts` and copy the shape from the `lint-report` fixture rather than guessing.

**Ticket `MACHINE.md`** in `gate-done`: a `type: story` sibling of `PASS`, fully bound (`ac_hash`, `verified: [ac-1, ac-2, ac-3, ac-4]`, `verified_hash`, `verified_commit`, a verification file whose `commit:` matches, four `## @ac-n` blocks with `Result: pass` and evidence citing `test/login.spec.ts`, and four `### @ac-n` notes each naming `src/auth/login.ts` with distinct Vietnamese prose). Its four scenarios are tagged `@ac-1 @test:test/login.spec.ts#skip1`, `@ac-2 @test:test/login.spec.ts#fail1`, `@ac-3 @test:nope#missing`, and `@ac-4` with no `@test:` tag and no `@ui`. Everything except the machine layer must be clean so the golden isolates exactly the four machine reasons. Obtain the hash by running the gate once and pasting the reported `acHash`.

**Fixture `gate-no-report`**: `accord/config.yml` identical to `gate-done`'s except `tests: { report: reports/gone.xml }`, with no `reports/` directory on disk. One ticket `GONE.md` and `GONE/verification.md`, both a fully bound copy of `PASS` whose two scenarios carry `@test:` tags. Add real `src/auth/login.ts` and `test/login.spec.ts` so the evidence and note references resolve.

**Checks added to `done.ts`**, each returning `[]` when the ticket is absent:
- `testTagMissing`: for each scenario whose `tags` do not include `@ui` and contain no tag starting `@test:`, one draft on the ticket file at the scenario's `line`, reason naming the scenario and saying it needs a `@test:<id>` tag or the `@ui` tag. This is a ticket property, so it runs regardless of the report configuration.
- `testsUnconfigured` (D-80): `snapshot.config?.tests?.report` is undefined **and** the ticket has at least one non-`@ui` scenario → one draft on `accord/config.yml` with pointer `/tests`, reason saying no test report is declared, so the machine layer cannot run and Done cannot pass. Do not use the word skipped in the reason; D-80 is precisely the decision that this is a failure and not a skip.
- `reportMissing`: `tests.report` is defined and `normaliseKey(report)` is not an own key of `snapshot.files` → one draft on `accord/config.yml` with pointer `/tests/report`, reason echoing the configured path verbatim, as the Phase 3 `lint.report-missing` reason does.
- `testUnknown` and `testNotPassed`: both return `[]` when `tests.report` is undefined or its file is absent, so one configuration problem yields one reason rather than one per scenario — the same shape as `tokensMissing` skipping the allowlist check under D-67. Otherwise, for each scenario not tagged `@ui` carrying a `@test:` tag, take the id after the `@test:` prefix and look it up with `Object.hasOwn(snapshot.tests ?? {}, id)`, never the `in` operator: absent → `testUnknown` draft at the scenario line whose reason echoes `config.tests.report` verbatim and the `classname#name` id shape, matching the Phase 3 `lint.test-id-unknown` reason; present but not `passed` → `testNotPassed` draft at the scenario line whose reason names the id and the recorded status verbatim.

**`rules.ts`** — add five rows to `DONE_RULES`, all `error`, both profiles: `gate.test-tag-missing`, `gate.tests-unconfigured`, `gate.report-missing`, `gate.test-unknown`, `gate.test-not-passed`.

**`gate.test.ts`** — add the case rows `gate-done.MACHINE.done`, `gate-no-report.GONE.done`, and `gate-ready.CLEAN.done` (the last reusing the 04-01 fixture, whose config declares no `tests:` key, so no new fixture is needed for the D-80 first branch). All three rows carry the shared `GIT`: `MACHINE` and `GONE` are fully bound copies of `PASS` whose `verified_commit` must match the gated commit, and giving `CLEAN` the same row shape keeps the table uniform. `GONE` in particular must not be left without `git` — a `git`-less row makes `gate.author-skipped` fire on it in Task 2 no matter what `GIT.authors` holds, which would move a golden Task 2 does not own. Add the pinned assertions for behaviours 1 to 6, plus a table assertion that every id added in this task is `level: 'error'` in `DONE_RULES`.

Regenerate the new goldens with scoped `-u`, then regenerate `verification-edges.A.done.json` (its config declares no `tests:` key, so it gains `gate.tests-unconfigured`), then the snapshot and lint goldens for both fixtures. Read every diff. Any golden belonging to an existing ticket that moves for a reason other than the machine layer is a finding for the SUMMARY. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core gate &amp;&amp; npm test -- --project core lint &amp;&amp; npm test -- --project core snapshot &amp;&amp; npm test -- --project core junit</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched; or `gate-done.PASS.done.json` gaining a finding whose `level` is `error`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'gate\.(test-tag-missing|tests-unconfigured|report-missing|test-unknown|test-not-passed)'" packages/core/src/gate/rules.ts` prints 5
    - `packages/core/test/__golden__/gate-done.MACHINE.done.json` contains `gate.test-not-passed`, `skipped`, `failed`, `gate.test-unknown`, and `gate.test-tag-missing`
    - `packages/core/test/__golden__/gate-no-report.GONE.done.json` contains `gate.report-missing`; `grep -c "gate.test-unknown" packages/core/test/__golden__/gate-no-report.GONE.done.json` prints 0
    - `packages/core/test/__golden__/gate-ready.CLEAN.done.json` contains `gate.tests-unconfigured`; `grep -ci "skipped" packages/core/test/__golden__/gate-ready.CLEAN.done.json` prints 0
    - `grep -c "Object.hasOwn" packages/core/src/gate/done.ts` prints at least 1
    - `grep -c "gate.test-" packages/core/test/__golden__/gate-done.UINOTE.done.json` prints 0 — no machine-layer rule reached a `@ui` scenario. Anchor on the `gate.test-` prefix and not on `gate.`: that golden legitimately carries `gate.note-pasted` from 04-03, so an unanchored count is non-zero whether or not a machine rule leaked in
    - `grep -c '"level": "error"' packages/core/test/__golden__/gate-done.PASS.done.json` prints 0
    - `npm test -- --project core gate` reports 0 failures
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>A muted test fails Done exactly as a failing one does, an undeclared or absent report fails Done rather than reporting a skip, and a `@ui` scenario is untouched by the machine layer.</done>
</task>

<task type="auto">
  <name>Task 2: The author warning and the host git facts, then the whole phase green</name>
  <read_first>
    - packages/cli/src/load/fs.ts (whole file, 76 lines): `gitTree`'s `execFileSync` shape — `git` by name, an explicit argument array, `stdio: ['ignore', 'pipe', 'pipe']`, `UsageError` on `ENOENT` — and the `.split(sep).join('/')` posix-ising at line 46
    - packages/cli/test/load.test.ts lines 1-80: `makeRepo` runs `git init -q` and never commits, so HEAD is unborn and the existing golden comparison depends on `git` being omitted entirely rather than set to `undefined`
    - packages/core/src/model/snapshot.ts: the `git?` field added in 04-01 Task 3 and its comment about the `authors` key set
    - packages/core/src/gate/done.ts (04-02 and Task 1) and rules.ts
    - .planning/phases/04-gates/04-CONTEXT.md, the "Author check (GATE-05)" section including the accepted finding that this warning fires on nearly every honest ticket
    - .claude/CLAUDE.md "What NOT to Use": never spawn `npm` or `npx`, never a `.cmd`, never `shell: true`; and `path.posix` for every stored or printed path
  </read_first>
  <files>packages/core/src/gate/done.ts, packages/core/src/gate/rules.ts, packages/core/test/gate.test.ts, packages/core/test/fixtures/gate-done/accord/tickets/SOLO.md, packages/core/test/fixtures/gate-done/accord/tickets/SOLO/verification.md, packages/core/test/__golden__/gate-done.SOLO.done.json, packages/core/test/__golden__/gate-done.PASS.noauthors.done.json, packages/core/test/__golden__/gate-done.snapshot.json, packages/core/test/__golden__/gate-done.lint.json, packages/cli/src/load/fs.ts, packages/cli/test/load.test.ts</files>
  <action>
**Author checks in `done.ts`.** A module-private `authorsOf(snapshot, id)` returns `{ commitAuthor, fileAuthor }` by reading `snapshot.git?.authors` at the key `snapshot.git.commit` and at the key `accord/tickets/<id>/verification.md`, or `undefined` for either when `git` is absent or the key is not an own property. Both checks return `[]` when `snapshot.verifications` has no own entry for the ticket, because `gate.verification-missing` already owns that case.
- `authorMatch` (GATE-05, D-79): both identities present and equal after `toLowerCase()` → one draft on `accord/tickets/<id>/verification.md` with no line, reason naming the shared identity and saying the review and the gated commit share an author, so no second pair of eyes touched this ticket.
- `authorSkipped`: either identity absent → one draft on the same file, reason saying the host supplied no author for the commit, for the review file, or for both, naming which.

**`rules.ts`** — add two rows to `DONE_RULES`, both `warning`, both profiles: `gate.author-match` and `gate.author-skipped`. They must be warnings: D-79 records that in the workflow accord itself prescribes, the authors match by construction, and an error there would block every solo ticket.

**Ticket `SOLO.md`** in `gate-done`: a fully bound, otherwise clean copy of `PASS` with two `@test:`-tagged scenarios pointing at the two passing report cases, so its only finding is the author warning and its verdict stays `pass`.

**`gate.test.ts`** — populate the shared `GIT.authors`. Its keys are fixture-relative posix paths and the one `GIT` object is spread onto every case row that supplies git, so the record must cover **every verification file on disk in every fixture a `GIT`-carrying row drives**, not only `gate-done`. A path missing from it is a hole in the test data rather than a fact about the ticket: in a real repository `gitFacts` walks `git log --name-only -- accord/tickets` and yields an author for every tracked verification file, so a fixture whose review file has no entry would make `gate.author-skipped` fire on an artifact of the harness. The deliberate skipped case is `PASS.noauthors`, which passes `authors: {}` on purpose. The record is therefore: the key equal to `GIT.commit` → `dev@example.test`; `accord/tickets/SOLO/verification.md` → `dev@example.test`; and `reviewer@example.test` for every other verification path on disk — the eight remaining `gate-done` ones, plus `accord/tickets/A/verification.md` (fixture `verification-edges`) and `accord/tickets/GONE/verification.md` (fixture `gate-no-report`). No two of those ticket ids collide, so one flat record serves all three fixtures.

**Expected golden delta from this task: `gate-done.SOLO.done.json` and `gate-done.PASS.noauthors.done.json`, and nothing else** — which is why those two are the only `.done.json` files in this task's `files`. Every other review path now resolves to `reviewer@example.test`, which differs from the commit author, so `authorMatch` stays silent and `authorSkipped` has both identities and stays silent too. The two goldens outside `gate-done` whose ticket carries a review file, `gate-no-report.GONE.done.json` and `verification-edges.A.done.json`, must come back byte-identical to what Task 1 produced; if either moves, the cause is a missing key in `GIT.authors` and the fix is the key, not a regenerated golden. `gate-ready.CLEAN.done.json` has no review file at all, so both author checks return `[]` for it and it is unaffected either way.

Add the case rows `gate-done.SOLO.done` and `gate-done.PASS.noauthors.done`, the latter reusing the `PASS` ticket with `git: { commit: GIT.commit, authors: {} }`. Pin: `SOLO` has exactly one `gate.author-match` at `warning` and `verdict: 'pass'`; `PASS.noauthors` has exactly one `gate.author-skipped` at `warning` and no `error`; `PASS` under the full `GIT` has neither, because its review author differs; `NOVERIF` has `gate.verification-missing` and neither author rule; a case supplying `authors` whose file identity differs only in letter case still matches, because the comparison lower-cases both sides.

**`packages/cli/src/load/fs.ts`.** Add a module-private `gitFacts(root)` returning `{ commit, authors }` or `undefined`:
- One `execFileSync('git', ['show', '-s', '--format=%H%x00%ae', 'HEAD'], { cwd: root, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] })`. Wrap it in a `try`, and on any failure — an unborn HEAD exits non-zero — return `undefined`. Re-throw the `ENOENT` `UsageError` the same way `gitTree` does only if `gitTree` has not already run; since `loadFromFs` calls `gitTree` first, a missing `git` binary has already produced that error, so here a failure simply means there is no commit.
- One `execFileSync('git', ['log', '--format=%x01%ae', '--name-only', '--', 'accord/tickets'], ...)`, also in a `try` returning an empty record on failure. Parse: a line beginning with the `U+0001` marker sets the current author to the rest of that line; any other non-empty line is a path, recorded only if not already recorded, because `git log` emits the most recent commit first. Posix-ise each path with `.split(sep).join('/')`, exactly as `accordFiles` does at line 46, so no key contains a backslash.
- Assemble `authors` as the per-file record plus one entry keyed by the commit sha carrying the commit author, and return `{ commit, authors }`.
In `loadFromFs`, call `gitFacts(root)` after the config pass and spread the result into the returned object only when it is defined, so the key is absent rather than `undefined` when there is no commit. Spawn `git` by name with an explicit argument array; never a `.cmd`, never `npm` or `npx`, never `shell: true`.

**`packages/cli/test/load.test.ts`.** Leave the existing golden comparison alone — `makeRepo` never commits, so `git` is absent and the comparison is byte-unchanged; add an explicit assertion that `loadFromFs(makeRepo())` has no own `git` property.

Add a second helper, `makeCommittedRepo()`, that copies the fixture into the existing `mkdtemp` sandbox that `cleanup` deletes and produces one commit inside it. **Every git invocation in this helper must carry an explicit `cwd` pointing at the sandbox.** `execFileSync` defaults to `process.cwd()`, which under vitest is the accord repository root; a single omitted `cwd` on the `add -A` / `commit` pair stages and commits the author's entire dirty working tree, silently, inside a green test run — and this repository's standing rule is that no task, and no test, ever runs `git commit` against it. Two structural guards, both required, neither optional:

1. The helper opens with a hard assertion before it writes anything: `tmp` must start with `realpathSync(os.tmpdir())` and `resolve(tmp)` must not equal `resolve(process.cwd())`. Throw, do not `expect`, so the guard fires even if the helper is ever called outside a test body.
2. The helper defines exactly one local wrapper and routes every git call through it — `const git = (...args: string[]) => execFileSync('git', args, { cwd: tmp, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] });` — so `cwd: tmp` is written once and cannot be forgotten on the third call. There must be exactly one `execFileSync('git'` in the whole file.

Through that wrapper: `git('init', '-q')`, then `git('-c', 'user.email=dev@example.test', '-c', 'user.name=Dev', '-c', 'commit.gpgsign=false', 'add', '-A')`, then the matching `commit -m seed` with the same `-c` flags. This is a throwaway temporary repository, not the accord repository.

Assert on the result: `git.commit` matches `/^[0-9a-f]{40}$/`; `git.authors[git.commit]` is `dev@example.test`; `git.authors['accord/tickets/LOGIN-1/verification.md']` is `dev@example.test`; no key of `git.authors` contains a backslash; and `loadSnapshot(input).git` carries the same object, so the field survives the loader. Add one more assertion that closes the loop on the guard: run `git('rev-parse', '--show-toplevel')` and assert its `realpathSync`d value equals the `realpathSync`d sandbox path and is **not** the accord repository root — so a helper that ever drifted onto the real repository fails the test rather than committing.

Finish with the full `npm run check` equivalent so Phase 4 is proved green on both projects. Leave everything uncommitted for the author to review.
  </action>
  <verify>
    <automated>H=$(git rev-parse HEAD) &amp;&amp; test -n "$H" &amp;&amp; C=$(grep -vE '^[[:space:]]*(//|\*|/\*)' packages/cli/test/load.test.ts) &amp;&amp; test "$(printf '%s' "$C" | grep -cF "execFileSync('git'")" = 1 &amp;&amp; test "$(printf '%s' "$C" | grep -cF 'cwd: tmp')" -ge 1 &amp;&amp; test "$(printf '%s' "$C" | grep -cF 'show-toplevel')" = 1 &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; test "$(git rev-parse HEAD)" = "$H" &amp;&amp; git diff --cached --quiet</automated>
    <fails_when>non-zero exit; or `No test files found`; or a vitest summary line reading `0 passed`; or a golden reported obsolete or mismatched in either project; or the cli `load` test reporting a file-list or tree mismatch; or `gate-done.PASS.done.json` carrying a finding whose `level` is `error`; or `gate-no-report.GONE.done.json` or `verification-edges.A.done.json` reporting a snapshot mismatch or containing `gate.author`, either of which means `GIT.authors` is missing that fixture's review path and the fix is the key rather than a regenerated golden; or the `test` guards failing, which means the comment-stripped `packages/cli/test/load.test.ts` has more than one git invocation site, none carrying `cwd: tmp`, or no `show-toplevel` assertion; or the trailing HEAD comparison reporting a different HEAD from the one captured before the suite ran, or `git diff --cached --quiet` exiting non-zero because the index no longer matches HEAD — either means the suite reached the accord repository's own git state, by committing or by staging, the outcome the standing rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "'gate\.(author-match|author-skipped)'" packages/core/src/gate/rules.ts` prints 2, and both rows carry `level: 'warning'`
    - `packages/core/test/__golden__/gate-done.SOLO.done.json` contains `gate.author-match` and `"verdict": "pass"`; `grep -c '"level": "error"' packages/core/test/__golden__/gate-done.SOLO.done.json` prints 0
    - `packages/core/test/__golden__/gate-done.PASS.noauthors.done.json` contains `gate.author-skipped`
    - `grep -c "gate.author" packages/core/test/__golden__/gate-done.PASS.done.json` prints 0
    - `grep -c "gate.author" packages/core/test/__golden__/gate-no-report.GONE.done.json` prints 0 and `grep -c "gate.author" packages/core/test/__golden__/verification-edges.A.done.json` prints 0 — the two goldens outside `gate-done` whose ticket has a review file gained nothing from this task and were not regenerated by it
    - `grep -cF 'accord/tickets/A/verification.md' packages/core/test/gate.test.ts` prints at least 1 and `grep -cF 'accord/tickets/GONE/verification.md' packages/core/test/gate.test.ts` prints at least 1 — `GIT.authors` covers the review paths of the two fixtures outside `gate-done` that git-carrying rows drive
    - `packages/cli/src/load/fs.ts` contains `'show'`, `'--name-only'`, and `split(sep).join('/')`; `grep -cE "shell:\s*true|'npx'|'npm'|\.cmd" packages/cli/src/load/fs.ts` prints 0
    - `packages/cli/test/load.test.ts` contains `commit.gpgsign=false` and `/^[0-9a-f]{40}$/`
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' packages/cli/test/load.test.ts | grep -cF "execFileSync('git'"` prints 1 — a single invocation site in code, the `git(...)` wrapper, so `cwd: tmp` is written once and cannot be omitted on a later call. Comments are filtered first, because this plan's own wording about the single site is exactly what a helper comment would restate
    - `grep -c "cwd: tmp" packages/cli/test/load.test.ts` prints at least 1, and that occurrence is inside the wrapper's options object
    - `grep -c "tmpdir" packages/cli/test/load.test.ts` prints at least 1 — the helper's opening guard asserts the sandbox lies under the OS temp directory before anything is written
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' packages/cli/test/load.test.ts | grep -cF show-toplevel` prints 1 — the helper asserts its repository root is the sandbox and not the accord repository
    - `npm test` reports 0 failures across both projects
    - `git status --porcelain` is non-empty, `git log -1 --format=%H` is unchanged, and `git diff --cached --quiet` exits 0 (work is left uncommitted and unstaged)
  </acceptance_criteria>
  <done>The CLI is the only producer of the host git facts, the author warning fires without ever changing a verdict, and the whole phase is green under build, lint, typecheck, and test on both projects.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 04-04)

**New exported values:** `testTagMissing`, `testsUnconfigured`, `reportMissing`, `testUnknown`, `testNotPassed`, `authorMatch`, `authorSkipped` (all in `packages/core/src/gate/done.ts`).

**Module-internal symbols:** `authorsOf` in `gate/done.ts`; `gitFacts` in `packages/cli/src/load/fs.ts`.

**New rule ids:** `gate.test-tag-missing`, `gate.tests-unconfigured`, `gate.report-missing`, `gate.test-unknown`, `gate.test-not-passed`, `gate.author-match`, `gate.author-skipped`.

**Changed behaviour:** `loadFromFs` now returns `SnapshotInput.git` when the repository has a commit, and omits the key otherwise.

**New fixture tickets (in `gate-done`):** `MACHINE`, `SOLO`, each with a `verification.md`; two additional `<testcase>` entries in `gate-done/reports/junit.xml` with ids `test/login.spec.ts#skip1` and `test/login.spec.ts#fail1`.

**New fixture:** `gate-no-report` — ticket `GONE`, `src/auth/login.ts`, `test/login.spec.ts`, config declaring `tests.report: reports/gone.xml` with no report file on disk.

**New goldens:** `gate-done.{MACHINE,SOLO}.done.json`, `gate-done.PASS.noauthors.done.json`, `gate-no-report.GONE.done.json`, `gate-ready.CLEAN.done.json`, `gate-no-report.{snapshot,lint}.json`; regenerated `verification-edges.A.done.json`, `gate-done.{snapshot,lint}.json`.

**New test-file symbols:** the populated `GIT.authors` record and the committing repository helper in `packages/cli/test/load.test.ts`.

**New CLI flags, subcommands, config keys:** none. Phase 4 adds no process surface; commander commands and exit codes are Phase 5.
</artifacts_produced>

<flagged_assumptions>
## Stated assumptions (owner may overturn)

Five of the 26 probe-surfaced edge items belong to this plan's requirements. Count for this plan: 4 authored into `must_haves.truths` (GATE-08 adjacency, empty, encoding, ordering) + 1 surfaced here (GATE-05 unclassified) = 5.

**Phase total across the four plans: 20 authored into `must_haves.truths` (19 plain, 1 `verification: backstop`) + 6 surfaced as flagged assumptions = 26.** No probe item was dropped.

| Source | Item | Disposition |
|---|---|---|
| Edge probe, GATE-05 (unclassified) | The probe could not classify GATE-05 | Unresolved. D-79 already records the important finding — the warning fires on nearly every honest ticket by construction — and the owner accepted it rather than resolving it. Revisit with GATE-13 |
| D-78 under-specified (`authors` key set) | The gated commit's author rides in `git.authors` under a key equal to the exact value of `git.commit`; file authors keep their repo-relative posix path keys | Chosen because D-78 pins the shape `{ commit, authors }` and a sha can never collide with a repo-relative path. Alternative rejected: adding a `git.author` scalar, which would change a locked decision's shape |
| CONTEXT discretion (identity format) | The identity is the git author email (`%ae`), compared after `toLowerCase()` on both sides | Chosen because `%ae` is one format-string away in the same `git show` and `git log` calls the loader already needs, and it compares exactly. Alternative: `%an`, the display name, which collides more easily |
| D-80 extended to the absent report | `tests.report` declared but the file absent from the snapshot fails Done with `gate.report-missing`, not a skip | Chosen. D-80 rejected the "skip when the key is absent, fail when the file is absent" reading because it rewards never switching the layer on; the symmetric hole — declare the key and delete the file — has to close the same way |
| GATE-08, suppression (not decided) | When `gate.tests-unconfigured` or `gate.report-missing` fires, the per-scenario `gate.test-unknown` and `gate.test-not-passed` are suppressed | Chosen so one configuration problem yields one reason rather than one per scenario, the same shape as `tokensMissing` skipping the allowlist check under D-67. `gate.test-tag-missing` is **not** suppressed, because it is a property of the ticket. Alternative: report every scenario, which is louder but buries the actual fix |
| GATE-05 suppression | `gate.author-skipped` is suppressed when the verification file is missing | Chosen so a ticket that never had a review is reported once, by `gate.verification-missing`, rather than twice |
| CLI, unborn HEAD | `gitFacts` returns `undefined` on any `git show` failure, and `loadFromFs` omits the `git` key entirely rather than setting it to `undefined` | Chosen because `packages/cli/test/load.test.ts` runs `git init` without committing and compares the whole loaded snapshot against the core golden; an omitted key keeps that byte-identical, and Done then fails cleanly with `gate.commit-missing` |
| CLI, author history scope | One `git log --name-only -- accord/tickets` walk, first author per path wins | Chosen to keep it to one subprocess. A repository with a very long `accord/tickets` history pays for the whole walk; the alternative, one `git log -1` per verification file, is one spawn per ticket, which is worse. Flagging it so the owner can revisit if the pilot repository grows |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JUnit XML from `tests.report` → the loader's scanner and the gate's join | A file any build step can write; the artefact the whole Machine layer trusts |
| `git` subprocess output → `SnapshotInput.git` | Text from a program the CLI spawns, parsed by a line scanner in the impure package |
| repository path names → the `git log --name-only` parse | Path text under attacker influence if someone can add a file to `accord/tickets` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-19 | Tampering | muting a test, or deleting the `tests.report` line, to walk past the Machine layer | high | mitigate | A `skipped` status fails Done exactly as `failed` does, and both an undeclared and an absent report fail rather than skip (D-80); Task 1 Tests 1, 3, and 4 pin all three doors |
| T-04-20 | Spoofing | a hand-written JUnit report declaring every id as passed | high | accept | Recorded in the Phase 3 security review as Phase 4 gate scope and accepted here: the Machine layer proves a report says the test passed, not that the report is honest. The Fresh-context and Human layers are what a hand-written report does not survive, and `docs/design.md` §5 states this ceiling |
| T-04-21 | Tampering | a `@test:` id crafted to match a real key through case folding, a basename fallback, or the prototype chain | high | mitigate | Exact code-point lookup with `Object.hasOwn` and no fallback of any kind, copied from the Phase 3 UAT fix; Task 1 Test 5 pins the case, the `toString` case, and the unhyphenated case |
| T-04-22 | Elevation of privilege | argument or command injection through a path or a ref reaching the `git` invocation | high | mitigate | `execFileSync` with `git` by name and a fully literal argument array; no value from the repository or from user input is interpolated into an argument, `shell: true` is never passed, and the acceptance criteria grep for `shell: true`, `npx`, `npm`, and `.cmd` |
| T-04-23 | Denial of service | a repository with a very long `accord/tickets` history, or a report with very many test cases, driving the loader | medium | mitigate | One bounded `git log` walk over one subtree in one spawn, and one linear parse of its output; the report is already scanned once by the Phase 3 loader and the gate only joins against the resulting record |
| T-04-24 | Information disclosure | author email addresses appearing in gate output and in `--json` under Phase 5 | low | accept | The identities come from the repository's own commit metadata, which every clone already carries; the reason names them so the developer can see which two identities matched |
| T-04-25 | Tampering | a path containing a newline breaking the `git log --name-only` parse and mis-attributing an author | low | accept | A verification path is `accord/tickets/<id>/verification.md` where `<id>` matches the schema pattern `^[A-Za-z0-9][A-Za-z0-9._-]*$`, so a newline cannot occur; a mis-parse elsewhere in the subtree only affects a key no check reads |
| T-04-SC | Tampering | npm, pip, or cargo installs | high | mitigate | No package is added by this plan; `package.json` dependency blocks are untouched |
</threat_model>

<verification>
- Task 2's gate is the full `npm run build && npm run lint && npm run typecheck && npm test`, which is the phase gate: the whole gate rule table, every gate golden, both vitest projects, on Windows and Ubuntu in CI.
- ROADMAP criterion 4 is fully proved here: `MACHINE` for the skipped and failed cases, `gate-no-report`/`GONE` and `gate-ready`/`CLEAN` for the two configuration branches, `SOLO` for the author match, and `PASS.noauthors` for the skipped author check.
- `gate-done.PASS.done.json` carrying zero `error`-level findings at the end of this plan is the single strongest signal that the four plans compose.
</verification>

<success_criteria>
- Every one of GATE-01 to GATE-11 has at least one rule id, one fixture, and one golden.
- `npm run check` is green on both projects.
- No commit, no push, no tag in the accord repository; the working tree is left for the author to review.
</success_criteria>

<output>
Create `.planning/phases/04-gates/04-04-SUMMARY.md` when done. Do not commit.
</output>
