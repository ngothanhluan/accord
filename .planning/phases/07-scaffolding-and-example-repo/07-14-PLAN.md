---
phase: 07-scaffolding-and-example-repo
plan: 14
type: execute
wave: 3
depends_on: ["07-13"]
files_modified:
  - test/helpers/denied.ts
  - packages/core/test/helpers/denied.ts
  - packages/core/test/examples.test.ts
  - packages/core/test/scaffold.test.ts
  - packages/core/test/skills.test.ts
  - packages/core/test/templates.test.ts
  - packages/core/src/scaffold/pointer.ts
  - packages/cli/test/bin.test.ts
  - packages/cli/src/render/table.ts
autonomous: true
gap_closure: true
status: pending
requirements: [CLI-01, SKILL-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The bytes `npm publish` uploads carry no denied name: a `deniedNames` scan runs over `packages/cli/dist/cli.js` — the npm tarball's only code, under `files: [\"dist\", \"README.md\"]` — and returns no offender (gap 2, owner ruling GAP-2a)"
    - "That scan reads a real bundle or fails loudly: a missing or truncated `dist/cli.js` makes the case fail naming `run npm run build first`, and a live probe proves the scan still matches a planted name before the real assertion is made"
    - "One denied list, one scan, every surface: `DENIED` and `deniedNames` live at `test/helpers/denied.ts` and are imported by both packages' test trees, so no second copy exists to go stale (owner ruling GAP-2b, and the sentence the file's own docstring has claimed since 07-04)"
    - "The JSDoc that put the name in the bundle documents an adapter key accord actually ships, so the scan is green because the breach is gone rather than because it was excused"
    - "No allowlist and no written carve-out was added: verified before planning that a full denied scan over `packages/cli/dist/cli.js` returns exactly one offender — at line 419 of the bundle as built on 2026-09-18, a position 07-12 shifts, which is why every check in this plan matches the path prefix and not the line"
  artifacts:
    - test/helpers/denied.ts
    - packages/cli/test/bin.test.ts
    - packages/cli/src/render/table.ts
  key_links:
    - "`packages/cli/test/bin.test.ts` already resolves `../dist/cli.js` and already carries a `run npm run build first` guard, so the scan joins an existing reader of that artifact instead of creating a second one"
    - "no tsconfig in this repository sets `rootDir` or `composite`, and every one sets `noEmit: true`, which is what lets a file outside a project's `include` join that project's program through an import without TS6059"
    - "CI runs `npm run build` before `npm test` in both jobs, and the verify commands here do the same — that, not the test, is what makes the scanned bundle fresh; a test cannot prove `dist` matches `src`"
  prohibitions:
    - statement: "no allowlist, no skip list, and no exception entry may be added to the denied scan — a constraint with an exception is one nobody can enforce later (owner ruling F-2, restated in GAP-2a)"
      status: unverified
      verification: judgment
    - statement: "`packages/cli/dist/cli.js` is a READ target only; it is a build output and must never appear as an edit target in any task"
      status: unverified
      verification: judgment
    - statement: "a core test must never read `../../cli/dist/**` — that inverts the package dependency direction and breaks `vitest --project core` in isolation (rejected alternative, ruling GAP-2b)"
      status: unverified
      verification: judgment
---

<objective>
Gap 2 of `07-VERIFICATION.md` (status `partial`, blocker), under owner rulings **GAP-2a** and **GAP-2b**.

`packages/cli/dist/cli.js:419` contains a tracker product name, preserved by the bundler from the JSDoc
at `packages/cli/src/render/table.ts:24` — tsdown strips line comments and keeps block comments.
`packages/cli/package.json` declares `files: ["dist", "README.md"]`, so this is a published byte and
Phase 9 is publish.

The structural reason nothing caught it: all of `deniedNames`'s call sites scan INPUTS to the build —
the templates record, the rendered skill bodies, the scaffold text, the emitted workflow, the example
prose. None scans its OUTPUT. Closing the templates surface in 07-11 is what made that visible.

The owner ruled both halves: add the scan over the built bundle, and move the list so both packages'
tests read one copy. Reword-only was rejected, because without the scan the next block comment
reintroduces the breach silently.

Purpose: the constraint is only held where a machine holds it. It is now held over every text accord
authors and over the one artifact it publishes.
Output: one relocated helper, four updated imports, one new scan, one reworded JSDoc.

**On SKILL-04 in this plan's `requirements`.** The touch is indirect and is declared as such: no task here
changes a skill definition, a rendered skill body, or the substance of the scan that guards them. What
changes is `packages/core/test/skills.test.ts` — its import specifier and one comment — because that file
is one of the four call sites of the list being relocated, and the scan it runs over the rendered skill
bodies is the machine half of SKILL-04's "never restates a rule the CLI enforces". The ID is claimed so
that the requirement's only guard is not silently re-pointed at a new module with nothing recording that
it moved; it is not claimed as new coverage of the requirement.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/07-scaffolding-and-example-repo/07-CONTEXT.md
@.planning/phases/07-scaffolding-and-example-repo/07-VERIFICATION.md
@.planning/phases/07-scaffolding-and-example-repo/07-REVIEW.md
@.planning/phases/07-scaffolding-and-example-repo/07-11-SUMMARY.md
</context>

<surfaced_assumptions>
- **A-45 — the bundle really does hold exactly one offender, and no allowlist is needed.** Re-run before
  planning, with the live `DENIED` list and the live `deniedNames` regex, over both published bundles:
  `packages/cli/dist/cli.js` returns one offender, at line 419, and nothing else. The
  `linear-gradient` false positive the report worried about does not occur in that file. This agrees with
  the owner's pre-ruling check, independently reached.

- **A-46 — `packages/core/dist/index.js` is a published byte, does hit the scan, and is a DECIDED
  non-breach (owner ruling GAP-2d).** `packages/core/package.json` declares
  `files: ["dist", "schemas", "templates", "README.md"]` and `@accord-dev/accord` depends on
  `@accord-dev/accord-core`, so that bundle ships too. Line 1259 of it matches `Lin`+`ear`, from the CSS
  function name inside the tokens lint rule's own regex
  (`/^(?:linear|radial|conic)-gradient\(/i`) — a gradient function in a regex literal, naming no product.
  The owner ruled it a non-breach, scanned the CLI bundle only, and settled the alternatives so Phase 9
  inherits a decision rather than a question. All three were verified before the ruling:
  - **Tighten the scan** to `(?<![\w-])name(?![\w-])` — does NOT clear the match (the word is preceded by
    `:` and followed by `|`; the hyphen sits after the `)`), and it loses real detections such as a
    hyphenated product name in prose.
  - **Reword the lint rule** to `/^[a-z]+-gradient\(/i` — clears it, but edits a working rule to satisfy a
    scan and loosens what that rule accepts.
  - **Allow the match** — the allowlist ruling GAP-2a forbids.

  Do not widen the scan to core's bundle, do not add an allowlist, and do not touch the tokens lint rule.
  Record the decision and its three rejected routes in the summary, as a decision.

- **A-47 — the relocation satisfies the documented convention rather than contradicting it.**
  `packages/cli/test/load.test.ts:16` records "Same replacer as `packages/core/test/helpers/fixture.ts`;
  not imported across packages". After this move nothing imports ACROSS a package boundary: both test
  trees import DOWNWARDS from a shared repository-root helper. (The same file at line 11 already reads
  core's fixture directory by relative URL, so reaching outside the package is established for data; the
  convention that is written down is about helper modules, and this plan removes the cross-package
  helper import rather than adding one.)

- **A-48 — the move typechecks and lints without configuration changes.** No tsconfig sets `rootDir` or
  `composite`; all set `noEmit: true`; `packages/core/tsconfig.test.json` and `packages/cli/tsconfig.json`
  both set `types: ["node"]`. A file outside `include` that is reached by an import joins the program.
  ESLint's `files: ['**/*.{js,mjs,cjs}', '**/scripts/**', '**/test/**']` block already gives the root
  `test/` directory node globals, and the core-purity `no-restricted-imports` block is scoped to
  `packages/core/src/**`, which the new location is not. Vitest collects only `test/**/*.test.ts` per
  project, so a non-`.test.ts` file at the repository root is never a suite. Every one of these is
  asserted by running `npm run lint` and `npm run typecheck`, not by this paragraph.
</surfaced_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: One denied list, at a path both test trees can reach</name>
  <files>test/helpers/denied.ts, packages/core/test/helpers/denied.ts, packages/core/test/examples.test.ts, packages/core/test/scaffold.test.ts, packages/core/test/skills.test.ts, packages/core/test/templates.test.ts, packages/core/src/scaffold/pointer.ts</files>
  <read_first>
    - packages/core/test/helpers/denied.ts (the whole file, 44 lines — the header explaining why the list moved out of `skills.test.ts` in 07-04, the `DENIED` array with its split literals, the `cmp` comparator, and `deniedNames`)
    - the four import sites, one line each: packages/core/test/examples.test.ts:12, packages/core/test/scaffold.test.ts:18, packages/core/test/skills.test.ts:15, packages/core/test/templates.test.ts:7
    - the three comments that name the old path: packages/core/test/scaffold.test.ts:382, packages/core/src/scaffold/pointer.ts:29, and packages/core/test/skills.test.ts:291 ("The list and the scan moved to `helpers/denied.ts` in 07-04")
    - packages/cli/test/load.test.ts (lines 11 and 16 — the cross-package fixture read and the written convention about helpers, which A-47 addresses)
    - tsconfig.base.json, packages/core/tsconfig.json, packages/core/tsconfig.test.json, packages/cli/tsconfig.json (no `rootDir`, no `composite`, `noEmit: true` everywhere — the reason the move needs no config change)
    - eslint.config.js (the `**/test/**` node-globals block and the `packages/core/src/**` purity block)
    - vitest.config.ts and both package `vitest.config.ts` files (`projects: ['packages/core', 'packages/cli']`, each including `test/**/*.test.ts`)
    - .planning/phases/07-scaffolding-and-example-repo/07-VERIFICATION.md `owner_rulings` entry GAP-2b (the ruling and its two rejected alternatives)
  </read_first>
  <behavior>
    - `test/helpers/denied.ts` exists at the repository root and exports `DENIED` and `deniedNames` unchanged in behaviour — same list, same split literals, same code-point sort, same `path:line: name` offender format.
    - All four core test files import it from the new path and their scans return exactly what they returned before.
    - `npm run typecheck` and `npm run lint` both exit 0 with no configuration file edited.
    - `npm test` collects the same number of test FILES as before — the relocated helper is a module, never a suite.
  </behavior>
  <action>
Move `packages/core/test/helpers/denied.ts` to `test/helpers/denied.ts` at the repository root, creating
that directory. The file's contents are unchanged except its header: add one sentence saying it sits at
the root because the constraint it holds is the repository's rather than one package's, and that both
test trees import downwards from here so that nothing imports a helper across a package boundary — which
is what `packages/cli/test/load.test.ts:16` asks for, not merely a rule it is exempt from. Delete the old
file; do not leave a re-export shim at the old path, because a shim is the second copy the list moved to
avoid.

Update the four import specifiers to `'../../../test/helpers/denied.js'` — three levels up from
`packages/<pkg>/test/` reaches the repository root, and the `.js` suffix is required by
`verbatimModuleSyntax` with `nodenext` resolution. Update all THREE comments that name the old path:
`packages/core/test/scaffold.test.ts:382`, `packages/core/src/scaffold/pointer.ts:29`, and
`packages/core/test/skills.test.ts:291`. The second is inside core's shipped source, so a stale path there
is a wrong statement in a published byte. The third reads "The list and the scan moved to
`helpers/denied.ts` in 07-04" — a sentence about history, but it names a path a reader will go looking
for and not find, so update the path and leave the history: it moved to `helpers/denied.ts` in 07-04 and
to the repository root here.

Add nothing else to the new directory. `test/helpers/` holds this one file; it is not a place for future
shared helpers to accumulate by default, and `packages/cli/test/init.test.ts:57-72` and
`packages/cli/test/skills-sync.test.ts` deliberately keep their own local copies of `listing`, which
stays true.

Run `npm run typecheck` and `npm run lint` before running any test, and record in the summary what each
reported — those two commands are the whole of the evidence that the move is configuration-free, and if
either needs a tsconfig or eslint edit to pass, STOP and report it rather than editing configuration to
chase the move.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck</automated>
    <fails_when>non-zero exit, or `TS6059`, `TS6307` or `TS2307` in the output — the first two mean a project boundary rejected the new location, the third means an import specifier is wrong</fails_when>
    <automated>npm run build &amp;&amp; npm test -- --project core</automated>
    <fails_when>non-zero exit, or `0 passed` in the vitest summary line, or a change in the reported number of test files — a relocated helper that got collected as a suite would raise it</fails_when>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or any suite reporting a failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/helpers/denied.ts` exists at the repository root and `packages/core/test/helpers/denied.ts` does not.
    - Each of `packages/core/test/{examples,scaffold,skills,templates}.test.ts` imports `deniedNames` from `'../../../test/helpers/denied.js'` — four files, one import line each, no other specifier for that module anywhere in the repository.
    - All three of `packages/core/src/scaffold/pointer.ts`, `packages/core/test/scaffold.test.ts` and `packages/core/test/skills.test.ts` name the new path in the comments that previously named the old one; no comment anywhere in the repository still points a reader at `packages/core/test/helpers/denied.ts`.
    - `DENIED`'s entries are unchanged, still split literals, and `Shortcut` is still absent from the list.
    - No configuration file was edited: `git status --porcelain tsconfig.base.json eslint.config.js vitest.config.ts packages/core/tsconfig.json packages/core/tsconfig.test.json packages/cli/tsconfig.json packages/core/vitest.config.ts packages/cli/vitest.config.ts` prints nothing. These eight are tracked and clean against `HEAD` today — unlike most of Phases 6 and 7 — so an empty result is a reachable passing state and not a check that could never pass.
    - `npm run lint` and `npm run typecheck` both exit 0, and the summary records their output.
  </acceptance_criteria>
  <reversibility rating="reversible">A file move and five import or comment lines, in test-only code plus one comment in core. Nothing is exported to a consumer, nothing is published, and reverting is `git mv` in the other direction with the same five lines.</reversibility>
  <done>The project's denied-name list lives where both packages' tests can read it, so the next surface to need the scan does not need a second copy of the list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The scan reaches the bytes the package publishes, and goes red</name>
  <files>packages/cli/test/bin.test.ts</files>
  <read_first>
    - packages/cli/test/bin.test.ts (the whole file, 30 lines — the `cli` constant at line 6 resolving `../dist/cli.js`, and the `run npm run build first` guard at 9-11 this case reuses rather than restates)
    - packages/core/test/bundle.test.ts (lines 12-17 and 33-40 — `assertBuilt` and the case proving its message fires on an empty directory; the established shape for making a missing build loud instead of vacuous)
    - packages/core/test/templates.test.ts (lines 139-155 — the live probe and the record-length floor, the exact guard-the-guard shape this case copies so the two scans read the same way)
    - test/helpers/denied.ts (as relocated by task 1 — `deniedNames` takes `{ path, text }[]` and returns `path:line: name` strings)
    - packages/cli/package.json (`files: ["dist", "README.md"]` — the line that makes `dist/cli.js` a published byte)
    - .github/workflows/ci.yml (both jobs run `npm run build` before `npm test`, which is what keeps the scanned bundle fresh on a runner)
  </read_first>
  <behavior>
    - Against the bundle as it stands before task 3, the case FAILS with exactly one offender, whose text begins `packages/cli/dist/cli.js:`. The line number it carries is recorded, not asserted — 07-12 shifts it.
    - After task 3 it passes with an empty offender list.
    - Given a missing or empty `dist/cli.js`, it fails naming `run npm run build first` rather than scanning nothing and passing.
    - The live probe still returns one offender for a planted name, so a scan that had silently stopped matching cannot read as a pass.
  </behavior>
  <action>
Add one case to `packages/cli/test/bin.test.ts`, named
`the published bundle names no other tool, plugin, harness, or planning system (CLAUDE.md)`, importing
`deniedNames` from `'../../../test/helpers/denied.js'`.

Inside it, in this order: assert `existsSync(cli)` with the message `run npm run build first`, matching
the existing case's wording so a missing build reads the same everywhere; read the file with
`readFileSync(cli, 'utf8')`; assert its length is greater than 1000, so a truncated or placeholder bundle
fails here instead of being scanned as an empty string; run the live probe — one `{ path, text }` whose
text carries a denied name assembled from two string halves, exactly as `templates.test.ts:153` does, and
assert `deniedNames` returns the single expected offender; only then assert
`deniedNames([{ path: 'packages/cli/dist/cli.js', text }])` equals `[]`.

Write the probe's name split across a concatenation, as `DENIED`'s own entries and both existing probes
are, so this test file does not contain verbatim the names it forbids and trip the scan it runs.

Add one comment naming the honest ceiling: no test can prove `dist` matches `src`, so freshness comes
from `npm run build` preceding `npm test` — in the verify commands here and in both CI jobs — and not from
this case. State it rather than implying the scan is stronger than it is.

Then run `npm run build && npm test -- --project cli bin` and CONFIRM RED. The received array must hold
exactly one string, beginning `packages/cli/dist/cli.js:` and naming a denied entry. Record the string
VERBATIM in the summary, including whatever line number it carries on the day it runs.

**Identify that red by content, never by the line number.** The offender sits at line 419 of the bundle
today, but 07-14 runs in wave 3 and 07-12 has already edited `packages/cli/src/commands/skills.ts` ahead
of it — the `writeSkillFiles` JSDoc, a preserved block comment at roughly `dist/cli.js:227-233`, is
extended by one or two sentences and the `const results` declaration below it is removed. Both sit above
419, the net delta is non-zero in almost any wording, and a check that demanded `:419` would report the
CORRECT red as a task failure. The property is that the scan finds the offender in the bundle, not that
the bundle's layout is stable.

This red is the deliverable of this task: a scan written after the breach was already fixed would never
have been seen to detect anything, which is the failure mode that let gap 2 ship in the first place.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm test -- --project cli bin</automated>
    <fails_when>exit 0, or a failure output that does not contain the substring `packages/cli/dist/cli.js:` — a scan that is green against a bundle known to carry the name is a scan that is not reading the bundle, and that is this task's only failure mode. Match on the path prefix, NOT on a line number: 07-12 has already edited `skills.ts` above the offender, so the line it lands on is not the line this plan was written against</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/cli/test/bin.test.ts` contains a case named `the published bundle names no other tool, plugin, harness, or planning system (CLAUDE.md)` importing `deniedNames` from `'../../../test/helpers/denied.js'`.
    - That case asserts, before its real assertion: `existsSync(cli)` with the message `run npm run build first`; a bundle length floor above 1000; and a live probe returning exactly one offender.
    - `npm run build && npm test -- --project cli bin` exits non-zero at the end of this task, and the failure output shows a received array of exactly one string beginning `packages/cli/dist/cli.js:`.
    - The summary carries that received offender string verbatim, line number included as observed, as the RED evidence. No acceptance criterion in this plan requires a specific line number.
    - No allowlist, skip list or exception array exists anywhere in the new case or in `test/helpers/denied.ts`.
  </acceptance_criteria>
  <done>The one shipped surface no scan covered is now read by the same list and the same scan as every other, and it has been seen to detect the breach that is actually there.</done>
</task>

<task type="auto">
  <name>Task 3: The JSDoc that reaches the bundle documents a key accord ships</name>
  <files>packages/cli/src/render/table.ts</files>
  <read_first>
    - packages/cli/src/render/table.ts (lines 8-28 — the `EMPTY_CELL` constant, the `GAP` constant, and the `flatten` function whose JSDoc at line 24 is the line the bundler preserves)
    - packages/core/schemas/config.schema.json (line 16 — the adapter enum `["none", "github-issues"]`, the source of the replacement key)
    - packages/cli/test/__golden__/valid-build.status.txt (line 4 — rendered from the `valid-build` FIXTURE's tracker map, not from this JSDoc; it does not change)
    - packages/core/test/fixtures/valid-build/accord/tickets/LOGIN-1.md (line 9 — the fixture that produces that golden line)
    - .planning/phases/07-scaffolding-and-example-repo/07-VERIFICATION.md `owner_rulings` entry GAP-2a (reword AND scan; reword-only was rejected)
  </read_first>
  <behavior>
    - `packages/cli/src/render/table.ts:24` documents `flatten` with a tracker key `config.yml`'s adapter enum actually names, and still says what the function does to more than one key.
    - `flatten`'s body is byte-identical — this is a comment change, and the renderer's behaviour must not move.
    - After `npm run build`, the scan added in task 2 returns an empty offender list and `npm test -- --project cli bin` exits 0.
    - `packages/cli/test/__golden__/valid-build.status.txt` is unchanged: it renders the fixture's data, which this task does not touch.
  </behavior>
  <action>
Reword the JSDoc above `flatten` in `packages/cli/src/render/table.ts` so its worked example uses
`github-issues`, the only adapter with ids that `config.yml` accepts, and so the space-joining of several
keys is stated in words rather than demonstrated with a second product name. One line in, one line out;
the block-comment form stays, because `EMPTY_CELL`'s and `GAP`'s neighbours are block comments and the
file reads as one style.

Change nothing else in the file. `flatten`, `COLUMNS`, `EMPTY_CELL` and `GAP` are behaviour and are
outside this gap.

Leave the fixture and the status golden alone: `packages/core/test/fixtures/valid-build/**` and
`packages/cli/test/__golden__/valid-build.status.txt` carry tracker keys as DATA, proving the flattening
and the column budget against a realistic multi-key map. Neither package's `files:` list includes `test/`,
so neither is a shipped byte, and editing them would move a golden for no gain.

Then run the verify command and confirm the scan from task 2 has gone from red to green. Record both
states in the summary — the offender string from task 2 and the empty array here — because the pair is
what proves the scan detects rather than merely agrees.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm test -- --project cli bin</automated>
    <fails_when>non-zero exit, or `0 passed` in the vitest summary line, or a failure output naming any `dist/cli.js` line — the reword did not reach the bundle, most likely because `npm run build` was skipped</fails_when>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or any suite reporting a failure — in particular `status`, whose golden renders `flatten`'s output and must be unaffected by a comment change</fails_when>
    <automated>npm run build &amp;&amp; npm test -- --project cli status</automated>
    <fails_when>non-zero exit, or a snapshot mismatch against `packages/cli/test/__golden__/valid-build.status.txt` — a comment change that moved a rendered byte means the edit went past the JSDoc</fails_when>
    <automated>git status --porcelain packages/cli/test/__golden__/valid-build.status.txt packages/core/test/fixtures/valid-build</automated>
    <fails_when>any output line — the fixture or its golden was modified. Both are tracked and clean against `HEAD` today, so empty is the reachable passing state</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/cli/src/render/table.ts` line 24's JSDoc contains `github-issues` and describes the space join in words.
    - `flatten`'s body — the `Object.entries` map and the `pairs.length === 0` ternary — is unchanged.
    - After `npm run build`, the case added in task 2 passes: `npm test -- --project cli bin` exits 0 with an empty offender list.
    - `git status --porcelain packages/cli/test/__golden__/valid-build.status.txt packages/core/test/fixtures/valid-build` prints nothing — both are tracked and clean against `HEAD` today, so an empty result is reachable and means the fixture and its golden were not touched.
    - The summary carries the red offender string from task 2 and the green empty result from this task, side by side.
  </acceptance_criteria>
  <reversibility rating="reversible">One comment line in unpublished source. Nothing is released yet, and the scan added in task 2 is what makes a future reintroduction loud rather than silent.</reversibility>
  <done>The bytes Phase 9 will publish carry no denied name, and a machine says so on every test run rather than a reader noticing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `packages/cli/src/**` → `packages/cli/dist/cli.js` | the bundler preserves block comments, so a comment crosses from developer-only text into a published byte |
| `packages/cli/dist/cli.js` → the npm tarball | `files: ["dist", "README.md"]` makes everything in that file public and permanent once published |
| `test/helpers/denied.ts` → every scan call site | one list governs several surfaces; a second copy anywhere splits the constraint in two |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-49 | Information disclosure | a block comment naming another product surviving into the published bundle | high | mitigate | Task 2 scans the built bundle with the same list and scan every other shipped surface uses; task 3 removes the name that is there. Verified red before green. |
| T-07-50 | Tampering | a scan that reads a stale, empty or missing `dist/cli.js` and passes | high | mitigate | Task 2's case asserts existence with the `run npm run build first` message, asserts a length floor, and runs a live probe before its real assertion; the verify commands and both CI jobs run `npm run build` first. The residual — a bundle that is present but older than `src` — is named in the test's own comment and in the summary. |
| T-07-51 | Tampering | a second copy of `DENIED` drifting from the first | medium | mitigate | Task 1 moves the single file and forbids a re-export shim at the old path; the four import sites are updated to one specifier. |
| T-07-52 | Information disclosure | a false positive driving someone to add an allowlist | medium | accept | Confirmed the CLI bundle has exactly one offender and no false positive. `packages/core/dist/index.js:1259` carries one — the CSS gradient function name in the tokens lint rule's regex — dispositioned by owner ruling GAP-2d as a decided non-breach, with all three alternatives (tighten the scan, reword the rule, allowlist) rejected and recorded. The scan stays scoped to the CLI bundle. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No dependency is added; only the existing `build`, `lint`, `typecheck` and `test` scripts run, against the committed lockfile. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name |
|------|------|
| module relocated | `packages/core/test/helpers/denied.ts` → `test/helpers/denied.ts` (repository root); exports `DENIED` and `deniedNames` unchanged |
| directory created | `test/helpers/` at the repository root |
| import specifier changed | `'../../../test/helpers/denied.js'` in `packages/core/test/{examples,scaffold,skills,templates}.test.ts` |
| comment path updated | `packages/core/src/scaffold/pointer.ts:29` and `packages/core/test/scaffold.test.ts:382` |
| test case (new) | `the published bundle names no other tool, plugin, harness, or planning system (CLAUDE.md)` — `packages/cli/test/bin.test.ts` |
| comment changed | the `flatten` JSDoc in `packages/cli/src/render/table.ts` |
| decision recorded | `packages/core/dist/index.js:1259` is a decided non-breach under owner ruling GAP-2d — the CSS gradient function name in the tokens lint rule's regex — with the three rejected alternatives written down so Phase 9 inherits a decision, not a question |
</artifacts_this_phase_produces>

<verification>
1. `npm run build && npm run lint && npm run typecheck && npm test` clean from the repository root.
2. The red-then-green pair is recorded: the offender string task 2 produced against the pre-reword bundle,
   and the empty result after task 3. A summary that carries only the green half has not demonstrated the
   scan detects anything.
3. `npm test -- --project core` on its own — the core project must still run in isolation, which is the
   property the rejected "core test reads `../../cli/dist`" alternative would have broken.
4. Read `test/helpers/denied.ts` once end to end. Its docstring claims one list for every shipped surface;
   after this plan that claim is true for the first time.
</verification>

<success_criteria>
- A `deniedNames` scan covers `packages/cli/dist/cli.js` and returns nothing, having first been seen to
  return exactly one offender whose text begins `packages/cli/dist/cli.js:` — identified by content,
  because 07-12 edits `skills.ts` above it and moves the line.
- The denied list exists once, at the repository root, imported by both packages' test trees.
- No allowlist, skip list or written carve-out was added anywhere, and `Shortcut` is still out of `DENIED`.
- No tsconfig, eslint or vitest configuration file was edited.
- `npm test -- --project core` passes in isolation.
- The GAP-2d decision on `packages/core/dist/index.js:1259` is recorded as a decision, with its three
  rejected routes, and the tokens lint rule is unmodified.
</success_criteria>

<output>
Create `.planning/phases/07-scaffolding-and-example-repo/07-14-SUMMARY.md` when done. It must carry the
red offender string from task 2 verbatim — line number as observed — beside the green empty result from
task 3; the `npm run lint` and `npm run typecheck` output from task 1; and the GAP-2d decision on
`packages/core/dist/index.js:1259` written as a settled decision with its three rejected routes, so
Phase 9 reads a conclusion rather than an open question.

**Do not commit.** Leave the working tree changed and unstaged for the owner's review. No `git commit`,
no `git push`, no pull request.
</output>
