---
phase: 07-scaffolding-and-example-repo
plan: 05
type: execute
wave: 2
# Was wave 1, beside the 07-01 tracer. Moved on the checker's dependency_correctness advisory: the two
# plans' `files_modified` are disjoint, but both run `npm run build` and a full-repo `npm test` in the
# same working tree, and Task 2 below runs `node packages/cli/dist/cli.js skills sync` twice against a
# `dist/` that 07-01 rebuilds from its `run.ts` and `index.ts` edits. Under parallel execution either
# plan's "no previously-passing suite regressed" verify can go red on the sibling's half-written files.
# Declaring the edge is also what tracer-first asks for: the tracer proves the architecture alone.
depends_on: ["07-01"]
files_modified:
  - packages/core/test/skills.test.ts
  - packages/core/skills/ba/SKILL.md
  - packages/core/skills/ba/story.md
  - packages/core/skills/dev/SKILL.md
  - packages/core/skills/dev/debug.md
  - packages/core/src/generated/skills.ts
  - .claude/skills/accord-ba/SKILL.md
  - .claude/skills/accord-ba/story.md
  - .claude/skills/accord-dev/SKILL.md
  - .claude/skills/accord-dev/debug.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [SKILL-04]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No rendered skill body contains any rule id from the real `RULES`, `READY_RULES`, or `DONE_RULES` tables — the ban is computed from the tables, so a rule added in a later phase is covered with no test edit"
    - "No rendered skill body contains the finding-level word `warning` or `warnings` — reporting a level is the CLI's job"
    - "No rendered skill body names a gate by its verdict noun (`Ready`, `Done`); a skill names `accord gate ready <id>` or `accord gate done <id>` and stops"
    - "No rendered paragraph that names an accord command also names a condition the promoted hygiene rules read (`## Open questions`, `assumptions:`, `status: draft`, `TODO`) — naming the command and then enumerating what it blocks on is the exact offence 06-06 upheld"
    - "The frontmatter `description:` field is exempt from the verdict ban and the body ban alike — it is how a runtime routes to the skill and must read as prose (SKILL-04 adjacency edge: the scan's unit is the body, and the paragraph is the unit for the command-adjacency ban)"
    - "A skill with zero matches passes, and a render with zero files or zero command mentions fails the guard-the-guard case rather than passing vacuously (SKILL-04 empty edge)"
    - "Offenders are collected across every rendered file and sorted by path then line with a code-point comparator before the assertion, so the failure output is byte-stable on every host and names every offender at once (SKILL-04 ordering edge)"
    - "Every existing skills case still passes: every SKILL.md still opens on `accord gate ready`, every file is still at most 120 lines, and every relative reference still resolves"
    - "Running `accord skills sync` twice after the edits leaves every rendered copy reported unchanged on the second run"
  artifacts:
    - packages/core/test/skills.test.ts
    - packages/core/skills/ba/SKILL.md
    - packages/core/skills/ba/story.md
  key_links:
    - "the rule-id ban imports the real tables the way `packages/core/test/gate.test.ts:9-10` already does, so the guard cannot drift from the engine"
    - "the definitions under `packages/core/skills/` are the source; `.claude/skills/accord-*` is rendered output refreshed by `accord skills sync` and is never hand-edited"
    - "`npm run gen` regenerates `packages/core/src/generated/skills.ts` from the definitions; the render, the generated module, and the installed copies all move in one commit or the drift test goes red"
  prohibitions:
    - statement: "no rendered skill text may restate a rule the CLI enforces — naming the command and then paraphrasing what it checks is the plan's own tie-breaker, upheld by the owner on 2026-09-16"
      status: unverified
      verification: backstop
    - statement: "`.claude/skills/**` and `.agents/skills/**` must never be hand-edited — they are rendered output, and an edit there is silently reverted by the next sync"
      status: unverified
      verification: backstop
    - statement: "nothing accord ships may name another tool, plugin, harness, or planning system"
      status: unverified
      verification: backstop
---

<objective>
Close SKILL-04, the Phase 6 carry-over (D-148, WINDOWS.md entry 6). Two halves: a guard that goes red
when a skill restates a rule the CLI enforces, and the prose edits that make it green.

The guard comes first and must fail on the current definitions before a single word is changed. A test
written after the edit proves only that the words are gone; a test written before it proves the words
were findable — which is the difference between closing the finding and re-opening it in Phase 9.

Purpose: an agent reading a skill learns what to run, not a second-hand copy of what the runner checks;
a copy drifts and nothing detects the drift.
Output: one new case in `skills.test.ts`, edits across four definitions, and the finding closed in both
tracking artifacts.
</objective>

<execution_context>
@C:/Users/LuanNgo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/LuanNgo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scaffolding-and-example-repo/07-CONTEXT.md
@.planning/phases/06-skills/06-VERIFICATION.md
</context>

<surfaced_assumptions>
- **A-16 — `error` is not banned; `warning` is.** `error` appears legitimately three times in the
  definitions and always about the reader's own software: an EARS example in `ba/story.md` ("IF the payment
  provider returns an error"), the `Error paths` review dimension in `dev/code-review.md`, and "Read the
  error completely" in `dev/debug.md`. `warning` appears once and always means an accord finding level. The
  asymmetry is deliberate and is stated in the test so nobody later "fixes" it into symmetry.

- **A-17 — the verdict ban is body-scoped, not whole-file.** `dev/SKILL.md` and `designer/SKILL.md` both use
  the verdict noun in their frontmatter `description:`, which is the sentence a runtime reads to decide
  whether to load the skill at all. A code span there would not render as prose. The ban applies to
  `bodyOf(text)`, which is what the existing `opens on the gate` case at `skills.test.ts:214` already scopes
  itself to.
  *Alternative:* ban it everywhere and rewrite two descriptions into something that reads worse to the one
  reader who has not loaded the skill yet.

- **A-18 — the verdict ban fires on more than the three sentences 06-06 named.** Grepping the definitions
  finds seven body occurrences of the bare verdict nouns, of which 06-06 named three. The other four are the
  same shape — `dev/debug.md` twice tells the reader what a gate "will ask for", `dev/SKILL.md` says a ticket
  must not be started until it is in a state rather than until a command passes. Fixing all seven is the
  smaller change: a ban with four hand-written exceptions is a ban nobody can reason about, and each of the
  four reads better naming the command.
  *Alternative:* an allowlist of four permitted occurrences, re-argued every time a definition is edited.

- **A-19 — the command-adjacency ban's unit is the paragraph.** A blank-line-delimited block is the smallest
  unit in which "names the command and then enumerates what it blocks on" is a single readable claim. Line
  scope would miss every wrapped sentence — all three findings span two or three lines. Section scope would
  flag a section that happens to both run a gate and, separately, teach what belongs under a heading, which
  is legitimate and is what `ba/story.md` section 4 does.
</surfaced_assumptions>

<tasks>

<task type="tdd">
  <name>Task 1: The guard — RED on the current definitions</name>
  <files>packages/core/test/skills.test.ts</files>
  <read_first>
    - packages/core/test/skills.test.ts (lines 160-260 in particular — `COMMAND` at :168, `bodyOf` at :171, `dirOf`/`baseOf` at :173-174, `output` at :176, `skillFiles` at :177, the guard-the-guard case at :183, the `opens on the gate` case at :214, and the collect-then-assert offender shape used by the `no rendered text sends its reader to a file that exists only in this repository` case)
    - packages/core/src/lint/rules.ts (lines 45-80 — the `RULES` table whose ids the ban is computed from)
    - packages/core/src/gate/rules.ts (lines 58-98 — `READY_RULES`, `DONE_RULES`, and `READY_PROMOTE`)
    - packages/core/test/gate.test.ts (lines 9-10 — the proven import path for those three tables from a core test)
    - packages/core/src/skills/targets.ts (line 25 — the code-point `cmp` comparator the offender sort uses)
    - .planning/phases/06-skills/06-VERIFICATION.md (the `gaps` entry naming the three offending sites and the owner's upheld verdict)
  </read_first>
  <behavior>
    - RED, before any definition is edited: the new case fails and its output names at least these three sites — `ba/SKILL.md` step 3, `ba/story.md` section 4, and `ba/story.md` section 6.
    - Ban 1: no rendered body contains any `id` string from `RULES`, `READY_RULES`, or `DONE_RULES`. The set is computed from the imported tables, never written out as literals.
    - Ban 2: no rendered body contains `warning` or `warnings`, case-insensitively, as a whole word. `error` is deliberately absent from the set (A-16).
    - Ban 3: no rendered body matches the whole word `Ready` or the whole word `Done`, capitalised. Frontmatter is outside the scan because the scan runs on `bodyOf(text)`.
    - Ban 4: no paragraph — a blank-line-delimited block of a body — that matches the existing `COMMAND` regex may also contain any of `## Open questions`, `assumptions:`, `status: draft`, or `TODO`.
    - Guard the guard: the case asserts the render output is non-empty AND that at least one command mention was seen across the whole scan, so a render that stopped producing text could not pass silently.
    - Offender reporting: one entry per hit, formatted `<path>:<line>: <ban name>: <the offending line, trimmed>`, collected across every file, sorted by path then line with the code-point comparator, and asserted with a single `toEqual([])` at the end.
  </behavior>
  <action>
Add one `it` case to the existing `describe` in `packages/core/test/skills.test.ts`, named
`no rendered text restates a rule the CLI enforces (SKILL-04, D-148)`. It reuses the module-level `output`,
`bodyOf`, and `COMMAND` that are already there — do not build a second render.

Import `RULES` from `../src/lint/rules.js` and `READY_RULES`, `DONE_RULES` from `../src/gate/rules.js`,
following `gate.test.ts:9-10`. Build `const ruleIds = [...RULES, ...READY_RULES, ...DONE_RULES].map((r) => r.id)`
and de-duplicate. Computing the set rather than listing it is what makes a rule added in a later phase
covered without a test edit.

Declare the other three sets as source literals beside it, each with a one-line comment naming what it
stands for:
- the level word set: `warning` only, as a case-insensitive whole-word regular expression. Carry the A-16
  note in a comment: `error` is excluded because every occurrence in the definitions is about the reader's
  own software, not about a finding level.
- the verdict set: whole-word `Ready` and `Done`, capitalised. Comment: a skill names the command, never the
  state the command reports.
- the promoted-condition set: `## Open questions`, `assumptions:`, `status: draft`, `TODO` — the subjects the
  `READY_PROMOTE` rules read. Comment: these are legitimate on their own; the offence is naming one in the
  same paragraph as the command that checks it.

Scan each `{ path, text }` in `output`. Take `const body = bodyOf(text)`, split it into lines once, and track
the 1-based line number so every offender can report one. Apply bans 1 to 3 per line over the whole body.
For ban 4, split the body into paragraphs on a blank line while keeping each paragraph's starting line
number, and test each paragraph for a `COMMAND` match and then for each promoted-condition literal; report
the paragraph's first line.

Reset the `COMMAND` regex's `lastIndex` or rebuild it per use — it carries the `g` flag at
`skills.test.ts:168` and a shared `g` regex tested with `.test()` in a loop is stateful. Use
`[...paragraph.matchAll(COMMAND)].length > 0`, which is the form the rest of the file already uses and which
does not have this hazard.

Sort the offender list with the code-point comparator (`(a, b) => (a < b ? -1 : a > b ? 1 : 0)`) — never
`localeCompare` — and assert `expect(offenders).toEqual([])`. Before that, assert the two guard-the-guard
conditions with their own messages.

Run the case and confirm it is RED, naming the three known sites plus whatever else it finds. Record the
full RED offender list in the plan summary — that list is the work order for Task 2.
  </action>
  <verify>
    <automated>npm test -- --project core skills</automated>
    <fails_when>the new case PASSES at this point — a guard that is green before the prose is fixed is a guard that matches nothing; or `0 passed` in the summary; or the reported offender list does not name `ba/story.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/skills.test.ts` contains a case named `no rendered text restates a rule the CLI enforces (SKILL-04, D-148)`.
    - That case imports `RULES`, `READY_RULES`, and `DONE_RULES` and derives its rule-id set from them rather than from literals.
    - Running it now fails, and the failure output contains `ba/SKILL.md`, `ba/story.md`, and at least three distinct line numbers.
    - The case asserts `output.length` is greater than 0 and that at least one command mention was seen, before the offender assertion.
    - The offender list is sorted with a code-point comparator, not `localeCompare`.
    - No other case in the file is edited, renamed, or skipped.
  </acceptance_criteria>
  <done>The guard exists and is red on the definitions as they stand, with a stable, complete offender list to work from.</done>
</task>

<task type="auto">
  <name>Task 2: The prose edits — GREEN, then re-render</name>
  <files>packages/core/skills/ba/SKILL.md, packages/core/skills/ba/story.md, packages/core/skills/dev/SKILL.md, packages/core/skills/dev/debug.md, packages/core/src/generated/skills.ts, .claude/skills/accord-ba/SKILL.md, .claude/skills/accord-ba/story.md, .claude/skills/accord-dev/SKILL.md, .claude/skills/accord-dev/debug.md</files>
  <read_first>
    - packages/core/skills/ba/SKILL.md (the whole file — steps 3 and 4)
    - packages/core/skills/ba/story.md (the whole file — sections 4 and 6)
    - packages/core/skills/dev/SKILL.md (the whole file — step 1 and the frontmatter description, which is exempt)
    - packages/core/skills/dev/debug.md (the whole file — the paragraph near the top about the reproduction test, the step naming the tagged test, and the table row about testing after the fix)
    - packages/core/test/skills.test.ts (the RED offender list from Task 1 is the authoritative work order; these file names are the expected result, not a substitute for it)
    - packages/core/skills/ba/ready.md, packages/core/skills/dev/review.md, packages/core/skills/dev/code-review.md, packages/core/skills/designer/SKILL.md, packages/core/skills/shared/prototype.md (read any of these the RED list names; do not edit one it does not)
  </read_first>
  <action>
Fix exactly the offenders the Task 1 RED names, and nothing else. The expected set, with the intended
replacement in each case:

- `ba/SKILL.md` step 3 currently states that the ticket stays in a draft state until the gate passes and then
  enumerates the three conditions that hold it there. Replace the whole step with two sentences: a ticket is
  not started before `accord gate ready <id>` passes, and run it and work from the list it prints rather
  than deciding for yourself what is still outstanding. Name no condition.
- `ba/SKILL.md` step 4 currently says editing the criteria after the verdict noun has a consequence. Replace
  the verdict noun with the command: after `accord gate ready <id>` has passed. The rest of the step stands —
  it already refuses to say what the consequence is.
- `ba/story.md` section 4 currently ends with a sentence saying the two hold the ticket short of the verdict
  until they are answered. Delete that clause and keep the reason for writing them down. Do NOT add a command
  mention to that paragraph: the paragraph legitimately names both promoted conditions, and a command beside
  them is ban 4.
- `ba/story.md` section 6 currently tells the reader to read the level the lint command prints and names the
  subject of one rule. Replace with: run `accord lint` and read what it reports. It then says the criteria are
  recorded once the verdict noun passes — replace that noun with `accord gate ready <id>`.
- `dev/SKILL.md` step 1 currently says not to start on a ticket that is not in the verdict state. Replace with:
  do not start until it passes.
- `dev/debug.md` — three sites: the paragraph stating that the gate still blocks until at least one scenario
  exists, the step naming the test the verdict gate will read, and the table row saying the verdict gate will
  ask for the test anyway. In each, name `accord gate ready <id>` or `accord gate done <id>` and drop the
  claim about what it checks.

If the RED list names a site not in this set, fix it the same way: name the command, delete the claim about
what the command checks. If the RED list does NOT name a site in this set, leave that site alone and record
the discrepancy in the summary — the test is the authority, this list is the expectation.

Constraints every edit must preserve, all already asserted by existing cases in the same file: each
`SKILL.md` body's FIRST command mention is still `accord gate ready`; each file is still at most 120 lines;
every `./<file>` reference still resolves inside the same render; no line contains a backslash; no tool,
plugin, harness, or planning system is named.

Then re-render, in this order: `npm run gen` to rebuild `packages/core/src/generated/skills.ts` from the
edited definitions, `npm run build`, then `node packages/cli/dist/cli.js skills sync` to refresh the copies
under `.claude/skills/`. Never hand-edit a file under `.claude/skills/` or `.agents/skills/` — those are
rendered output, and an edit there is reverted by the next sync with an `overwrote local edits` line.
  </action>
  <verify>
    <automated>npm run gen &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test -- --project core skills</automated>
    <fails_when>non-zero exit; or any case in the `skills` suite reports a failure — in particular `no rendered text restates a rule the CLI enforces`, `every SKILL.md opens on the gate`, `every SKILL.md is at most MAX_SKILL_LINES lines`, or `every relative reference resolves inside the same render`</fails_when>
    <automated>npm run build &amp;&amp; node packages/cli/dist/cli.js skills sync &amp;&amp; node packages/cli/dist/cli.js skills sync</automated>
    <fails_when>non-zero exit, or the second invocation prints any line beginning `updated` or `overwrote` — the copies did not settle</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any previously-passing suite reports a failure</fails_when>
  </verify>
  <acceptance_criteria>
    - The `no rendered text restates a rule the CLI enforces (SKILL-04, D-148)` case passes.
    - `grep -n 'Ready\|Done' packages/core/skills/ba/SKILL.md packages/core/skills/ba/story.md packages/core/skills/dev/SKILL.md packages/core/skills/dev/debug.md` returns matches only on lines beginning `description:`.
    - The first `accord` command named in each `SKILL.md` body is still `accord gate ready`.
    - `packages/core/src/generated/skills.ts` differs from its pre-edit state, and the four edited definitions are reflected in it verbatim.
    - `.claude/skills/accord-ba/SKILL.md`, `.claude/skills/accord-ba/story.md`, `.claude/skills/accord-dev/SKILL.md`, and `.claude/skills/accord-dev/debug.md` all differ from their pre-edit state.
    - A second `accord skills sync` reports every path `unchanged`.
    - No file under `packages/core/skills/` other than the four named (or the ones the RED list added) is modified.
  </acceptance_criteria>
  <done>The definitions name the commands and stop; the guard is green; the generated module and the installed copies match the definitions.</done>
</task>

<task type="auto">
  <name>Task 3: Close the finding in both tracking artifacts</name>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 74 — the SKILL-04 checkbox — and line 190, its traceability row reading `| SKILL-04 | Phase 6 | Complete |`)
    - .planning/WINDOWS.md (entry 6 — the open ledger row naming the three sentences and the owner's deferral, in both the table and the trailing JSON block)
    - .planning/phases/06-skills/06-VERIFICATION.md (the `overrides` block that produced the contradiction being corrected)
  </read_first>
  <action>
`.planning/REQUIREMENTS.md` currently ticks SKILL-04 at line 74 while its traceability row attributes the
closure to Phase 6 — but that tick recorded the owner's verification override, not a fix. The tick is now
true. Correct the attribution so the two artifacts agree: change the traceability row from
`| SKILL-04 | Phase 6 | Complete |` to `| SKILL-04 | Phase 6, Phase 7 | Complete |`. Leave the line 74
checkbox as `- [x]`. Change nothing else in the file.

Mark WINDOWS.md entry 6 fixed through the tool rather than by editing the table by hand — the file carries a
trailing JSON block and a frontmatter count block that must stay consistent with the table, and the tool
updates all three:

`node "C:/Users/LuanNgo/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 6`

If that verb is unavailable, edit all three regions by hand in one pass: the table row's `status` cell to
`fixed` and its `resolved_at` cell to the current UTC timestamp, the matching JSON object's `status` and
`resolved_at`, and the frontmatter `open_count` (7 to 6) and `fixed_count` (0 to 1).
  </action>
  <verify>
    <automated>grep -c 'SKILL-04 | Phase 6, Phase 7 | Complete' .planning/REQUIREMENTS.md</automated>
    <fails_when>output is `0` — the traceability row was not corrected</fails_when>
    <automated>node "C:/Users/LuanNgo/.claude/gsd-core/bin/gsd-tools.cjs" windows list 2>/dev/null | grep -v '^#' | grep -c 'id: 6.*open'</automated>
    <fails_when>output is greater than `0` — entry 6 is still open; or the command errors, in which case verify the three WINDOWS.md regions by reading them</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the row `| SKILL-04 | Phase 6, Phase 7 | Complete |` and no longer contains `| SKILL-04 | Phase 6 | Complete |`.
    - `.planning/REQUIREMENTS.md` line 74 still begins `- [x] **SKILL-04**`.
    - `.planning/WINDOWS.md` entry 6 has `status` `fixed` in both the table row and the JSON object, with a non-null `resolved_at` in both.
    - `.planning/WINDOWS.md` frontmatter `open_count` is 6 and `fixed_count` is 1.
    - No other WINDOWS.md entry changed status.
  </acceptance_criteria>
  <done>SKILL-04 is closed, attributed to the phase that actually fixed it, and the ledger entry that survived the Phase 6 close is marked resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill definition → any agent that loads it | the rendered text is instruction an agent acts on without a human in the loop |
| definition → rendered copy | `packages/core/skills/` is source; `.claude/skills/accord-*` is generated output a human can also edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Spoofing | a skill restating a gate rule | medium | mitigate | The whole point of the plan. A restated rule is a second, unversioned copy of the engine's behaviour that an agent trusts and that nothing keeps in step; the new guard is the detection the finding asked for, and it derives its rule-id set from the live tables rather than from a snapshot of them. |
| T-07-24 | Tampering | a hand-edit under `.claude/skills/` | low | accept | Pre-existing and deliberate: D-112 makes overwriting the stated contract of the rendered file, and the sync report says `overwrote local edits` when it happens. This plan adds no new exposure; it only re-renders. |
| T-07-25 | Repudiation | the finding silently re-opening | medium | mitigate | The guard is written RED first and its failure output is recorded in the summary, so "the test passes" cannot mean "the test matches nothing". The two tracking artifacts are corrected in the same plan so they stop disagreeing. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<artifacts_this_phase_produces>
This plan creates or changes:

| Kind | Name |
|------|------|
| new test case | `no rendered text restates a rule the CLI enforces (SKILL-04, D-148)` in `packages/core/test/skills.test.ts` |
| edited skill definitions | `ba/SKILL.md` (steps 3, 4), `ba/story.md` (sections 4, 6), `dev/SKILL.md` (step 1), `dev/debug.md` (three sites) |
| regenerated data | `packages/core/src/generated/skills.ts` |
| re-rendered output | `.claude/skills/accord-ba/**`, `.claude/skills/accord-dev/**` |
| requirement closed | SKILL-04 |
| ledger entry resolved | WINDOWS.md entry 6 |
</artifacts_this_phase_produces>

<verification>
1. The guard was red before the edits and green after — the RED offender list is in the summary.
2. `npm run gen && npm run build && npm run lint && npm run typecheck && npm test` clean from the repository
   root.
3. `node packages/cli/dist/cli.js skills sync` twice: the second run reports every path unchanged.
4. Read the four edited definitions end to end once. Each one still reads as a workflow, not as a set of
   sentences with holes in them where a claim used to be.
</verification>

<success_criteria>
- A guard exists that fails when a skill restates a rule the CLI enforces, derives its rule set from the
  live tables, and cannot pass vacuously.
- Every definition it named is fixed by naming the command and dropping the claim.
- The generated module and the installed copies match the definitions.
- SKILL-04 and WINDOWS.md entry 6 are both closed and no longer contradict each other.
</success_criteria>

<output>
Create `.planning/phases/07-scaffolding-and-example-repo/07-05-SUMMARY.md` when done. Include the full RED
offender list from Task 1 verbatim — it is the evidence that the guard matches something.
</output>
