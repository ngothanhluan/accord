---
phase: 02-core-model-and-loading
plan: 07
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/cli/src/load/fs.ts
  - packages/cli/test/load.test.ts
autonomous: true
requirements: [CORE-06, FMT-08]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`loadFromFs(root)` builds a `SnapshotInput` from a real repository: `tree` is the output of `git ls-files --cached --others --exclude-standard -z` (tracked plus untracked-but-not-ignored, forward slashes, sorted), `files` holds every file under `accord/` plus the file named by `config.design.tokens` when it exists inside the repository (D-51, D-30)"
    - "`stableJson(loadSnapshot(loadFromFs(<copy of the valid-build fixture with git init>)))` equals the text of `packages/core/test/__golden__/valid-build.snapshot.json` read as UTF-8, compared with `toBe` and no trimming on either side — vitest 5 saves a `toMatchFileSnapshot` file verbatim from the received string and compares raw snapshots without trimming, so the golden holds exactly the `stableJson` string core produced; CLI and core see the same snapshot (D-51 parity; ROADMAP criterion 4, Windows path case)"
    - "On Windows every key in `files` and every entry of `tree` uses forward slashes; no backslash reaches core (PITFALLS §12)"
    - "A file matched by `.gitignore` is absent from `tree`; the `.gitignore` itself is present"
    - "A directory that is not a git repository, or a machine without `git`, raises `UsageError` (exit code 2 in Phase 5) with a message naming the cause; `git` is spawned by name with a fixed argument list, never through a shell and never as `.cmd` (D-51, CLI-07)"
  artifacts:
    - path: "packages/cli/src/load/fs.ts"
      provides: "loadFromFs(root) and UsageError"
      exports: ["loadFromFs", "UsageError"]
    - path: "packages/cli/test/load.test.ts"
      provides: "temp-repo tests: parity with the core golden, ignore handling, forward slashes, usage errors"
      min_lines: 50
  key_links:
    - from: "packages/cli/src/load/fs.ts"
      to: "@accord-dev/accord-core"
      via: "import { loadSnapshot } and import type { SnapshotInput } — the first pass reads config.design.tokens"
      pattern: "from '@accord-dev/accord-core'"
    - from: "packages/cli/test/load.test.ts"
      to: "packages/core/test/__golden__/valid-build.snapshot.json"
      via: "readFileSync of the core golden compared to stableJson(loadSnapshot(loadFromFs(tmp)))"
      pattern: "valid-build\\.snapshot\\.json"
  prohibitions:
    - statement: "No task in this plan runs `git commit`, `git push`, or `git tag`; every change is left in the working tree for the author's review"
      status: resolved
      verification: judgment
    - statement: "The CLI never spawns `npm`, `npx`, `gh.cmd`, or any `.cmd`/`.bat`, and never passes `shell: true`; the only child process is `git` by bare name with a literal argv (CLI-07, PITFALLS §12)"
      status: resolved
      verification: test
    - statement: "The loader never reads a file outside the repository root, even when `config.design.tokens` points there (`../` or absolute); such a path is skipped (path containment)"
      status: resolved
      verification: test
---

<objective>
**As a** developer running accord from a terminal on Windows or Linux, **I want** the CLI to build the same snapshot from my working tree that the MCP host builds from the GitHub API, **so that** an evidence line passes or fails identically from the command line and from chat.

Purpose: D-28 to D-30 define the contract; this slice is the first real host of it. `git ls-files` gives `tree` parity with the GitHub tree (D-51) and the CLI gets its loader before Phase 5 wires commander.

Output: `packages/cli/src/load/fs.ts` and `packages/cli/test/load.test.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-core-model-and-loading/02-CONTEXT.md
@.planning/phases/02-core-model-and-loading/02-PATTERNS.md
@.planning/phases/02-core-model-and-loading/02-01-SUMMARY.md
@.planning/research/PITFALLS.md
@packages/cli/src/index.ts
@packages/cli/tsconfig.json
@packages/cli/test/bin.test.ts
@packages/core/test/purity.test.ts
@packages/core/test/bundle.test.ts
@packages/core/test/helpers/fixture.ts

<interfaces>
```typescript
// packages/cli/src/load/fs.ts (D-51)
import { loadSnapshot } from '@accord-dev/accord-core';
import type { SnapshotInput } from '@accord-dev/accord-core';
export class UsageError extends Error { readonly exitCode = 2 }
export function loadFromFs(root: string): SnapshotInput;
// Impure by design: node:fs, node:path, node:child_process are allowed in packages/cli.
// CLI tests import '@accord-dev/accord-core' from packages/core/dist — run `npm run build` first (bin.test.ts does the same).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `loadFromFs` — git tree, accord files, tokens file, forward slashes, usage errors</name>
  <precondition>`git --version` exits 0 from the repo root (git 2.x on PATH; this machine reports 2.55.0.windows.2) — the loader and its tests spawn `git` by name</precondition>
  <read_first>
    - .planning/phases/02-core-model-and-loading/02-CONTEXT.md D-51 (git ls-files flags, spawn by name, usage error), D-30 (what `files` and `tree` hold), D-29 (forward slashes)
    - .planning/research/PITFALLS.md Pitfall 12 (EINVAL on `.cmd`, `path.posix`, spawn only `.exe` by name)
    - .planning/phases/02-core-model-and-loading/02-PATTERNS.md `packages/cli/src/load/fs.ts` section
    - packages/core/test/purity.test.ts lines 60-75 (`execFileSync` with `encoding: 'utf8'` and `stdio: 'pipe'`)
    - packages/cli/tsconfig.json (`types: ["node"]`, include `src` and `test`)
  </read_first>
  <files>packages/cli/src/load/fs.ts</files>
  <action>
Create `packages/cli/src/load/fs.ts` (`// D-51 filesystem loader` header):
1. `UsageError extends Error` with `readonly exitCode = 2` and `name = 'UsageError'`.
2. `tree`: `execFileSync('git', ['ls-files', '--cached', '--others', '--exclude-standard', '-z'], { cwd: root, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] })`; split on `\0`, drop empty strings, sort with the default comparator, de-duplicate (a file both cached and untracked-modified appears once). Wrap the call: when the thrown error has `code === 'ENOENT'` throw `UsageError('git is required but was not found on PATH')`; otherwise throw `UsageError('not a git repository (or git failed): ' + root + '\n' + (stderr trimmed))`. Never enable the shell option; spawn no package-manager binary and no Windows batch shim — `git` by bare name is the only child process, and the verify grep also counts comment text, so do not name the forbidden binaries in comments.
3. `files`: `const accordDir = join(root, 'accord')`; when it is not a directory throw `UsageError('no accord/ folder in ' + root)`; `readdirSync(accordDir, { recursive: true, withFileTypes: true })`, keep entries where `isFile()` is true (symlinks and directories skipped), absolute path `join(d.parentPath, d.name)`, key `relative(root, abs).split(sep).join('/')`, value `readFileSync(abs, 'utf8')` (core strips BOM and CRLF itself, D-38; do not normalise here).
4. Tokens file (D-30): `const cfg = loadSnapshot({ files, tree }).config`; `const t = cfg?.design.tokens`; when `t` is a non-empty string: `const rel = t.replace(/\\/g, '/').replace(/^\.\//, '')`; `const abs = resolve(root, rel)`; `const back = relative(root, abs)`; only when `back` is non-empty, does not start with `..`, is not absolute (`!isAbsolute(back)`), and `existsSync(abs)` with `statSync(abs).isFile()`, set `files[back.split(sep).join('/')] = readFileSync(abs, 'utf8')`; otherwise skip silently (Phase 3 LINT-04 reports a missing tokens file). This containment check is the T-02-20 mitigation.
5. Return `{ files, tree }`. Export nothing else. Do not touch `packages/cli/src/index.ts` (Phase 5 wires commander).
Run `npm run build && npm run typecheck && npm run lint`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run build && npm run typecheck && npm run lint && ! grep -Eq "npx|npm|\.cmd|shell: *true" packages/cli/src/load/fs.ts</automated>
    <fails_when>non-zero exit from build, typecheck, or lint; or the final grep finds a forbidden spawn token in fs.ts (the `!` inverts it, so a match yields non-zero exit)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "execFileSync('git'" packages/cli/src/load/fs.ts` prints 1 and `grep -c "'--exclude-standard'" packages/cli/src/load/fs.ts` prints 1
    - `grep -c 'class UsageError' packages/cli/src/load/fs.ts` prints 1 and `grep -c 'exitCode = 2' packages/cli/src/load/fs.ts` prints 1
    - `grep -c "startsWith('..')" packages/cli/src/load/fs.ts` prints at least 1 (containment) and `grep -c 'isAbsolute' packages/cli/src/load/fs.ts` prints at least 1
    - `grep -c 'path.join\b\|join(' packages/cli/src/load/fs.ts` may be non-zero only for filesystem access; `grep -c "split(sep).join('/')" packages/cli/src/load/fs.ts` prints at least 2 (keys are always posix)
    - `git diff --quiet -- packages/cli/src/index.ts packages/cli/package.json package-lock.json` exits 0
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>A repository root becomes a `SnapshotInput` through `git` and `readdir`, with posix keys, the configured tokens file contained to the root, and typed usage errors for the two environment failures.</done>
</task>

<task type="auto">
  <name>Task 2: Temp-repo tests — parity with the core golden, ignore handling, backslash-free keys, usage errors</name>
  <read_first>
    - packages/cli/src/load/fs.ts (Task 1)
    - packages/core/test/bundle.test.ts lines 30-40 (`mkdtempSync` / `rmSync` try-finally pattern) and packages/cli/test/bin.test.ts (`fileURLToPath(new URL(...))` for repo-relative paths)
    - packages/core/test/helpers/fixture.ts (`stableJson` — re-implement its 6-line sort-keys replacer inline in this test; do not import across packages)
    - packages/core/test/__golden__/valid-build.snapshot.json (the bytes the parity test must reproduce)
  </read_first>
  <files>packages/cli/test/load.test.ts</files>
  <action>
Write `packages/cli/test/load.test.ts`:
1. Helpers: `fixture = fileURLToPath(new URL('../../core/test/fixtures/valid-build/', import.meta.url))`; `golden = fileURLToPath(new URL('../../core/test/__golden__/valid-build.snapshot.json', import.meta.url))`; `stableJson` (same replacer as core's helper); `makeRepo()` = `mkdtempSync(join(tmpdir(), 'accord-fs-'))`, `cpSync(fixture, tmp, { recursive: true })`, `execFileSync('git', ['init', '-q'], { cwd: tmp })`, returns `tmp`; cleanup with `rmSync(tmp, { recursive: true, force: true, maxRetries: 5 })` in `finally` (git object files are read-only on Windows).
2. `it('matches the core golden exactly')`: `const input = loadFromFs(tmp)`; `expect(stableJson(loadSnapshot(input))).toBe(readFileSync(golden, 'utf8'))` — no `trimEnd`, no CRLF replacement on either side. This is exact by construction: vitest 5 writes a `toMatchFileSnapshot` file verbatim from the received string and compares raw snapshots without trimming (`pass = expected === received`), so the golden is the `stableJson` string core produced, and `.gitattributes` `* text=auto eol=lf` keeps it LF on every checkout. If this assertion fails only on a trailing newline or on CRLF, that is a finding about how the golden was written or checked out — report it in the SUMMARY; do not trim or normalise to make it pass. Also assert `Object.keys(input.files).sort()` equals `['accord/config.yml','accord/product/glossary.md','accord/tickets/EPIC-1.md','accord/tickets/LOGIN-1.md','accord/tickets/LOGIN-1/verification.md','src/styles/tokens.css']` (accord/** plus the configured tokens file, nothing else) and `input.tree` equals the eight paths in the golden's `tree`.
3. `it('keys never contain a backslash')`: for every key of `files` and entry of `tree` `expect(k).not.toContain('\\')` (meaningful on the Windows CI job).
4. `it('respects .gitignore')`: in a fresh repo write `.gitignore` containing `ignored.txt` and a file `ignored.txt`; `tree` excludes `ignored.txt` and includes `.gitignore`.
5. `it('skips a tokens path outside the repository')`: in a fresh repo rewrite `accord/config.yml` so `tokens` is `../outside.css` and create that file one level above `tmp` (inside a second mkdtemp parent so it is cleaned up) → `files` has no key ending in `outside.css`.
6. `it('a directory that is not a repository is a UsageError')`: `mkdtempSync` without `git init` → `expect(() => loadFromFs(dir)).toThrow(UsageError)` and the message contains `not a git repository`. (If the machine's temp directory sits inside a git work tree this test cannot distinguish; note it in a comment — CI runners and the author's machine do not.)
7. `it('missing accord folder is a UsageError')`: repo with `git init` but no `accord/` → throws `UsageError` whose message contains `no accord/ folder`.
Run `npm run build && npm test -- --project cli load`. Leave uncommitted.
  </action>
  <verify>
    <automated>npm run build && npm test -- --project cli load</automated>
    <fails_when>non-zero exit; or `No test files found`; or the `|cli|` summary reports fewer than 7 passing tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/cli/test/load.test.ts` contains `valid-build.snapshot.json`, `ignored.txt`, `outside.css`, `not a git repository`, `no accord/ folder`, and `maxRetries`
    - `npm test -- --project cli load` reports at least 7 passing tests and 0 failures
    - `npm test -- --project cli` reports 0 failures (the whole-suite `npm test` is the Wave 2 wave-end check, not this plan's gate: sibling core plans may still be mid-fixture)
    - `git log -1 --format=%H` is unchanged (no commit)
  </acceptance_criteria>
  <done>The CLI loader reproduces the core golden from a real git working tree, honours ignore rules, keeps every path posix, contains the tokens path, and fails with typed usage errors.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- CLI loader: `packages/cli/src/load/fs.ts` exporting `loadFromFs(root: string): SnapshotInput` and `class UsageError extends Error { exitCode: 2 }`
- Test: `packages/cli/test/load.test.ts`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions

| Source | Item | Disposition |
|---|---|---|
| D-51 | `files` vs ignore rules | Assumed literal D-51: every file under `accord/` is read even if gitignored; only `tree` honours ignore rules. A gitignored ticket therefore loads but its path is absent from `tree`. Alternative: filter `files` by `tree`. |
| D-30 | tokens file missing on disk or outside the root | Assumed: skipped silently; Phase 3 LINT-04 reports. |
| D-51 | symlinks under `accord/` | Assumed: skipped (`Dirent.isFile()` is false for symlinks); a symlinked ticket is not loaded. |
| D-51 | `execFileSync` vs `spawnSync` | Chosen `execFileSync` (Claude's Discretion): simpler error surface, same by-name spawn. |
| D-51 | git absent (ENOENT) | Implemented and typed; exercised only by code reading — the test suite cannot remove `git` from PATH without an injectable env. Flagged for the Phase 5 spawn test on a runner where git is guaranteed. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.yml design.tokens` → filesystem read | A repo-authored string chooses a file to read into the snapshot |
| CLI → `git` child process | Fixed argv; `root` is the only variable and is the cwd, not an argument |
| working tree → `files` | Every file under `accord/` is read as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Information disclosure | `design.tokens` set to `../../.ssh/id_rsa` or an absolute path pulls a file outside the repo into the snapshot (later printed or sent by MCP) | high | mitigate | `relative(root, resolve(root, rel))` must be non-empty, not start with `..`, and not be absolute; Task 2 case 5 proves the skip |
| T-02-21 | Elevation of privilege | shell injection through the spawn | medium | mitigate | `execFileSync` with a literal argv and no `shell`; `root` is `cwd`, never interpolated; the verify grep forbids `shell: true` |
| T-02-22 | Tampering | a malicious `git` earlier on PATH | low | accept | Out of scope: the developer's PATH is trusted by every tool on the machine (canon — covered by OS/security tooling, not minted here) |
| T-02-23 | Denial of service | huge binary under `accord/` read as utf8 | low | accept | The convention holds Markdown and one CSS file; size discipline is a Phase 3 lint concern |
| T-02-SC | Tampering | npm installs | high | mitigate | No packages added; `packages/cli/package.json` and the lockfile unchanged (acceptance criterion) |
</threat_model>

<verification>
- `npm run build && npm test -- --project cli` green locally; the Windows CI job is where the backslash and `rmSync` assertions matter.
- Wave-end check, not a per-plan gate: once every Wave 2 plan (02-02..02-05, 02-07) has finished, `npm test` (all projects) must report 0 failures.
</verification>

<success_criteria>
- D-51 implemented and proven against the core golden (parity), ignore rules, posix keys, containment, and usage errors; D-29/D-30 honoured by the first real host.
- Nothing committed.
</success_criteria>

<output>
Create `.planning/phases/02-core-model-and-loading/02-07-SUMMARY.md` when done. Do not commit.
</output>
